{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:search && npm run build:history && npm run build:personality && npm run build:follow-list && npm run build:book-readers && npm run build:block-list && npm run build:profile && npm run build:books && npm run build:dashboard && npm run build:onboarding && npm run build:study",
    "build:search": "cross-env APP=search vite build",
    "build:history": "cross-env APP=history vite build",
    "build:personality": "cross-env APP=personality vite build",
    "build:follow-list": "cross-env APP=follow-list vite build",
    "build:book-readers": "cross-env APP=book-readers vite build",
    "build:block-list": "cross-env APP=block-list vite build",
    "build:profile": "cross-env APP=profile vite build",
    "build:books": "cross-env APP=books vite build",
    "build:dashboard": "cross-env APP=dashboard vite build",
    "build:onboarding": "cross-env APP=onboarding vite build",
    "build:study": "cross-env APP=study vite build",
    "test": "vitest run",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@vitejs/plugin-vue": "^6",
    "@vue/test-utils": "^2",
    "cross-env": "^7",
    "jsdom": "^26",
    "sharp": "^0.35.4",
    "typescript": "^5",
    "vite": "^6",
    "vitest": "^4",
    "vue": "^3.5"
  },
  "dependencies": {
    "@tiptap/core": "^3.31.3",
    "@tiptap/extension-highlight": "^3.31.3",
    "@tiptap/extension-list": "^3.31.3",
    "@tiptap/markdown": "^3.31.3",
    "@tiptap/pm": "^3.31.3",
    "@tiptap/starter-kit": "^3.31.3",
    "@tiptap/suggestion": "^3.31.3",
    "@tiptap/vue-3": "^3.31.3"
  }
}
