{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:garden && npm run build:search && npm run build:history && npm run build:personality && npm run build:follow-list && npm run build:book-readers",
    "build:garden": "cross-env APP=garden vite build",
    "build:search": "cross-env APP=search vite build",
    "build:history": "cross-env APP=history vite build",
    "build:personality": "cross-env APP=personality vite build",
    "build:follow-list": "cross-env APP=follow-list vite build",
    "build:book-readers": "cross-env APP=book-readers vite build",
    "test": "vitest run"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5",
    "@vue/test-utils": "^2",
    "cross-env": "^7",
    "jsdom": "^26",
    "phaser": "^3.80.1",
    "sharp": "^0.35.2",
    "typescript": "^5",
    "vite": "^6",
    "vitest": "^3",
    "vue": "^3.5"
  }
}
