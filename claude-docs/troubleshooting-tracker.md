# 재발·승격 트래커 (동결 — 2026-09-25 분할 이관)

> 단일 파일 시절(2026-06-25~2026-09-25) 손으로 갱신하던 표. **더 갱신하지 않는다** — 회차는 각 `troubleshooting/T-###.md` 끝 `- **N회차**` 줄, 승격은 frontmatter `promoted:`/`guard:`, 교차 프로젝트 재발은 `~/.claude/scripts/scan-troubleshooting-recurrence.ps1` 보고서가 센다. 44군의 ✅ 하드픽스 경로를 `guard:`로 옮기는 소급은 별건.

## 🔁 재발·승격 트래커

> **2회 이상** 재발한 트랩은 참조용 T-### 누적에 더해 **CLAUDE.md(항상 로드)·훅·하드픽스 중 하나로 승격**한다(CLAUDE.md 합의). 카운팅을 모델 기억에 안 맡기게, **같은 트랩군의 T-###를 새로 쓸 때 이 표의 회차·승격상태를 갱신**한다 — "지금 2회+인데 미승격"을 안 놓치는 단일 출처(완벽한 과거 감사가 아니라 살아있는 표). 신규 트랩은 1회라 안 올리고, **재발(2회째)이 되는 순간** 군을 만들어 올린다. 승격은 prose 한 줄보다 **하드픽스(훅·스크립트로 트랩 자체 제거)**가 우선.

> ⚠️ **1회 과거 백필(2026-06-30)**: 이 "살아있는 표"는 forward-only(새 T-###를 쓸 때만 갱신)라, **규칙 도입(2026-06-25, #508) 이전에 이미 2회+ 재발하고 그 뒤 재발이 멈춘 옛 트랩군**은 구조적으로 표에 안 올라간다. 이 사각을 1회 전수 감사(T-001~T-117, 21-에이전트 워크플로)해 진짜 누락 2군 — **csrf-buffer-commit(T-033·T-049)·fk-child-cleanup(T-023·T-029)** — 을 아래에 소급 등재했다. (나머지 후보 대부분은 "표면만 비슷한 별개 트랩"이거나 "재발이 과거에 멈춰 승격 실익 낮음"으로 판정해 제외.) 이후로는 forward-only를 유지한다 — 같은 사각이 또 의심되면 그때 1회 백필 감사를 반복한다.

| 트랩군 | 발생 T-### | 회차 | 승격 상태 |
|---|---|---|---|
| **게이트 훅이 보는 곳과 명령이 실제로 건드리는 곳이 어긋나 조용히 통과한다** — 명령이 스스로 스테이징해 PreToolUse 시점 인덱스가 비어 있음(T-228) · 명령이 `cd "<다른 워크트리>" &&`로 옮겨 가 훅은 세션 cwd를 검사함(T-242) | T-228 · **T-242** | **2** | ✅ **하드픽스(2026-09-13)** — 공유 헬퍼 `.claude/hooks/lib/resolve-target-cwd.ps1`이 명령 속 `cd`·`Set-Location`·`git -C`를 따라가 git이 실제로 도는 **워크트리 최상위**를 돌려주고, git 상태를 읽는 PreToolUse 훅 7개(테스트·번들·CSS 주석·목차·커밋 메시지 `-F`·changelog 중복·main push)가 전부 그 값을 쓴다. 경로가 확장식(`cd "$WT"`)이라 원리상 알 수 없으면 게이트형 5개는 **차단**하고 fail-open 2개(목차·메시지)만 세션 cwd로 폴백한다. 이동이 없어도 최상위로 올려 **세션 cwd가 하위 폴더일 때 `gradlew.bat`을 못 찾아 fail-open 하던 변종**까지 닫았다(상대 `-F`를 읽는 메시지 훅만 `-NoToplevel`로 git이 실제로 도는 폴더를 본다). 닫힌 서브셸·heredoc 본문·커밋 뒤의 이동은 걷어 내 오탐 차단을 막는다. 테스트 `.claude/hooks/tests/test-hook-target-cwd.sh` 57건 — 첫 38건은 구현자가 고른 돌연변이 5종을 다 잡았지만 **독립 리뷰가 고른 16종 중 8종이 살아남았다**(규칙이 있는데 잠근 테스트가 없음). 그 8개 규칙마다 테스트를 더했다. ⚠️ 두 회차의 뿌리가 같다 — 훅은 「PreToolUse 시점 + 세션 cwd」라는 **자기 관점**에서 상태를 읽는데 명령은 그 관점 밖(실행 도중 스테이징 / 다른 폴더)에서 일한다. 그래서 「게이트 통과」가 「게이트가 돌았다」를 뜻하지 않았다. 둘 다 **테스트 결과 폴더·산출물처럼 게이트가 남겼어야 할 흔적의 부재**로 발견됐다. 3회차가 나오면 이 관점 차이가 남은 자리부터 본다 — 알려진 곳: 게이트 훅 6개가 stdin을 `[Console]::In`(CP949)으로 읽어 한글이 든 명령에서 판독이 빗나갈 수 있다(별건) |
| **훅이 막은 명령의 앞부분이 함께 죽고 다음 명령은 그걸 모른다** — 잔재 `.commit-msg-tmp`로 옛 메시지 커밋(1회차) · `git add && git commit` 묶음이 막혀 스테이징 빠진 `--amend`가 push(2회차 변종) | **T-221** | **2** | ⚠️ **prose만**(CLAUDE.md 「🈲 한글 커밋 메시지」 절 — 쓰기·스테이징·커밋을 별도 명령으로, 커밋 직전 `git status --short`). hookify warn 후보 `git add[^\n]*&&[^\n]*git commit`은 3회차에 검토 |
| **시간 의존 통합 테스트가 자정 경계에서만 붉어진다 — 고정 클락 미주입** | T-039 · **T-230** | **2** | ⚠️ **prose만**(T-039 처방 = nested `@TestConfiguration` + `@Primary Clock.fixed`) — 정적 감지가 「없음」을 못 재서 hookify로 못 올린다(룰은 지금 쓰는 내용만 보고 파일 전체의 **부재**를 모른다). 승격 후보 = `@SpringBootTest` + `clock.instant()`인데 `Clock.fixed`가 없는 테스트 클래스를 스캔하는 자체 테스트 — 지금 켜면 5개+가 즉시 붉어져 별건이다 |
| changelog 행이 rebase에서 중복 생성 — `merge=union`이 「한 브랜치의 추가→수정」을 양쪽 다 남긴다 | **T-210** | **7** | ✅ **하드픽스**(`require-changelog-no-dup.ps1` — `git push` 게이트, 같은 날짜+같은 굵은 제목 행이 둘 이상이면 차단. 테스트 8건, 비인접 중복 포함). ⚠️ 2회 모두 **같은 세션 안에서** 났다 — 리뷰 반영이 있는 PR은 「기능 커밋에서 행 추가 → 리뷰 커밋에서 그 행 보강」이 되어 이 구조에 자동으로 들어간다. 원천인 T-098(merge=union)은 **여전히 옳다**(두 세션이 각자 행을 붙이는 원래 시나리오엔 정답) — 고칠 것은 union이 아니라 「한 브랜치가 자기 행을 두 번 건드리는」 쪽이다. ⚠️ PowerShell 훅이라 **원격(웹) 세션엔 안 붙는다**(T-208) — 그 환경에선 rebase 직후 `git diff origin/main -- claude-docs/changelog.md`로 자기 행이 1줄인지 손으로 확인. ⚠️ **3회차(#1038, 2026-09-05)** — 예고된 구조 그대로 또 났다(기능 커밋에서 행 추가 → 리뷰 반영 커밋에서 보강 → rebase). 다만 **훅은 이번에도 안 울렸다**: push 전에 ②(수동 diff 확인)가 먼저 잡았다. 즉 이 하드픽스는 **실전에서 한 번도 발화한 적이 없고** 근거는 테스트 8건뿐이다 — 「옳다」가 아니라 **아직 시험되지 않았다**. ⚠️ **4회차(#1099, 2026-09-11)** — 같은 구조(기능 커밋 행 추가 → 리뷰 커밋 보강 → rebase)로 또 났고 또 수동 확인이 잡았다(훅 미발화 4연속). 곁다리로 **검사 기준 시점** 함정을 적었다(T-210 ⑥): BEHIND 구간의 `git diff origin/main`은 남의 행을 내 삭제처럼 보여 유실로 오판시킨다 — 리베이스 전은 merge-base 기준, 후는 origin/main 기준. ⚠️ **5·6회차(#1113·#1115, 2026-09-13)** — 병렬 PR 둘에서 또 났고 **변종**이 나왔다: 정리 커밋으로 지운 초판 행이 다음 rebase에서 되살아난다(union이 수정·삭제를 표현 못 해 git이 정리 커밋을 `patch contents already upstream`으로 drop — T-210 ⑦, 스크래치 재현). 처방은 **한 커밋으로 합친 뒤 rebase**이고 CLAUDE.md 머지 절 T-210 게이트에 한 문장으로 올렸다. 훅은 이번엔 push가 다른 워크트리로 `cd`해서 나가 원리상 발화할 수 없었다(T-242) — 효력 검증은 #1118 머지 뒤로 미뤄진다. ⚠️ **7회차(#1124, 2026-09-13)** — 6회차 변종이 **실전에서** 그대로 재현됐다(정리 커밋 drop → 초판 행 복귀), 한 브랜치에서 세 번 연속. 6회차 처방(한 커밋으로 합친 뒤 rebase)을 **읽고도** 첫 재발에 정리 커밋을 쌓았다 — prose가 안 먹은 사례. 소프트 리셋으로 3커밋을 1커밋으로 합친 뒤엔 main이 또 전진해도 재발 0. 훅은 push 전 수동 확인이 매번 먼저 잡아 **여전히 미시험**(실전 미발화 7연속). ✅ **하드픽스 2단**(2026-09-13, rebase 직전 게이트 `require-single-changelog-commit-before-rebase.ps1` — `git rebase`/`git pull --rebase` 직전에 `merge-base..HEAD`에서 changelog를 건드린 커밋을 세어 2개 이상이면 차단하고 `git reset --soft <base>`를 안내한다. 우회 `ALLOW_MULTI_CHANGELOG_REBASE`, 테스트 35건, 돌연변이 6종 사살. ⚠️ **훅만으론 안 막혔다** — 세션이 실제로 치는 명령은 `bash .claude/scripts/pr-merge.sh <PR> --rebase`라 `git rebase` 토큰이 없어 게이트를 그대로 지나갔다(리뷰 실측) → 그 스크립트의 `try_rebase()`에도 같은 판정을 심었다(판정이 두 군데인 것이 이 구조의 비용). 곁다리로 **두 훅의 한글 stderr가 CP949로 깨지던 것**을 고쳤다 — `[Console]::Error.WriteLine`을 원바이트 UTF-8 쓰기(`Write-StderrUtf8`)로 바꾸고, 계측은 문자열이 아니라 UTF-8 **바이트 이스케이프**로 세웠다(Git Bash grep은 한글 리터럴에 무성 실패한다). push 게이트가 늦어서 7회 연속 미발화였으니 손상이 생기는 자리 바로 앞으로 옮긴 것 — 정규식이 아니라 git 상태 판독이라 hookify로는 불가) |
| PR 머지 자동화(`pr-merge.sh`) — 백그라운드 hang·미완료·헛폴링·DIRTY-blind·BEHIND 무한대기 | T-083 · T-088 · T-091 · T-094 · T-102 · T-111 · T-141 · T-208 · **T-240** | **9** | ✅ 하드픽스(`pr-merge.sh`: gh API 브랜치삭제 + `timeout`, T-094) + 절차(머지=완료확인) + CLAUDE.md Git워크플로 DIRTY 진단. **T-102(5회차): 하드픽스 안 쓰고 손수 워처→DIRTY 누락. T-111(6회차): "up-to-date 필수 + `--auto` + BEHIND = 무한대기" — `pr-merge.sh`에 BEHIND `gh pr update-branch` 자동해소 + `--arm`("걸고 떠나기") 모드 추가·표준 경로로 승격, bare `--auto` 단독 금지** **T-141(7회차): gh 전멸 시 --arm 거짓 성공 — PIPESTATUS·빈상태·autoMergeRequest 증거 검증 하드픽스** ⚠️ **T-208(8회차)은 이 군의 새 축이다 — 하드픽스가 `gh` CLI에 묶여 있어 `gh`가 없는 원격(웹) 세션에선 통째로 증발했다.** 앞선 7회는 「스크립트를 안 쓰고 손수 했다」(T-102)인데 이번은 **쓸 수 없었다** — MCP로 auto-merge만 걸고 `pr-merge.sh`가 대신 해주던 BEHIND 점검이 빠져 T-111이 그대로 재현됐다. **레포의 승격 산출물을 믿기 전에 「이 환경에서 실행 가능한가」부터 본다** ⚠️ **T-240(9회차)은 T-102의 반복인데 한 칸 어긋났다** — `--arm`까지는 `pr-merge.sh`를 썼고 **그 뒤 「머지됐나」 확인만 손수 루프**로 짰다(`until state = MERGED`). PR이 `DIRTY`(그사이 `#1092`가 들어와 충돌)라 서버가 머지할 수 없었는데 루프는 성공만 겨눠서 **30분을 침묵**했고, 사용자가 물어서야 알았다. CI 3종이 전부 초록이라 「곧 되겠지」로 읽힌 것도 거들었다 — **체크 통과와 머지 가능은 다른 축이다**. 교훈: **도구를 썼다는 사실이 그 도구가 안 덮는 단계를 안전하게 만들어 주지 않는다**(`--arm`은 「걸고 떠나기」라 완료 확인은 애초에 그 스크립트의 몫이 아니다). 대기 루프 일반 원칙은 T-240으로 분리했다 |
| **토스 심사 — 진입 직후 사용자에게 들이대는 것**(설명 없는 즉시 로그인 · 자동 오버레이) | 2026-08-12 인트로 반려(plan 「심사 반려 대응」) · T-183(코치마크 자동 노출) | 2 | ✅ CLAUDE.md 「미니앱 개발 루프」에 원칙 승격(진입 직후 자동 노출 금지 — 시트·딤·모달·툴팁 전부). 훅 강제 불가(「진입 직후 뜨는가」는 정적 분석으로 판정 불가)라 prose + 목 모드 첫 진입 캡처가 게이트다 |
| **네이티브 명령의 종료코드가 파이프·리디렉트에 오염됨 — 양방향이다**(`\| sed`·`\| tee` 뒤 `$?` → **가짜 성공** / `2>$null` 뒤 `$LASTEXITCODE` → **가짜 실패**) | T-126 · T-141 · **T-206** | **3** | ⚠️ **prose 승격(2026-08-23, 3회차)** — `pr-merge.sh`는 PIPESTATUS 하드픽스로 이미 닫혀 있고, 이번에 **군의 이름을 넓혔다**: 옛 이름이 「실패 exit를 가림 → 가짜 성공」이라 **반대 방향(성공을 실패로 읽음)이 같은 군으로 안 읽혔고, 그 경고를 아는 채로 T-206을 밟았다**. 처방은 방향 무관하게 하나다 — **성패를 종료코드가 아니라 산출물로 판정한다.** hookify 승격은 **보류 유지**: `2>$null`+`$LASTEXITCODE` 동시 등장은 정규식으로 잡히지만 **룰은 bash 명령줄만 보고 `.ps1` 파일 내용은 못 본다** — T-126·T-141·T-206 셋 다 스크립트 **파일 안**이라 애초에 안 잡혔을 것이다(룰을 만들면 「가드가 있다」는 착각만 남는다). 4회차가 **명령줄에서** 나오면 그때 승격 |
| **문장이 「저쪽」을 근거로 들었다가 거짓이 됨 — 값은 멀쩡해 테스트가 원리상 못 잡는다** | **T-211** | **6** | ⚠️ **prose만 가능**(2026-08-24) — 「이 문장이 지금 사실인가」는 정적 판정 대상이 아니라 훅·hookify 승격 불가다. 그 사실을 항목에 명시해 다음 사람이 훅을 찾다 헤매지 않게 했다. 처방은 둘로 갈린다: **낡아서 거짓**(축을 뒤집는 PR에서 값과 함께 「그 값을 설명하던 문장」을 훑는다 — 「~와 같은 이유」·「~와 같다」·수치 인용이 후보) / **태어날 때부터 거짓**(시안·설계 단정은 그 자리에서 원본을 연다). ⚠️ **5회차(#953)에서 등급이 올랐다** — 앞의 넷은 코드 주석(거짓이면 사람을 오도)인데, 그건 **게이트가 소비하는 절차 문서**(거짓이면 그 문장이 경고하던 실패를 스스로 일으킨다). **다음 실행의 입력이 되는 문장**(마커·자격 조건·소진 여부)은 낡음의 대가가 오해가 아니라 사고다. ⚠️ 5회 중 넷을 **남이 잡았다**(독립 리뷰 2 · 동료 세션 2) — 내가 쓴 문장을 내가 사실이라 믿으므로 자가 검증이 원리상 약한 군이다 |
| **DOM 계측기가 의도한 요소를 안 잡았는데 결과는 초록이다 — 방향이 둘이다**(못 찾음 → 「문제 없음」 / 엉뚱한 걸 찾음 → 그 값이 사실) | **T-212** | **3** | ⚠️ **prose + 코드 패턴**(2026-08-24) — 훅 승격 불가(선택자가 지금 이 DOM에서 무엇을 잡는지는 정적 판정 대상이 아니다). ⚠️ 이 군이 위험한 이유는 **실패가 통과와 같은 얼굴**이라는 것 — 「잘린 요소 0」은 「검사했고 없다」와 「검사한 적 없다」를 구별하지 못한다. **반대 방향이 더 위험하다**: 0건은 그나마 의심하지만 **값이 나오면 믿는다**(래퍼 div가 잡혀 상속값 `16px`이 나오자 「규칙이 안 먹었다」로 오진 — 실제 잎은 `12px`였다). 처방이 자리마다 다르다 — **남는 도구**엔 「못 찾으면 throw」(`shoot.mjs`가 이미 그 모양), **일회용 측정 스크립트**엔 「필터를 먼저 반드시 잡혀야 하는 것으로 시험(positive control) + 뽑은 요소의 정체를 값과 함께 찍기」. 3회 중 둘이 **같은 필터**(`children.length === 0`)였다 — 잎 가정은 이 앱에서 특히 자주 틀린다(값 조각을 `span`으로 감싸는 패턴이 많다) |
| Vue 섬 번들 stale(산출물 미커밋·CI 사각) | T-063 · T-082 | 2 | ✅ 훅 `require-bundle-build.ps1`(전 10섬) + CI 확장 |
| Service Worker stale 캐시(파일명 고정 자산) | T-071 · T-075 · T-080 | 3 | ✅ 코드 패턴(SW `NETWORK_FIRST` 배열 + `res.ok` 가드 + `CACHE` 버전업) |
| **로컬 브라우저 실측에서 서비스워커가 옛 자산을 먹여 「고친 게 안 먹었다」로 오판** — 옛 번들(#1147) · 옛 CSS(#1151) | **T-251** | **2**(2026-09-16 같은 날) | ✅ **하드픽스(머신 로컬, 2026-09-17)** — 뿌리는 `sw.js`의 cache-first가 기대는 콘텐츠 해시 URL이 **로컬 검증 서버(`bootTestRun`)에서만 꺼져** 있던 것(운영은 해시 URL — `curl` 대조로 확정). `build/run-local-h2.cmd`에 해시 환경변수 두 줄을 넣고 로컬 URL에 해시가 붙는 것·받은 CSS 바이트가 레포와 같은 것을 확인했다. ⚠️ 그 파일은 gitignore된 머신 로컬이라 레포에 안 실린다 — 재생성 절차는 메모리 `local-browser-login-by-user`가 들고 있다. 같은 세션 2회라 「다른 맥락 2회」 기준엔 못 미치지만 올린 이유: 2회차가 1회차 처방(매 이동 차단)을 **따른 채로** 났고, 그 처방용 스텁이 진단 API까지 가렸다 — 의식을 늘리는 대신 뿌리를 뽑아야 한다는 신호였다. 새 PC·새 워크트리에서 3회차가 나면 스크립트 재생성 누락부터 본다 |
| 커밋/git 무한 hang(멀티세션 gradle 데몬·빌드락) | T-078 | 3+ (2026-06-30 45분 freeze 3회차) | ✅ **하드픽스(2026-07-01): 커밋 훅 `gradlew test`에 8분 타임아웃 — 초과 시 `taskkill /T`+`gradlew --stop` 자가복구 후 차단(exit 2)** + CLAUDE.md(bootRun 정리=8080+`--stop`·「무한 hang」절) + 강제정리 절차. ⚠️ **그 상한이 2026-09-09에 거꾸로 물었다(T-235)** — 스위트가 **9분 6초**로 자라 정상 빌드가 8분 게이트에 걸리면서 `.java`가 든 커밋이 전부 막혔다. 하드픽스가 세운 상한은 **스위트가 자라면 만료된다**. 더 나쁜 것은 차단 메시지가 원인을 「likely gradle daemon/lock contention」이라 **단정**해, 경합이 아닌데도 경합을 두 번 확인하게 만든 것이다 — 자가복구 메시지가 진단을 한 방향으로 몰면 **다른 원인은 보이지 않는다** |
| **커밋 테스트 게이트 8분 상한을 정상 스위트가 넘긴다 — 경합이 아닌데 훅 메시지는 경합이라 한다** | **T-235** | **3** (2026-09-09 9분 6초 → 09-11 8분 19초 → **09-21 13분 13초**, 전부 `BUILD SUCCESSFUL`) | 🔜 **승격 대기(2026-09-21 3회차로 조건 충족)** — 임시 처방은 T-235에 있다(게이트 밖 `./gradlew test` 1회 완주 → up-to-date → 커밋, `SKIP_TESTS` 금지). 소요가 회차마다 늘어(3분 23초 → 13분 13초) 「조건부」가 아니라 **스위트 성장**으로 기운다. 남은 작업 = `BOOKTIMER_TEST_GATE_TIMEOUT_MS` 상향(또는 게이트를 변경 모듈로 축소) + 훅 메시지의 경합 단정을 「원인이 둘」로 고치는 **훅 변경 PR 1건**. |
| **돌연변이 주입기가 테스트를 한 번도 안 돌리고 「전부 사살」을 보고한다 — 실행 실패를 테스트 실패로 읽는다** | **T-254** | **2** (2026-09-21, 한 세션 안에서 구현 서브에이전트·리뷰어가 서로 모른 채 각 1회) | ✅ **하드픽스(주입기 3게이트, 2026-09-21)** — ① 주입 단언(치환 1건 매치 + 디스크 바이트 변경 확인) ② **실행 단언(JUnit XML `timestamp`가 그대로면 크래시)** ③ 원본 바이트 복원 + `git diff --numstat` 0 확인. 셸은 `bash.exe` 명시, 사살 목록은 exit code가 아니라 XML `<failure>` 이름에서 뽑는다. ⚠️ 주입기는 **세션마다 새로 쓰는 스크래치 스크립트**라 훅으로 못 막는다 — 다음에 쓸 때 T-254의 3게이트를 복사한다. 3회째면 주입기 템플릿을 `.claude/scripts/`에 상주시켜 매번 다시 짜는 경로 자체를 없앤다. |
| 검증 환경이 실환경보다 관대해 그 클래스가 가짜 green으로 통과(헤드리스 로드순서 → 데스크톱 크롬 ↔ 실기기 → 정적 렌더 ↔ 실 브라우저) | T-053 · T-054(헤드리스가 `defer` 단순화) · T-176(합성·성능) · T-177(iOS UA 기본 글자색) · T-182(정적 렌더에 effect·레이아웃이 없다) · T-183(같은 하니스, 심사 반려 대응의 구멍 2건) · T-191(픽스처에 사진이 없어 못 잡음) · T-192(정지 화면만 봐서 못 잡음) · T-193(**토큰값만 보고 합성 결과를 안 재** 종이가 칙칙해진 걸 못 잡음) | 9 | ✅ CLAUDE.md 「🖥️ 프론트 검증」 — 2회차에 **실 브라우저 게이트**(N-082·N-083), 4회차에 **미니앱 실기기 게이트 + 「미검증 가정은 머지 전 체크리스트」**로 확장. ⚠️ 3·4회차가 말하는 건 축이 하나 더 있다는 것 — 「헤드리스 vs 실 브라우저」로 닫았더니 **「데스크톱 크롬 vs 실기기」**에서 두 번 샜다(성능 여유 / 엔진별 UA 기본값). **5회차(T-182)는 승격이 작동한 사례다** — 미니앱 하니스는 jsdom이 없어 effect·레이아웃·스크롤이 아예 없는데(그 클래스는 단위로 못 잡는다), 이미 세워 둔 실 브라우저 게이트가 오버레이 결함 2건을 잡아 머지 전에 고쳤다. 훅 승격 불가(정적 감지 대상이 아님) — 판단 규칙이 유일한 계측기라, 그 환경에서만 갈리는 축(합성·메모리·UA 기본값·safe-area·effect·레이아웃)을 만나면 그 자리에서 실 브라우저·실기기 확인을 게이트로 세운다 **6회차(T-183)도 그 게이트가 값을 했다** — 심사 반려를 고치는 PR에서 **고친 코드의 새 구멍 2건**(리렌더 스킵 · 인라인 게이트가 열려 재발)을 단위 736건이 전부 GREEN인 채로 통과시켰고 실 브라우저가 잡았다. 즉 이 군의 교훈은 「기능 추가」에만이 아니라 **「반려 수정」에도 적용된다** — 반려 사유를 고쳤다는 판정 자체를 실환경에서 해야 한다. **7회차(T-191)는 축이 또 하나 다르다 — 엔진이 아니라 픽스처다.** 전면 종이결 레이어가 책 표지를 바래게 했는데, 목 픽스처의 표지는 `coverUrl: null`(연한 파스텔 플레이스홀더)이라 **결함이 드러나는 유일한 콘텐츠(어두운 사진)가 검증 경로에 아예 없었다**. 즉 환경을 실환경에 맞춰도 **거기 흘려보내는 데이터가 실데이터보다 순하면** 같은 사각이 다시 생긴다 — 사진·긴 제목·빈 목록처럼 「최악의 콘텐츠」를 픽스처에 한 건씩 심어 둔다. **8회차(T-192)에서 축이 또 갈렸다 — 이번엔 콘텐츠가 아니라 「동작」이다.** 종이 결이 `position: fixed`라 스크롤해도 제자리에 남았는데, 나는 **정지 스크린샷과 computed style만** 봤다. 그 결함은 스크롤이라는 동작 **중에만** 존재해서 정지 관찰로는 원리상 보이지 않는다. **7·8회차를 합치면 규칙은 하나다 — 「그 결함이 사는 차원을 검증이 한 번이라도 지나가는가」**: 콘텐츠(어두운 사진)든, 동작(스크롤·전환)이든, 엔진(실기기)이든. 전면에 깔리는 시각 요소를 만들거나 고쳤으면 **스크롤해 보고** 판정한다. **9회차(T-193)는 축이 「측정 대상」이다** — 환경·콘텐츠·동작이 다 맞아도, 내가 본 것이 **소스의 토큰값**이면 화면의 픽셀은 검증되지 않는다. 종이색 토큰은 #F3EEE4인데 그 위 결이 픽셀의 61%를 덮어 실제로 보이는 색은 #E5E0D6이었고, 나는 스크린샷을 「보고」 넘어갔지 **재지** 않았다. 합성(오버레이·투명도·블렌드)이 끼면 **눈과 소스는 둘 다 증거가 아니다 — 캔버스로 최종 픽셀을 뽑아 수치로 판정한다.** |
| PowerShell 한글 깨짐(커밋 메시지·.ps1 스크립트) | T-026 | 2+ | ✅ CLAUDE.md 「🈲 한글 커밋 `.commit-msg-tmp`」 + 훅 `check-commit-message.ps1` |
| 필터로 흔든 선을 `border-image`로 쓰기 — 타일 이음매 어긋남·`stretch` 압축으로 픽셀 깨짐 | T-188(`round` 이음매) · T-190(`stretch` 압축) | 2 | ✅ **하드픽스: 굴곡(`feDisplacementMap`)을 아예 걷고 농도 얼룩(고주파 알파 노이즈)으로 전환** — 굴곡이 없으면 두 증상이 동시에 원천 소멸한다. 미니앱 테스트가 `PENCIL_FRAME`에 `feDisplacementMap` 부재 + `feComposite` 존재를 못 박는다 |
| 워크트리 머지 `--delete-branch`(로컬 main checkout 충돌) | T-095 | 2 (#511 → #515) | ✅ CLAUDE.md Git워크플로 auto-merge caveat(워크트리에선 `--delete-branch` 빼고 머지 → 원격=gh api·로컬=수동 정리) |
| changelog 멀티세션 동시 append → 같은 위치 머지 충돌 | T-098 | 5+ (2026-06-26 하루, #516·#518·#520·#523) | ✅ 하드픽스(`.gitattributes` `claude-docs/changelog.md merge=union` — 양쪽 새 행 자동 병합으로 rebase 충돌 자체 제거) |
| 전역 `button` 속성 누수(컴포넌트가 명시 안 한 속성을 상속) | T-056 · T-081 · T-099 | width 4+ (이 PR `.pv-hud-dex` 도감 버튼 풀폭) · radius 1 | ✅ 코드 패턴(flex 안 칩·탭·세그먼트엔 `width:auto`·`border-radius:0`로 상쇄, 인라인 주석) — 이번엔 in-code `#286 가드` 주석이 재디버깅을 막아줌(패턴 작동) |
| CSS 주석 속 `*/`(특히 wildcard-slash `.foo-*/.bar-*`)가 주석 조기 종료 → 다음 규칙 침묵 드랍 | T-087 | 3 (#522 .dash-card → #526 .book-*/.record- → 이 PR .oauth-*/.entry-hero) | ✅ **하드픽스 훅 `require-css-comment-safe.ps1`**(3회차에 승격) — 주석 닫는 `*/`가 **양옆 모두 셀렉터문자에 붙은** 경우만 차단(=기존 보류 사유 FP 해소: ` */color`는 앞이 공백·`/*x*/`+개행은 뒤가 공백이라 통과, 잔여 FP는 `/*c*/.sel`류 희귀패턴+우회 토큰 `SKIP_CSS_COMMENT_CHECK`). 보조: 예방 규칙(슬래시→`·`/`과`) + §11 실 브라우저/static-preview 게이트(N-118) |
| 도구 재생성 시 BOM/EOL 미보존 → phantom diff(첫 줄·전체 줄끝) | T-093 · T-103 · T-172 · **T-209 곁다리** | **4** | ⚠️ **기존 승격(코드 패턴)이 3회차를 못 막았다** — 그건 「우리가 짜는 PowerShell 재생성기」에만 적용되는 규약이라, **남이 만든 도구**(`sed -i`)에는 걸 자리가 없다. T-172는 `find -exec sed -i`가 **패턴이 안 맞는 파일까지 전부 다시 써서** 42개 템플릿의 EOL이 뒤집힌 것(git 정규화가 흡수해 무해했지만, `.gitattributes`가 없는 레포였다면 42파일 phantom diff). 승격 후보 = 「`sed -i`를 `find -exec`/글롭으로 다중 파일에 돌리면 경고」 hookify warn 룰(대상 한정 치환은 Edit 도구나 파일별 루프로). ✅ **그 사이 hookify warn 룰로 승격됐다** — 글로벌 `warn-inplace-replace-crlf`(4개 프로젝트 7건 근거). 그 룰 본문은 「돌연변이 확인에서 이게 치명적이다 — 주입 안 됨과 테스트가 못 죽임이 똑같이 초록으로 보인다」까지 이미 적고 있다. ⚠️ **그런데 4회차(T-209 곁다리)를 못 막았다 — 정규식이 내가 쓴 도구를 안 덮어서다.** 패턴이 `sed|perl … -i`와 PowerShell `.Replace(…)…Set-Content` 둘뿐인데, 범인은 **세션이 그 자리에서 짠 python 일회용 스크립트**(`io.open(...,'w').write(s.replace(...))`)였다. 즉 **좋은 룰이 있어도 그 정규식이 내가 지금 쓰는 도구를 안 덮으면 없는 것과 같다.** 승격안(별 레포 `~/.claude/hookify/`): 패턴에 python 제자리 치환을 더한다(예: `io\.open\([^\r\n]{0,80}?['\"]w['\"]` 또는 `python[^\r\n]{0,200}?\.write\(`). 그때까지의 규약은 한 줄이다 — **파일을 통째로 다시 쓸 땐 `newline=''`로 읽고 같은 개행으로 쓴다.** 곁다리 피해가 phantom diff에 그치지 않은 것도 이번이 처음이다 — 개행이 바뀌자 **뒤이은 앵커 매칭이 조용히 전부 빗나가** 원인 추적이 길어졌다 |
| 빈 워크트리 폴더가 cwd 점유로 안 지워짐 | T-105 · T-115 | 2 | ✅ 절차(정션 끊기+`worktree prune`+`branch -D`로 실질 정리 후 빈 폴더는 세션 종료 후 `rmdir` / 워크트리 정리는 그 세션 아닌 **메인·다른 세션**에서) |
| CSRF 숨김필드 lazy 세션 ↔ 응답 버퍼 커밋 타이밍(큰 SSR·익명 폼 500) | T-033 · T-049 | 5+ | ✅ prose CLAUDE.md 「🔒 CSRF 폼 세션 선확정」(N-077) + **공유 헬퍼 `CsrfTokenUtil.precommit`로 9곳(기존3+신규6) 통일**. 1회 백필 소급 등재(2026-06-30) → 누락 6곳(Settings·AdminFeedback·Feedback·EmailVerification·Unsubscribe·Onboarding) **수정 완료**. 훅 미채택=컨트롤러↔템플릿 역매핑·POST 재렌더 FP 과다. **5회차(2026-09-15)**: 양옆 바 공통 조각 추가로 `/sessions/manual` 500 → precommit 10곳째. 기존 MockMvc 테스트가 잡아 새 가드 불요 |
| FK 자식 미정리로 부모 삭제 실패(테스트가 못 잡음) | T-023 · T-029 · T-168 | 3 | ✅ **하드픽스(2026-08-15): `FlywayMigrationTest`가 「실 마이그레이션 스키마의 users FK 집합 == `purge()`가 지우는 집합」을 양방향 단언**. ⚠️ **2회차 승격(prose "H2 통합테스트 필수")은 부족했다** — 그 통합테스트가 이미 있었는데도 운영 27명 중 2명이 탈퇴 불가였다. 메인 스위트의 H2 스키마는 **엔티티 매핑에서** 생성돼 JPA 미매핑(Flyway 전용) 테이블이 아예 없고, 픽스처도 손으로 고르는 방식이라 목록 밖 테이블은 영원히 시야 밖이기 때문. 손으로 고르는 테스트 대신 **스키마에서 파생되는** 단언이어야 한다 |
| author `display`(flex/grid)가 UA `[hidden]`/`<details>` `display:none`을 origin 우선으로 덮어 숨김 실패 | (#189) · T-035 · T-123 | 3 | ✅ **하드픽스(전역 `[hidden]{display:none!important}` 리셋을 `app.css` 베이스에 추가)** — `[hidden]` 속성 변형을 앱 전역에서 제거(T-123의 페이지 스코프 `.settings-page [hidden]` 리셋은 이걸로 승격돼 삭제). ⚠️ 닫힌 `<details>` 자식 변형(T-035)은 `[hidden]` 속성이 아니라 UA `details:not([open])` 메커니즘이라 이 리셋 밖 — 개별 `:not([open])` 재숨김 유지 필요 |
| docker exec mysql 한글 시드 INSERT 깨짐(CP949/이중 인코딩/mojibake) | T-085 · T-119 | 3 | ✅ 절차 확립(T-085 보강 — UTF-8 파일 + `docker exec -i … < file.sql` + `--default-character-set=utf8mb4`, `HEX()` 검증; T-119의 `UNHEX()` 주입은 대안). 검증 데이터 셋업 한정·저빈도라 CLAUDE.md/훅 승격은 보류 — 4회차 나오면 재검토 |
| 어필리에이트 클릭 추적 무성 실패(생성 링크·제휴키·옵션 미탑재로 클릭 0) | T-129 · T-131(알라딘·YES24) · **T-202(알라딘 `&amp;`)** | **3** | ⚠️ **판정 기준을 격상했다(2026-08-22, 3회차)** — 옛 기준 **"링크에 추적자가 실제로 실렸나"가 바로 이 3회차를 통과시켰다**: `&amp;ttbkey=` 에도 `ttbkey` 문자열은 실려 있어서 문자열 존재 검사로는 정상으로 보인다(백필의 마커 판정도 같은 이유로 47권 전부를 건너뛰었다). **새 기준 = "추적이 실제로 먹히나"** — 링크를 한 건 열어 **귀속 신호**(알라딘 `partner=UID=…&pname=…` 쿠키 / 쿠팡 파트너스 리포트 클릭 / YES24 linkprice 래퍼 통과)를 확인한다. ⚠️ **T-131의 「닫았다」가 거짓이었다**: `includeKey=1`+백필(#644, 2026-07-04)로 ttbkey를 링크에 실었지만 그 링크가 `&amp;`라 여전히 무추적이었고, **그 뒤 7주를 더 그렇게 굴렀다**(운영 실측 2026-08-22: 47권 중 47권 깨짐·정상 0·클릭 22회 전부 무추적, 최근 등록 08-13). 즉 이 군에서 「제공자별 가드로 하나씩 닫는 중」이라는 상태 표기 자체를 믿으면 안 된다 — **닫혔다는 판정을 무엇으로 했는지**까지 봐야 한다. **범용 런타임 assert는 이번에도 보류** — 관문이 제공자마다 다르다는 원래 근거는 그대로고, 3회차가 드러낸 것은 관문 종류가 아니라 **판정 방법**이라 승격할 것은 assert가 아니라 위 기준이다. 알라딘은 회귀 테스트로 닫았다(`AladinBookSearchClientTest` 엔티티 복원 + 정상 링크 불변). 4회차가 나오면 그때는 저장 직전 링크의 쿼리 파라미터를 실제 파싱해 제휴 키가 **독립 파라미터로 서는지** 검사하는 공통 가드로 승격한다 |
| 공허한 테스트 — 통과하는데 겨눈 규칙을 지워도 안 깨진다(검사 키가 남의 문자열과 겹치거나, 매처가 그 실패를 안 매칭하거나, 하니스가 그 동작을 아예 안 돌림) | T-144(정적 마크업 `toContain` × TDS 주입 CSS) · T-145(Mockito `never()`+`anyString()`이 null 미매칭) · T-149(정적 렌더가 effect 미실행 → 부정 카운트 단언 상시 통과) · T-161(테스트 스키마에 그 DB 제약이 아예 없음) · T-180(목 모드가 fetch를 안 태워 네트워크 탭 요청 계수가 항상 0) · T-181(`indexOf` 순서 단언이 대상 부재 시 `-1`로 상시 참) · T-205(`toContain`이 주석 처리된 줄까지 매칭) · **T-241(새 선행 가드가 옛 테스트의 픽스처를 거부해 게이트에 닿지 못함)** · **T-243(한글 픽스처가 CP949로 읽어도 파싱을 안 깨뜨림 — 끝바이트 정렬)** | **9** | ⚠️ **prose 승격 유지**(글로벌 CLAUDE.md·implementer 규약의 「돌연변이 확인」이 담당 — T-144·T-145는 돌연변이 확인이 잡아냈고, **T-149(3회차)는 같은 규약 덕에 작성 전에 사전 회피**됐다. 규칙이 세 번 연속 작동해 새 계단은 여전히 불필요). **T-161(4회차)은 탐지 경로가 다르다** — 돌연변이 확인이 아니라 **설계 문서가 깐 전제("H2가 실 DB 제약을 탄다")를 구현자가 의심해** 작성 전에 회피했다. 즉 이 군의 방어선은 돌연변이 확인 하나가 아니라 「계측기가 그 실패를 볼 수 있는가」를 **쓰기 전에 묻는 습관**이고, 그건 정규식으로 잡을 수 없어 계단을 올릴 대상이 아니다. **T-181(6회차)은 세 번째 탐지 경로다** — RED 단계에서 「예상 실패 수 ≠ 실제 실패 수」라는 산술이 잡았다(돌연변이 확인은 GREEN 이후라 이보다 늦다). 「RED를 봤다」가 아니라 「예상한 것이 전부 죽었나」를 세는 것이 이 군의 가장 이른 방어선이다. **hookify warn 후보** `never()`+`anyString()`/`anyLong()` 동시 등장 감지는 보류 유지 — 3회차(T-149)는 그 조합이 아니라 별개 메커니즘(정적 렌더 × 부정 단언)이라 정규식 한 줄로 못 잡는다; Mockito 조합이 재출현하면 그때 승격. **T-205(7회차)는 이 군에서 처음으로 앞선 그물이 전부 뚫린 사례다** — 돌연변이 확인·전제 의심·RED 산술 셋 다 놓쳤고(세 단언 모두 「지우면 깨진다」는 통과했다. 구멍은 지우기가 아니라 **주석 처리**에서만 열렸다) **머지 후 `reviewer` 독립 리뷰**가 잡았다. 3건이 2026-08-12부터 그 상태로 운영에 실려 있었다. 그래도 계단은 안 올린다 — 처방이 「부분문자열 대신 줄 앵커」라는 **코드 패턴**이고, 「이 단언이 raw 텍스트를 겨누는가」는 정규식으로 판정할 수 없다(`toContain` 전수 금지는 오탐이 압도적이다). 대신 이 군의 표준 검사에 **「지우기뿐 아니라 주석 처리로도 죽여 본다」**를 더한다. ⚠️ 묶는 근거는 도구(vitest/Mockito)가 아니라 **"통과가 증거가 아닌 상태"**다(탐지 수단은 돌연변이 확인 하나가 아니라 넷으로 늘었다 — 돌연변이·전제 의심·RED 산술·독립 리뷰). **T-241(8회차)은 T-205와 같은 경로로 잡혔다** — 앞선 셋이 놓치고 독립 리뷰의 **재선정 돌연변이**가 잡았다(2회 연속). 새 메커니즘은 「새 선행 가드가 옛 테스트의 최소 픽스처를 거부값으로 만든다」이고, 표준 검사에 **「가드를 앞단에 끼우면 옛 게이트를 제거해 옛 테스트가 붉어지는지 본다」**를 더한다. 계단은 여전히 안 올린다 — 「이 픽스처가 새 가드에 걸리는가」는 정규식 판정 대상이 아니다. **T-243(9회차)은 RED 산술 경로로 잡혔다** — 재현 실패(29조합 전부 초록)를 「버그 없음」이 아니라 「픽스처가 그 실패를 못 일으킨다」로 의심해 디코더 출력을 덤프했다. 이번엔 **기존 테스트 자체가 그 회귀를 겨눈다고 주석에 적혀 있었다** — 의도를 적어도 판별력은 생기지 않는다. 표준 검사에 **「인코딩·경계 픽스처는 수정 전 코드에서 붉어지는지 먼저 본다」**를 더한다. 계단은 안 올린다 — 「이 한글이 CP949 선행바이트로 끝나는가」는 픽스처 선택의 문제라 정규식 대상이 아니다 |
| `deploy.sh --expect` 마커가 번들에 없어 배포 차단 — 소스엔 있는데 산출물엔 그 형태로 안 실린다 | T-153 (① minify 변형 `slice(0,15)`→`slice(0,EGe)` · ② 런타임 조립값 `11시간 5분`) | 2 | ✅ **prose 승격 유지·보강**(CLAUDE.md 「미니앱 개발 루프」) — 기준을 "UI 한글 문자열"에서 **"소스에 따옴표째 적혀 있어 번들에 그대로 실리는 문구"**로 고쳤다(1회차 기준으로는 2회차를 못 걸렀다 — `11시간 5분`도 한글 UI 문구다). 판별법 = 소스에서 따옴표째 검색 가능한가. **새 UI 문구가 0인 변경은 마커 없이 배포**(클린 빌드 + `.ait` 해시 검증). 훅 승격 보류 — 마커 자격은 "그 문자열이 소스에 리터럴로 있는가"라 정적 정규식으로 못 판정하고, 게이트가 이미 배포를 **차단**하는 쪽으로 실패해 사고로 안 이어진다(비용=재배포 1회) |
| 자동화가 정상 배선인데 산출물 0건 — 확인 스텝이 없어 무성 실패 | T-129 · T-131(제휴 추적 클릭 0) · T-138(백업 cron 0건) · **T-214(예산 경보 미도달)** | **4** | ⚠️ **부분 승격** — 배포에는 `deploy.yml`의 `Verify live health`(산출물=실서비스 200)가 이미 이 역할을 하고, 제휴는 제공자별 가드로 하나씩 닫는 중. **백업엔 대응물이 없어서 엿새를 몰랐다** → 다음 승격 후보 = 산출물 존재 확인 자동화(백업 성공 시 CloudWatch 커스텀 메트릭 방출 + 36h 미도달 알람). ⚠️ 메커니즘은 군마다 다르다(URL 조립 / cron 권한) — 묶는 근거는 **"배선·상태는 전부 정상으로 보이는데 산출물만 0이고 아무도 안 알려준다"**는 형태와, 그 해법이 하나같이 **산출물을 직접 세는 게이트**라는 점이다. ⚠️ **4회차(T-214)가 이 군의 한계를 드러냈다** — 앞의 셋은 산출물이 우리 쪽에 있어(클릭 테이블·S3 버킷) 「직접 세는 게이트」가 원리상 가능했는데, **경보 이메일은 산출물이 남의 받은편지함이라 셀 대상 자체가 없다**. 이 얼굴엔 게이트가 아니라 **설정 직후 도달 1회 확인**이 유일한 처방이다 |
| 테스트 스키마 출처 착각 — 마이그레이션에만 있는 테이블·제약은 메인 스위트(Hibernate 생성)에 **존재하지 않아 위반될 수도 없다**(무성 그린 / 영영 안 되는 RED) | T-168(테이블·FK) · T-169(제약·UNIQUE) | 2 | ✅ **승격(2026-08-15): hookify warn 룰 `.claude/hookify/warn-migration-schema-test.md`** — `db/migration/V*.sql`에 DDL(create/alter table · add constraint · index)을 쓰는 순간 ① 검증은 `FlywayMigrationTest`에(엔티티 매핑에 있나 마이그레이션에만 있나로 자리 결정, 손으로 고른 목록 대신 스키마 파생 단언) ② 버전 번호는 `origin/main` 기준(T-170도 같은 트리거라 한 룰이 덮는다)을 띄운다. T-168의 하드픽스(FK 집합 양방향 대조)는 *그 결함* 전용이라 「자리 선택」 자체는 무방비였다 — 그래서 같은 날 제약 면에서 또 밟혔다. 발동 실측 5케이스(마이그레이션+DDL 2건 발동 / UPDATE만·다른 경로 .sql·java 3건 침묵). block이 아닌 warn인 이유: 「검증을 제대로 뒀는가」는 정규식으로 판별 불가라 차단하면 오탐이 확실 |
| TDS 컴포넌트가 넘긴 `style`의 키를 걸러낸다 — 소스엔 있는데 DOM엔 없어 정렬·표시가 조용히 죽는다 | T-198(기록 화면 `textAlign: 'right'`) · T-216(빈 상태 안내 5곳 `textAlign: 'center'`) · **T-219(공부 일정 요일 머리글·월 제목 — T-216 sweep 이틀 뒤 #997이 같은 꼴을 새로 썼다)** | **3** | ✅ **하드픽스(2026-09-02, 3회차)**: `miniapp/src/text-style-guard.test.ts`가 제품 `.tsx`의 모든 `<Text …>` 여는 태그를 훑어 `style={{ … }}` 리터럴에 `textAlign` 키가 있으면 파일·태그째 나열하며 실패한다(빈 가드 하한 + 계측기 양성·음성 대조 포함). 정렬은 TDS 공개 prop `textAlign="center"`로 준다(래퍼 div도 유효). ⚠️ **2회차의 「훅 승격 불가 — 부모 상속까지 봐야 죽은 키인지 정해져 정적 판정이 안 된다」는 판정 대상을 잘못 잡은 말이었다** — 「화면이 틀렸는가」는 부모에 달렸지만 「이 키가 DOM에 실리는가」는 부모와 무관하게 **항상 아니오**라 정적으로 정해진다(죽은 키 삭제는 언제나 안전). 2회차 처방 「리포 전체 sweep」은 기존 자리만 닫고 **새로 쓰는 손**을 못 막아 이틀 만에 재발했다 — 그래서 가드다. `display` 키도 같은 함정(`'block'`→`inline-block`)이나 96곳/16파일이 남아 이번 가드엔 안 넣었다(정리하는 날 `DEAD_KEYS`에 추가) |
| 맨 `<button>`의 UA 기본값·기본 동작이 안쪽 콘텐츠로 샌다(코드엔 흔적이 없다) | T-177(iOS 기본 글자색 = 시스템 블루) · T-178(늘어난 버튼이 내용을 세로 가운데로 밀어 표지가 어긋남) | 2 | ⚠️ **전역 리셋으로 닫히는 건 절반뿐** — 색은 `global.css`의 `button { color: inherit }` 한 줄로 전 호출부가 닫혔지만(T-177 하드픽스), **내용 세로 가운데 정렬은 전역으로 못 끈다**(`button { display: block }` 전역화는 인라인 배치를 다 깨뜨린다) → 호출부 규약(`<button>`을 격자·flex 칸으로 쓰면 컨테이너에 `align-items: start`)이 유일한 수단. hookify 승격 보류 — 「격자 칸이 버튼인가」는 JSX 구조 판정이라 정규식 한 줄로 안 된다. 위 「전역 `button` 속성 누수」 군(T-056·T-081·T-099)과 **방향이 반대**다(그건 author 규칙이 버튼으로 새는 것, 이건 UA 기본값이 콘텐츠로 새는 것) |
| 규칙을 **설명하는 주석**이 그 규칙의 **소스 문자열 계측**을 깨뜨린다(구현은 옳은데 테스트가 붉어짐) | T-203(css 전환 규칙 → 여백 진입 게이트) · **T-215(plan.md 마커 인용 → 마커 집계)** | **3** | ✅ T-203 등재 + 계측 전 주석 제거를 절차로. ⚠️ **3회차는 매체가 다르다** — T-215는 소스 테스트가 아니라 **마크다운 문서의 마커 집계**라, 예고해 둔 `readSourceWithoutComments()` 헬퍼가 애초에 안 맞는다. **그 헬퍼 승격 트리거는 「소스 문자열 계측」 얼굴 기준으로 여전히 2회**이고, 문서 얼굴엔 별도 처방을 박았다(plan.md 머리에 「옛 마커를 산문에 인용할 땐 이모지를 그대로 쓰지 말 것」). 교훈: **같은 뿌리라도 계측기가 사는 곳이 다르면 처방이 갈린다** — 회차만 세고 예고된 승격을 기계적으로 당기면 안 맞는 도구를 만든다 |
| 배경 탭이 CSS 트랜지션을 얼려 `getComputedStyle`이 보간값을 준다 — 앱 결함으로 위장 | T-196(독서등 토글, 「일부만 낡음」으로 감별) · **T-207(로드 시 발화 — 전부 한 값이라 비대칭이 없고, 얼어붙은 시작값이 UA 기본 검정이라 「전역 스타일이 안 붙었다」로 위장)** | **2** | ⚠️ **기존 승격(글로벌 CLAUDE.md 「트랜지션 건 속성은 판정 근거가 못 된다」)이 2회차를 못 막았다** — 그 문구의 감별 신호가 **「값이 일부만 낡았다」는 비대칭**인데, 상속 속성(`color`)에 걸면 화면의 거의 모든 글자가 **같은 값으로** 얼어 비대칭 자체가 없다. 그래서 감별을 관찰에서 **프로브**로 바꾼다: `body.style.setProperty(prop, <새 값>, 'important')` 직후 computed가 **안 움직이면 관측 경로가 얼어붙은 것**이다(1콜·항상 결론, 강제 layout으로는 안 풀리고 스크린샷으로 페인트를 강제해야 풀린다). 두 번째 축은 **트리거**다 — T-196의 프레이밍은 「토글을 눌렀다」인데 T-207은 **페이지 로드**였다(정적 css가 안 잡는 상속 속성을 런타임 주입 스타일이 나중에 바꿈). 그래서 코드 쪽 처방도 한 줄 생겼다: **상속 속성에 트랜지션을 걸면 그 초기값을 정적 css에서 확정한다**(로드 시 발화 제거 = 계측 신뢰도 회복 + 실사용자의 로드 페이드 제거). 훅 승격 불가(관측 시점 문제라 정적 감지 대상이 아님) |

| 소스 문자열 단언이 공허하다 — 「존재」만 봐서 선언문·머리 호출·목 우회에 걸려 배선을 지워도 초록(무계측 배선) | 1회차(2026-09-01 공부 서재 PR-2 리뷰 W3 — 항목 없이 changelog에만, 「무계측 배선」이라 이름만 붙임) · **T-218(2회차, 2026-09-02 타이머–책 연결 3개 PR 리뷰에서 8종 생존)** | 2 | 소프트 — T-218 재발방지 ①~③(건수·전체 인자열 · 돌연변이는 리뷰어가 고른다 · 목 왕복은 클라 함수 경유). **3회째면 CLAUDE.md 「미니앱 테스트 하니스」 절에 승격**(정적 감지 불가라 훅은 아님) |
| 브라우저 자동화의 CDP 명령이 타임아웃 — 원인이 코드가 아니라 그 탭·패널의 상태다 | T-154(클로드 내부 패널 미개방) · T-179(데스크톱 크롬 기기 모드) · T-184(같은 탭 재사용, 입력에 더해 **스크린샷까지**) · 4회차(2026-08-19, T-184 후일담에 병합 — 새 탭으로 시작했는데도 도중에 상함) · 5회차(2026-08-19 독서등 검증, 같은 후일담에 병합 — 새 탭도 몇 번 만에 상해 **재발률이 낮지 않음**이 굳어짐) · 6·7회차(2026-08-19·20, 같은 후일담에 병합 — **새 절차가 두 번 연속 값을 했다**: 판정 수치를 먼저 뽑아 둬 잃은 것이 그림뿐이었다) · 8회차(2026-08-20 기록 더미, 같은 후일담에 병합 — 세 번째로 값을 했고, 이번엔 **그림을 얻고 나서 죽어** 손해가 0이었다) · **9회차(2026-08-20 둘러보기 화면, 같은 후일담에 병합 — 처음으로 탭 교체가 안 통했고 내부 브라우저 대체 경로도 T-154로 동시에 막혀 그림을 끝내 못 얻었다. 판정은 수치 선행 덕에 완결)** · **10회차(2026-08-21 세션 캐시 검증, T-201 — 이번엔 CDP가 상한 게 아니라 **어제 승격한 1순위 표면 자체가 안 떴다**: `chrome-devtools` MCP가 프로필을 배타 점유해 `The browser is already running for …chrome-profile`로 첫 호출부터 즉사. 내부 패널의 **동기 `javascript_tool` 판독**으로 우회해 판정 완결, 그림만 못 얻음)** · **11·12회차(2026-09-02 타이머–책 연결, T-218 ④에 병합 — 이번엔 탭이 상한 게 아니라 **엉뚱한 서버를 보고 있었다**: `preview_start`가 `.claude/launch.json`을 세션 cwd에서 찾아 남의 워크트리 vite를 5300에 띄운 것 + 다른 세션이 공용 `seed` 탭을 자기 포트로 돌린 것. 둘 다 「내 변경이 화면에 없다」로 나왔고 `fetch('/src/<파일>')`의 신규 ASCII 식별자 유무로 갈랐다)** | 12 | ✅ **예고대로 절차 승격**(CLAUDE.md 「🖥️ 프론트 검증」) — 검증 시작 시 `tabs_create_mcp`로 **전용 탭을 새로 연다**. ⚠️ 4·5회차가 그 절차의 한계를 실측했다: 새 탭으로 시작해도 **세션 도중에 죽는다**. 절차는 유지하되(비용 0) 「한 번 열면 안전」이라는 기대는 버리고, 죽으면 그 자리에서 또 갈아탄다 — 승격 단계를 더 올릴 자리는 아니다(훅으로 감지할 수 있는 대상이 아니고, 재시도 비용이 이미 최소다). 대신 **검증 순서를 바꾸는 것이 값을 한다**: 스크린샷보다 `javascript_tool` 수치 측정을 앞에 두면(CDP를 안 타는 경로) 탭이 상해도 판정 자체는 남는다. ⚠️ 3회차가 군 정의를 넓혔다: 「읽기는 되는데 **입력만**」인 줄 알았는데 `Page.captureScreenshot`도 같이 죽었다 — 안 죽는 건 **CDP를 안 쓰는 경로**(`javascript_tool`·콘솔 읽기)라, 판별 기준은 「읽기냐 입력이냐」가 아니라 **「CDP를 타느냐」**다. ⚡ **2026-08-21 — 이 군을 피하는 경로를 찾았다(T-199)**: `chrome-devtools` MCP는 확장과 **별개 크롬·별개 CDP**라 탭이 상하든 패널이 닫혔든 영향을 안 받고, 기기 모드도 `emulate` 명령으로 건다. 9회차가 끝내 못 얻은 그 그림을 첫 시도에 얻었다. 이제 순서는 **확장 1회 갈아타기 → `chrome-devtools` MCP → 수치 마감**이고, 로그인이 필요 없는 화면이면 애초에 `chrome-devtools`가 1순위다(빈 프로필이라 인증 화면엔 못 쓴다). **이 군은 이제 「예상된 비용」에서 「우회 가능」으로 내려간다**. ⚠️ **하루 만에 그 1순위의 반례가 나왔다(T-201, 10회차)** — `chrome-devtools`는 전용 프로필을 **배타 점유**해서, 앞선 크롬이 그 프로필을 쥔 채 남아 있으면 첫 호출부터 통째로 안 뜬다. 「별개 CDP라 면역」은 **다른 함정에 안 걸린다는 뜻이지 항상 뜬다는 뜻이 아니다**. 그래서 계단이 하나 더 필요하다 — 두 크롬이 다 막히면 **내부 패널의 동기 `javascript_tool` 판독**이 마지막 수단이다(합성이 죽어도 동기 JS 실행·텍스트 읽기는 살아 있다). 결론은 9회차와 같다: **판정 수치를 먼저 뽑고 그림은 나중에** 두면, 어느 표면이 죽어도 잃는 건 그림뿐이다. 🌍 **2026-08-21 글로벌 승격** — 전 프로젝트 재발 스캔에서 boardgame-hub에도 같은 군이 2건(T-003 `visibilityState` 정지 · T-008 클릭·스크린샷·타이머 동시 사망) 발견돼 **2개 프로젝트 11회**가 됐다. 규칙 본체를 글로벌 `~/.claude/CLAUDE.md` 「🖥️ 브라우저 확인」으로 올리고, 이 레포 CLAUDE.md엔 포인터 + 고유분(목 모드 포트·스토어 스크린샷)만 남겼다. ⚡ **합치면서 우리가 몰랐던 절반이 붙었다** — 같은 원인이 스크린샷·클릭만 죽이는 게 아니라 **`setTimeout`/`setInterval`을 스로틀링하고 `document.visibilityState`를 `'hidden'`으로 만들어 앱 폴링까지 설계대로 멈춘다**(그래서 "화면이 굳었다"로 보이고 앱 버그로 오진한다). 우회는 게터 덮어쓰기 |
| Windows aws CLI ↔ 한글 = CP949 치명 실패(요청 paramfile 디코드 불가 · 응답 인코드 불가) | (요청 방향: 2026-08-18 정기작업 지시서에 선기록) · T-186(응답 방향) | 2 | ⚠️ prose(T-186) — **한 방향만 막으면 반대 방향에서 또 밟는다**. 지시서가 「요청 JSON에 한글 넣지 마라」를 이미 못 박아 뒀는데도, 응답에 한글이 실려오는 것까지는 안 적혀 있어 그대로 죽었다. 해법이 양방향 공통(원격에서 `base64 -w0` → 로컬 디코드)이라 규칙도 **"aws CLI와는 ASCII만 주고받는다"** 한 줄로 합친다. hookify warn 후보 = `aws ssm send-command`에 `base64` 부재 시 경고 — 빈도가 낮아 보류, 3회차에 승격 |
| 목 픽스처가 서버 계약을 재현 못 해 **정상 경로가 목에서만 실패**(자기모순 → 기능 확장 뒤 협소) | T-175(1회차 자기모순 · 2회차 책축 경로 누락) | 2 | ⬜ 미승격 — 발견 지점이 브라우저 수동 게이트뿐이라 훅·정적 검사로 못 잡는다. 3회차면 「목↔서버 계약 대조 테스트」 도입 검토 |
| **토스 심사 — 내비게이션·탭바 브랜딩 규칙**(토스 셸이 이미 제공하는 것을 미니앱이 자체 구현해 **중복**) | 2026-08-12 탭바 플로팅 반려(plan 「심사 반려 대응」) · **T-220**(자체 「돌아가기」 알약이 네이티브 뒤로가기와 동시 노출) | **2** | ✅ **CLAUDE.md 「미니앱 개발 루프」에 한 줄 승격** — 검수 요청 직전 [비게임 출시 체크리스트](https://developers-apps-in-toss.toss.im/checklist/app-nongame)의 **필수 항목을 대조한다**. ⚠️ 두 번 다 **직전 통과분 diff에 없던 것**이 반려됐다(알약은 08-16부터 열 번쯤 통과했다) — 그래서 이 군에서는 T-185의 「diff에 안 닿으면 무변경 재제출」이 **위험하다**: 판정 편차와 규칙 위반은 증상이 같고(직전 통과분과 무관한 사유), 가르는 것은 diff가 아니라 **그 사유가 공개 필수 항목에 있는가**다. 위 「진입 직후 들이대는 것」 군과 뿌리는 같은 토스 심사지만 **축이 다르다** — 그건 타이밍·동의이고 이건 **토스 셸과의 중복**이다. 훅 강제 불가(「네이티브와 동시 노출인가」는 정적 판정 대상이 아니다) — 보조 계측기로 소스 가드 `miniapp/src/native-back-guard.test.ts`가 「돌아가기」 리터럴의 **재출현**만 막는다 |
| **실행 범위를 좁히면 실패가 안 보인다 — 타깃·단일 파일 실행은 속도용이지 판정용이 아니다** | T-229 · **T-238** | **2** | ⚠️ **prose만**(2026-09-11) — 훅 승격 불가에 가깝다: 「지금 안 돌린 파일에 실패가 있는가」는 돌려 봐야만 알 수 있고, **커밋 게이트의 전체 스위트가 이미 그 역할을 한다**(잡는 시점이 늦을 뿐이다). 두 회차의 얼굴이 다른 것이 핵심이다 — T-229는 **돌연변이 생존 판정**(다른 파일의 테스트가 그 돌연변이를 잡는데 안 돌려서 「생존」으로 오판), T-238은 **회귀 탐지**(`@InjectMocks`에 `@Mock`이 빠져 남의 파일 4건이 NPE인데 타깃 실행은 영영 초록). 공통 뿌리는 **「내가 보는 범위」와 「실패가 사는 범위」가 어긋났는데 결과가 초록이라 어긋남 자체가 안 보인다**는 것. 처방도 하나다 — **속도를 위해 좁혀 돌리는 것은 좋지만, 「됐다」는 판정은 전체 스위트에서만 한다.** 특히 ⓐ 공유 서비스(purge·공통 헬퍼)에 의존을 더했을 때 ⓑ 돌연변이 생존을 선언할 때는 좁은 실행 결과를 근거로 쓰지 않는다 |
| **bootRun이 서빙하는 정적 리소스가 소스보다 낡다 — 실체는 `build/resources/main` 복사본** | T-187(① 복사본 미갱신 · ② 해시 URL) · **2026-09-11 #1098 재발(①, 가짜 RED)** | **2** | ✅ **하드픽스(2026-09-12)** — `frontend/e2e/pencil-frame.spec.ts` 첫 테스트가 서빙 `app.css`와 소스의 sha256을 대조해 불일치면 `processResources`를 돌리라며 **이름 붙여 실패**한다. 판별력 실측: 소스에만 마커를 넣은 낡은 상태에서 가드만 빨갛고 **나머지 9건은 옛 CSS를 재고 초록**(가드 없으면 그 초록이 「통과」로 읽힌다). 기각한 대안 = e2e 스크립트에서 gradle 호출(bootRun과 빌드 락 경합, T-078). ⚠️ 한계: CSS만 본다(`js`·템플릿은 미포함)·`pencil-frame` 스위트에만 붙어 있다. |
| **미니앱·웹 번들이 이번 소스·lock이 아닌 옛 입력으로 조용히 빌드된다 — 산출물 검사로는 신·구를 못 가른다** | T-150(① 빌드 무성 no-op → 옛 dist) · **T-244(② 정션이 가리키는 main의 옛 `node_modules` → 옛 SDK)** · **T-246(③ 워크트리 `frontend/node_modules`가 dependabot 전 설치본 → 웹 섬 11개가 옛 vue로 재빌드)** | **3** | ✅ **하드픽스** — `miniapp/deploy.sh` ① 클린 빌드 + 이번 릴리스 마커(T-150) · ⓪ **빌드 전에** 직접 의존성 설치본을 `package-lock.json`과 대조해 다르면 차단(T-244, 2026-09-14) · 커밋 훅 `require-bundle-build.ps1`도 재빌드 **전에** 같은 대조(`.claude/hooks/lib/deps-match-lock.js`)로 차단(T-246, 2026-09-15). 스모크 `miniapp/tests/test-deploy-gate.sh` Case 6·8 · `.claude/hooks/tests/test-require-bundle-build.sh` Case 8·9. ⚠️ 세 회차 모두 번들 **내용** 검사(마커·env·해시·「재빌드 diff 0」)는 통과하는 모양이었다 — 옛 입력으로 만들면 커밋된 번들과 재빌드가 **같이 낡아** diff가 0이다. 4회차가 나오면 「빌드에 들어간 입력」 중 아직 대조하지 않은 것(env 파일·node 자체 버전·전이 의존성)부터 본다. |
| **컴포넌트가 재마운트될 거라 믿었는데 React가 같은 fiber로 이어 간다 — 「분기가 다르다」는 재마운트의 근거가 아니다**(키 없는 최상위 프래그먼트는 배열로 풀려 맞춰진다) | **T-256** | **2**(#1186 「책 \| 여백」 탭이 같은 함정을 진단 없이 우회 → #1187에서 뿌리 확정) | ✅ **하드픽스**(`miniapp/src/profile-identity-guard.test.ts` — 소스 스캔으로 `<Profile>` 호출부마다 `key`와 `loginId`가 같은 식인지 본다) + 호출부 3곳에 `key={loginId}`. ⚠️ **가드가 확정하는 것은 「호출부가 key를 단다」까지다** — 재마운트 자체는 미니앱 하니스(`renderToStaticMarkup`, jsdom 없음)가 원리상 계측 못 해 **목 모드 실측이 게이트**다(T-149 계열). 다른 컴포넌트로 3회차가 나오면 이 가드를 이름 인자로 일반화한다(`openingTags(src, name)`는 이미 공용) |

## 누적 갱신 잔여

| 2026-05-31 | 초안 + T-001~T-004 |
