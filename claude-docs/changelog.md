# BookTimer — 갱신 이력 (changelog)

> plan.md의 `## 🔄 갱신 이력` 표를 여기로 분리했다(2026-06-08). plan.md는 본문(전략·로드맵·설계·백로그)만 두어 통독 가능하게 유지하고, 작업별 변경 일지는 여기에 누적한다.
>
> **누적 규칙**: 모든 의미 있는 작업(PR)은 이 표 **맨 아래에 한 줄**(일자 / 한 일·PR 번호)을 추가한다 — 해당 PR 브랜치 안에서(사후 보충 금지). 디테일은 링크된 PR body·git 로그·[learning-notes](learning-notes.md)에 있으니 여기선 한 줄 요약 + PR 번호면 충분하다. **plan.md에는 갱신 이력을 두지 않는다** — 본문(전략·로드맵·설계)만 두고, 변경 일지는 이 파일을 단일 출처로 한다(2026-06-10 발췌 폐지).
>
> 관련: [CLAUDE.md](../CLAUDE.md) · [plan.md](../plan.md) · [learning-notes](learning-notes.md) · [troubleshooting](troubleshooting.md)

| 일자 | 내용 |
|---|---|
| 2026-06-02 | plan.md 신설 — HTTPS(ALB TLS termination) 항목 + 기존 로드맵 정리 |
| 2026-06-02 | HTTPS·OAuth(구글) 완료 반영 + 기술부채(Flyway 도입/404 핸들러/Actions Node20) 추가 |
| 2026-06-02 | Flyway 도입 완료 처리 + 회원 인증/계정 보안 하드닝 항목 추가(상세는 private 노트) |
| 2026-06-02 | Fargate CPU 상향(로그인 BCrypt 지연) 항목 추가 |
| 2026-06-02 | 보안 하드닝 #1 OAuth email_verified·#2 brute-force 완료 처리(N-026) |
| 2026-06-02 | GitHub Actions Node 24 갱신 완료(checkout@v6/setup-java@v5/configure-aws-credentials@v6) |
| 2026-06-02 | 세션 외부화(Spring Session JDBC) 완료 — 재로그인 해결(N-029/T-020), 무중단 배포(ECS 롤링) 백로그 추가, 향후 Redis 전환 명시 |
| 2026-06-02 | 무중단 배포 — ECS deploymentConfiguration(min=100/max=200 + circuit breaker rollback) 멱등 적용 워크플로 신설(N-030) |
| 2026-06-02 | 무중단 배포 검증 완료 — 워크플로 적용 후 실배포 중 /actuator/health 폴링이 끊김 없이 200(503 없음) |
| 2026-06-02 | 세션 쿠키 SameSite=Lax 완료(명시 CookieSerializer 빈) + prod Secure/HttpOnly 잠재 갭 동시 수정(T-021/N-031). 404→500 stale 항목 #72 완료로 정리 |
| 2026-06-02 | 독서 잔디(컨트리뷰션 그래프) 완료 — /history에 1년치 히트맵(ContributionGraphBuilder 순수 빌더 + 서비스 + Thymeleaf/CSS), TDD |
| 2026-06-02 | 책 단위 기록 1단계 완료 — /books 등록·목록(알라딘 검색 포트/어댑터 + 수동 폴백, 상태·삭제·소유권), Flyway V3, 제휴 토대. TDD |
| 2026-06-03 | 책 단위 기록 2단계 완료 — 세션↔책 연결(Flyway V4)·타이머 시작 시 책 선택·책별 누적 시간 집계/표시. T-022 기록. TDD |
| 2026-06-03 | 알라딘 TTBKey SSM 주입(검색·제휴 라이브) + 도서 검색 페이징(이전/다음, totalResults 기반). TDD |
| 2026-06-03 | (계획) 독서 잔디 색 농도를 하루 목표(증가값) 기준 비율 4단계로 — 설계만 기록, 구현은 별도 PR(워크트리 분리) |
| 2026-06-03 | 개인정보처리방침 페이지(`GET /privacy`, 공개) 추가 — OAuth 동의 화면 게시 체크리스트 ① 완료(②③은 Console 수동). TDD |
| 2026-06-03 | Safe Browsing "위험한 사이트" 오탐(`.click` 신규 도메인+로그인/OAuth) — T-027/N-036 기록 + 도메인 TLD 이전(.com/.app) 백로그 추가 |
| 2026-06-03 | OAuth 동의 화면 게시 완료(②③ 체크) — 프로덕션 전환, 100명 제한 해제(non-sensitive라 한도 미적용). Safe Browsing 오탐은 Search Console 인증 후 자연 해소(T-027 보강) |
| 2026-06-03 | 독서 잔디 색 농도를 하루 목표(증가값) 기준 비율 4단계로 — 완료(빌더 goalSeconds 인자·교차곱, 서비스 타이머 조회, 범례 "목표 대비"). TDD |
| 2026-06-04 | 한국 저작권 등록 가이드 신설(claude-docs/copyright-registration.md) — 조건·서류·절차·비용 정리. 법무 섹션 + README 링크 추가 |
| 2026-06-03 | 독서 잔디 대시보드 노출 + 열 순서 반전(최근=왼쪽, 스크롤 없이 최근 보이게) — 빌더 단일 소스로 세 잔디 일관. TDD |
| 2026-06-03 | SNS 기능 항목에 "구현 전 설계 먼저(필수)" 강조 + 착수 전 합의 사항(공유범위·관계모델·IDOR·스키마·악용·SSR) 명시 |
| 2026-06-03 | 측정 카드 — 타임존(원시 시작시각) 노출 제거 → 읽는 책 + 책별 누적 독서 시간 표시(#97). TDD |
| 2026-06-03 | SNS 확장 시 새로 저장할 건 독서기록 아닌 관계+공개범위 — N-037 + plan.md SNS 보강(#98) |
| 2026-06-03 | 가입 후 온보딩 페이지 — 타이머 초기값(시작 잔여)+증가값+상한 직접 설정(Flyway V6 users.onboarded, 첫 진입 게이트, 기존 사용자 백필 true). 가입 시 무조건 1h 시드를 사용자 선택으로. TDD |
| 2026-06-03 | SNS — 책 인기 카운트(집계 노출) 항목 추가: ① 검색 결과 리스트에 "읽는 중 N·완독 M" 작게 표시, ② 내 책장에 "몇 명이 읽는 중" 표시. ISBN 키 distinct user count 재사용, 관계 없이 선출시 가능, 소수 재식별·N+1·ISBN 정규화 선결 |
| 2026-06-03 | 관리자(개발자) 대시보드 항목 추가 — 매번 RDS 접속 없이 운영 데이터·통계를 웹에서 확인. 접근 제어(ADMIN만, `/admin/**` hasRole) 선결, ADMIN 승격 경로 필요, 통계는 읽기 전용 집계(N-037). 온보딩 타임존 백로그도 함께 |
| 2026-06-04 | 프론트/앱↔SNS 선후 의존 정리 메모 추가 — SNS는 프론트 교체의 선행조건 아님(촉발 사유일 뿐), 앱은 SNS가 아닌 JSON API에 의존, 실질 결정은 "SNS UI를 SSR→SPA로 두 번 짤 것인가". SNS 데이터 설계는 프론트와 독립이라 선행 가능 |
| 2026-06-04 | SNS 설계 문서(claude-docs/sns-design.md) 작성 — 공개범위(PRIVATE 기본 opt-in)·관계(팔로우 단방향)·IDOR canView 게이트·스키마(follow V7/visibility V8)·4단계 로드맵·화면별 SSR/SPA 판단·열린 질문. 결론: SNS 대부분 SSR로 충분 → API-first big-bang 불필요 |
| 2026-06-04 | SNS 사용자 요구사항 1차 확정 반영 — 공개 단위를 프로필 토글→**책 단위**로, 인기 카운트를 전역→**팔로우 스코프**로, **닉네임 유니크**(검색·핸들) 추가. 잔디 viewer 가시성 필터(비공개 책 간접 누출 차단)·canViewBook·닉네임 백필 선결 반영. 로드맵 재정렬(①닉네임+책공개→②프로필→③팔로우→④팔로우스코프 카운트→⑤악용) |
| 2026-06-04 | 잔디 프라이버시 확정 — 공개 안 한 책은 타인 잔디·총시간에서 제외(PUBLIC 책 세션만, book=null도 제외) |
| 2026-06-04 | SNS 1단계 일부 — **닉네임 유니크화 구현**(Flyway V7 중복 백필+`uk_users_nickname`, LOCAL 가입 중복 거부, 소셜 자동 유일화 `NicknameAllocator`, 설정 변경 중복 거부, `existsByNickname`). TDD. 남은 1단계: 책별 공개 토글(`book.visibility`) |
| 2026-06-04 | 소셜 사용자 닉네임 직접 지정 — 구글 가입자가 자동 임시닉 대신 **온보딩에서 닉네임 직접 입력**(폼 필드 추가·prefill·중복 거부). 자동 배정은 온보딩 완료 전 임시값. TDD |
| 2026-06-04 | SNS 1단계 완성 — **책별 공개 토글**(BookVisibility PRIVATE/PUBLIC, Flyway V8 default PRIVATE 백필, `POST /books/{id}/visibility` 소유권 강제, 책장 셀렉트). 닉네임 유니크와 합쳐 1단계 완료 → 다음은 2단계 프로필 페이지(/u/{nickname}). TDD |
| 2026-06-04 | 책장 긴 제목 깨짐 수정 — `.book-meta flex:1 1 220px` + `.book-row flex-wrap` + `word-break:keep-all`(PR #110, CSS만) |
| 2026-06-04 | SNS 2단계 — **개인 공개 프로필 페이지 `GET /u/{nickname}`**(SSR). PUBLIC 책장 + 공개 잔디(PUBLIC 책 세션만, §3.5). 닉네임 404·비로그인 차단·본인도 PUBLIC만(공개 미리보기). `ProfileService`, `publicDailyHistory`/`publicTotalSecondsByBook` 가시성 필터, 대시보드 진입 링크. 신규 마이그레이션 없음. TDD Red→Green 2사이클 |
| 2026-06-04 | SNS 3단계 — **닉네임 검색 + 팔로우**. `GET /search`(부분일치·최소2글자·상한20), `Follow`(Flyway V9)+`FollowService`(자기팔로우 금지·멱등·언팔즉시), `POST /follow`·`/unfollow`(오픈리다이렉트 방어), 프로필에 팔로워/팔로잉 카운트+팔로우 버튼. 검색결과=닉네임+공개책수+팔로우버튼. 탈퇴 시 follow 정리. 대시보드 검색 링크. TDD. (별도 발견: 탈퇴 시 book 미삭제 FK 위반 — 후속 분리) |
| 2026-06-04 | 대시보드 UX 수정 — 하단 바로가기 링크를 퀵 액션 타일 2열 그리드로(한글 글자깨짐 구조 해결, PR #116), 측정 카드 '읽을 책' 라벨 세로깨짐·select 카드밖 넘침 수정(`.book-pick` flex+nowrap+min-width:0, PR #117). CSS/템플릿만 |
| 2026-06-04 | SNS 보강 — **본인 팔로워/팔로잉 목록**(`/me/followers`·`/me/following`, PR #119). 본인 프로필에서만 카운트가 목록으로 링크(남 프로필은 카운트만, privacy 유지). 경로에 닉네임 없어 항상 본인 기준(보안 경계 자동). 행은 검색과 동일(`UserRowAssembler` 공용 추출→검색·목록 재사용). `FollowListService`+`findByFollowee/FollowerOrderByCreatedAtDesc`. Flyway 신규 없음. TDD |
| 2026-06-04 | SNS 4단계 — **팔로우 스코프 인기 카운트**(PR #118). 책장·검색결과 각 책에 "👥 팔로우 중 N명 원함 · M명 읽음". 원함=WANT_TO_READ·읽음=READING∪FINISHED, k익명 임계 없음(drill-down 없어 위험 제한적). `BookRepository.followScopePopularity`(isbn 일괄 group by·팔로우 theta조인·PUBLIC·distinct, N+1 회피)+`FollowScopePopularityService`+`books.html` 프래그먼트. Flyway 신규 없음. TDD(서비스 단위·집계 통합·컨트롤러 끝단) |
| 2026-06-04 | SNS 5단계 시작 — **차단(block)**(PR #121, 대칭). 차단 관계면 서로 팔로우 불가·서로 프로필 404(`existsBetween` 양방향 게이트). 차단 시 기존 팔로우 양방향 해제. `Block`(V10)+`BlockService`+`BlockRepository`, `/block`·`/unblock`·`/me/blocks`(해제는 여기서 — 차단 후 상대 프로필 404라). 프로필에 차단 버튼, 탈퇴 정리에 block 추가. 함정: 동적 path 변수 표현식은 앞서 회피. TDD(서비스·컨트롤러·purge inOrder). 남은 5단계: 신고·레이트리밋·열거완화 |
| 2026-06-04 | UI — ① 하단 네비(`.link-row`)를 대시보드 퀵 액션과 **동일한 타일 양식으로 통일**(CSS만, 전 페이지 일괄 — 표준 양식 확정). ② 내 책장 **상태 필터**(전체/읽고싶음/읽는중/완독, 서버사이드 `?status=`, 부합 책만 표시, q 유지, 필터 칩 활성 표시). `BookController` status 파싱·필터, `books.html` 칩+빈 메시지 분기, `.shelf-filter` CSS. TDD(컨트롤러 필터). (PR #122) |
| 2026-06-04 | 책 검색 **기준 분리**(제목/저자) — 기존 알라딘 `QueryType=Keyword`가 제목·저자를 함께 매칭해 "모기" 검색 시 저자명 매칭이 먼저 떠 의도와 어긋남. 기본=제목, 라디오로 저자 전환. `BookSearchType` enum(제목/저자→`QueryType` Title/Author, `from()` 폴백), 포트 시그니처에 검색기준 추가, `AladinBookSearchClient.buildSearchUrl` 정적 추출(URL 단위테스트), 컨트롤러 `?type=` 파싱·페이징 유지, `books.html` 라디오 + `.search-type`/`.search-row` CSS. TDD(enum·서비스 위임·URL·컨트롤러 모델). (PR #123) |
| 2026-06-04 | 책 검색 정확도·폼 레이아웃 — ① **제목 검색 후필터**(`BookService.filterToSearchType`): 알라딘 `QueryType=Title`이 문서와 달리 저자까지 섞어 반환(비엄격)하는 걸 방어, 고른 기준 필드에 검색어가 실제로 든 결과만 남김(공백·대소문자 정규화 contains, #123 후속). ② 검색 폼 **세로 배치**(넓은 바 위 + 전체폭 버튼 아래, `.search-row` column). 알려진 한계: 페이저가 필터 전 totalResults로 계산해 과대 집계(무해). TDD(제목/저자 후필터·정규화). (PR #125) |
| 2026-06-04 | (아이디어 기록만) **독서 성향 분석 "책장 기반 MBTI"** 항목 추가 — 책장(보유/읽는중/완독+책별 시간)을 AI 또는 알고리즘으로 분석해 독서 성향 도출 → ① 개인화 책 추천 ② 사용자 상호작용(성향 비교·매칭) 컨텐츠. 입력은 기존 데이터(N-037), SNS 토대 재사용. 미확정 多(축 설계·PRIVATE 책 프라이버시·ISBN/장르·A규칙 vs B AI vs 하이브리드). 구현 전 설계 합의 필수. 문서만 |
| 2026-06-04 | **홍보/마케팅 섹션 신설** — 북카페 등 독서 커뮤니티 기반 홍보 아이디어만 가볍게 기록(세부 미정). 문서만 |
| 2026-06-04 | plan.md 정리 — **"실사용에서 발견한 문제(계획 외 UX/사용성)" 섹션 신설**. 스크린샷 피드백으로 발견·수정한 6건(PR #110·#116·#117·#122·#123)을 갱신 이력에서 한 표로 모음(증상·원인·해결·PR) + 후속 후보(출판사 검색 차원·공개여부 필터·ISBN 정규화) 명시. 로드맵 줄에 #123 부수 픽스 반영. 문서만 |
| 2026-06-04 | **전역 인기 카운트 철회 → 팔로우 카운트 drill-down 채택**(4단계+). 전역 카운트는 *팔로우 없이도* 공짜 사회적 증거를 줘 팔로우 가치를 희석한다고 판단(사용자 결정). 대신 카운트 배지 클릭 시 "그 책 원함/읽음인 내 팔로우 명단"(`GET /books/readers`) — 카운트와 같은 게이트(팔로우·PUBLIC·distinct)로 신원만 펼침(새 노출 0, IDOR 없음), 기존 `UserRowAssembler` 재사용. `BookRepository.followScopeReaders` + `FollowScopeReadersService`/`FollowScopeReaders` + `book-readers.html`. Flyway 신규 없음. TDD(서비스 통합·컨트롤러 끝단). 함정 T-### 기록(Thymeleaf th:each+th:replace 우선순위·파라미터 fragment 인라인 NPE) |
| 2026-06-04 | **Fargate CPU 상향**(로그인 BCrypt 지연 완화) — `deploy/task-definition.json` `cpu` 256→512(0.5 vCPU), `memory` 512→1024. 원인은 DB 아닌 BCrypt(의도된 CPU 집약)×0.25 vCPU 스로틀. 강도(10)는 유지(보안), CPU만 상향이 정답. 배포 검증은 run. 설정만(PR #132) |
| 2026-06-04 | **측정에 책 필수화** — 책 미지정 측정 금지(어떤 책을 얼마나 읽었는지 명확화). 강제는 유스케이스 경계: `ReadingSessionService.start`에 `book!=null` 가드(2-arg 오버로드 제거), 컨트롤러는 bookId 없거나 내 책 아니면(IDOR) "책 선택" 에러+세션 미생성, 대시보드는 "선택 안 함" 제거·`select required`·책 0권이면 시작 폼 숨김. 엔티티/`book_id`는 nullable 유지(레거시 null-book 세션 읽기·집계 보존), **DB NOT NULL 제약은 보류**(기존 null 행에 마이그레이션 실패 위험 → 위 후속 항목). 부수로 CSRF 잠복 버그(T-033/N-044) 잡음: 책 0권 사용자에게서 상단 시작 폼이 사라지자 큰 페이지 하단 로그아웃 폼 CSRF가 응답 커밋 후 세션 생성 시도→500, 컨트롤러에서 렌더 전 토큰 선확정으로 해결. TDD(서비스 불변식·컨트롤러 끝단). (PR #133) |
| 2026-06-05 | **측정 책 드롭다운 상태별 그룹화 + 최근 읽은 책 자동선택** — 드롭다운("읽을 책")에서 완독/읽는중이 안 나뉘고 무조건 맨 위(최근 등록) 책이 잡히던 문제(스크린샷 피드백). 읽고싶음(WANT_TO_READ) 제외(아직 안 편 책 측정은 무의미), `<optgroup>`「읽는 중」/「완독」으로 시각 구분, 가장 최근 측정한 책(startedAt 최신 세션의 책) 미리 선택(이력 없으면 브라우저 기본=첫 옵션). `DashboardModel` `books`→`readingBooks`/`finishedBooks`+`recentBookId`, `ReadingSessionRepository.findRecentlyReadBookIds(user, Pageable)`. Flyway 신규 없음. TDD(상태별 분리·recentBookId, Red→Green). (PR #134) + 학습노트 **N-045**(Spring Data 최신 N건은 Pageable로 limit, PR #135) |
| 2026-06-05 | **독서 기록에서 세션 "횟수" 제거 → 읽은 책 표시**(스크린샷 피드백) — 1분도 안 읽고 멈춘 측정까지 "N회"로 세어 횟수 지표가 부풀고 의미가 약했음. 횟수를 빼고 그날 **읽은 책 제목**(+총 시간 유지)을 보여줌. `DailyReadingRecord.sessionCount`(int)→`bookTitles`(List, 중복 제거·읽은 순서), `ReadingHistoryService`는 책 미지정(레거시 null) 세션은 제목 없이 시간만 합산, book-detail은 한 책이라 횟수만 제거. history.html 날짜+책 2단 레이아웃, `.record-count` CSS 제거. Flyway 신규 없음. TDD(같은날 책제목 모음·중복제거·공개잔디 제목, Red→Green). (PR #138) |
| 2026-06-05 | **사용자 검색 입력칸 세로로 길쭉하던 버그 정상화**(스크린샷 피드백) — `/search` 닉네임 입력칸이 비정상적으로 높았음. 원인은 `.book-search-form`이 column flex인데 input이 폼 직계라 `flex:1 1 160px`의 basis가 너비 아닌 **높이**로 먹힌 것(책 검색은 `.search-row`가 리셋, 사용자 검색은 래퍼 없음). `.book-search-form > input[type=text]`에 `flex:none; width:100%; box-sizing:border-box`로 한 줄 리셋 — 직계(`>`) 셀렉터라 `.search-row` 하위인 책 검색 input은 무영향. CSS만. (PR #139) |
| 2026-06-05 | **독서 성향 분석("책장 기반 MBTI") 설계 메모 작성** — 사용자가 "책장 보고 MBTI식 자유 설명문" 원함, AI 필요성·비용 논의. 합의 방향: **"AI를 쓰냐"가 아니라 "뭘 시키냐"** — 사실 집계(장르·완독률·시간 분포)는 코드($0·결정적), **해석·서술만 LLM**(소형 모델·호출당 1원 미만), 입력은 압축 "독서 프로필"(raw 안 던짐→토큰·환각↓), 출력 2겹(설명문+비교용 태그), 결과 저장·캐시(비용+공유 일관성), 프라이버시는 PUBLIC 책만 노출. `claude-docs/reading-personality-design.md` 신설(sns-design 양식) + plan.md §독서 성향에 링크. **구현 금지 유지**(🟡 항목 확정 후 착수). 문서만. (PR #140) |
| 2026-06-05 | **비즈니스 모델 섹션 신설 — "독서 소셜" 월 정액 구독 구상** — 수익 토대가 제휴(간헐·소액) 하나뿐이라 반복 매출(MRR) 모델 기록. ① AI 책장 성향 ② 사람 추천(비슷+**정반대**) ③ 채팅을 **하나의 흐름(나를 알고→사람 잇고→대화)으로 묶어 한 구독**으로 판매. LLM 변동비를 구독료가 상쇄, 무료(타이머·잔디·책장·기본 SNS)/유료(성향·추천·채팅) 경계 제안. 선결: 채팅 운영·법적 부담(모더레이션)·프라이버시(PUBLIC만)·결제 인프라·콜드스타트·가격(미정). 선후 1→2→3, **구현 금지**. 문서만(PR #140에 동봉). |
| 2026-06-05 | **비즈니스 모델 §「가격 가설」 추가** — 월 정액 적정가 논의. 출발 가설 **4,900~5,900원/월(연 49,000), 만원 밑 단일 티어 → 밀도 차면 9,900까지**. 근거: 원가 아닌 **가치(WTP) 기준**(LLM 마진≈100%), 밀리(전자책 무제한 9,900) **아래 앵커링**(우리는 콘텐츠 아닌 소셜/재미 레이어), "가볍게" 포지셔닝, **침투가격**(콜드스타트), 연간 묶기(현금흐름·리텐션), **앱스토어 30% 컷** 반영, 락인 전 Van Westendorp 검증 권장. 가설일 뿐·미확정. 문서만(PR #141). |
| 2026-06-05 | **대시보드 '재미' 동선 승격 — 하이라이트 밴드 신설**(스크린샷 피드백) — 이 서비스의 재미 요소인 `내 책장`·`내 공개 프로필`이 맨 밑 바로가기 5타일에 설정·검색과 섞여 묻혀 있던 문제. 측정 카드 바로 밑에 **`.highlight-actions` 밴드**(포인트 테두리·큰 아이콘·부제 2칸, 좁은 화면 1열)를 신설해 둘을 승격, 맨 밑 `.quick-actions`에선 빼 중복 제거. **타이머는 히어로로 유지**(앱 정체성·매일 쓰는 동작이라 위로 안 밀고, 그 아래로 재미 동선을 끌어올림). dashboard.html + app.css만(로직·테스트 영향 없음). (PR #142) |
| 2026-06-05 | **login_id PR-1 — 스키마 + 도메인** — 설계 메모 PR 단계 ①. `V13__user_login_id.sql`(`login_id varchar(50)` nullable + `uk_users_login_id` unique), `User.loginId` 필드 + `assignLoginId`(입력 소문자 정규화 → `a-z0-9_`/3~20자 형식·예약어(admin·root 등) 차단, 유니크는 DB/서비스 몫) + `getLoginId`. **동작 변화 없음**(인증은 아직 email, login_id는 null로 시작 — 가입/온보딩 채움은 PR-2). 팩토리(`User.of`) 시그니처는 안 건드림(파급 최소화 — 메모 계획보다 작게). TDD(Red: assignLoginId 부재 컴파일 실패 → Green) + FlywayMigrationTest + 전체 스위트 그린(nullable unique가 다중 NULL 허용 → 기존 다수 유저 생성 테스트 무사). (PR #146) |
| 2026-06-05 | **login_id PR-2 — 불변 + 닉네임 중복허용 + 온보딩 캡처 (🔁 모델 전환: login_id=공개 @핸들)** — 사용자 결정으로 설계 전환: "검색을 닉네임 아닌 **아이디로**(인스타·X처럼)", "닉네임 중복 OK·수정 자유", "**아이디는 한번 정하면 영원히 불변**". 보안 본질(공개 *이메일*을 로그인 식별자에서 분리)은 유지 — email은 여전히 비공개, 공개 핸들만 nickname→login_id로 이동(@핸들은 알려져도 비밀번호를 못 뚫음). ① `assignLoginId` **불변 가드**(재설정 시 `IllegalStateException`) + `normalizeLoginId` 정적 추출(유니크 사전확인이 self-flush 오탐 안 나게). ② **nickname 유니크 제거** — `V14__drop_nickname_unique.sql`(`uk_users_nickname` drop) + register/onboarding/settings/oauth-provisioning 중복검사 4곳 제거 + dead코드(`NicknameAllocator`·`NicknameAlreadyExistsException`·`existsByNickname`) 삭제. ③ **온보딩에서 전원 login_id 캡처**(가입폼 아님 — 단일 경로·불변 무충돌): `OnboardingForm.loginId`(@Pattern), `OnboardingService.complete`가 `existsByLoginId` 사전확인+`assignLoginId` 확정, `LoginIdAlreadyExistsException`, `onboarding.html` 아이디 필드(불변 안내)+nickname 라벨 정정("검색되는 이름"→"표시 이름"). 로그인·검색은 아직 email/nickname(컷오버는 PR-3 핸들/PR-4 인증). TDD(불변·중복허용·온보딩 캡처/유니크/예약어 Red→Green) + 전체 그린. PR 단계 ③④⑤로 재편(핸들 컷오버 신설). 학습노트 N-046·047·048 동봉. (PR #147) |
| 2026-06-05 | **login_id PR-3 — 공개 핸들 컷오버(검색·프로필·관계 nickname→login_id)** — 설계 PR 단계 ③. 사용자가 원한 "인스타·X식 아이디 검색"을 실제로 켬. `UserRepository` 검색/조회 메서드를 nickname→login_id로 교체(`findByLoginId`·`findTop20ByLoginIdContainingIgnoreCaseOrderByLoginIdAsc` 추가, nickname 버전 제거). `UserSearchResult`·`ProfileView`에 `loginId` 필드 추가(닉네임은 **표시 이름**으로 유지 — 핸들/표시 분리, X 모델). 프로필 라우트 `/u/{loginId}`. 팔로우/차단/신고 컨트롤러 대상 식별을 `@RequestParam loginId`+`findByLoginId`로 — **닉네임 중복 허용(PR-2) 이후 `findByNickname`이 "첫 일치 1명"이라 엉뚱한 사람을 팔로우/차단/신고하던 정합성 버그를 동시 해소**. self-프로필 링크용 `loginId` 모델 속성(Dashboard/FollowList/Block), 템플릿 6종(`search`/`profile`/`follow-list`/`block-list`/`book-readers`/`dashboard`) `/u/{loginId}`+폼 `name="loginId"`+`@핸들` 표시. **인증은 아직 email**(principal·14곳 컨트롤러는 PR-4). TDD(검색이 login_id 기준·닉네임 중복 시 login_id로 정확 식별 Red→Green) + 전체 스위트 그린. (PR #148) |
| 2026-06-05 | **login_id PR-4 — 인증 컷오버(email→login_id 전면, 가장 큰 PR)** — 설계 PR 단계 ④. 로그인 식별자를 email에서 login_id로 전면 전환(공개 이메일을 로그인 표적에서 분리). **wipe(prod 그린필드 리셋) 선행 완료.** ① `loadUserByUsername`=`findByLoginId`만(email 폴백 없음 → 이메일 로그인 차단), principal=login_id, username=login_id. ② **로컬 가입에서 login_id 캡처** — 로그인이 login_id 기준이라 가입 시 있어야 함(`SignupForm.loginId`+@Pattern, `register` 7-arg 오버로드가 `existsByLoginId`+`assignLoginId`; 6-arg는 login_id 지연 확정용 유지). 온보딩 login_id 캡처는 **OAuth 전용으로 이동**(`OnboardingService.complete`가 `loginId==null`일 때만 확정, `needsLoginId` 모델 플래그로 입력칸 조건부). ③ **OAuth**: `BookTimerOidcUser`로 `getName()`=login_id(있으면)/email(온보딩 전 첫 세션) — login_id는 id_token claim이 아니라 override. ④ **`CurrentUserService` 공유 리졸버**(findByLoginId 우선 → OAuth 첫 세션만 findByEmail 브리지)로 13개 컨트롤러 통일(중복 currentUser 헬퍼 제거). `SettingsController` 4곳은 principal→User→`getEmail()`로 서비스(findByEmail) 시그니처 보존. ⑤ **admin 시드 `BOOKTIMER_ADMIN_EMAILS`→`BOOKTIMER_ADMIN_LOGIN_IDS`**(`findByLoginId`, 입력 소문자 매칭). 로그인폼 라벨 이메일→아이디·type text. **설계 미세 보정**(OAuth 첫 세션은 login_id 부재라 principal=email 임시 → 리졸버 email 브리지; 로그인 폼만 login_id면 보안 목표 달성하나 설계대로 principal 전면 전환 선택). Flyway 없음(스키마 무변경 — PR-1의 nullable unique 그대로, NOT NULL은 PR-5). TDD(login_id 로그인·이메일 차단·리졸버 login_id/email 브리지·OIDC name·온보딩 조건부·시드 login_id·login_id principal 컨트롤러 경로 Red→Green) + 전체 372+ 그린. **⚠️ 배포 전 prod ENV를 `BOOKTIMER_ADMIN_EMAILS`→`BOOKTIMER_ADMIN_LOGIN_IDS`(login_id 값)로 교체 필요.** (PR #149) |
| 2026-06-05 | **운영자(ADMIN) 동선 분리 — 로그인 시 /admin 직행 + 검색에서 제외** — 운영자는 책을 읽는 주체가 아니다. ① `DashboardController` `/`에서 도메인 `User.getRole()==ADMIN`이면 `redirect:/admin`(폼·OAuth 공통 착지점이라 로그인 경로 무관, **온보딩 게이트보다 먼저** 둬 운영자는 온보딩도 안 거침). ② `UserSearchService.search`에 `role != ADMIN` 필터 추가(block 필터와 같은 스트림 단계) — 일반 사용자의 친구 검색에 운영자 노출 안 됨. 프로필 직접 조회(`/u/{handle}`)는 의도적으로 그대로 둠(요청 범위=검색). Flyway 없음. TDD(admin→/admin 리다이렉트·검색 admin 제외 Red→Green) + 전체 스위트 그린. (PR #150) |
| 2026-06-05 | **운영자(ADMIN) 프로필 직접 조회 차단** — #150 후속(검색뿐 아니라 핸들 직접 입력도 막아 운영자 존재 자체를 숨김). `ProfileService.profileOf`에 `target.getRole()!=ADMIN` 필터 추가(차단 관계 필터와 같은 단계) → 빈 Optional → 컨트롤러가 **404**(존재 누설 회피, 차단 사용자와 동일 처리). 대상이 ADMIN일 때만 숨기므로 **운영자가 일반 사용자 프로필 보는 건 영향 없음**(향후 모더레이션 대비). 운영자 본인이 자기 핸들을 조회해도 404(소셜 프로필 비대상 — 운영자 영역은 `/admin`). Flyway 없음. TDD(admin 프로필 404·admin 본인 404 Red→Green) + 전체 스위트 그린. (PR #151) |
| 2026-06-05 | **관리자 대시보드 2단계 — 통계 요약(읽기 전용 집계)** — "매번 RDS 접속 번거롭다"의 알맹이. `/admin`에 운영 지표 카드: 가입자 수(USER만·**ADMIN 제외**), 온보딩 완료자, **최근 7일 활성 사용자**(세션 startedAt distinct), 총 책/세션 수, 총·가입자당 평균 독서 시간. `AdminStatsService.summary()`→`AdminStats`(record), 집계 쿼리만(N-037: 새 저장 아님) → **Flyway 없음**: `UserRepository.countByRole`·`countByRoleAndOnboarded`, `ReadingSessionRepository.countActiveUsersSince`(주입된 `Clock`로 창 계산 → 테스트 결정적)·`sumAllDurationSeconds`. 평균은 가입자 0명일 때 0(0 나눗셈 회피). `admin.html` 통계 그리드(인라인 스타일·자족적). TDD(역할별 카운트·활성 창·합계·평균·0명 경계 / `/admin` 모델 stats Red→Green) + 전체 스위트 그린. 남은 단계: 데이터 조회(사용자 목록·드릴다운). (PR #152) |
| 2026-06-05 | **관리자 대시보드 3단계(데이터 조회) 설계 메모 작성** — 마지막 단계. PII 노출 폭이 가장 큰 화면이라 구현 전 합의. 결정: 라우팅 `GET /admin/users`(목록·페이징·login_id/nickname 검색)·`GET /admin/users/{loginId}`(드릴다운), **email은 마스킹 기본+클릭 시 전체**(`g***@gmail.com`, 사용자 결정), **비밀번호 해시 비노출**, **드릴다운=타이머 설정+최근 N세션+책장 요약**, **감사로그·운영 액션(수정/삭제)은 보류**(읽기 전용 유지 → **Flyway 없음**). 접근 경계는 기존 `/admin/**` hasRole 그대로. `claude-docs/admin-data-lookup-design.md` 신설 + plan.md §관리자 데이터 조회 항목에 링크. **구현은 다음**(문서만). (PR #153) |
| 2026-06-05 | **관리자 대시보드 3단계 — 데이터 조회 구현(마지막 단계 ✅)** — 설계(#153)대로 사용자 목록 + 드릴다운. **목록** `GET /admin/users`: 페이징(가입일 내림차순·기본 20) + login_id/nickname 부분일치 검색(email 검색 안 함), 표에 @아이디·닉네임·email(마스킹)·provider·가입일·온보딩·역할. **드릴다운** `GET /admin/users/{loginId}`: 계정 메타 + 타이머 설정(없으면 폴백) + 최근 10세션(책 제목·시간) + 책장 상태별 요약, 없는 id면 404. **email 마스킹**(`EmailMask`: `g***@gmail.com`, `<details>` 토글로 원문) + **비밀번호 해시 DTO 제외**(`AdminUserRow`/`AdminUserDetail` record). 세션→책 제목 lazy는 `findRecentWithBookByUser`의 `left join fetch`로 트랜잭션 안에서 해소(뷰 lazy 예외 방지). 추가 쿼리: `UserRepository.findByLoginIdContainingIgnoreCaseOrNicknameContainingIgnoreCase`(페이징), `BookRepository.countByUser`·`countByUserAndStatus`. 읽기 전용(GET만)이라 CSRF 표면 없음, **Flyway 없음**. `admin.html`에 진입 링크. TDD(마스킹 규칙 경계·목록 검색/페이징·드릴다운 조립/타이머 폴백/최근 N 한도·없는 id 404·**USER 403 회귀** Red→Green) + 전체 스위트 그린. **부수: `/admin`에 로그아웃 폼 추가**(운영자가 대시보드에서 로그아웃 못 하던 문제 — `POST /logout`, Thymeleaf CSRF 자동 주입, dashboard와 동일 패턴. TDD: `/admin` 응답에 logout 폼 렌더 Red→Green). 관리자 대시보드 4단계 전부 완료. (PR #154) |
| 2026-06-05 | **로그인 아이디(login_id) 도입 설계 메모 작성** — 운영자 이메일이 홈페이지에 공개돼 "이메일로 로그인하면 안 되겠다"는 문제 제기에서 출발. 전제 정정(이메일 공개≠admin 시드 탈취, 진짜 약점은 email이 로그인 식별자라 표적 노출)부터, **principal=login_id 전면 전환(B안)** 합의 — email은 속성으로 강등, nickname은 공개 핸들로 별개 유지. **기존 사용자 전부 wipe**(출시 전·친구뿐)로 백필·dual-lookup·임시값·게이트 전부 제거(그린필드), OAuth는 온보딩에서 아이디 직접 선택, 인증은 `findByLoginId`만, 형식 `a-z0-9_`/3~20자/소문자/예약어 차단, 시드 `BOOKTIMER_ADMIN_LOGIN_IDS`. `claude-docs/login-id-design.md` 신설 + plan.md §관리자에 항목·링크. **구현 금지**(PR-1~4 단계 정의, 형식 확정 후 착수). 문서만. (PR #145) |
| 2026-06-05 | **관리자 대시보드 1단계 — 접근 경계 + ADMIN 시드 승격** — "운영 데이터 보려고 매번 RDS 직접 접속 번거롭다"의 선결(접근 제어)부터. `/admin/**`→`hasRole("ADMIN")`(default-deny 위 역할 매처), `AdminController GET /admin` + 최소 랜딩(`admin.html`). **ADMIN 승격은 ENV 부트스트랩 시드 채택**(사용자 결정) — `BOOKTIMER_ADMIN_EMAILS`(relaxed binding→`booktimer.admin.emails`)를 기동 시 `AdminAccountSeeder`→`AdminAccountService.seedAdmins`로 승격, `User.promoteToAdmin()`(멱등). 가입은 USER 고정 유지(권한 상승 차단), 이메일은 repo 미커밋(공개 저장소). TDD(Red→Green 가시화): 도메인 멱등 승격 / 시드(승격·멱등·미존재 무시) / 인가 경계(미인증→`/login`·USER→403·ADMIN→200). 전체 스위트 그린. 통계 요약·데이터 조회는 PR-B. (PR #144) |
| 2026-06-05 | **DB 쿼리 헬스체크 + `book.isbn13` 인덱스 추가** — "코드 쌓였는데 최적화 한 번 안 해도 되나?"에 **읽기 전용 헬스체크** 먼저. 결과: 데이터 계층 건강(전 연관 LAZY·인기 카운트 group-by 일괄·FK 자동 인덱스·진행중 세션 fetch join)이라 전면 리팩터는 **조기 최적화로 보류**. 손댈 가치 있는 단 하나만 반영 — 팔로우 인기 카운트/드릴다운이 `isbn13`으로 필터하는데 FK 아니라 자동 인덱스 없음 → `V12__book_isbn13_index.sql`(`create index ix_book_isbn13 on book (isbn13)`). 의식적 보류: 잔디 집계 `findByUser`→lazy `getBook()` N+1(1차 캐시로 완화), `UserRowAssembler` 1+3K(javadoc에 기록된 트레이드오프), 중복 인덱스(무해). `FlywayMigrationTest`(격리 H2 적용+validate) 통과. (PR #143) |
| 2026-06-05 | **login_id PR-5 — 무결성 강화(조건부 NOT NULL = CHECK)** — 설계 PR 단계 ⑤(마지막). 단순 `login_id NOT NULL`은 OAuth와 충돌함을 발견 — OAuth 사용자는 `OAuthUserProvisioningService.provision`→`registerOAuth`로 **login_id=null인 row를 먼저 INSERT**하고 온보딩에서 비로소 login_id를 정한다(불변이라 X식 자동 핸들도 불가). 그 전환 창의 null은 정상이므로, 박고 싶은 진짜 불변식인 **`onboarded = true ⟹ login_id IS NOT NULL`을 조건부 CHECK로** 좁힘(`V15__user_login_id_when_onboarded_check.sql`, `ck_users_login_id_when_onboarded`, `check (onboarded = false or login_id is not null)`). MySQL 8·H2(MySQL 모드) 모두 CHECK 강제. 메인 테스트 스위트는 Hibernate가 스키마를 생성해 이 제약이 없으므로 무영향(V14 nickname-unique-drop과 동일) — `FlywayMigrationTest`가 Flyway 스키마에 적용해 3경계 검증(온보딩+login_id없음→거부·온보딩전 null→허용·정상→허용). `User.loginId` Javadoc을 "최종 NOT NULL"→조건부 CHECK 현실로 정정. **email 로그인 잔재 없음 재확인**(`loadUserByUsername`=`findByLoginId`만·폴백 없음, 남은 `findByEmail`은 설정 조회·OAuth 첫 세션 브리지 등 *로그인 아님*). Flyway만(V15) — 코드 동작 변화 없음. TDD(Red: CHECK 부재로 온보딩+null 거부 안 됨 → Green) + 전체 스위트 그린. login_id 도입 5단계 전부 완료 ✅. (PR #156) |
| 2026-06-05 | **README 전면 최신화** — README가 "MVP·시간만 기록·이메일 로그인·SNS 추후 예정"에 머물러 그간 출하분과 크게 벌어져 있던 것을 현행화. 반영: ② 인증/식별 모델(login_id 공개 @핸들·nickname 표시·email 비공개 3분할), ③ 책 단위 기록·책장(상태·공개여부·알라딘 검색·제휴 링크·측정 책 필수), ④ 잔디·일자별 읽은 책, ⑤ 소셜(공개 프로필·검색·팔로우·차단·신고·팔로우범위 인기 drill-down), ⑥ 운영자 대시보드(통계·데이터 조회·PII 최소노출), ⑦ 미구현 설계(독서 MBTI·구독). 기술 스택에 Flyway·세션 외부화(JDBC) 추가, 로드맵 체크 갱신(책·SNS·admin·login_id 완료), 아키텍처에 Spring Session/Flyway, 도메인 규칙에 식별 분리·측정 책 필수·공개 경계, 관련 문서에 설계 메모 6종 링크. 문서만. (PR #157) |
| 2026-06-05 | **회원가입 폼 UX 개선**(스크린샷 피드백) — 가입 화면이 직관적이지 못했음. ① **입력 순서 재배치**: 이메일이 맨 위라 어색했던 것을 로그인 흐름과 중요도에 맞게 **아이디 → 비밀번호 → 닉네임 → 이메일 → 타임존**으로 변경(로그인 식별자인 아이디·비밀번호를 위로). ② **비밀번호 안내 추가** — 아이디에만 있던 형식 힌트를 비밀번호에도(`최소 8자`, `@Size(min=8)`과 일치, `minlength`/placeholder/`field-hint`). ③ **타임존 설명 추가** — 사용자가 왜 고르는지 몰라 막연하던 것을, "하루의 시작·끝(자정) 기준 → 잔디·일일 기록이 거주 지역 날짜에 맞게 쌓임"으로 안내. `signup.html`만(로직·검증 규칙 무변경, .java 없음). (PR #158) |
| 2026-06-05 | **회원 인증 하드닝 — 소셜 탈퇴 재확인 + 가입 이메일 열거 완화** — §하드닝 "(검토)" 두 항목 구현. ① **가입 이메일 열거 완화**: login_id 도입으로 email이 **비공개 속성**이 됐는데 가입 시 "이미 가입된 이메일입니다"가 이를 확인해 줘 열거가 됐다. `UserRegistrationService.register` 검사 순서를 **login_id(형식→유니크) 먼저·email 마지막**으로 바꾸고, `SignupController`가 `EmailAlreadyExistsException`·`DataIntegrityViolationException`을 **가입 성공과 동일한 `redirect:/login?registered`로 흡수**(계정 미생성, 응답만 동일 → 이메일 존재 여부 미노출). login_id는 공개 @핸들이라 "사용 중" 노출이 무해·UX상 필요해 그대로 필드 에러. 트레이드오프: 이메일 발송 인프라 없어 통지는 불가(잊고 재가입 시 로그인 단계에서 인지 — 열거 저항 표준 비용). ② **소셜 탈퇴 재확인**: OAuth는 비번이 없어 탈퇴 재인증이 없던 무방비(`confirm()` JS뿐)를, 본인 **@핸들(login_id) 타이핑** 서버사이드 게이트로 보강(GitHub "저장소 이름 입력" 패턴). `AccountService.deleteSocialAccount(email, confirmHandle)`(공백·선행 @·대소문자 정규화 비교)+`AccountDeletionConfirmationException`, `SettingsController` 분기·`settings.html` 소셜 전용 핸들 입력칸, LOCAL은 비번 재확인 유지. Flyway 없음. TDD(서비스 일치/관대매칭/불일치/LOCAL거부·컨트롤러 끝단·가입 silent-success Red→Green) + 전체 412 그린. (PR #159) |
| 2026-06-05 | **§하드닝에 「이메일 발송 인프라」 후속 항목 신설** — 세션 질문(로컬↔구글 같은 이메일 교차)에서 N-053(가입 이메일 미검증 → account pre-hijacking 정방향 갭) 발견 후, N-052(열거 통지)·N-053(가입 이메일 인증)·비밀번호 재설정 세 보안 갭이 **모두 "메일 발송 가능"을 공통 선결조건**으로 둠을 정리. 비용 구조 명시 — **직접 요금 ≈ 0**(SES 1,000통당 $0.10·월 센트, Resend/Brevo 무료 티어, 예산 무위협), **진짜 비용은 셋업·운영**(발신 도메인 검증·SPF/DKIM/DMARC·딜리버러빌리티·연동 코드). 보류 사유는 돈 아닌 ROI·운영 손. **착수 트리거**: `.click`→`.com`/`.app` 도메인 이전(§도메인 TLD 이전)과 묶으면 DNS·ACM 작업이 겹쳐 한계비용 최소. 학습노트 N-052·N-053 연계. 문서만. (PR #162) |
| 2026-06-05 | **순서 제약 명시 — 결제·채팅 전에 이메일 인증 선결** — "지금 이 위험 감수할 만한가?" 논의 결론을 박음. pre-hijacking(N-053)은 친구·소규모·가벼운 데이터 맥락에선 *감수 가능*(가능성·피해 둘 다 낮음, 이미 가입한 사람은 비대상)이지만, **계정에 돈(결제)·사칭 표면(채팅)이 묶이면 탈취 가치가 폭증**해 보류 불가가 된다. 따라서 §하드닝 「이메일 발송 인프라」(가입 이메일 인증)를 **§비즈니스 모델 구독 3단계(채팅)·결제 인프라의 하드 선행조건**으로 양쪽에 상호참조로 명시(비즈니스 모델 선결 목록 + 이메일 인프라 착수 트리거). 나중에 구독 착수 시 놓치기 쉬운 의존성 고정. 문서만. (PR #163) |
| 2026-06-05 | **ISBN 정규화 — 적재 시점(빈 값→null·하이픈/공백 제거)** — 책 동일성 키 `isbn13`이 인기 카운트의 group-by 키인데, 알라딘이 ISBN 없는 책에 `""`를 반환하면 서로 다른 책이 `""` 하나로 뭉치고 하이픈 유무로 같은 책이 쪼개진다. `Isbn.normalize`(하이픈·공백 제거 후 빈 값이면 null)를 신설하고 **`Book` 생성자 단일 통로**에서 적용(검색·수동·미래 경로 일괄) + 기존 행 백필 `V16__book_isbn13_normalize.sql`(MySQL·H2 공통 nullif/replace). 인기 카운트 쿼리는 이미 null/blank 키를 거르므로 무변경. ISBN10→13·개정판/세트 동일성은 후속(별도 과제)으로 분리. TDD(Isbn 경계값 Red→Green + Book.register 정규화 와이어링). (PR #164) |
| 2026-06-05 | **ISBN 개정판/세트 동일성을 "보류 — 아마 불필요"로 강등** — 능동 백로그에서 빼고 판단 근거만 남김(손대지 않기로 결정). 근본 원인: ISBN은 작품(Work)이 아니라 판본(Manifestation)을 가리킴(FRBR) → "같은 작품 다른 판본"은 태생적으로 다른 ISBN이라 isbn13 카운트가 조각남. 개정판=합칠 수도 있으나 작품 ID 상위 키를 알라딘이 안 줘 fuzzy 추론 필요(정답 없음), 세트=조회 막는 문제 아니라 세트 ISBN≠낱권 ISBN 동일성 모호. ROI는 사용자·책 규모 커져 조각남이 눈에 띌 때만 생김 → 그 전엔(영영) 불필요. ISBN10→13(#3)은 명확한 착수 트리거(수동 ISBN 입력) 있어 후속 유지. 문서만. (PR #166) |
| 2026-06-05 | **목표 달성 알람 백로그 항목 추가** — 측정 중 누적 잔여가 0이 되는 순간(=오늘 목표 달성) 사용자에게 알림을 주고 싶다는 요구. 타이머가 JS에서 1초마다 도므로(`dashboard.js`의 `goalMet`) "잔여 0 순간"을 이미 클라이언트가 계산 → 서버·DB·마이그레이션 불필요. 범위는 ①탭 안 알람(소리+화면 플래시)+②Web Notifications API(탭 떠 있으면 백그라운드여도 OS 알림)로 한정, ③Push API+Service Worker(탭 닫아도 알림)는 오버킬로 안 함(측정은 페이지 열어둔 채 일어남). 구현 메모: 상태 아닌 **전이(false→true) 순간 1회**에 걸기, 측정시작 클릭 제스처로 오디오 차단 없음, 권한 거부/구형 브라우저는 ①로 폴백. 문서만. (PR #167) |
| 2026-06-05 | **대시보드 인사말 → 작가 격언 랜덤 노출 백로그 항목 추가** — `님, 환영합니다`(`dashboard.html:20`) 자리에 작가 격언을 페이지 로드마다 랜덤 노출(설정 폼 없이 자연스럽게). 인사말이 htmx 라이브 영역 밖이라 측정 start/stop엔 안 바뀌고 전체 페이지 로드 때만 갱신 — 원하는 동작이 구조상 자연히 나옴. 소스 MVP=앱 내장 큐레이션 목록(외부 API는 핫패스 네트워크·영어·장애로 비추), 서버사이드 랜덤(SSR·JS 불필요), `QuoteService`+`Random` 주입(테스트 이음새, Clock 패턴), `DashboardController`에만 추가(`DashboardModel`은 htmx 공유라 금지), `@Cacheable` 금지. 🔮 미래 진화 3단계: ①전역 랜덤 → ②책장 작가(`book.author`) 필터 → ③사용자 저장 문장(하이라이트 신규 엔티티) 회전, `QuoteSource` 이음새로 소스만 교체. 문서만. (PR #168) |
| 2026-06-05 | **대시보드 인사말 → 작가 격언 랜덤 노출 — MVP 구현** — 백로그(#168)대로 인사말을 작가 격언으로 교체. `com.booktimer.quote` 패키지 신설: `Quote`(record), `QuoteService`(classpath `quotes.json` 적재 + `Random` 주입 이음새—Clock 패턴 N-010—로 `random()` 결정적 테스트, 매 호출 새로 뽑아 캐시 없음), 격언 18개 큐레이션(`quotes.json`). SSR 앱 ObjectMapper 빈 없음(T-022) → `new ObjectMapper()` 직접 적재. `DashboardController`가 **전체 페이지 경로에서만** `quote` 모델 적재(`DashboardModel` 아님 — htmx 라이브 영역과 공유라 start/stop마다 헛돌고 프래그먼트 밖이라 안 그려짐; 잔디 graph와 같은 자리). `dashboard.html` 인사말을 `${quote.text()} — ${quote.author()}`로 교체(`.greeting.quote` CSS — 작가 작게·흐리게). 인사말이 라이브 영역 밖이라 측정 start/stop엔 안 바뀌고 페이지 로드 때만 랜덤 갱신. TDD(QuoteServiceTest: 적재·주입 Random 결정적 선택·항상 목록 내 / DashboardControllerTest: 모델 quote 적재 Red→Green) + 전체 스위트 그린. 🔮 미래(②책장 작가 필터 ③사용자 저장 문장)는 백로그 유지. (PR #169) |
| 2026-06-05 | **작가 격언 — DB 이전 + 운영자(admin) 관리** — 격언을 정적 JSON에서 DB로 옮겨 **재시작 없이 추가/삭제**(운영자 요청). `Quote` record→`@Entity`(text varchar500/author varchar100, BaseTimeEntity), `QuoteRepository`(`findAllByOrderByIdDesc` 최신 먼저), 선택 로직은 순수 `QuotePicker`(Random 주입 결정적 테스트)로 분리, `QuoteService`는 repo 백업 + **빈 목록이면 폴백 격언 반환**(운영자가 다 지워도 대시보드 안 깨짐). Flyway `V17__create_quote_table.sql`(테이블 + 기존 18개 seed, created_at/updated_at은 INSERT가 auditing 안 타므로 `current_timestamp` 직접). 운영자 화면 `/admin/quotes`(`AdminQuoteController` — 목록+추가+삭제, POST는 CSRF, 공백은 service가 거부→플래시 흡수, `/admin` 랜딩에 진입 링크). `quotes.json` 삭제, `dashboard.html`은 record→entity 따라 `${quote.text}`(getter)로. **함정: 생성자 2개(public 주입 + 테스트용 package-private)면 Spring이 no-arg를 찾다 NoSuchMethodException → 주입 생성자에 `@Autowired` 명시**. TDD: QuotePickerTest(결정적 선택)·QuoteServiceTest(@DataJpaTest add/all 최신순/delete/폴백/공백거부)·AdminQuoteControllerTest(미인증 리다이렉트·USER 403·ADMIN 추가/삭제/공백 Red→Green) + FlywayMigrationTest(V17 validate) + 전체 스위트 그린. (PR #170) |
| 2026-06-05 | **plan.md 정리 — 격언 기능 섹션 실제 구현과 화해** — 세션 작업(#167~#170) 기록 점검. 격언 섹션의 초기 설계 기록이 실제 출하와 어긋나던 부분 정리: ③ DB는 "오버킬"→PR #170에서 채택으로, "QuoteService 불변 목록"→repo 백업으로, 소스 비교에 채택 PR 표기, 구현 메모에 "⚠️ PR #170에서 바뀐 것"(JSON→DB·record→entity·템플릿 getter) 추가. 근거는 보존하되 무엇이 실제로 출하됐는지 명확화. 문서만. (PR #171) |
| 2026-06-05 | **admin 대시보드 로그아웃 위치 정상화**(스크린샷 피드백) — 운영자 화면에서 로그아웃 버튼이 인사말 바로 밑(상단)에 있어 일반 대시보드와 불일치하고 어색했음. `admin.html`의 로그아웃 form을 상단에서 떼어내 **화면 최하단**(`← 대시보드` 링크 다음)으로 이동 — dashboard.html과 동일한 "로그아웃은 항상 화면 끝" 패턴. 순수 템플릿(.html)만, 로그아웃 action(`/logout`)·로직·테스트 무변경. (PR #173) |
| 2026-06-05 | **admin 대시보드 `← 대시보드` 링크 제거**(스크린샷 피드백) — 운영자 메인에서 일반 대시보드로 가는 링크가 불필요(운영자 동선상 의미 없음). `admin.html`에서 `← 대시보드` link-row 삭제. 순수 템플릿(.html)만, 테스트 영향 없음(테스트는 링크 미검증). (PR #174) |
| 2026-06-05 | **admin 사용자 목록 박스 폭 확대**(스크린샷 피드백) — `/admin/users` 표가 7컬럼인데 모바일 우선 폭(`.container` 460px)에 갇혀 가입일·온보딩 컬럼이 세로로 깨져 보기 불편했음. 운영자 화면은 데스크톱 사용이 대부분이라, `admin-users.html` 페이지 내 `<style>`에서 `.container { max-width: 920px }`로 **이 페이지에서만** 덮어씀(페이지 style이 app.css 링크보다 뒤라 다른 화면 460px 무영향). 순수 템플릿(.html)만, 테스트 영향 없음. (PR #175) |
| 2026-06-05 | **사용자 검색 페이지 — 친구 추천(무작위 MVP) 추가 + 하단 버튼 정정**(스크린샷 피드백) — `/search`가 아이디 검색만 있어 휑했음. ① **친구 추천**: `아이디로 찾기`↔하단 버튼 사이에 무작위 10명을 추천 카드로 노출. `UserSearchService.recommend(viewer, limit)` 신설 — 검색과 동일 노출 불변식(운영자·본인·차단 대칭 제외) 후 메모리 셔플→상위 N. **지금은 단순 무작위**가 핵심(콜드스타트엔 어차피 성향 추천도 정밀도 낮음, 라인 528) — 후속 고도화는 이미 백로그에 있는 **비즈니스 모델 구독 2단계 「사람 추천」(성향 기반, 라인 503)** 의 MVP 전초로 연결(이 PR이 그 프리미엄 기능을 완료하는 건 아님). 검색 레이트리밋과 무관하게 페이지 기본 구성으로 항상 렌더. 추천 행은 검색 결과 행 마크업(프로필 링크+공개책수+팔로우 버튼) 재사용. ② **하단 버튼 정정**: `📚 내 책장`(`/books`)→`📖 공개된 내 책장`(`/u/{내loginId}`) — 검색→내 비공개 책장 동선은 거의 없고, 프로필 페이지는 본인이 봐도 PUBLIC만 보이는 = 정확히 "공개된 내 책장". `← 대시보드`는 유지. TDD(추천 본인·운영자 제외/최대 10명 cap/myLoginId 모델 노출 Red→Green) + 전체 스위트 그린. (PR #176) |
| 2026-06-05 | **공개 프로필 책장 — 상태 필터 추가**(스크린샷 피드백) — 공개 프로필(`/u/{loginId}`)의 "공개 책장"이 그냥 리스트로 나열만 했음. 내 책장(`/books`)은 읽고 싶음/읽는 중/완독으로 필터링되게 발전했는데 여기는 안 됐음 → **동일 패턴으로 통일**. `ProfileController`에 `?status=` 파라미터(`parseStatus` — 없거나 잘못된 값이면 전체, BookController와 동일 규칙) 추가해 PUBLIC 책 목록만 상태로 거르고 `shelfFilter`·`statuses` 모델 노출. **책 목록만** 필터하고 잔디·팔로우 등 다른 영역은 무영향. `profile.html`에 책장과 같은 `shelf-filter` 칩 nav(전체+상태 3개, 활성 표시) + 필터 인지 빈 메시지 추가(기존 `.shelf-filter` CSS 재사용, 새 스타일 없음). TDD(상태 필터 일치/status 없으면 전체+statuses 제공/잘못된 status는 전체 Red→Green) + 전체 스위트 그린. (PR #177) |
| 2026-06-05 | **개인정보처리방침 갱신 — 실제 저장 데이터와 화해** — 방침(`privacy.html`, 시행일 06-03)이 그간 추가된 저장 속성과 어긋나 있어 정정. ① `이메일` 설명 "계정 식별자(로그인 ID)"가 사실과 다름 — 로그인 식별자는 이제 `login_id`(V13, 공개 @핸들). **로그인 아이디(@핸들)** 항목 신설(로그인·식별·공개 프로필 URL `/u/아이디`·검색 노출, 불변)하고 이메일은 "생성·복구·문의용(비공개)"으로 수정. ② **소셜 활동 데이터**(팔로우/팔로워·차단 목록·신고 내역[사유+자유 입력 설명]) 항목 추가 — V9~V11에서 저장하나 방침에 누락이었음. ③ 신설 §3 **「다른 이용자에게 공개되는 정보」** — @핸들·닉네임·공개 책장·독서 잔디가 검색·공개 프로필로 노출됨을 고지(이메일·비번·비공개 책·신고는 비공개). ④ §2 이용 목적에 소셜 기능·신고/차단 운영 추가, 이하 섹션 번호 +1, 개정일 표기. Quote(V17)는 운영자 큐레이션이라 개인정보 아님 → 제외. 순수 템플릿(.html)+문서만, PrivacyControllerTest("이메일" 포함 단언) 무영향. (PR #178) |
| 2026-06-05 | **개인정보처리방침 문의처 이메일 교체 + 전용 메일 분리 백로그 명시** — 방침 문의처에 노출되던 개인 Gmail(`kimsadol369785@gmail.com`)을 결합이 적은 다른 개인 메일(`tlatldhs0504@naver.com`)로 교체. 원래 의도는 **BookTimer 전용 메일** 생성이었으나 같은 전화번호로 Gmail을 너무 자주 만들어 **Google이 신규 발급 차단** → 당장 불가. 전용 메일 분리는 미해결로 남기고 plan.md 법무 섹션에 백로그 항목 신설(이메일 3층 분리 — A.앱 문의처 / B.OAuth 동의화면 support·developer / C.GCP 소유 계정은 이전 불필요). 순수 템플릿(.html)+문서만, 테스트 무영향. (PR #179) |
| 2026-06-06 | **대시보드 '남은 독서 시간'·'측정' 두 카드 → 한 카드로 병합**(스크린샷 피드백) — 타이머 카드와 측정 카드가 따로 있고, 측정 시작 시 `진행 중인 측정이 없습니다.`가 `📖 읽는 중: <책>`으로 바뀌는 상태 문구가 **이미 책 선택이 눈에 보여 중복**이었음. 두 `.card`를 하나의 `.card.timer-card`로 합치고 `측정` 헤더와 상태 문구(`진행 중인 측정이 없습니다.`/`읽는 중: …`/`측정 진행 중(책 미지정)`) 제거. 유지: 타이머·경과·배지·`읽을 책` 선택+`측정 시작`·활성 중 `이 책 누적 독서`+`측정 종료`·책 0권 안내. 같은 `dashboard :: live` fragment(id·구조 유지)라 htmx start/stop swap 무영향, `DashboardControllerTest`는 드롭다운 분류만 단언해 무영향. 큰 숫자 밑 답답함 방지로 `.timer-controls{margin-top:18px}` 1줄 추가. dashboard.html+app.css만(.java 없음). (PR #180) |
| 2026-06-06 | **목표 달성 알람 구현 (백로그 #167 완료)** — 측정 중 잔여가 0이 되는 **전이(미달→달성)**에 1회 알람. ①소리(WebAudio "딩-동" 비프, 에셋 불필요)+화면 플래시(`.timer-card.goal-flash` keyframe, prefers-reduced-motion 무효화) + ②Web Notifications(권한 허용 시, 측정 진입 시 1회 요청, 거부/구형은 ①로 폴백). 서버·DB·Flyway 0(`dashboard.js`+app.css+dashboard.html `:class` 바인딩만, .java 없음). 전이 로직은 부수효과 없는 순수 `createGoalAlarm`(prime/update — armed 플래그로 rising edge만 발화, 미달 시 재무장)으로 분리, 브라우저/Node 양용 `module.exports` 가드. JS 테스트 인프라가 없어 **Node 내장 러너**(`src/test/js/goal-alarm.test.js`, 무의존성, `node --test`)로 Red→Green: 미달→달성 1회·로드시 이미달성 무발화·계속미달 무발화·재무장 재발화 4케이스. (PR #181) |
| 2026-06-06 | **공개 프로필 → "책방" 리브랜딩(컨셉 확립)** — 다른 사용자에게 보이는 공개 프로필 페이지(`/u/{loginId}`)를 **개인의 "책방"** 컨셉으로 앞으로 통일(사용자 결정). 사용자 노출 문구만 교체(로직·라우트·모델 무변경): 프로필 페이지 제목/헤딩 `{닉네임}님의 (공개) 프로필`→`{닉네임}님의 책방`, 대시보드 하이라이트 타일 `내 공개 프로필`/`남에게 보이는 내 모습`→`내 책방`/`남에게 보이는 내 책방`, 검색 하단 `📖 공개된 내 책장`→`📖 내 책방`, 팔로우/차단 목록 하단 `👤 내 프로필`→`📖 내 책방`, 가입폼 아이디 힌트 `프로필 주소`→`책방 주소(/u/아이디)`, privacy 방침은 법적 명확성 위해 `공개 책방(공개 프로필)`로 병기. 컨셉상 "책방=개인이 책을 진열한 공개 공간"(향후 SNS·추천 동선의 정체성). 순수 `.html` 7종만(.java 없음 → 테스트 게이트 자동 skip). 검증: 템플릿 렌더 테스트(Profile/Dashboard/Privacy/FollowList/BlockController) `--rerun-tasks` BUILD SUCCESSFUL(Thymeleaf 파싱 정상). (PR #182) |
| 2026-06-06 | **§전략 우선순위(retention 우선 / 2-audience) 신설 + 도서성향·구독 섹션 audience 주석** — 마케팅·재방문율 논의 결론을 plan.md 최상단 렌즈로 박음. ① thesis(입문/포기형 위한 "하루 최소 시간") 재확인 + **두 엔진 구분**(A 습관 타이머=입문자·솔로·콜드스타트 면역 / B 사람 잇는 재미=헤비 리더·네트워크 retention), **Strava 모델**(혼자 들어와 사람 때문에 남는다)로 도서 성향을 "분석 정확도"가 아닌 "소셜 연료"로 재정의. ② **순서**: A retention 먼저 → 밀도 차면 B → 채팅 맨 끝. ③ **retention 레버 백로그 신설(현재 최우선)**: ①재참여 알림(일일 넛지, 떠난 사용자 호출 — 현재 전무, 푸시/이메일 인프라 선결) ②사후 수동 입력(측정 깜빡한 독서 유실 방지) ③부채 용서 장치(streak freeze, 입문자 죄책감 이탈 차단). ④ §도서 성향·§구독에 "엔진 B=헤비 리더용·밀도 선결·채팅 맨끝" audience 주석 추가. 메모리 `project-thesis` 저장. 문서만. (PR #183) |
| 2026-06-06 | **재참여 알림 채널·트리거 결정 (서버발 = 이메일)** — "목표 알람이 실제로 안 온다"(웹 인앱 알림 한계: 탭 죽으면·모바일 백그라운드 throttle로 전이 감지 불가)에서 출발. **채널=이메일로 확정**(모든 사용자가 가입 시 이메일 필수 → 기기 권한 불필요·아이폰 제약 우회·크로스플랫폼). **역할 분담**: 실시간 "목표 달성 순간"=인앱 알람 유지 / "오늘 안 읽었어요" 재참여=이메일(retention 가치 큰 쪽). **트리거=안 읽은 날 저녁 넛지**(유저 TZ 그날 세션 0이면 1통). 선결: 이메일 발송 인프라(§하드닝, 같은 작업)·가입 이메일 인증과 묶어 딜리버러빌리티·정보통신망법(광고성 opt-in·수신거부·야간발송 제한) 검토. §전략 레버 ① 갱신 + §이메일 발송 인프라에 4번째 소비자(재참여 넛지) 추가 + 우선순위 중→상 승격 신호. 문서만. (PR #184) |
| 2026-06-06 | **리브랜딩(서비스명 변경) 예약 항목 신설** — 현재 이름 `BookTimer`가 *타이머*(엔진 A=입문자 기능)만 강조해, 타깃이 확장된 엔진 B(헤비 리더)에게 안 와닿는다는 문제 제기. 지금은 더 나은 이름 미정·SNS 미완이라 **확정 보류**하되, §전략 우선순위에 「🔖 리브랜딩」 예약 절 신설 — **트리거(=헤비 리더 SNS 기능 어느 정도 완성·출하 시점, 도서성향/사람추천 중 1+ 가동)** 와 비용 최소화 묶음(§도메인 TLD 이전·§이메일 인프라와 함께)·새 이름 방향("책방" 컨셉 결)만 박음. Claude가 그 시점에 먼저 리브랜딩을 제안하도록 한 *예약 신호*. 문서만(plan.md만). (PR #187 예정) |
| 2026-06-06 | **대시보드 로그아웃 버튼 간격·hover 통일**(스크린샷 피드백) — 일반 대시보드에서 로그아웃 버튼이 바로 위 `설정` 타일과 너무 붙어 있고, hover 피드백(`.btn-ghost:hover`는 배경만 살짝)이 다른 타일(테두리·그림자 강조)과 달라 통일성이 없었음. ① 간격: 로그아웃 `<form>`에 `.logout-form{margin-top:18px}`로 설정 타일에서 띄움. ② hover: `.btn-logout:hover`에서 글자·테두리를 빨강(`--danger`)으로 강조(다른 타일처럼 명확한 hover 피드백 + "떠나는" 동작이라 빨강). 다른 `.btn-ghost`(책장·페이저 등)는 무영향(전용 `.btn-logout` 클래스). 순수 `dashboard.html`+`app.css`만(.java 없음 → 테스트 게이트 skip). (PR #186 예정) |
| 2026-06-06 | **목표 달성 인앱 알람 제거 (PR #181 철회)** — 실사용 결과 실효 없음: 인앱 알람은 *탭 살아있고 측정 중*에만 동작하는데 모바일에서 책 읽으며 화면 꺼지면 백그라운드 throttle로 전이 감지조차 못 함(아예 안 울림) + 못 지킬 약속으로 알림 권한 요청하는 UX 마이너스. #181의 "탭 열어둔 채 측정" 가정이 종이책·모바일 현실에서 깨짐. **제거**: `createGoalAlarm`·소리(WebAudio)·OS알림·권한요청·플래시(CSS `.goal-flash`+keyframe)·전이 로직·테스트(`src/test/js/`). **유지**: 달성 배지("오늘 목표 달성! 🎉" `goalMet`)·타이머 초록색(`.met`) — #181 이전부터 있던 항상-동작 시각 피드백. 달성 순간을 탭 밖으로 알리는 건 서버발(이메일/푸시) 인프라 붙을 때 재검토(§전략 레버 ①). plan.md 화해: §전략 레버 ① 역할분담·기능 섹션 상태(완료→제거)·#184 "인앱 알람 유지" 정정. dashboard.js+html+css만(.java 없음 → 테스트 게이트 skip). 검증: `node -c` 문법 + 템플릿 렌더 테스트. (PR #185 예정) |
| 2026-06-06 | **내 책장 무한 스크롤 추가**(실사용 피드백) — `/books` "내 책장"이 `myBooks` 전체를 한 번에 렌더해 책이 많으면 리스트가 길어지고 하단 네비(← 대시보드 등)까지 스크롤이 멀었음. 숫자 페이지(1·2·3) 대신 **스크롤 기반(무한 스크롤)** 요구(모바일 UX). **클라이언트 사이드**로 구현(개인 책장은 보통 수십 권 → 전체를 이미 받아옴, 서버 왕복 없이 즉시 펼쳐져 스크롤이 빠름·서버/DB/Flyway 무변경): `books.js`가 처음 5권만 보이고 나머지는 `hidden`, 리스트 끝 센티넬이 화면에 들어오면(`IntersectionObserver`, rootMargin 160px) 다음 5권 펼침. 5권 이하·IO 미지원·JS 꺼짐이면 전체 표시 폴백. 표지 `loading="lazy"`로 안 펼친 책 표지는 미리 안 받음. `books.html`(스크립트 링크·`#shelf-list` id·표지 lazy) + `books.js` 신설 + app.css `.shelf-sentinel`만(.java 없음 → 테스트 게이트 skip). 검색결과 페이저(숫자)는 별개라 그대로 둠. (PR #188) |
| 2026-06-06 | **무한 스크롤 즉시 핫픽스 — `[hidden]`이 안 먹던 버그**(#188 직후 실사용 무효과) — 배포 후 "바뀐 게 없고 다 보인다"는 피드백. 원인은 **CSS 특정성 함정**: `books.js`가 `li.hidden=true`로 숨겨도 `app.css`의 `.book-row{display:flex}`(author 규칙)가 브라우저 기본 `[hidden]{display:none}`(UA 스타일·최저 우선순위)을 이겨 **하나도 안 숨겨짐**. 수정: `.book-row[hidden]{display:none}` 추가(속성 선택자라 `.book-row`보다 특정성↑ → !important 불필요). app.css 1줄(+주석)만(.java 없음 → 테스트 게이트 skip). (PR #189) |
| 2026-06-06 | **내 책장 무한 스크롤 철회 → 고정 높이 스크롤 박스로 전환**(#189 후에도 "바뀐 게 없다") — 배포·캐시(`no-store`)·헤더 다 확인해 파일은 최신·정상이었으나, **무한 스크롤 방식 자체가 큰 화면에서 무효**였음: `IntersectionObserver`가 5권 펼친 뒤에도 센티넬이 뷰포트 안이면 연쇄로 계속 펼쳐(cascade) 데스크톱에선 사실상 전부 노출 + 무한 스크롤은 스크롤할수록 목록이 늘어 *하단 네비 도달*이라는 **원래 목표와 상충**. 사용자 선택으로 **고정 높이 스크롤 박스** 채택: `#shelf-list{max-height:60vh;overflow-y:auto;scrollbar-gutter:stable;overscroll-behavior:contain}` — 목록만 박스 안에서 스크롤, 페이지는 짧게 유지돼 하단 메뉴 늘 보임. 책 적으면 max-height 미도달로 스크롤바 안 생김(자연 폴백). 상태 필터 칩은 ul 밖이라 고정. **books.js 삭제**(JS 불필요), books.html 스크립트 태그 제거(`#shelf-list` id·표지 `loading="lazy"`는 유지). app.css+books.html만(.java 없음 → 테스트 게이트 skip). (PR #190 예정) |
| 2026-06-06 | **책방(공개 프로필) 공개 책장에도 고정 높이 스크롤 박스 적용**(#190 후속 — 같은 UX를 책방에도) — `/books` 내 책장에만 있던 스크롤 박스를 책방(`/u/{loginId}`, profile.html)의 "공개 책장"에도 동일 적용. 책이 많으면 공개 책장도 똑같이 길어져 하단 네비가 멀어지는 문제. **id→공용 클래스로 일반화**: app.css `#shelf-list`→`.shelf-scroll`(규칙·주석 그대로, 두 페이지 공용), books.html `id="shelf-list"`→`class="...shelf-scroll"`, profile.html 공개 책장 `<ul>`에 `shelf-scroll` 부여. 겸사겸사 profile.html 표지 `loading="lazy"`도 books.html과 통일. `#shelf-list` id는 JS 참조 없음(books.js는 #190에서 삭제됨) 확인 후 안전 전환. app.css+books.html+profile.html만(.java 없음 → 테스트 게이트 skip). (PR #191 예정) |
| 2026-06-06 | **내 책장 '책 검색'+'직접 추가' 두 카드 → 한 '책 추가' 카드로 통합**(실사용 피드백) — `/books` 상단에 "책 검색"·"직접 추가"가 별개 `<section class="card">` 2개였는데 둘 다 책을 추가하는 박스라 중복·산만. 사용자 선택으로 **검색 우선 + 직접입력 펼치기**(progressive disclosure) 채택: 한 "책 추가" 카드 안에서 검색(알라딘, 표지·ISBN까지 받는 주 경로)을 위에 두고, 직접 추가 폼은 native `<details class="manual-add">`(summary "찾는 책이 없나요? 직접 추가", ▸/▾ 토글)로 접어둠. **검색 0건이거나 검색 비활성이면 `th:open`으로 자동 펼침**(Thymeleaf 고정값 boolean 속성 — true면 `open` 렌더, false면 생략)이라 못 찾았을 때 바로 직접 입력 가능. **JS 0**(native `<details>`, CSS만)·서버/DB/모델 무변경. books.html(두 섹션 병합·details 래핑)+app.css(`.manual-add` 스타일, `.report-box` <details> 패턴 차용)만(.java 없음 → 테스트 게이트 skip). 검증: `BookControllerTest`(GET /books 실제 렌더, searchEnabled=false라 th:open 분기 통과) BUILD SUCCESSFUL. (PR #192 예정) |
| 2026-06-06 | **'직접 추가' `<details>`가 안 접히던 버그 핫픽스**(#192 배포 후 브라우저 검증서 발견) — 배포 후 라이브 DOM 측정 결과 `details.open=false`(닫힘)인데 폼 `offsetHeight=135`(보임): **접어도 직접 추가 폼이 늘 노출**됐음. 원인은 **cascade origin 함정** — `.book-manual-form{display:flex}`(author 규칙)가 브라우저 UA의 닫힘-숨김(`details:not([open])>*:not(summary){display:none}`)을 이김(author > UA origin이라 특정성과 무관하게 author 승). 예전 `[hidden]`이 `.book-row{display:flex}`에 져서 안 숨겨지던 함정(#189)과 **동일 계열**. 수정: `.manual-add:not([open]) .book-manual-form{display:none}`로 닫힘일 때만 재숨김(특정성 0,3,0 > `.book-manual-form` 0,1,0). 검증: 라이브 페이지에 규칙 주입 → 닫힘 폼높이 135→0, 펼침 135 유지(FIX OK). app.css 1줄(+주석)만(.java 없음 → 테스트 게이트 skip). `th:open` 서버 렌더는 정상이었음(무결과 URL 서버 HTML에 `open="open"` 확인). (PR #193 예정) |
| 2026-06-06 | **책 공개/비공개 드롭다운 → "책방 공개" 토글 스위치**(실사용 피드백 — 공개 설정이 직관적이지 않음) — 책장 각 책의 공개여부 `<select>`가 ① 2-state엔 부적합한 affordance(현재 상태가 한눈에 안 보임) ② "공개/비공개"가 *무엇에* 공개되는지 안 알려주는 추상 문구 ③ 책방과의 연결 단서 없음. 사용자 선택으로 **토글 스위치** 채택: `<button name="visibility" value="(현재의 반대)">`가 클릭 시 반대 값을 POST해 상태를 뒤집고(JS 0·순수 폼 제출), 라벨/아이콘이 현재 상태를 보여줌(🌍 책방 공개 accent / 🔒 비공개 muted), 미니 트랙+노브로 on/off 시각화, `aria-pressed`로 토글 상태 보조기술 전달. **결과 문구**로 직관 강화 — "공개"→"책방 공개". 추가로 내 책장 상단에 **"🌍 책방 공개로 켠 책은 내 책방에서 누구나 볼 수 있어요"** 안내문구(`/u/{loginId}` 링크 겸 — 기존에 없던 books→내 책방 동선 신설)로 *공개=책방 노출* 인과를 명시. 백엔드 `POST /books/{id}/visibility` 무변경(같은 param)이라 기존 소유권·IDOR 테스트 유지, 안 쓰게 된 `visibilities` 모델 속성 제거. 링크용 `loginId` 모델 적재는 **TDD Red→Green**(OAuth 첫 세션 principal=email 위험 회피 위해 `#authentication.name` 아닌 도메인 `user.getLoginId()` 사용), 토글 마크업은 MockMvc 통합 렌더 테스트가 검증. books.html+app.css+BookController.java+테스트. (PR #195 예정) |
| 2026-06-06 | **공개 토글 변경 시 뜨던 성공 플래시 메시지 제거**(#195 후속 실사용 피드백) — 토글을 누르면 책 추가 박스 상단 `.alert`에 "'…'을(를) 공개로 바꿨습니다." 메시지가 떴는데, **토글 스위치(🌍/🔒)가 바뀐 상태를 이미 보여줘 중복 알림**이라 군더더기. `BookController.setVisibility` 성공 경로의 `addFlashAttribute("message", …)` 제거(에러 플래시는 IDOR 시에만·드물어 유지). 공유 `.alert`는 책 추가/삭제용이라 그대로 둠(visibility 성공만 무메시지). **TDD Red→Green**: `setVisibility_byOwner`에 `flash().attributeCount(0)` 단언 추가 → 성공 시 플래시 0 확인. BookController.java+테스트만. (PR #196 예정) |
| 2026-06-06 | **CLAUDE.md 🧪 TDD에 「테스트 깊이 가이드」 추가** — "Claude가 빨리 짜니 사람 기준보다 더 자세히 짜도 되지 않나"라는 논의 결론을 작업 규칙으로 박음. 핵심: 작성 시간 제약은 풀렸지만 **유지보수·브리틀니스/실행 시간/신호 희석/독립성 함정(구현·테스트를 둘 다 Claude가 짜면 같은 잘못된 가정 공유)**은 안 사라지므로, 늘어난 예산은 "양산"이 아니라 **옳은 곳에 깊이**로 쓴다. 원칙(테스트마다 "단독으로 잡는 진짜 실패가 무엇인가"에 답 가능하면 추가, 못 하면 보류) + 깊게/누르기 2열 표(경계값 전수·실패/권한 경로·불변식 vs 프레임워크 동작·UI 문자열·getter/구현 디테일). Red→Green 절차와 커밋 게이트 사이에 신설. 문서만(CLAUDE.md+plan.md). (PR #197 예정) |
| 2026-06-06 | **친구 추천에 온보딩 전(아이디 미정) 사용자가 노출되던 버그 수정**(실사용 피드백) — `/search` 친구 추천에 회원가입 직후(아이디·닉네임 미정) 사용자가 보였음. 원인: OAuth는 `registerOAuth`로 `login_id=null, onboarded=false` row를 먼저 INSERT(온보딩에서 login_id 확정 — 정상, N-051)하는데, `UserSearchService.recommend`가 `findAll()`+메모리 필터(운영자·본인·차단)에서 **핸들(login_id) 유무를 안 봐** null-state 사용자가 그대로 추천됨 → 카드의 `/u/{loginId}` 링크가 `/u/null`로 깨지고 팔로우도 불가. 검색(`search`)은 `login_id LIKE`라 `NULL` 미매칭으로 자연 제외돼 무영향(차이의 핵심). 수정: `recommend`에 `.filter(u -> u.getLoginId() != null)` 1줄 — "공개 핸들 있는 사용자만 추천". 불변식은 실패 모드(깨진 링크·팔로우 불가)에 정확히 대응하는 가장 좁은 `loginId!=null` 선택(`isOnboarded()`는 LOCAL 온보딩전까지 배제하는 과잉). **TDD Red→Green**: `recommend_excludesUsersWithoutLoginId`(OAuth 온보딩 전 사용자 추천 제외) Red 확인 → fix → Green + 기존 search/recommend 회귀 그린. 학습노트 **N-055** + CLAUDE.md 테스트 깊이 가이드에 "발견/목록 기능은 null-state 엔티티 배제 경계 테스트 필수" 예시 동봉. UserSearchService.java+테스트+문서. (PR #198 예정) |
| 2026-06-06 | **남의 책방에서 바로 구매 — 구매 버튼을 공개 프로필 책장에 추가**(실사용 피드백) — 다른 사람의 책방(`/u/{loginId}`)에서 책을 보다 사고 싶어도 구매 버튼이 없어, 내 책장으로 가 검색·추가 후에야 구매할 수 있던 비효율 동선. 공개책 행에 구매 버튼 추가. 기존 `/books/{id}/buy`는 소유권(IDOR) 전용이라 재사용 불가 → **공개(PUBLIC) 여부를 게이트**로 둔 `BookService.recordPublicPurchaseClick` + 엔드포인트 `GET /u/{loginId}/books/{id}/buy` 신설(비공개·없는 책은 임의 id 탐침을 거부, `loginId`는 라우팅·복귀용·조회는 `bookId`). 클릭 집계 주체는 **책 주인 카운트**(사용자 결정 2026-06-06). **TDD Red→Green**: 서비스 4(공개+링크 집계 / 비공개 거부 / 링크없음 / 없는 id) + 컨트롤러 2(공개 리다이렉트·집계 / 비공개 프로필 복귀) + 프로필 렌더 2(구매 앵커 노출 / 링크 없으면 미노출). 렌더 음성 테스트는 주석 속 리터럴 `{loginId}`에 안 걸리게 **해석된 href**(`/u/openking/books/`)로 단언(처음 `/buy`로 단언했다 주석 누설로 Red — 정밀화). profile.html+BookService.java+BookController.java+테스트. (PR #199 예정) |
| 2026-06-06 | **클릭 카운트 "책 주인 귀속" 결정의 근거를 코드/문서에 기록**(#199 후속 — 결정은 했으나 근거가 없던 것 보강) — "왜 viewer가 아니라 책 주인 행에 집계하나" 질문에 답: 카운트 목적이 *"어떤 책이 구매 의향을 내나"(제휴 수익 분석, 알라딘 3%가 현 유일 수익 토대)*이고 **행(사용자별 책 1권) 단위 적립→ISBN 롤업**으로 읽으므로 '주인'은 화면에 뜬 행일 뿐 보상·귀속 의미가 없다(주인 수수료 정산 모델이 없는 한). 세 선택지 중 "집계 안 함"이 최대 손실(남의 책방발 구매 의향=소셜 커머스 핵심 신호). **재검토 트리거**: 보상 정산 도입 시 viewer/owner 분리. 확인된 사실: `getClickCount`는 **테스트에서만** 호출 — 현재 카운트는 소비처 없는 write-only 적립(결정 실효 위험 낮음). `recordPublicPurchaseClick` javadoc + plan.md ②-b에 근거 기록(문서·주석만, 로직 무변경). (PR #200 예정) |
| 2026-06-06 | **상단 로고 클릭 → 메인 대시보드 이동**(실사용 피드백 — 편의성) — 메인 대시보드로 가려면 매 페이지 최하단 '← 대시보드' 버튼까지 스크롤해야 했음. 상단 로고(`📚 BookTimer`)를 `<a class="brand-home" th:href="@{/}">`로 감싸 클릭 시 홈(`/`)으로(웹 표준 "로고=홈" 관행). 링크 기본 스타일(파란색·밑줄) 제거하고 호버 시에만 제목 밑줄로 단서. **적용 범위**: 로그인 후 앱 페이지 10곳(대시보드·책장·책방·검색·팔로우목록·차단목록·설정·기록·책상세·책읽는사람). **제외**: 로그인/회원가입/온보딩(`/`가 인증 리다이렉트로 되돌아오는 무의미 루프)·약관/에러·관리자 페이지(로고가 아니라 페이지 제목이고 허브는 `/admin`). HTML/CSS만이라 테스트 게이트 skip(`.java` 무변경), 전체 스위트로 템플릿 렌더 회귀만 확인. 10 templates+app.css. 곁들여 `.gitignore`에 `.pr-body-tmp` 등록(이번에 `git add -A`로 PR 본문 임시파일이 실제로 커밋에 딸려 들어가 발견 — `.commit-msg-tmp`만 있고 `.pr-body-tmp`는 빠져 있었음, T-026 확장). (PR #201 예정) |
| 2026-06-06 | **본인 책방에선 구매 버튼 숨김**(#199 후속 실사용 피드백) — 남의 책방엔 구매 버튼이 맞지만 내 책방엔 불필요(내 책은 이미 내 것). 같은 `profile.html`이 본인/남 양쪽을 그리는데 모델에 이미 `self` 플래그가 있어, 구매 버튼·제휴 안내문 조건에 `and !self`만 더하면 됨. **헛디딤**: 처음 `${b.purchaseLink and !self}`로 했더니 9개 테스트가 `SpelEvaluationException` — 단독 `th:if="${문자열}"`은 Thymeleaf truthiness로 되지만 `and`로 묶으면 SpringEL이 피연산자를 boolean으로 강제해 String을 못 받음 → `${!#strings.isEmpty(b.purchaseLink) and !self}`로 명시 술어화(**T-031 확장**에 기록). **TDD Red→Green**: 본인 책방+공개책+링크여도 구매 라우트 미렌더 음성 테스트 추가 Red 확인 → 템플릿 수정 → Green + 기존 "남의 책방엔 버튼 노출" 회귀 그린. profile.html+ProfileControllerTest+troubleshooting. (PR #202 예정) |
| 2026-06-07 | **사용자 검색 친구 추천·검색 결과 '팔로우' 버튼 크기를 '언팔로우'에 맞춰 통일**(실사용 피드백 — 팔로우가 부담스럽게 큼) — 리스트 행 버튼 크기 축소 규칙 `.book-actions .btn-ghost, .book-actions .btn-danger { padding:6px 10px; font-size:.85rem }`에 `btn-primary`(팔로우)가 빠져 있어, 팔로우만 베이스 크기(`padding:12px 18px`+기본 폰트)로 남아 ~14px 더 컸음. 그 규칙에 **`.book-actions .btn-primary` 추가**해 통일. 영향 범위는 `.book-actions` 안 리스트 행 팔로우 버튼만(search 친구추천·검색결과, follow-list, book-readers) — 독립 `btn-primary`(검색·추가·측정 시작·로그인 등)와 프로필 상단 팔로우(`.profile-actions`)는 `.book-actions` 밖이라 무영향. **검증**: 정적 프리뷰 하네스(`.preview/`, gitignore)로 computed style 측정 — 두 버튼 padding 6/10·font-size 13.6px·line-height 20.4px 일치, 렌더 높이 32 vs 34(차이 2px=ghost 1px 외곽선뿐). CSS 1줄(`.java` 무변경 → 테스트 게이트 skip). app.css. (PR #203 예정) |
| 2026-06-07 | **헤비 리더 SNS(엔진 B) 로드맵 정리 + 책BTI(독서 성향 분석) 설계 확정 + 명명** — "이제 헤비 리더 SNS로 가자" 요청에 plan.md·sns-design·reading-personality-design 정독해 로드맵 도출(토대=프로필·팔로우·검색·카운트·차단/신고는 이미 완료, 남은 건 성향→추천→채팅→구독 레이어). 진입점으로 **독서 성향 분석**을 선택(밀도 독립·획득 후크라 §전략 "밀도 먼저"와 화해). 코드 확인서 **두 발견**: ① 장르(`categoryName`)·출간연도(`pubDate`)가 `Book`·`AladinBookSearchClient.parse()` 둘 다 없어 적재를 새로 깔아야 함 ② 책 공개 기본 PRIVATE라 "공개용 분석"은 대부분 텅 빔 → v1은 본인용·비노출. 미결 🟡 3개 사용자 결정(장르 적재 추가 / v1 본인용 / LLM=Gemini Flash) → reading-personality-design.md를 **설계 확정본**으로 갱신(구현 단계 §10 Phase1~5+후속), plan.md §독서성향 절을 "구상→확정"으로·§전략 순서 화해 메모 추가. 문서만(reading-personality-design.md+plan.md). |
| 2026-06-07 | **책BTI Phase 1a — 장르/출간일 전방 적재**(검색→Book→DB) — 책BTI 입력인 장르·출간연도가 `Book`·`AladinBookSearchClient.parse()` 둘 다 없던 걸 새로 적재. **사슬 전체**: 알라딘 `parse()`가 `categoryName`·`pubDate` 매핑 → `BookSearchResult`에 2필드(+6-인자 편의 생성자로 기존 호출처 4곳 무변경) → 검색결과 add 폼 hidden 2개 → `POST /books/add` 파라미터 → `addFromSearch` → `Book.register` 오버로드(10-인자, 8-인자는 null 위임해 19개 테스트 churn 0)·`blankToNull` 정규화 → **Flyway V18**(`category varchar(300)`, `pub_date varchar(20)` nullable). 백필(Phase 1b)은 외부 API 호출이라 위험 프로파일 달라 별도 PR로 분리. **register/BookSearchResult 시그니처 churn 회피 = 오버로드+편의 생성자**(브리틀니스 가이드). **TDD Red→Green 4사이클**: parse 매핑(누락 null) / Book 적재(8-인자 null 불변식) / addFromSearch 전달 / 컨트롤러 사슬 적재. FlywayMigrationTest(ddl-auto=validate)가 엔티티↔V18 동기화 검증. BookSearchResult·AladinBookSearchClient·Book·BookService·BookController·books.html·V18+테스트 4. (PR #205 예정) |
| 2026-06-07 | **책BTI Phase 1b — 기존 책 카탈로그 백필**(알라딘 ItemLookUp) — Phase 1a는 *앞으로 추가되는* 책만 적재하므로, 이미 책장에 있는 책(장르·출간일 null)을 ISBN으로 단건 조회해 채운다. **포트** `BookSearchClient.lookupByIsbn(isbn)`(알라딘 어댑터는 `ItemLookUp.aspx` 호출 + 기존 `parse()` 재사용 — 매핑 단일 소스) + `Book.applyCatalogMetadata`(blank→null 동일 정규화) + `BookCatalogBackfillService.backfill(limit)`: `category IS NULL AND isbn13 IS NOT NULL` 책만 후보(멱등·null-state 제외), **외부 HTTP는 트랜잭션 밖**(커넥션 점유 회피)에서 lookup 후 채운 책만 `saveAll`, `limit`으로 호출량 통제. **트리거 = 관리자 버튼** `POST /admin/books/backfill-catalog`(ADMIN·CSRF, 결과 플래시 "조회/채움/미발견/잔여" — 남으면 다시 클릭). 기동 러너 대신 관리자 트리거 선택(부팅 시 외부 호출이 헬스체크·circuit breaker 위협). **새 Flyway 없음**(V18 컬럼 재사용). **TDD Red→Green 4 increment**: ItemLookUp URL·lookup 가드 / `applyCatalogMetadata` / 백필 6(채움·멱등·null-isbn 제외·미발견·disabled·limit) / 관리자 인가(ADMIN 실행·USER 403). BookSearchClient·AladinBookSearchClient·Book·BookRepository·BackfillResult·BookCatalogBackfillService·AdminBackfillController·admin.html+테스트 3. (PR #206 예정) |
| 2026-06-07 | **§수익화에 "eBook 제휴 링크" 백로그 추가**(아이디어만, 구현 금지) — 알라딘 제휴 정산 논의 연장선. 현재 검색은 종이책만(`SearchTarget=Book`)이라 종이책 링크로 들어가 eBook으로 갈아타면 귀속이 모호 → eBook 직접 검색(`SearchTarget=eBook`)으로 처음부터 eBook 링크를 주자는 구상. **핵심 기록**: 직접 링크는 (a) 추적/귀속 모호만 없애고 (b) *eBook이 수수료 대상인가·율이 얼만가*는 알라딘 정책이라 안 풀림 → **착수 전 (b) 검증 필수**(TTB 실적/테스트, self-referral 제외 주의). 설계 시 isbn 동일성(eBook ISBN 상이/부재)·노출 방식(토글 vs 두 링크 동시) 결정. 문서만(plan.md). |
| 2026-06-07 | **책BTI Phase 2 — 독서 프로필 집계**(코드·결정적) — 책장(book+reading_session)에서 책BTI 입력 "사실"을 뽑는 순수 함수 `ReadingProfileAggregator.aggregate(books, sessions)` + 출력 레코드 `ReadingProfile`/`LabeledCount` + 얇은 `ReadingProfileService`(레포 배선·전체 책 기반=본인용). **집계 사실**(§3): 권수·상태분포·완독률(0권 0나눗셈 회피) / 총독서시간·평균세션=정독↔다독(진행중 세션 제외) / 저자편향·다양성 / 장르편식·잡식(장르=알라딘 categoryName **대분류=2번째 세그먼트**, 1번째 "국내도서" 등 판매구분은 제외) / 출간연대=신·구(pubDate 앞4자리→연대, 최신 먼저). **결정적**: 분포 동률 시 라벨 오름차순 타이브레이크(Phase4 입력 시그니처·단언 안정). **null-state 제외**(N-055): 저자·장르·출간일 없는 책은 해당 분포에 안 샘. **저장/LLM 없음**(순수 read, Flyway 무변경 — 저장은 Phase 4). **TDD Red→Green 3 increment**: 상태수·완독률·시간/평균세션(+0권·null입력·active제외) / 저자·장르·연대 분포(+동률·상한·단일세그먼트 폴백·연도 파싱·null-state 제외) / 서비스 배선·**교차 사용자 누출 없음**. 순수 로직은 빠른 단위(11), 서비스는 @SpringBootTest 배선(2). personality 패키지 신설(ReadingProfile·LabeledCount·ReadingProfileAggregator·ReadingProfileService)+테스트 2. (PR #208 머지) |
| 2026-06-07 | **책BTI Phase 3 — LLM 포트 + Gemini 어댑터**(서술 생성·폴백) — 사실(프로필)을 받아 자연어 성향 서술+태그를 만드는 포트 `ReadingPersonalityNarrator` + Gemini Flash 어댑터 `GeminiReadingPersonalityNarrator`. **알라딘 어댑터와 동일 패턴**: `@Value` 키 주입·`isEnabled()` 게이트(키 없으면 폴백)·`RestClient`·자체 ObjectMapper·예외→empty 격리. **키는 `x-goog-api-key` 헤더로**(민감정보 URL/로그 노출 회피; 운영은 ECS env `BOOKTIMER_LLM_API_KEY`, repo 미커밋). 출력 레코드 `PersonalityNarration`(narrative+tags)·결합 `ReadingPersonality`(profile+narration, **narration null=폴백** + `hasNarration()`). 오케스트레이션 `ReadingPersonalityService.analyze(user)` = 프로필 집계(Phase 2)→서술→**폴백 결합**(서술 비면 사실만). **HTTP/JSON 분리로 네트워크 없이 단위테스트**: `buildPrompt`(그라운딩 "지어내지 마라"+사실 JSON 주입)·`buildRequestBody`(Jackson 직렬화로 따옴표/줄바꿈 이스케이프 보장·temp 0.4·responseMimeType=application/json)·`parseNarration`(Gemini 봉투 candidates>content>parts>text **2단 파싱** + ```json 펜스 제거 + narrative 비면 폴백). **TDD Red→Green 3 increment**: parseNarration(봉투+펜스+빈/오류) / 게이트·프롬프트 그라운딩·요청본문 이스케이프·narrate 가드(네트워크 없이) / 서비스 배선·폴백 경로(가짜 narrator). 단위 7 + 배선 2. **저장 없음**(Flyway 무변경 — 캐시는 Phase 4). personality 패키지 추가(PersonalityNarration·ReadingPersonality·ReadingPersonalityNarrator·GeminiReadingPersonalityNarrator·ReadingPersonalityService)+테스트 2. (PR #209 머지) |
| 2026-06-07 | **책BTI Phase 4 — 저장·캐시·갱신**(LLM 호출 빈도=비용 바닥으로) — 비싼 LLM 산출물(서술+태그)을 입력 시그니처와 함께 사용자당 1행 캐시. 캐시 엔티티 `ReadingPersonalityCache`(user_id unique·narrative·tags(개행 join)·input_signature·generated_at, BaseTimeEntity) + **Flyway V19** `reading_personality`(additive) + 레포 `ReadingPersonalityCacheRepository`. **입력 시그니처** `ProfileSignature.of(profile)` = 프로필 구조(권수·상태·저자/장르/연대 분포) **+ 시간 hour 버킷**의 SHA-256 — *초 단위 raw를 빼서* 측정 세션마다 재생성(thrash) 회피(같은 시간 안 변화는 같은 시그니처). 진입점 `ReadingPersonalityService.analyzeCached(user, force)`: ①콜드스타트(`COLD_START_MIN_BOOKS=5` 미만)면 LLM·캐시 없이 사실만 보류 / ②캐시 히트(force 아니고 시그니처 일치)면 **LLM 건너뜀** / ③재생성(force·캐시 없음·시그니처 변동)이면 narrate→캐시 upsert(같은 행 갱신=unique 보존, generated_at은 주입 `Clock`) / ④LLM 실패면 사실만 폴백·**캐시 안 건드림**. 회원 탈퇴 정리(`AccountService.purge`)에 캐시 삭제 추가(user_id FK 위반 방지). **TDD Red→Green 3 increment**: ProfileSignature(결정적·구조 민감·hour 버킷 thrash 회피) / 캐시 6경로(첫 생성·히트·시그니처 변동·force·콜드스타트·LLM 실패) / 계정 삭제 정리(inOrder). 단위 5 + 통합 6 + AccountServiceTest 업데이트. FlywayMigrationTest(validate)가 엔티티↔V19 동기화 검증. personality 추가(ProfileSignature·ReadingPersonalityCache·ReadingPersonalityCacheRepository, ReadingPersonalityService 확장)+AccountService. (PR #210 머지) |
| 2026-06-07 | **책BTI Phase 5 — 본인 화면 노출 → 🎉 v1 완료** — 전용 페이지 `GET /personality`(대시보드 바로가기 타일에서 진입, 사용자 선택=전용 페이지). `PersonalityController`가 `analyzeCached(user, false)` 결과를 표시 모델 `PersonalityView`로 매핑 — **3상태**: `READY`(서술) / `COLD_START`(책<`COLD_START_MIN_BOOKS`, "조금 더 읽으면" 안내) / `FALLBACK`(책 충분하나 LLM 실패, "잠시 후 다시 분석"). "서술 없음"의 이유(책 부족 vs LLM 실패)를 totalBooks vs 임계로 갈라 문구를 다르게 — 경계: 정확히 임계면 콜드스타트 아님. 템플릿 `personality.html`(history 구조 미러): 상태별 카드 + **책장 사실 요약**(콜드스타트·폴백에서도 보유/완독·총시간·top 장르·top 저자) + **정확도 고지**("MBTI처럼 가볍게"). **"다시 분석"** = `POST /personality/refresh`(CSRF·PRG) → `analyzeCached(force=true)`. 태그 v1 비노출(저장만). 누출 0(본인만, `anyRequest().authenticated()` 자동 보호). **TDD Red→Green 2 increment**: PersonalityView 3상태 분류(+정확히 임계 경계) 단위 4 / 컨트롤러 인증 게이트·3상태 라우팅·force 재생성·CSRF 통합 6. web 추가(PersonalityController·PersonalityView·personality.html)+dashboard.html 타일+테스트 2. **책BTI v1 전 구간 출하**(집계→서술→캐시→화면). (PR #211 머지) |
| 2026-06-07 | **책BTI Gemini 키 배포 배선**(운영 활성화) — `deploy/task-definition.json` `secrets[]`에 `BOOKTIMER_LLM_API_KEY` → SSM `/booktimer/LLM_API_KEY` 참조 추가(알라딘 TTBKey와 동일 패턴) + `deploy-aws.md §12-3`(키 발급·SSM 등록·배선·모델 변경) 신설. **순서 주의(T-011)**: SSM 파라미터를 *먼저* 만든 뒤 머지해야 새 태스크가 기동 실패 안 함(secrets는 태스크 시작 시 필수 로드). 실행역할 IAM은 기존 `ssm:GetParameters`(/booktimer/*)로 충분. 키 없으면 `isEnabled()`=false → 사실만 폴백(무해). 문서/배포설정만(`.java` 무변경 → 테스트 게이트 skip). (PR #212 머지) |
| 2026-06-07 | **책BTI Gemini 인증을 `?key=` 쿼리파라미터로 — 신형 `AQ.` 키 호환**(라이브 서술 살림) — 배포 후 서술이 안 켜지고 `401 ACCESS_TOKEN_TYPE_UNSUPPORTED`. 원인: Google이 API 키를 `AIza`(Traffic)→`AQ.`(Authentication)로 교체 중인데 일부 계정은 `AQ.` 키만 발급되고, 이 키는 기존 `x-goog-api-key` 헤더(·`Authorization: Bearer`)로는 401, **`?key=` 쿼리파라미터로만** 통한다(CloudShell `curl ...?key=`로 모델 목록 200 확인). 어댑터를 `GeminiReadingPersonalityNarrator.buildEndpoint(base,model,key)`로 추출해 `?key=`(URL 인코딩)로 호출·헤더 제거 → `AIza`·`AQ.` 둘 다 호환. 기본 모델도 이 키로 접근 확인된 `gemini-2.5-flash`로 상향(2.0→2.5). URL에 키가 실리므로 catch에서 URL·요청 로그 안 남김. **TDD Red→Green**: `buildEndpoint_putsKeyAsQueryParam`(키가 헤더 아닌 `?key=`에 실림) Red(메서드 없음 컴파일 실패) 확인 → 구현 → Green. 함정 **T-037**(헤더 테스트는 멀쩡한 AQ 키도 401이라 오진, `-H "값"`만 주면 403)·deploy-aws §12-3 AQ 주의 동봉. GeminiReadingPersonalityNarrator.java+테스트+deploy-aws.md+troubleshooting.md. (PR #213 머지) |
| 2026-06-07 | **책BTI v1 라이브 서술 동작 확인 + 개념 노트 정리**(세션 마무리) — #213 배포 성공 후 `/personality`에서 **성향 서술 문단이 라이브로 노출됨을 확인**(폴백 아님) → 책BTI v1(집계→서술→캐시→화면→운영 LLM)이 **전 구간 실동작**. 인증 채널 비동치(헤더 vs `?key=` vs Bearer)와 Google `AIza`→`AQ.` 키 세대 교체를 면접 설명 수준 개념 노트 **N-056**으로 정리(트랩 T-037의 "왜"). 문서만(learning-notes.md+plan.md). (PR 예정) |
| 2026-06-07 | **retention 레버 ② — 사후 수동 입력**(엔진 A 복귀, §전략 최우선 백로그) — 책BTI v1 출하 후 §전략 순서대로 엔진 A retention으로 복귀. 측정 깜빡한 독서를 나중에 직접 기록해 "어차피 기록 안 됐네" 이탈 차단. 전용 페이지 `GET/POST /sessions/manual`(`ReadingSessionController`)+`manual-session.html`+대시보드 퀵액션 "✍️ 빠뜨린 기록". 서비스 `ReadingSessionService.recordManual` = `start`(책 필수)+`stop`(누적 차감) 합친 완료 세션(진행 세션 무관). 날짜 안착=startedAt을 유저 TZ로 묶으므로(N-010) 오늘은 endedAt=now·과거는 그날 정오 종료. 차감은 단일 누적 부채(N-001)라 과거 기록도 현재 잔여를 갚음. **DB 무변경(Flyway 없음)**. TDD Red→Green: 서비스 5(완료세션+차감·책null·순서역전·타이머없음·읽고싶음전환)+컨트롤러 7(폼·해피·과거날짜 안착·책없음·IDOR·미래날짜·0분). 전체 스위트 그린. (PR #215 머지) |
| 2026-06-07 | **세션 비활성 타임아웃 30일 — 독서 중 로그아웃 해결**(실사용 피드백) — 책 읽다 "측정 종료" 누르려니 로그아웃·~50분 비웠다 오니 재로그인 요구. 원인: 타임아웃 미설정 = **Boot 기본 30분** + 독서 타이머는 클라이언트(JS)에서만 돌고 읽는 동안 서버 요청이 0이라 30분 뒤 세션 만료(오래 읽는 핵심 사용자일수록 더 잘 터짐). 해결: 비활성 타임아웃 30일 + 쿠키 Max-Age 30일(브라우저 닫아도 유지), 세션 MySQL 외부화라 비용 0. **Boot 4 함정**: `server.servlet.session.timeout` 프로퍼티가 Spring Session JDBC 저장소에 안 먹어(기본 30분 그대로, cookieSerializer·ForwardedHeaderFilter 계열) `WebConfig#sessionTimeoutCustomizer`(`SessionRepositoryCustomizer`)로 직접 `setDefaultMaxInactiveInterval`+`cookieSerializer.setCookieMaxAge`. TDD Red→Green: `SessionCookieSameSiteTest`에 createSession 타임아웃 30일(프로퍼티만이면 30분 Red로 함정 포착)+쿠키 Max-Age=2592000 단언. 전체 스위트 그린. learning-notes N-057·troubleshooting T-038(Boot 4 세션 타임아웃 프로퍼티 무동작). (PR #216 머지) |
| 2026-06-07 | **부채 모델 전환 — 단일 롤링 카운터 → 7일 윈도우 per-day**(사용자 방향 결정, 레버 ②·③ 동시 해결) — 발단: "한 달 전 1시간"을 빠뜨린 기록으로 넣으니 *오늘* 잔여가 줄어 오늘 목표를 채운 걸로 처리됨(실사용 버그). 뿌리: 부채가 날짜별 원장이 아니라 매일 증가하는 **단일 누적 카운터([[N-001]])**라 "지금 잔액"만 알아 백데이트를 올바른 날에 못 꽂음. *오늘만 차감* 임시 픽스를 거쳐, 근본적으로 모델 전환: **하루 부채=`max(0,목표−그날 읽은 초)`(날짜별 독립), 활성 최근 7일(그 이전 자동 용서=cap·streak-freeze 흡수), 부채는 완료 세션에서 유도**(`WeeklyDebtCalculator`·`ReadingDebtService`). 대시보드=오늘 부채 헤드라인+이번 주 빠뜨린 날 목록(채우기 `?date=` 프리필), 수동입력은 윈도우 안만·차감 로직 제거(세션 저장이 곧 그 날 부채 감소), 설정/온보딩 cap·초기값 제거, 관리자 표시 교체. **PR-1=사용자 대면 전환(무파괴, 엔티티 컬럼·accrual은 vestigial 잔존)**, PR-2=죽은 컬럼/클래스 Flyway drop. DB 무변경(PR-1). TDD Red→Green(WeeklyDebtCalculator 10·ReadingDebtService 2 신규 + 대시보드·수동입력·온보딩·설정·관리자·통합 테스트 갱신, 전체 그린). 개념·일반 교훈 [[N-058]], §부채 모델·레버 ②③ 본문 갱신. (PR #217 머지) |
| 2026-06-07 | **부채 모델 전환 PR-2 — 죽은 컬럼/클래스 제거(코드·DB 위생)** — #217(7일 윈도우 per-day, 무파괴)에서 미사용 vestigial로 남겨 둔 옛 단일-카운터 모델의 잔재를 제거. `ReadingTimer`에서 `remainingSeconds/capSeconds/lastAccrualDate` 필드 + `accrueUntil`/`deduct`/`isAtCap`/`applyInitialSetup` 메서드 삭제 → 엔티티의 유일한 상태는 **하루 목표**(`dailyIncrementSeconds`)만 남음. `AccrualCalculator`·`ReadingTimerService`(+테스트) 삭제. **`V20__drop_reading_timer_legacy_columns.sql`로 세 컬럼 DROP**(FlywayMigrationTest가 `ddl-auto=validate`로 컬럼 drop↔엔티티 매핑 정합 검증). 호출부 정리: `startFor`/`of`를 하루목표 1-인자로, `UserRegistrationService`(cap 상수 제거·`DEFAULT_CAP_SECONDS` 삭제), `OnboardingService`/`UserSettingsService` cap pass-through 제거, `ReadingSession` 주석 화해. **가입 API의 `startDate` 인자는 유지**(이제 미사용이나 제거 시 호출부 30여 곳 영향 → 범위 밖, Javadoc에 명시). 제거 PR이라 신규 행동 테스트 없이 기존 스위트가 안전망 — 죽은 테스트 정리 후 전체 그린. (PR #218 머지) |
| 2026-06-07 | **"이번 주 빠뜨린 날" 섹션을 대시보드 → 독서 기록(`/history`)으로 이전 + "빠뜨린 기록" 버튼 제거**(실사용 피드백) — 빠뜨린 날 채우기는 "과거 기록 보정"이라 기록 화면이 자연스러운 자리. `HistoryController`에 `ReadingDebtService` 주입해 `weeklyShortfall`(7일 윈도우 미충족 과거 날) 모델 적재, `history.html`에 섹션(채우기=`/sessions/manual?date=` 프리필) 추가. 대시보드는 `weeklyShortfall` 적재·섹션 제거(헤드라인 오늘 부채만), 퀵액션 "빠뜨린 기록" 타일도 제거(수동입력 진입을 빠뜨린 날 "채우기"로 일원화). `DashboardModel`에서 `weeklyShortfall` 속성 제거(미사용)·Javadoc 화해. TDD Red→Green: HistoryControllerTest에 모델 적재+렌더(섹션·채우기 링크) 단언 신규(Red 확인 후 컨트롤러 배선), DashboardController`weeklyShortfall` 테스트는 history로 이전하고 대시보드엔 섹션·버튼 부재 렌더 단언 추가. 전체 스위트 그린. 문서: §부채 모델 본문 대시보드 줄 화해. (PR #219) |
| 2026-06-07 | **독서 기록 화면 — "이번 주 빠뜨린 날" + "일자별 독서 시간" 두 카드를 한 장으로 합치고 폴더 탭 디자인**(실사용 피드백) — 두 박스를 한 카드로 합쳐, 상단 모서리에서 "이번 주 빠뜨린 날"이 폴더/브라우저 탭처럼 솟고 그 아래 빠뜨린 날 목록 → 구분선 → 일자별 독서 시간 목록을 한 장에 모음. 순서는 독서 잔디 → 합친 카드. 빠뜨린 날이 없으면 탭·목록·여백이 사라지고 평범한 "일자별 독서 시간" 카드로 우아하게 강등. 순수 템플릿(`history.html`)+CSS(`app.css` `.record-card`/`.card-tab`/`.missed-section`)만 — 컨트롤러·모델·로직 무변경. 기존 `HistoryControllerTest` 렌더 단언("이번 주 빠뜨린 날"·`/sessions/manual`)이 합친 카드에서도 그대로 통과해 안전망(시각 전용 변경이라 신규 테스트는 브리틀 노이즈라 보류). (PR #220) |
| 2026-06-07 | **독서 기록 화면 — 두 카드를 합친 모양을 "둘 다 보임"→탭 전환 UI로 정정**(#220 디자인 피드백) — #220은 한 카드에 빠뜨린 날·일자별을 둘 다 쌓아 보였는데, 사용자 의도는 **헤드의 탭 버튼 2개로 바디 한 곳을 전환**하는 것(일자별 독서 시간 ↔ 이번 주 빠뜨린 날). 누른 탭의 데이터만 바디에 표시, 활성 탭은 액센트 밑줄. **JS 없이** 라디오(`:checked`)+형제 선택자로 패널 토글, 활성 탭 표시는 **인접 형제(`:checked + .record-tab`)** 로 — 형제-후손 선택자는 일부 렌더러가 `:checked` 변경 시 갱신을 빠뜨려 밑줄이 안 따라옴을 로컬 프리뷰로 잡아 인접 형제로 교정(`.matches()`로 선택자 적중 확인). 빈 상태: 빠뜨린 날 없으면 그 탭에 "이번 주는 빠뜨린 날이 없어요 🎉". 순수 `history.html`+`app.css`(`.record-tab`/`.tab-panel`)만, 로직 무변경. 기존 렌더 단언 통과(안전망). (PR #221) |
| 2026-06-07 | **빠뜨린 날을 "그날의 목표"로 판정 — per-day 목표 스냅샷 + 1분 미만 용서**(실사용 피드백) — 부채는 `max(0, 하루목표 − 그날 읽은 초)`인데 하루 목표를 **현재 평면값 하나**(`ReadingTimer.dailyIncrementSeconds`)로만 잡아, 사용자가 목표를 **올리면**(60분→61분) 옛 목표를 채웠던 과거 날이 새 목표로 재판정돼 빠뜨린 날로 둔갑했다("0분 부족" 모순). 뿌리: 표시 상태를 가변 파라미터에서 매번 재유도 → 파라미터를 건드리면 과거 판정까지 소급해 다시 쓰임([[N-059]]). **두 겹 해결**: ① **1분 미만 부족 용서**(`WeeklyDebtCalculator.MIN_MISSED_DEBT_SECONDS`, `debt>0`→`debt>=60`) — 이미 지나간 옛 목표는 데이터 복원 불가라 분 단위 인상이 만든 "0분 부족" 잔재를 커버(6월 5일). ② **목표 변경 이력**(`ReadingGoalChange`/`reading_goal_change`, V21 + 백필) — 목표가 설정·변경되는 시점마다 한 행을 남기고 `GoalSchedule.goalFor(date)`(floorEntry)로 과거 날을 그날 목표로 판정 → 앞으로의 목표 변경은 둔갑 없음. 기록은 온보딩·설정(목표 실제 변경 시), `ReadingTimer`는 현재 목표 캐시로 유지. 계산기는 per-day `compute(reads, goalByDate, today)` 오버로드 신설(기존 uniform 오버로드 유지). TDD Red→Green: `GoalScheduleTest`·`WeeklyDebtCalculatorTest`(per-day 회귀)·`ReadingGoalServiceTest`(upsert·유저TZ)·`ReadingDebtServiceTest`(이력 배선)·온보딩/설정 이력 기록·`ReadingDebtPerDayGoalIntegrationTest`(목표 인상 후 과거 날 안 뜸, 실DB) + 전체 스위트 그린(FlywayMigrationTest가 V21↔엔티티 검증). 범위 밖: 독서 잔디 동일 소급 버그(후속, 같은 `GoalSchedule` 재사용). 본문 §부채 모델 갱신. (PR #222) |
| 2026-06-07 | **빠뜨린 날 — 가입(첫 목표) 이전 날짜 제외**(#222 후속, 실사용 피드백) — #222로 per-day 스냅샷을 깔았는데, 목표를 70분으로 올리자 **가입 전 날(6/1~4)이 "70분 부족"으로 뜨는** 잔재가 남았다. 뿌리: 그 날짜들은 목표 이력의 첫 행(baseline)보다 이르러 `GoalSchedule.goalFor`가 **폴백(현재 목표)** 을 돌려줬고, 그게 "그 옛 날의 진짜 목표"가 아닌데도 빠뜨린 날 판정에 쓰임. 해결: `GoalSchedule.earliestEffectiveDate()`(=baseline) 추가 → `ReadingDebtService`가 윈도우에서 **baseline 이전 날은 목표를 안 넣어**(부채 0) 빠뜨린 날에서 제외. "사용자가 아직 시작하기 전 날"이라 못 지킴이 아님(입문자 친화 thesis). 이력이 비면(레거시) baseline 없음 → 옛 동작대로 폴백 전체 적용. TDD Red→Green: `GoalScheduleTest`(earliestEffectiveDate)·`ReadingDebtServiceTest`(baseline 이전 제외·이후 포함)·`ReadingDebtPerDayGoalIntegrationTest`(0읽음 가입전 날 안 뜸, 실DB) + 전체 그린. 계산기·DB·마이그레이션 무변경(서비스 배선만). 본문 §부채 모델 갱신. (PR #223) |
| 2026-06-07 | **독서 잔디 색 농도도 "그날 목표"로 — per-day 소급 재채색 차단**(#222 범위 밖이던 후속 마무리) — 잔디(전체·책별) 칸 색은 달성 비율(하루 목표 대비 0~4 레벨)인데, 빠뜨린 날과 똑같이 **현재 평면 목표 하나**로 전 칸을 판정해, 목표를 올리면 옛 목표를 채운 과거 칸 색이 새 기준으로 내려가 소급 재채색됐다(N-059의 잔디 판). 해결: `ContributionGraphBuilder`에 날짜별 목표 오버로드(`build(reads, today, ToLongFunction<LocalDate>)`) 신설(기존 평면 오버로드는 위임 유지) → `ReadingContributionService`·`BookContributionService`가 빠뜨린 날과 **같은 `GoalSchedule.goalFor`** 를 넘겨 칸마다 그날 목표로 색칠. 두 서비스에 `ReadingGoalChangeRepository` 주입(이력 비면 타이머 목표 폴백=옛 동작). 잔디는 baseline 이전 날도 독서 0이라 색 변화 없음(컷오프 불필요). TDD Red→Green: `ContributionGraphBuilderTest`(per-day 오버로드: 목표 올려도 옛 met칸 lv 유지)·`ReadingContributionServiceTest`·`BookContributionServiceTest`(이력 배선) + 전체 그린. 빌더·DB 무변경(서비스 배선+오버로드만). 본문 §부채 모델 "범위 밖" → 완료로 갱신. (PR #224) |
| 2026-06-07 | **직접 채운 날(수동 입력) 잔디 테두리 구분**(실사용 피드백) — 빠뜨린 날 채우기로 손수 채운 잔디를 실시간 측정과 시각 구별하고 싶다는 요청. 측정값과 손으로 채운 값의 출처를 구분하려면 **쓰기 시점에 의도를 기록**해야 정확하다(타임스탬프 추론은 손실 있는 근사) → `ReadingSession.manualEntry` 플래그 신설(V22 `reading_session.manual_entry` boolean), `ReadingSession.manual` 팩토리로 수동 입력 세션에 박고 `recordManual`이 사용. 파이프라인: `DailyReadingRecord.manuallyFilled`(그날 수동 세션 하나라도) → `ContributionGraphBuilder` manualDates 오버로드 → `ContributionDay.manual` → 템플릿 `.grass-cell.manual`(앰버 인셋 테두리 box-shadow, 레이아웃 불변) + 범례 "직접 채움". 본인 뷰(history·dashboard·book-detail)만 표시, **공개 프로필 미표시**(백필 노출 방지). 기존 행 백필 휴리스틱 `date(started_at)<>date(created_at)`(=백데이트, 같은 날 수동은 못 가리나 이후 신규는 정확). TDD Red→Green: `ReadingSessionTest`(manual 팩토리)·`ReadingHistoryServiceTest`(집계 플래그)·`ContributionGraphBuilderTest`(manualDates)·`Reading·BookContributionServiceTest`(배선)·`HistoryControllerTest`(종단 렌더) + 전체 그린(FlywayMigrationTest가 V22↔엔티티). 본문 ② 수동 입력 절 갱신. (PR #225) |
| 2026-06-07 | **디스플레이 광고(Google AdSense) 도입 — 비침습·config-gated 스캐폴드**(수익화, 사용자 요청) — "수입을 조금이라도, 단 사용 불편할 만큼은 아니게". "마찰 최소 독서 습관" 정체성과 충돌 않도록 **부가 화면 하단에만 1개씩**(독서 기록·책 목록·책 상세·프로필·검색) + **대시보드 하단**(htmx 라이브 영역 밖 — 측정 start/stop 시 광고 재요청 안 됨), 핵심 동선(타이머 상단·온보딩·로그인)엔 광고 0. **config-gated**: `booktimer.ads.client-id`(=`ADSENSE_CLIENT_ID` ENV)가 비면 `AdsProperties.isEnabled()=false` → fragment가 아무것도 안 렌더(깨진 빈 광고·정책 위반 방지) — AdSense 승인 전까진 ID 비워 두고 승인 후 ENV/SSM 주입(실값 커밋 금지). 공유 레이아웃이 없어 재사용 fragment `ads.html`(`loader`=head 스크립트·`unit`=본문 광고) + 전역 `@ModelAttribute`(`AdsModelAdvice`)로 `ads` 주입, `.ad-slot`에 "광고" 라벨(투명성). TDD Red→Green: `AdsPropertiesTest`(빈/공백/실값 enabled 경계)·`AdsRenderingTest`(ID 설정 시 /history에 로더+단위 렌더)·`HistoryControllerTest.history_noAdsWhenDisabled`(미설정 시 미렌더 안전 불변식) + 전체 스위트 그린(전역 advice 회귀 없음). 남은 외부 작업(코드 아님): AdSense 가입·사이트 심사·SSM 주입, EEA 트래픽 시 CMP 검토. 본문 §비즈니스 모델 갱신. (PR #226) |
| 2026-06-07 | **AdSense 소유권 검증 — ads.txt 공개 서빙**(#226 후속, 광고 승인 절차) — AdSense 사이트 등록(게시자 `pub-9272639329788723`) 후 소유권 확인 단계. 이 앱은 default-deny + 루트→`/login` 리다이렉트라 "코드 스니펫" 검증은 크롤러가 공개 HTML에서 코드를 봐야 해 까다로움 → **정적 `static/ads.txt`** 한 줄(`google.com, pub-9272639329788723, DIRECT, f08c47fec0942fa0`)로 검증(로그인·리다이렉트·JS 영향 0, 추후 "수익 위험" 경고도 예방). `SecurityConfig` permitAll에 `/ads.txt` 추가(비인증 공개 — default-deny라 명시 안 하면 로그인 리다이렉트돼 크롤러가 못 봄). 검증(소유권)과 serving(광고 노출)을 분리 — ads.txt는 소유권만, 광고는 #226 config-gated client-id/slot로 승인 후 ON. TDD Red→Green: `AdsTxtTest`(미인증 GET /ads.txt → 200 + 게시자 라인; 보안 permit 누락 시 302 리다이렉트로 Red) + 전체 그린. 본문 §비즈니스 모델·디스플레이 광고 절 갱신. 후속 후보: 심사의 콘텐츠 크롤링이 로그인 벽에 막힐 수 있어 공개 소개 페이지 필요할 수 있음. (PR #227) |
| 2026-06-07 | **robots.txt 공개 서빙 — 크롤러 신호 정리**(#227 후속, 광고 심사 대비) — ads.txt 소유권 검증 점검 중 `/robots.txt`가 default-deny로 **로그인에 302 리다이렉트**됨을 발견. robots.txt가 HTML 로그인으로 튕기면 크롤러에 모호한 신호고, 다음 단계 콘텐츠 심사 크롤링에도 악영향. 정적 `static/robots.txt`(`User-agent: *` / `Allow: /`) + `SecurityConfig` permitAll에 `/robots.txt` 추가(ads.txt와 한 묶음, 비인증 공개). TDD Red→Green: `RobotsTxtTest`(미인증 GET /robots.txt → 200 + 허용 지시문; permit 누락 시 302 Red) + 전체 그린. (PR #228) |
| 2026-06-08 | **자정·타임존 경계 플레이키 테스트 3건 고정 클락으로 결정적화**(#228 배포 차단 해소) — #228 머지 직후 CI가 `546 tests, 3 failed`로 **배포 skip**(robots.txt 미반영). 원인은 robots 변경과 무관 — `UserSettingsServiceTest`·`DashboardControllerTest`·`ReadingSessionControllerTest` 3개가 `@SpringBootTest`라 운영 `Clock.systemUTC()`(실시간)를 쓰는데, "오늘 부채/오늘 수동입력"을 `now` 기준으로 만들어 **자정 직후(06-08 00:0x KST)엔 `now−30분`이 전날로 넘어가** 단언이 깨짐(+ UserSettings는 tz를 NY로 바꿔 목표 이력 날짜가 SEOUL today()와 어긋나는 tz 경계도 겹침). 각 클래스에 nested `@TestConfiguration`으로 `@Primary Clock.fixed(2026-06-17T09:00:00Z)` 주입 — 18:00 KST=05:00 EDT라 SEOUL·NY 같은 날짜+자정 회피로 결정적. 운영 코드 무변경(테스트 결정성만). 전체 스위트 그린. 교훈: 실시간 시계 통합 테스트는 경계에서 플레이키 → 고정 클락(TimeConfig javadoc 지침), troubleshooting 후보. (PR #229) |
| 2026-06-08 | **공개 소개(랜딩) 페이지 — AdSense 콘텐츠 심사 대비**(#227·#228 후속, 광고 승인 절차) — 심사는 콘텐츠 크롤링도 하는데 이 앱은 본문이 전부 로그인 뒤라 크롤러가 볼 게 거의 없어 "저가치 콘텐츠" 반려 위험이 컸다. 루트 `/`를 **익명이면 공개 소개 페이지(`landing.html`), 로그인이면 기존 대시보드**로 분기(`DashboardController`에 `principal==null` 가드) — Google이 크롤하는 루트 도메인에 "무엇을 하는 서비스인가"(소개·핵심 기능·동작 방식·CTA) 실본문을 노출. `SecurityConfig`에 `/` permitAll 추가하되 대시보드 데이터는 principal 있을 때만 로드돼 노출 0(보호 경로 default-deny 유지). 랜딩 전용 CSS(`app.css` `.landing-*`/`.btn-secondary`). 기존 `SecurityConfigTest`의 "보호 경로 미인증→/login" 프로브를 `/`(이제 공개)→`/books`로 교정. TDD Red→Green: `LandingPageTest`(익명 GET / → 200·view=landing·실본문·로그인/가입 링크; 변경 전엔 302 Red) + 전체 스위트 그린. 동의(CMP)는 Google CMP "3가지 선택" 선택(코드 변경 0, `adsbygoogle.js`가 자동 게재). 본문 §비즈니스 모델·디스플레이 광고 절 갱신. (PR #230) |
| 2026-06-08 | **랜딩 '시작하기'에 Google 원탭 진입(OAuth) 추가**(#230 후속, UX) — 이메일 가입 폼보다 마찰이 적은 구글 진입을 랜딩 히어로에 노출. 구글 OAuth는 신규면 자동 가입(온보딩으로)이라 "무료로 시작하기"와 같은 '시작' 동선. 히어로 CTA를 로그인 카드와 동일한 세로 스택으로 재구성 — 무료로 시작하기(이메일) → "또는" 구분선 → `G Google로 시작하기`(`/oauth2/authorization/google`), 기존 로그인 OAuth 버튼 스타일(`.btn-oauth`/`.oauth-divider`) 재사용. 중복이던 히어로 "로그인" 버튼은 제거(하단 "이미 계정이 있나요? 로그인" 링크로 일원화) → 그에 쓰이던 `.btn-secondary`는 죽은 CSS라 삭제, `.landing-cta`는 row→column. TDD Red→Green: `LandingPageTest.landing_offersGoogleSignIn`(랜딩에 `/oauth2/authorization/google` 노출; 추가 전 Red) + 전체 스위트 그린. 본문 §디스플레이 광고 랜딩 절 갱신. (PR #231) |
| 2026-06-08 | **갱신 이력 표를 plan.md → claude-docs/changelog.md로 분리**(문서 경량화) — plan.md의 48%(110KB/140행)이던 갱신 이력 표를 sibling changelog.md로 통째 이관(삭제 0·서사 100% 보존), plan.md엔 본문 + 최근 10개 발췌만 남겨 통독 가능 크기로(약 58K→30K 토큰). CLAUDE.md sweep 규칙(Git 워크플로 4·작업 종료 최신화·다중세션 공유문서)의 갱신 이력 추가 대상을 changelog.md로 갱신. 문서만. (PR #232) |
| 2026-06-08 | **사용자 피드백/문의 기능**(사용자 요청) — 일반 사용자가 버그·제안을 개발자에게 보내고, 개발자(ADMIN)가 읽음/처리완료를 표시하면 그 표시는 작성자 본인만 본다. 사용자 `GET/POST /feedback`(유형 버그/제안/기타+제목+내용, "내 문의"에 본인 글만 상태 배지)·`POST /feedback/{id}/delete`(본인 글만, IDOR 방어). 관리자 `GET /admin/feedback`+`read`/`resolve`/`delete`(ADMIN+CSRF). 노출 스코핑=`findByAuthor` 쿼리, 삭제 IDOR=작성자 일치 검사, 상태 enum SUBMITTED→READ→RESOLVED(단조). 모델 `Feedback`+`FeedbackType`/`FeedbackStatus`+V23 `feedback`(report 구조 모방), 관리자 목록은 `AdminFeedbackRow` DTO(LAZY author OSIV 비의존), 탈퇴 정리 `deleteByAuthor` 추가, 보안은 기존 매처로 충분(SecurityConfig 무변경). 답장·수정 없음(v1). TDD Red→Green: 도메인 6·서비스 5·컨트롤러 13(미인증·USER 403·스코핑·삭제 IDOR) + 전체 그린. (PR #233) |
| 2026-06-08 | **피드백 후속 — 개발자 답장 + 유형 필터**(사용자 요청) — #233 피드백 기능에 둘 추가. ① 개발자 **단일 답장**(`Feedback.reply` 컬럼·V24, 덮어쓰기) — `POST /admin/feedback/{id}/reply`, 저장 시 자동 "읽음"(`applyReply`, 처리완료는 별도), 작성자 본인 "내 문의"에만 노출. ② 관리자 **유형 필터** 탭(`GET /admin/feedback?type=`, `FeedbackType.parse`가 없음/잘못된 값을 null=전체로 — `from`의 ETC 폴백과 구분). 답장↔상태는 "자동 읽음, 처리는 별도"로 결합(사용자 결정), 작성자 수정은 여전히 없음(삭제만). `AdminFeedbackRow`에 reply 추가, 사용자/관리자 템플릿에 답장 노출·필터 탭·답장 폼. TDD Red→Green: 도메인 +4·서비스 +2·컨트롤러 +3(답장 저장+자동읽음·USER 403·유형 필터) + 전체 그린(V24↔엔티티). (PR #234) |
| 2026-06-08 | **책BTI 견고화 — 빈 화면·지연 방어**(실사용 피드백, [[N-060]]) — "책BTI에 한 번씩 접근이 오래 걸리고, 어쩔 땐 분석이 통째로 비며, 다른 페이지 갔다 오면 다시 보임". 뿌리: `/personality`는 캐시 미스(입력 시그니처=권수·분포+시간버킷 변동) 시점에 **요청 스레드에서 Gemini를 동기 호출**하고 그 결과를 바로 렌더 — 호출이 느리면 지연, 실패/빈응답이면 사실만 폴백으로 빈 화면, 재진입 시 재호출 성공으로 다시 보임(세 증상 한 뿌리). 네 겹 처방: ① **serve-stale-on-error**(`ReadingPersonalityService.analyzeCached`) — 재생성 실패 시 사실만이 아니라 *직전 캐시(stale)* 우선 노출(캐시를 hit/miss 2상태가 아니라 fresh/stale/absent 3상태로), 기존 캐시 행은 덮어쓰지 않음 ② LLM 호출을 트랜잭션 밖으로(`propagation=SUPPORTS` — 네트워크 시간 동안 DB 커넥션 점유 회피) ③ `RestClient` connect/read 타임아웃 5s/20s(기본 무한 대기 차단, 실패를 빠르게 → stale 폴백도 빠르게) ④ Gemini 빈 출력 방어(`buildRequestBody`에 `maxOutputTokens`=2048 + `thinkingConfig.thinkingBudget=0` — gemini-2.5-flash는 thinking 기본 ON이라 출력 예산을 thinking이 삼키면 `parts[0].text`가 빈 문자열로 와 폴백됨, 200인데 알맹이 없음이라 catch에 안 걸림). TDD Red→Green: 캐시 stale 폴백 2(시그니처 변동·force 둘 다 stale 노출)·요청본문 빈출력 방어 1 + 전체 스위트 그린. DB·마이그레이션 무변경(서비스·어댑터 배선만). 본문 §책BTI 진행 갱신, learning-notes N-060. (PR #235) |
| 2026-06-08 | **책BTI 서술 — 책 내용만으로 성향, 독서 습관 제외**(실사용 피드백) — "결과물에 독서 습관은 빼고, 무슨 책을 읽느냐로 이 사람의 성격·가치관·취향만 담아달라". 원인: `GeminiReadingPersonalityNarrator.buildPrompt`가 `ReadingProfile`을 통째로 직렬화해 독서시간(`totalReadingSeconds`)·평균세션·완독률·상태별 권수 같은 **습관 신호까지 [사실]로 주입** → 모델이 정독형/완독러 등 행동 패턴을 서술에 담았다. 두 겹 처방: ① 새 private `bookFactsJson`로 **책 내용 신호만**(장르·저자·출간연대 분포 + 다양성 distinct 수 + 표본용 총권수) 골라 주입(습관 필드 통째 제외) ② 프롬프트 지시를 "오직 '읽은 책'이 드러내는 **성격·가치관·취향**만 다루고, 독서 습관(시간·완독률·정독/다독·읽은 권수)은 **절대 언급 말라**"로 교체. 설계 원칙: 지시만으론 모델이 새기 쉬워 **입력 사실 자체를 제거(능력 제거) + 지시**를 함께 건다. TDD Red→Green: `buildPrompt_excludesReadingHabits`(습관 수치 99999/88888·`finishedRatio` 미노출 + 성격/가치관/습관 지시 단언)·`buildPrompt_groundsAndCarriesBookFacts`(장르·저자 유지) + 전체 그린. DB·Flyway 무변경. 본문 §책BTI 갱신. (PR #236) |
| 2026-06-08 | **learning-notes N-061 추가**(#236 후속 — 일반 원칙 캡처) — "LLM 출력에서 원치 않는 차원을 막으려면 지시보다 입력을 차단하라(능력 제거 > 지시 의존)". 책BTI에서 도출한 패턴을 면접 설명 가능 수준으로 박음(배경·왜·일반 원칙·Q&A). 자매 N-060(같은 경로 운영 측면)·N-041(입출력 대칭)·N-050(능력 제거 > 표시 억제 층) cross-link. TOC·본문·누적 갱신 3곳. 문서만. (PR #237) |
| 2026-06-08 | **책BTI 성향 입력을 완독 책만으로 + 책장 요약 정리**(실사용 피드백) — "보유/완독·총 독서 시간은 빼고, 읽고싶음·읽는중 말고 완독한 책만으로 성향을 뽑아 달라". 그동안 `ReadingProfileService.profileOf`가 **전체 책**을 집계해 읽고싶음·읽는중 책의 저자·장르·연대까지 Gemini로 갔다. 처방: ① `profileOf`가 **완독(FINISHED) 책만** 필터해 집계 → 분포·표본 모두 완독분(읽고싶음/읽는중은 어떤 분포에도 안 샘) ② 콜드스타트 판정을 `finishedBooks()` 기준으로(`ReadingPersonalityService`·`PersonalityView` — 읽고싶음만 쌓아두면 분석 보류) ③ `bookFactsJson` 표본 키를 `finishedBooks`로 ④ **`ProfileSignature`에서 독서시간(hours) 제거** — 시간이 더 이상 성향 입력이 아니니 시간만 쌓여도 재분석되던 낭비 제거 ⑤ `personality.html` 책장 요약에서 `보유/완독`·`총 독서 시간` 행 삭제, 콜드스타트 문구를 "완독한 책 N권"으로. TDD Red→Green: `profileOf` 완독-only·읽고싶음/읽는중 제외 2·`PersonalityView` 완독 기준 콜드스타트 1·`ProfileSignature` 시간 무영향 1 + 전체 그린. DB·Flyway 무변경. 본문 §책BTI 갱신. (PR #238) |
| 2026-06-08 | **홍보 전 선수과정(서버 용량 보강)을 plan.md에 게이트로 명문화**(사용자 요청) — "홍보로 한순간에 유입되면 서버가 터질까 봐 미리 알고 싶다". 실제 운영 스펙(`deploy/task-definition.json` cpu 512/mem 1024·**desired-count=1**, `application-prod.properties` Spring Session **JDBC**=세션을 DB에 저장, `deploy-aws.md` RDS **db.t3.micro**, Hikari 기본 풀 10)을 읽어 진단: SSR+DB 앱이라 CPU보다 **DB 커넥션 풀·세션 저장소·단일 인스턴스**가 먼저 막힌다(데이터 수천 명 무난, 동시 유입 수백 명대부터 위험, 오토스케일링 없어 자동 확장 X). plan.md §홍보/마케팅에 **「⚠️ 홍보 전 선수과정」** 추가 — **Claude 고정 지시**(다음에 "홍보글 써줘" 하면 글보다 먼저 이 체크리스트 안내) + 병목 순서 + 체크리스트(오토스케일링 min2/max4 → desired=2 → 세션 Redis 외부화 → k6 부하테스트 → RDS/CloudWatch 알람). 코드·DB 무변경, 문서만. (PR #239) |
| 2026-06-08 | **책BTI 책방 노출 — 공개 책 기반 opt-in**(사용자 요청, 설계 Phase 6 전반부) — "내 책BTI를 책방(공개 프로필)에서 남들도 보게". 핵심 충돌은 프라이버시: 기존 책BTI는 **전체 책(비공개 포함)** 기반이라 그대로 노출하면 비공개 책에서 나온 취향이 샌다(설계 §7 "한 번 새면 회수 불가"). 사용자 결정 = **①공개(PUBLIC)+완독 책만 재계산**(본인 `/personality`와 달라질 수 있음 — 의도된 경계, "공개를 더 켤수록 본인 것에 수렴") + **②opt-in 토글**(기본 비노출). 5단계 TDD: **P1** `User.personalityPublic` 플래그(기본 false, V25, opt-in이라 기존 사용자 false 유지) · **P2** `ReadingProfileService.publicProfileOf`(공개+완독 책 **+ 그 책 세션만** — 집계기가 세션을 책과 대조 안 해 비공개 책 독서시간이 새는 걸 서비스가 차단) · **P3** 별도 테이블 `reading_personality_public`(V26) + 엔티티/레포 + `analyzePublicCached(user,force)`(본인용과 캐시 규칙 공유 — `CacheStore` 추상화로 저장소만 교체, 콜드스타트·serve-stale·캐시 비손상 불변식 한 곳에) + 탈퇴 정리(FK) · **P4** 책방 노출 — `ProfileService`가 opt-in+캐시 있을 때만 `ProfileView.personality` 채움(**방문자 조회는 캐시 읽기 전용 — LLM 미호출**), `profile.html` 책BTI 카드("공개 책 기준" 명시+고지) · **P5** `/personality` 노출 토글 + `POST /personality/visibility`(CSRF, 켤 때 공개 책BTI 즉시 생성=소유자 행동에서만 LLM, "다시 분석"도 공개 동기화). 별도 테이블 선택 이유: V19 user_id unique를 (user_id,scope) 복합으로 바꾸면 제약 드롭 SQL이 MySQL/H2에서 갈려 이식 위험 → CREATE만 하는 별도 테이블이 안전+기존 경로 무손상. TDD Red→Green 각 단계: User 3·publicProfileOf 2·공개 캐시 6·책방 노출 3·토글 3 + 전체 그린. 설계 §10 Phase 6 전반부 완료 표기. (PR #240) |
| 2026-06-08 | **책BTI "다시 분석" 일일 횟수 제한(악의적 반복 클릭 → LLM 남용 방어)**(사용자 요청) — "다시 분석을 악의적으로 반복 클릭하면 서버에 부담 → 하루 3번으로 제한". `POST /personality/refresh`는 매번 `force=true`로 Gemini를 강제 호출하므로 반복 클릭이 곧 LLM 남용. 처방: **일일 카운터를 User 엔티티에 DB로** 두고(인메모리 X — ECS 인스턴스 여럿이라도 한도 일관·재기동 유지), **사용자 타임존 기준 자정 롤오버**로 리셋. ① `User.tryConsumePersonalityRefresh(today)` — 날짜가 바뀌면 카운트 0 리셋 후 센다, 한도(`DAILY_PERSONALITY_REFRESH_LIMIT`=3) 내면 +1·true, 초과면 상태 불변·false / `remainingPersonalityRefreshes(today)` 읽기(부수효과 없음, 표시·버튼 비활성용) ② **V27** `users.personality_refresh_count`(default 0)·`personality_refresh_date`(null — 기존 사용자는 첫 클릭 시 채워져 자연히 한도 적용) ③ `PersonalityController.refresh`가 소비 시도 → 초과면 LLM 호출 없이 `redirect:/personality`+flash `refreshLimited`, GET에 잔여 횟수 모델 노출(`Clock` 주입, 타임존으로 오늘 계산) ④ `personality.html` 남은 횟수 표시·0이면 버튼 비활성·초과 안내 문구. "오늘"을 도메인이 직접 계산 안 하고 인자로 받는 이유=타임존/시계 결정을 호출자(주입 `Clock`)에 둬 테스트에서 자정 경계 고정. TDD Red→Green: 도메인 3(한도까지 허용·4번째 거부·자정 리셋·잔여 읽기 불변)·컨트롤러 1(3번 후 4번째 LLM 미호출+flash) + 전체 그린. 본문 §책BTI 진행 갱신. (PR #241) |
| 2026-06-08 | **책BTI 공개/비공개 분기 폐지 → 공개 단일·항상 노출**(사용자 재결정) — "책BTI는 사용자끼리 즐기는 재미 요소. 나만 보는 비공개 성향은 재미가 아니다 → 공개 책으로만 뽑고 무조건 책방에 공개". 어제(PR #240) 만든 이원화(본인용 전체 책 + 공개용 공개 책 + opt-in 토글 + 별도 테이블)가 거꾸로 군더더기가 돼 일원화: 성향을 **공개(PUBLIC)+완독 책만**으로 단일 생성(`analyze`·`analyzeCached`가 `publicProfileOf` 사용)해 **단일 캐시 `reading_personality`**에 저장하고 **항상** 책방 노출(opt-in 게이트 제거 — 본인 `/personality`와 책방 `/u/{loginId}`가 같은 결과). 제거: 별도 테이블 `reading_personality_public`·`PublicReadingPersonalityCache`(엔티티/레포)·`analyzePublicCached`·`CacheStore` 추상화(저장소 하나라 `findCached`/`upsertCache`로 인라인)·`User.personalityPublic` 플래그(+게터/세터)·`POST /personality/visibility` 토글(컨트롤러·템플릿)·`ProfileService`의 opt-in 게이트 — 모두 **V28**(`drop table reading_personality_public` + `alter table users drop column personality_public`)로 스키마 정리, `AccountService` 탈퇴 정리에서 공개 캐시 삭제 제거. 콜드스타트(공개 완독 5권 미만)면 캐시 없어 책방 카드 숨김(방문자 LLM 미트리거 유지). **프라이버시 오히려 강화**: 유일 입력이 공개 책뿐이라 §7 누출 차단이 분기 없이 자명. 영향 테스트 전부 공개 책 기준으로 갱신(픽스처 `makePublic`), opt-in/토글 테스트 삭제, `ProfileControllerTest` 캐시 있으면 노출·없으면 숨김 2케이스로, `ReadingPersonalityServicePublicCacheTest` 삭제 + 전체 그린(V28 H2 적용 검증). 설계 §10 Phase 6a에 반전 기록(근거 보존), 본문 §책BTI 화해. (PR #241) |
| 2026-06-08 | **책BTI 화면 안내 배치 다듬기**(실사용 UX) — 운영 확인 후 두 군데 위치 조정(`personality.html`만, 로직·모델·테스트 무변경). ① "오늘 남은 횟수 N/3"을 별도 줄에서 **"다시 분석" 버튼 안으로** 넣어 한눈에(작은 `.refresh-count` span, 옅게) ② 페이지 하단의 큰 "책방에 공개돼요" 카드를 없애고 그 안내문을 **"내 독서 성향" 서술 바로 밑 작은 안내 톤**(`.personality-public-note`, 옅은 윗선)으로 이동 — 성향과 묶여 읽히게. 안내문은 READY 상태(성향 있을 때)에만 노출. CSS 2클래스 추가, DB·Flyway·테스트 무변경(PersonalityControllerTest로 템플릿 렌더 검증). (PR #242) |
| 2026-06-08 | **책방 — 책BTI + 공개 책장을 한 카드 2탭으로 합침**(실사용 UX) — 책방(`/u/{loginId}`)에서 따로 떠 있던 책BTI 박스와 공개 책장 박스를 **독서 기록 카드와 동일한 CSS-only 라디오 탭 패턴**(`record-card` 재사용, JS 없이 `:checked` + 인접/형제 선택자)으로 한 카드에 합침 — 탭 ① 책BTI / ② 공개 책장. 제휴 안내문은 공개 책장 탭 안으로 이동. **기본 탭 동적**: 성향 있으면 책BTI(헤드라인) 먼저, 콜드스타트(공개 완독 책 부족=`personality==null`)면 공개 책장 먼저(`th:checked="${personality != null}"` / `==null`) — 빈 탭 착지 방지. **콜드스타트 처리 변경**: 이전 PR #241에서 "캐시 없으면 책BTI 카드 숨김"이었으나, 탭은 항상 2개라야 레이아웃 대칭이라 **숨김 대신 안내문 empty-state**(독서 기록 카드의 "빠뜨린 날 없어요"와 동일 방식, self/방문자 문구 분기)로. CSS는 패널 토글 선택자에 `#tab-bti`/`#tab-shelf` 추가뿐(탭 스타일 전부 재사용). 컨트롤러·모델 무변경 → `ProfileControllerTest` 모델 단언(캐시 있으면 `personality` 노출·없으면 null) 그대로 통과(MockMvc가 뷰 렌더라 템플릿 문법도 검증), `hidesCard`→`emptyNarrative`로 이름·설명만 정정. DB·Flyway 무변경. (PR #243) |
| 2026-06-08 | **관리자 신고함 — 신고 내역 검토 화면 신설**(사용자 요청, SNS 5단계 후속 여지 해소) — "사용자가 신고하면 어드민에서 개발자가 신고 내역을 봐야 하는데 화면이 없어 후속 처리 불가". 기존 `report`는 **저장만** 하고 검토 UI가 추후로 미뤄져 있던 것을 문의함(`/admin/feedback`)과 동일 패턴으로 구현: `ReportRepository.findAllByOrderByCreatedAtDescIdDesc`(최신순, **id desc tiebreak**으로 같은 초 신고도 결정적 정렬) · `AdminReportRow` DTO(트랜잭션 안에서 신고자·대상 LAZY 평면화=OSIV 비의존) · `ReportService.reportRows()`/`deleteByAdmin(id)` · `AdminReportController`(GET `/admin/reports`, POST `/admin/reports/{id}/delete` CSRF) · `admin-reports.html`(신고자→대상·사유·상세·시각·대상책방 링크·삭제) · admin 홈에 "🚨 신고함" 카드. 부수: admin.html head에 `_csrf` meta 추가 — 본문이 길어져 첫 POST 폼이 응답 버퍼 commit 경계 뒤로 밀리며 터지던 "Cannot create a session after response committed"를 세션 조기 생성으로 차단(N-062 후보). 접근 경계는 `/admin/**`→ADMIN(SecurityConfig) 그대로. TDD Red→Green: 서비스 2(행 조립·최신순/삭제)+컨트롤러 5(미인증→login·USER→403·ADMIN 조회·삭제·USER 삭제 403) + 전체 그린. DB·Flyway 무변경. (PR #245) |
| 2026-06-08 | **책BTI 콜드스타트 임계 5권 → 1권**(사용자 결정) — "책이 한 권이어도 책BTI가 나오게 하자. 정확도는 낮아도 어떤 결과라도 보여주는 게 재미고, 책을 쌓을수록 결과가 바뀌는 것도 재미다". `ReadingPersonalityService.COLD_START_MIN_BOOKS` **5 → 1** 한 줄 튜닝 — 완독(공개) 0권만 보류하고 1권부터 (정확도 낮아도) 서술 생성. 책 추가 시 `ProfileSignature` 변동으로 재생성되는 기존 캐시 로직이 "결과 변화의 재미"를 그대로 제공(추가 코드 없음). 화면 문구는 `${view.coldStartMinBooks()}` 동적 바인딩이라 "최소 1권쯤 완독하면"으로 자동 갱신, 책방 탭 empty-state도 완독 공개책 0권일 때만 노출. TDD Red→Green: `oneBook_generatesPersonality_notColdStart`(완독 1권 → 서술·캐시 생성, min=5에선 Red)로 의도 못 박고 상수 변경으로 Green, 콜드스타트 테스트 2건을 0권 기준으로 교정(`get_coldStart_whenFewBooks`→`whenNoFinishedBooks`) + 전체 그린. `PersonalityViewTest`는 임계를 주입받아 View 로직만 보므로 무변경. 설계 §6·백로그 "잠정 5권" → 1권 확정(근거 보존). DB·Flyway 무변경. (PR #244) |
| 2026-06-08 | **책BTI 분석 히스토리(최대 3개) + 대표 고르기**(실사용 피드백) — "책 추가 후 다시 분석했는데 성향이 안 바뀐 것 같다. 비슷한 책이라 비슷한 값인지, 정말 안 바뀐 건지 모르겠다. 하루 3번뿐이라 또 못 돌린다 → 분석을 돌려도 과거 문장이 안 사라지고 최대 3개까지 남게, 그중 하나를 골라 비교하게". 기존 `reading_personality`는 **1행/유저**라 "다시 분석"이 덮어써 과거 서술이 소실 → 비교 불가가 근본 원인. 모델 전환: **최대 3행/유저 + `selected`(대표) 플래그**(유저당 0~1). 대표=공개 책방 노출 + 교체에서 보호. 사용자 결정 3: ①고른 게 **책방 대표+삭제 보호** ②\"다시 분석\"은 **후보로만 추가**(자동 대표 X, 기존 선택 유지) ③새 행은 **'다시 분석' 버튼에서만** 쌓임(단순 방문은 교체 안 함). 서비스 3진입점: `currentPersonality`(GET — 대표 읽기, 비었고 책 충분하면 첫 1개 부트스트랩=자동 대표, **책장 바뀌어도 재생성 안 함**) · `reanalyze`(\"다시 분석\" — 후보 추가 후 4행이면 *대표 뺀 가장 오래된 후보* 1개 교체, LLM 실패 시 stale 대표 유지·새 행 X) · `select`(대표 변경, LLM 無, **본인 행만=IDOR 가드**). `history`는 최신순+대표+**stale**(이 분석 이후 책장 바뀜) 표시. 교체 정렬은 `generated_at`+**id tiebreak**(같은 초도 결정적, #245 신고함과 동일 교훈). 공개 책방은 `findByUserAndSelectedTrue`로 대표만 노출. **V29**: user_id unique 드롭이 MySQL/H2로 갈리는 함정(V26 주석) 회피 위해 **CREATE+INSERT…SELECT+DROP+RENAME**로 테이블 재생성(기존 1행→`selected=true` 이관). 템플릿 `personality.html` 3카드(대표 뱃지·분석 시각·책장변경 안내·\"이걸로 대표 선택\" 폼). TDD Red→Green: 도메인 히스토리 10(교체·보호·선택·재생성안함·serve-stale·IDOR·부트스트랩·stale)·GET경로 6·뷰 1·컨트롤러 3(히스토리 표시·대표선택·CSRF) + 전체 그린(FlywayMigrationTest로 V29 H2 검증). 본문 §책BTI·설계문서 화해, learning-notes N-063 후보. (PR #246) |
| 2026-06-08 | **책BTI 한도 소진·분석 시각 UX 3종 수정**(실사용 혼란 진단 후속) — 사용자가 "다시 분석을 눌러도 성향이 안 바뀌고 후보도 안 뜬다"고 보고. **진단(타임스탬프 대조)**: 화면 대표 분석 시각 17:43 KST < 이 히스토리 기능(#246) 배포 ~19:00 KST → **버그 아님** — 오늘 3회를 신기능 배포 *전(구버전 덮어쓰기)*에 소진해, 신기능이 quota 0으로 처음 만난 것(자정 KST 리셋 전엔 막힘 → reanalyze 실행 0 → 후보 행 0, 화면엔 마이그레이션된 대표 1개만). 혼란의 진짜 원인은 **0/3 상태가 설명 안 되는 UX**라 3종 동시 수정: ① **비활성 버튼 시각화** — `app.css`에 `button:disabled,.btn-primary:disabled{opacity:.5;cursor:not-allowed}`(+disabled hover 색 고정). 배경색을 명시한 `.btn-primary`가 disabled여도 그대로 눌리는 것처럼 보이던 문제 ② **한도 안내 상시 표시** — 안내문 조건을 `refreshLimited`(플래시)→`refreshRemaining==0`으로. 버튼이 disabled라 POST가 안 가 플래시가 영영 안 뜨던 **catch-22** 해소(차단 직후도 remaining==0이라 같이 덮임) ③ **분석 시각 사용자 타임존 표기** — 그동안 템플릿 `#temporals.format(Instant,…)`가 서버 JVM 기본(컨테이너=UTC) 기준이라 한국 사용자에게 9시간 어긋난 시각(17:43→08:43)으로 보임 → `PersonalityView`에 `ZoneId zone`+`formatTime(Instant)`(UTC 저장값→사용자 타임존 변환) 추가, 템플릿이 `${view.formatTime(...)}` 호출(컨트롤러가 `ZoneId.of(user.timezone)` 주입). 부수: 템플릿 개발 주석을 표준 `<!-- -->`(출력에 그대로 나가 응답 버퍼를 키우고 내부 한글 주석이 사용자에게 노출)에서 **파서 수준 `<!--/* */-->`(출력 제거)** 로 — 주석이 길어지며 `get_ready` 렌더가 응답 버퍼 commit 경계를 넘겨 "Cannot create a session after response committed"(CSRF 세션, #245와 같은 함정)를 재유발한 것을 차단. TDD Red→Green: `formatTime_rendersInUserZone`(UTC 08:43Z→KST 17:43 — 스텁 `""` RED→구현 GREEN)·`formatTime_nullSafe` + 전체 그린(617). 로직 변경은 타임존 변환뿐, DB·Flyway 무변경. 본문 §책BTI 갱신. troubleshooting T-041·learning-notes N-064 후보. (PR #247) |
| 2026-06-08 | **책BTI 책장 요약 레이아웃 + 개발 주석 유출 수정**(실사용 화면 확인) — 두 결함: ① **개발 주석이 화면에 노출** — #247에서 넣은 파서 수준 주석 본문에 **닫힘 토큰을 글자로 인용**(`파서 수준 주석(<!--/* */-->)`)해, Thymeleaf가 *맨 처음 만난* 닫힘 토큰에서 주석을 조기 종료 → 뒤 텍스트(`…응답 버퍼 비대화/주석 유출 방지. */-->`)가 일반 텍스트로 렌더됨. "주석은 안 샌다"는 주석이 닫힘 토큰을 인용한 탓에 정작 자기가 샌 자기참조 함정. 주석에서 닫힘 토큰 인용을 빼고 풀어 설명(+재발 경고 한 줄). ② **책장 요약 행 세로 스택** — `.record-row`(기본 `flex; space-between`)라 "자주 읽는 저자" 라벨과 긴 저자 목록이 가로로 붙어 라벨이 줄바꿈돼 보기 나빴음 → 라벨 위/내용 아래로 세로 스택하는 `.summary-list` 변형을 **전역 `.record-row` 안 건드리고**(타 페이지 회귀 방지) `app.css`에 추가(인라인 `<style>` 아님 — SSR 응답 본문 비대화 회피=버퍼 commit 함정도 같이 피함), `personality.html` `<ul>`에 `summary-list` 클래스 부여. TDD Red→Green: `get_doesNotLeakDevComment`(렌더 출력에 `*/-->`·`응답 버퍼` 없음 단언 — 주석 유출 시 RED→주석 수정 GREEN). 이 테스트는 미리 만든 `MockHttpSession`을 줘 맨 아래 CSRF 폼의 세션 생성이 버퍼 commit 경계에 휘둘리지 않게 고정(로그인 유저는 세션 이미 존재=프로덕션 일치, T-033). 전체 그린. 로직·DB·Flyway 무변경. (PR #249) |
| 2026-06-08 | **책BTI "내 독서 성향" 설명 문구 3줄 제거**(실사용 UX — 화면 정리) — "사용자가 UI만으로 충분히 이해할 디자인이어야지 문구로 일일이 설명하면 화면이 지저분". "내 독서 성향" 헤드 밑 안내문(`'다시 분석'을 돌릴 때마다 과거 결과가 최대 3개까지… ⭐ 대표로 고르면 공개 책방에 노출돼요`, `.status-line muted` 한 문단)을 삭제 — 대표 뱃지·후보 카드·"이걸로 대표 선택" 버튼·하단 공개 안내문이 이미 같은 내용을 UI로 전달해 군더더기였음. `personality.html` `<p>` 한 문단 제거뿐, 로직·모델·CSS·DB 무변경(부수효과로 SSR 응답도 줄어 버퍼 경계 더 여유). 이 문구를 단언하는 테스트는 없음(주석·DisplayName에만 등장) → PersonalityControllerTest 렌더로 무회귀 확인. (PR #250) |
| 2026-06-08 | **공개 책방 — 필터 클릭 시 책BTI 탭으로 튕기던 버그 수정**(실사용 피드백) — 공개 책방(`/u/{loginId}`)은 책BTI/공개 책장 **CSS 라디오 탭**(기본=성향 있으면 책BTI)인데, 공개 책장 탭의 상태 필터(전체/읽고싶음/읽는중/완독)는 **서버 링크 `?status=`**(리로드)라 둘이 다른 상태 계층에 살았음. 필터를 누르면 리로드→컨트롤러가 `personality` 재계산→`th:checked="${personality != null}"` 재적용→**책BTI 탭으로 리셋**(클라 탭 상태를 서버가 모름)→사용자가 공개 책장을 다시 눌러야 필터 결과가 보이는 왕복 불편. 처방: "책장을 보는 중" 신호를 URL에 실어 서버가 탭 기본값을 보정 — `ProfileController`가 `tab` 파라미터를 받아 `shelfActive = (shelfFilter != null) || "shelf".equals(tab)` 모델 추가, 탭 `th:checked`를 `personality != null and !shelfActive` / `personality == null or shelfActive`로, 필터 링크 전부(전체 칩 포함) `tab='shelf'`를 실음. 첫 진입(신호 없음)은 기존 의도(성향 있으면 책BTI 먼저) 유지. 서버 라운드트립·CSS 탭·`/books` 일관성 모두 보존, DB·CSS 무변경. TDD Red→Green: 필터 시 shelfActive=true·tab=shelf("전체")도 true·첫 진입 false 3경계(모델 속성 단언) + 전체 그린. (PR #252) |
| 2026-06-08 | **프로필 헤더를 한 카드로 묶기**(실사용 UX — 화면 정리) — 책방 페이지(`/u/{loginId}`) 상단의 책방명(`…님의 책방`)·@핸들·팔로워/팔로잉 카운트 3요소가 각각 독립 블록이라 "둥둥 떠다닌다"는 피드백. 원인은 페이지 나머지(잔디·책BTI)가 전부 `.card` 박스인데 **이 헤더만 비(非)카드**라 시각적으로 붕 떠 보인 것. 처방: 세 요소를 `<div class="card profile-card">`로 감싸 페이지 카드 스택과 통일. `app.css`에 `.profile-card` 내부 여백 정돈 3줄(카드 패딩 24px이 바깥 여백을 담당하므로 `.greeting`·`@핸들`·`.profile-follow`의 큰 마진 제거·간격 재설정). 신고 disclosure·잔디 카드는 박스 밖 유지(요청 3요소만 묶음). 순수 템플릿+CSS 변경, 로직·모델·DB 무변경 → 커밋 테스트 게이트 스킵, UI/CSS는 브리틀 영역이라 단위 테스트 비대상. (PR #254) |
| 2026-06-08 | **성장 잔디 — 연속 독서 일수에 따라 자라는 식물 뱃지**(유희성 기능) — 잔디 컨셉의 연장: 잔디를 연속으로 심을수록 독서 잔디 카드 우측 상단의 식물이 **땅→새싹→꽃→나무**로 자란다. 사다리(넓게): `0=🟫땅 · 1~3=🌱새싹 · 4~13=🌷꽃 · 14+=🌳나무`. **연속(streak) 정의**: 오늘부터 거꾸로 "읽은 날(초>0)"이 끊기지 않고 이어진 일수 — 목표 달성 무관(문턱 낮게, 수동 입력일도 초>0이라 포함). **오늘 유예**: 오늘 아직 안 읽었으면 어제부터 셈(자정마다 0 리셋 방지), 오늘·어제 모두 비면 0, 중간 구멍은 끊음. 끊기면 식물이 땅으로 리셋(=연속을 깨지 마라 동기 장치). 구현: 추가 쿼리 없이 빌더가 이미 가진 `secondsByDate`+`today`로 계산 — `ContributionGraphBuilder.currentStreak()` 추가, `ContributionGraph`에 `currentStreak` 필드 + `growthStage()` 파생(신규 enum `GrowthStage.of(streak)`=땅/새싹/꽃/나무, 이모지·라벨·임계 한 곳에 모아 추후 SVG 교체 쉽게). 노출: 대시보드·히스토리·프로필 잔디 헤더를 `.grass-head`(flex)로 감싸 우측 상단 뱃지(이모지+`N일 연속`). 책 상세 per-book 잔디는 헤더가 달라 자동 제외(per-book streak 무의미). DB·Flyway 무변경(기존 세션 집계로 계산), 레코드 필드 추가가 닿는 생성처는 빌더 1줄뿐이라 회귀 위험 낮음. TDD Red→Green: streak 경계 9(빈값·오늘만·오늘+어제·오늘유예·끊김·중간구멍·0초·14연속·build반영) + GrowthStage 경계 5(0/1·3/4·13/14·100) — 스텁(of→땅·streak→0) RED→구현 GREEN, 전체 그린. 뷰 문자열·CSS는 브리틀이라 단위 테스트 비대상. (PR #255) |
| 2026-06-09 | **밀린 부채를 헤드라인 타이머에 합산 표시 + 설정 토글(기본 ON)**(실사용 피드백) — "50분 남기고 자정 지났는데 타이머가 증가량 70분으로 리셋, 갚을 50분이 사라짐". #217 7일 윈도우 per-day의 '이월 없음'이 의도대로 동작한 것(버그 아님)이나, 사용자가 누적 부채를 원해 **헤드라인에 윈도우 미상환 부채를 합산**하도록 전환 + 설정 on/off(`ReadingTimer.debtCarryover`, V30, 기본 true — **표시 전용**, 부채 모델은 per-day 독립 유지). `DashboardModel`: 토글 ON이면 `remainingSeconds=WeeklyDebt.totalDebtSeconds()`(오늘+빠뜨린 날 합)·`carriedDebtSeconds`=floor(빠뜨린 날 합), OFF면 오늘 부채만·floor 0. **측정 차감 일관성**: `dashboard.js`가 `data-floor`에서 멈춰(어제 빚은 오늘 읽어 못 갚음=날짜별 독립과 일관) 측정 종료 후 서버 재계산과 안 어긋남, `goalMet`=잔여가 floor 도달. 설정 체크박스 + 헤드라인 보조문구 "지난 밀린 N분 포함". TDD Red→Green: ReadingTimer 2·DashboardModel 2(ON/OFF)·UserSettingsService 1·SettingsController 2·DashboardController 1(합산 통합) + 전체 그린. (PR #257) |
| 2026-06-09 | **plan.md 백로그 — 다국어(i18n) Accept-Language 항목 기록** — 영어권 확장 시점 검토용(우선순위 낮음·기록만). `Accept-Language` 협상(브라우저 언어설정 기반, IP/지역 아님)·Spring `AcceptHeaderLocaleResolver`(헤더 읽기는 공짜, 진짜 일은 한글 하드코딩 문구 추출=messages 번들 0 확인)·실무 함정 3종(수동 토글 병행·SEO는 URL 분리+hreflang·동적 데이터 미번역)·트리거(§리브랜딩·§도메인 이전과 묶기). 사용자 질문("Accept-Language로 한/영 분기 가능한가")에서 출발한 백로그 기록. 문서만(plan.md 1파일). (PR #258) |
| 2026-06-09 | **plan.md 운영 후속 — AdSense 크롤러 로그인 정보(로그인 뒤 페이지 광고 품질) 기록** — 광고 단위가 로그인 뒤 화면(대시보드·`/history`·`/books`)에 있어 AdSense 크롤러가 로그인 뒤를 못 보면 맥락 광고 매칭이 약해 관련성 낮은 광고가 뜰 수 있음(빈 광고 아님) → 콘솔에 크롤러 로그인 정보 등록 권장. *심사 통과·접근성*은 공개 랜딩(#230)+robots `Allow:/`+permitAll로 이미 충족, 이건 **게재 품질** 후속(구글 AdSense "사이트에 접근할 수 없는 경우" 체크리스트 3번). 사용자 질문(애드센스 체크리스트에 우리 코드가 걸리나)에서 출발 — robots/Security/랜딩/ads.txt 점검 결과 코드는 무해 확인, §광고 「남은 외부 작업」 아래 한 항목 추가. 문서만. (PR #259) |
| 2026-06-09 | **CLAUDE.md — 계획 md 핸드오프(계획↔구현 세션 분리) 규칙 추가** — 사용자 작업방식(계획=강모델/고effort, 구현=약모델/저effort를 다른 세션에 배정)을 명시화. 큰 작업 한정으로 계획 세션이 `claude-docs/plans/<날짜>-<요약>.md` 1개 생성 → 구현 세션이 읽고 TDD 구현. 트리거(큰/다단계/리스크만·작은 건 한 세션)·md 수명(임시, repo 로드맵 plan.md와 구분, 완료 후 삭제/흡수)·드리프트 규칙(계획 어긋나면 멈추고 보고, 설계 흔들리면 계획 세션 재호출)·통제력↔마찰 트레이드오프 명시. 「계획 우선」 §「어떻게」와 「예외」 사이에 삽입. 문서만(CLAUDE.md). (PR #260) |
| 2026-06-09 | **`.gitignore` — 계획 핸드오프 md 디렉토리(`claude-docs/plans/`) 무시** — PR #260 후속. 계획↔구현 세션 분리의 임시 계획 md를 추적 안 하게(작업용 산출물·완료 후 삭제/PR 흡수라 커밋 부담 0). `.gitignore` Project-specific 섹션에 한 줄(`.commit-msg-tmp`·`private-docs/` 옆). (PR #261) |
| 2026-06-09 | **대시보드 헤드라인 아래 '지난 밀린 N분 포함' 보조문구 제거** — `dashboard.html` 46-49행(`<p class="timer-carry">` + 주석) 삭제, `app.css` 110-114행(`.timer-carry` 규칙) 삭제. `data-floor`·`carriedDebtSeconds` 모델·JS floor 카운트다운 유지(합산 설정 ON/OFF 동작 보존). 로직·모델·DB·테스트 무변경, 템플릿+CSS만. (PR #262) |
| 2026-06-09 | **책장 「책방 공개」 토글 시 스크롤 점프 제거 — htmx 부분 swap 적용** — `/books`의 공개 토글이 `redirect:/books`(풀 리로드)라 클릭마다 윈도우·`.shelf-scroll` 스크롤이 맨 위로 튀던 문제. 대시보드 `respond()` 선례를 따라 `HX-Request` 헤더로 분기: htmx면 갱신된 `Book`을 `books :: visToggle` 조각(200)으로, 에러(IDOR)면 422로. `books.html`에 htmx CDN 로더 + `.vis-toggle-form`에 `th:fragment="visToggle"`·`th:attr="hx-post=@{...}"`·`hx-target="this"`·`hx-swap="outerHTML"` 추가. 비-htmx 폴백(기존 redirect)은 그대로라 JS-off 환경도 작동. CSS·DB·Flyway 무변경. TDD Red→Green: htmx 200 조각 반환+DB 반영·htmx IDOR 422·기존 비-htmx redirect 2개(회귀 가드) 총 4테스트 그린. (PR #263) |
| 2026-06-09 | **책BTI 출간연대 신호 제거(`pubDecades` C2 완전 제거)** — 알라딘 `pubDate`는 에디션 인쇄일(원작 최초 출간일 아님)이라 고전 재판(죄와 벌 1866→2015 인쇄)이 "2010년대 출간작"으로 오판, LLM이 확신형 오서술 생성. `ReadingProfile` record·`ReadingProfileAggregator`(decade 집계·`decadeOf`·`decadesNewestFirst`)·`GeminiReadingPersonalityNarrator`(`bookFactsJson` pubDecades 주입·프롬프트 문구)·`ProfileSignature`(`;decades=`) 완전 제거. 테스트 5파일 생성자 인자·단언 정리. 시그니처 변경으로 기존 캐시 1회 재생성(스테일 오서술 자연 청소). DB·Flyway·화면 변경 없음. TDD Red→Green: `buildPrompt_omitsPublicationEra` 회귀 가드 포함. (PR #264) |
| 2026-06-09 | **책BTI 분석 카드 — 세로 스택 → 가로 캐러셀(CSS scroll-snap)** — `/personality` 분석 카드 최대 3개를 가로 스와이프 캐러셀로 전환(JS 0줄, CSS `scroll-snap-type: x mandatory` 네이티브). 순서: 대표(⭐ selected) 무조건 첫 칸 → 나머지 최신순. `PersonalityView.displayEntries()` 파생 접근자 추가(`entries()` 최신순 계약 불변). 캐러셀 CSS는 app.css 하단(인라인 → 공통 스타일로 응집 — 인라인 유지 시 응답 버퍼 8KB 초과 → CSRF 세션 생성 실패 함정 발견). TDD Red→Green: `displayEntries_putsRepresentativeFirstThenNewest`·`singleEntry`·`noSelected` 3 + 기존 회귀 0. (PR #265) |
| 2026-06-09 | **책BTI 캐러셀 2차 — 중앙+양옆 엿보기 & 데스크탑 마우스 드래그 넘기기** — #265 캐러셀을 (1) 활성 카드 중앙 정렬+양옆 엿보기(`scroll-snap-align: center`, 카드 `flex 0 0 80%`, 첫/끝 카드 `margin 10%`로 끝 카드도 중앙—`padding-inline` 안 쓴 이유: `flex-basis %`가 content-box 기준이라 컨테이너 패딩이 카드 폭 이중 축소), (2) 데스크탑 마우스 드래그(외부 `static/js/personality.js`, pointer 이벤트, `pointerType==='mouse'`만 가로채 모바일 터치 무손상)로 개선. 드래그/드래그 클릭 충돌은 6px 임계+capture 단계 click 1회 흡수로 카드 안 '대표 선택' 버튼 보호. **구현 중 발견**: 기존 `.personality-carousel`의 `scroll-behavior: smooth`가 드래그 중 `scrollLeft` 직접 대입을 애니메이션화해 1:1 추적이 깨짐 → 드래그 중 `scrollBehavior='auto'` 토글, 놓을 때 복원(탄력 스냅 유지)으로 보정(계획 갭, 사용자 승인 후 적용). CSS는 app.css에만·JS는 외부 파일(T-033/#266 응답 버퍼 원칙), 서버·DB·테스트 무변경. preview 목업(`.preview/personality-mock.html`)으로 센터링 기하·드래그/클릭 분기 수동 검증(JUnit 0). (PR #267) |
| 2026-06-09 | **CLAUDE.md — PR·머지 담당 = 구현 세션 명문화**(PR #260 후속) — 계획↔구현 세션 분리 규칙(#260)에서 "그럼 PR·머지는 누가 하나"가 비어 있던 것을 못 박음. 「계획 우선」 §「세션 분리 — 계획 md 핸드오프」의 드리프트 규칙 바로 아래에 한 줄 추가: **PR 생성 → (사용자 확인) → 머지 → 로컬 `main` 갱신·브랜치 정리까지 구현 세션이 맡는다.** 근거: PR/머지는 설계가 아니라 기계적 마무리(git·요약·sweep·문서 한 줄)라 저-effort 쪽 일이고, 방금 diff를 만든 세션이 PR body·changelog를 가장 정확·싸게 쓴다(설계 세션이 하면 구현 컨텍스트를 강한 모델에 재로딩 = 낭비). 문서 처리는 계획 md가 선결정해 둬 구현 세션은 판단 없이 실행. 예외: 회귀 리스크 큰 작업만 머지 전 독립 리뷰 패스(설계 세션·`/code-review`) opt-in. 문서만(CLAUDE.md), 코드·로직 무관. (PR #268) |
| 2026-06-09 | **책BTI 캐러셀 데스크탑 내비 교체 — 마우스 드래그 → 좌우 화살표 버튼**(#267 후속) — #267의 데스크탑 마우스 드래그가 "매우 어색하다"는 사용자 피드백 → 드래그를 통째로 제거하고 오버레이 좌우 화살표 버튼(‹ ›, 사용자 선택: 양옆 가장자리 겹침)으로 교체. **중앙+양옆 엿보기 슬라이드 CSS(#267, `.personality-slide` flex/margin/snap, `:only-child`)는 무변경 유지.** `static/js/personality.js` 전면 교체(드래그 IIFE 49줄 → 버튼 핸들러 `track.scrollBy({left:±step})`. step=카드폭+gap이라 한 칸이 정확히 다음 카드 중앙 스냅; `behavior` 생략→CSS `scroll-behavior` 따라 smooth·reduced-motion 자동 존중, JS 미디어쿼리 불필요. `sync()`로 양끝 버튼 dim·overflow 0이면 `.nav-hidden`). `templates/personality.html` 트랙을 `.personality-carousel-wrap`로 감싸고 버튼 2개 추가(내부 슬라이드 `th:each` 한 글자도 무변경). `static/css/app.css` grab `@media(pointer:fine)` 블록 제거(드래그 종료) + 래퍼·버튼 CSS 추가(`.nav-hidden .carousel-nav{display:none}` — `display:flex`가 `[hidden]` 이기므로 클래스로 숨김; `@media(pointer:coarse)` 터치 기기는 버튼 숨겨 네이티브 스와이프 유지). 드래그 제거로 '대표 선택' 버튼 클릭/드래그 충돌 보호 로직(6px 임계+capture click 흡수)이 원천 소멸 — 코드 단순·견고화. 서버·도메인·DB·Flyway·JUnit 0(.preview 목업으로 step 기하 340·340→680=overflow, 양끝 dim 토글, 단일카드 `.nav-hidden`+`:only-child` 100%, coarse/nav-hidden CSS·grab 제거 수동 검증). (PR #269) |
| 2026-06-09 | **learning-notes N-066 추가 — `scrollBy`의 `behavior` 생략 시 CSS `scroll-behavior`를 따름(reduced-motion 자동 존중)** — #269 캐러셀 작업의 sweep 후보(사용자 판정: 박기). `el.scrollBy({left})`에 `behavior`를 안 적으면 그 스크롤은 요소의 CSS `scroll-behavior`를 따른다 → 모션 정책을 CSS 미디어쿼리 한 곳에 두면 버튼 스크롤도 reduced-motion에서 JS 분기 없이 자동 즉시 점프. `'auto'`(기본=생략)는 "즉시"가 아니라 "CSS를 따름"(즉시는 `'instant'`). N-065 ④(드래그 1:1 추적엔 smooth가 방해)의 반대면. 문서만(learning-notes.md). (PR #270) |
| 2026-06-09 | **책장 필터 클릭 시 스크롤 맨 위 점프 제거 — 내 책장·공개 책방 필터 htmx 부분 swap** — 상태 필터 칩(전체/읽고싶음/읽는중/완독)이 평범한 `<a href>` 풀 리로드라 클릭마다 페이지 스크롤이 맨 위(scrollY=0)로 리셋되던 문제. #263 공개 토글의 htmx 부분 swap 패턴을 필터에도 확장: 필터 칩+책 목록을 `th:fragment="shelfPanel"`(`id="shelf-panel"`)로 묶고, 두 컨트롤러(`BookController.books`·`ProfileController.profile`)에 `HX-Request` 헤더 분기 추가(htmx면 `:: shelfPanel` 조각만, 아니면 풀 뷰). 필터 `<a>`에 `th:attr="hx-get=@{...}"`+공통 `hx-target/hx-swap=outerHTML/hx-push-url`을 `<nav>`에 호이스트(자식 상속)→클릭 시 책 목록 패널만 교체, 페이지 스크롤 불변·URL은 갱신(공유·뒤로가기 유지). no-JS면 `th:href` 풀 리로드 폴백(점진적 향상). **profile.html은 `<head>`가 독립이라 htmx CDN 스크립트 직접 추가**(빠뜨리면 공개 책방만 조용히 폴백). 부수 효과: 공개 책방 부분 swap이라 탭 라디오를 안 다시 그려 "필터 누르면 책BTI 탭으로 튕김"도 htmx 경로에선 소멸(`shelfActive`/`tab=shelf` 폴백 기계는 보존). 필터링 로직·모델 적재·book-row 마크업 무변경, CSS·DB·Flyway 무변경. TDD Red→Green: 두 컨트롤러 htmx 분기(조각 반환+hx-get·책목록 포함·전체 문서 아님) + 비-htmx 풀 뷰 회귀 가드 4테스트 그린. (PR #271) |
| 2026-06-09 | **프론트엔드 디자인 작업 워크플로 + 템플릿 데이터 계약 문서화** — 입구(랜딩·가입·온보딩·대시보드 첫인상) 미감 이탈을 막는 디자인 트랙을 시작하기 전, 백엔드 세션(컨트롤러·데이터)과 디자인 세션(미감·CSS)을 충돌 없이 병렬로 굴리는 방법을 못 박음. `claude-docs/frontend-design-workflow.md` 신설(SSR Thymeleaf·단일 app.css·`.preview` 목업이라는 현실 전제 / "새 디렉토리"=정적 샌드박스(A) vs 워크트리(B) / 4단계 흐름: 진단→디자인 시스템(CSS 토큰) 먼저→정적 목업 반복→포팅 / 소유권 분할: 백=데이터 바인딩·디자인=app.css 단독+마크업 / 공유 계약 2개·충돌 핫스팟 회피). `claude-docs/template-data-contract.md` 신설(백→디자인 핸드오프 — landing은 "모델 데이터 0, 완전 정적"으로 확정 기입, signup/onboarding/dashboard는 도달 시 채우는 스텁; N-055 정신의 빈 상태 명세). 코드·DB·Flyway 무관, 문서만. 다음: 랜딩부터 디자인 트랙 진입. (PR #272) |
| 2026-06-09 | **책 검색 결과에 "이미 책장에 있음" 배지 + 추가 폼 숨김** — `/books` 검색 결과 중 이미 내 책장에 있는 책을 행을 숨기지 않고 명시(followPopularity 사회신호 배지와 같은 메타 줄, 중립 톤으로 구분). 재추가가 `addFromSearch` 무방비(중복 행 생성) 풋건이라 소유 시 "추가" 폼을 `th:unless`로 숨겨 UI 경로 재추가 차단. 소유 판정은 컨트롤러에서 `myBooks` 전체(상태 필터 무관)의 isbn 집합 `myShelfIsbns`를 모델에 실어(followPopularity 동형 패턴, 새 쿼리 0·메모리 집합) 템플릿은 `th:with="owned=…"`로 조회만. null isbn(수동 등록)은 집합에서 제외(N-055 경계). CSS `.shelf-owned-badge` app.css에만(T-033). TDD Red→Green: 소유 isbn 노출·null 누출 방지·상태 필터 무관 3테스트(검색 결과 행 렌더는 테스트 프로필 searchEnabled=false라 미검증, 모델 시드로 단언). 서버측 `(user,isbn13)` 중복 가드는 범위 밖(후속 분리). (PR #273) |
| 2026-06-09 | **책 추가 서버측 중복 가드 — 같은 (user, isbn13) 재추가 시 멱등 no-op** — #273이 UI(추가 폼 `th:unless` 숨김)는 닫았지만 `/books/add` 직접 POST로는 여전히 중복 책장 행이 생기던 데이터 무결성 풋건(`addFromSearch` 무방비)을 서버에서도 막음. `BookService.addFromSearch`에 가드: 들어온 isbn을 `Isbn.normalize`로 정규화(저장값과 같은 표기)해 `BookRepository.findFirstByUserAndIsbn13`로 기존 책 조회 → 있으면 **기존 책을 반환(새 행 생성 X), 상태는 보존**("추가"가 기존 상태를 몰래 바꾸지 않게). isbn null(수동 등록)은 동일성 키가 없어 가드 미적용(여러 권 허용 — null끼리 거짓 병합 방지). Option A 채택(서비스 레벨, 멱등). DB `(user_id,isbn13)` 유니크 제약(B)은 기존 중복 행 정리 마이그레이션+FK unlink 부담 커 후속 하드닝으로 보류. 동시 두 POST 경합 창은 미차단(1인 규모 무시 가능, 주석 명시). TDD Red→Green: 중복 행 미생성·기존 상태 보존(Red→Green) + null isbn 다중 허용·다른 사용자 격리(과차단 가드, 유지) 4테스트. 컨트롤러·마크업·DB·Flyway 무변경. (PR #274) |
| 2026-06-09 | **force-push-to-main 하드 차단 + push 감지 false-positive 수정** — `.claude/hooks/block-main-push.ps1` 확장. ① main/master로의 **force-push**(`--force`/`-f`/`--force-with-lease`/`--force-if-includes`/단축 묶음 `-fu`)를 전용 차단 — 일반 main push override(`ALLOW_MAIN_PUSH`)로도 안 뚫리고 전용 토큰 `ALLOW_FORCE_PUSH_MAIN`로만 통과(공유 히스토리 덮어쓰기 방지, 사용자 요청). force 판정을 `ALLOW_MAIN_PUSH` 분기보다 **앞**에 둬 일반 허용이 force를 새지 않게. ② **구현 중 발견·수정**: 기존 게이트가 `\bpush\b`라 브랜치명/파일경로/커밋메시지 속 'push'(하이픈도 단어경계)까지 git push로 오판 → 실제로 `chore/…-push-…` 브랜치 생성이 오차단됨. 게이트를 `git` **서브커맨드** push만 잡게 정밀화(`git push`/`git -C <path> push`/전역옵션 뒤 push). targetsMain 판정(명시 ref/현재 브랜치/upstream)을 단일 불리언으로 리팩터(중복 제거). 합성 stdin JSON 파이프 **16케이스** 검증(force+main 차단·force+feature 허용·비-force+main 차단·commit 무시·override 2종·'push'/'main' 이름 오탐 회귀). 훅(.ps1)만 — 앱 코드·DB·Flyway·JUnit 무관. (PR #275) |
| 2026-06-09 | **검색결과 '추가' 버튼 콤팩트화·우측 정렬 + 계획-우선 규칙에 '구현 승인 분리' 명문화** — `/books` 검색 결과 행의 "추가" 버튼이 전체폭으로 셀렉트 아래 좌측에 떨어져 "거대+좌측 하단"이던 UX 교정. 원인: 전용 규칙이 없어 베이스 `button{width:100%; padding:12px 18px}`을 그대로 받아 100%폭 버튼이 `flex-wrap` 폼에서 셀렉트 아래로 떨어지고 좌측 정렬됨(반면 책장 행 `.book-actions`는 이미 콤팩트+`margin-left:auto` 처리됨). `app.css`에 6줄 추가 — `.book-add-form{margin-left:auto}`(상태 셀렉트+버튼 묶음을 행 우측 끝으로=우측 하단)+`.book-add-form .btn-primary{width:auto; padding:6px 14px; font-size:.85rem}`(콤팩트). `.book-actions`와 같은 톤으로 통일. 셀렉터가 `.book-add-form` 한정이라 다른 버튼 영향 0; '직접 추가' 폴백·'검색' 버튼은 전체폭이 맞는 맥락이라 의도적 유지. 마크업·서버·DB·Flyway·JUnit 무변경(CSS만). 검증: `.preview` 정적 하니스에 실 `app.css`를 링크해 데스크탑 카드(410px)·모바일(375px) DOM 지오메트리 측정 — 버튼 width auto **55px**(전체폭 ~410→55)·셀렉트+버튼 묶음 행 **우측 끝 정렬**(`btnAtRightEdge`/`formAtRightEdge` true) 확인(스크린샷 도구는 프리뷰 렌더러 행으로 미사용 → inspect/geometry로 대체, 더 정확). 부수: **CLAUDE.md 「계획 우선」§어떻게에 '계획 요청 시 구현 승인은 별개' 경고 추가** — 설계 방향 AskUserQuestion 옵션 선택을 구현 승인으로 오해해 계획 없이 구현까지 나아간 이 세션 사례의 재발 방지(명시적 "구현해" 전엔 코드 미수정). (PR #276) |
| 2026-06-10 | **이메일 발송 인프라 — 법적 부담 기준 2단계 분리(plan.md)** — 재참여 넛지(retention 레버 ①) 착수 전 법·보안·평판 리스크를 정리. 핵심: transactional(가입 이메일 인증·비번 재설정·열거 통지)은 *서비스 이행 안내*라 정보통신망법 광고성 규제 **무관** → **1단계**로 먼저 깐다(보안 '높음' 갭 3개 pre-hijacking·열거·비번복구 닫힘·법적 리스크~0). 재참여 넛지는 *영리목적 광고성 정보*(AdSense·제휴 수익)라 **제50조**(사전 옵트인 기본OFF·제목 `(광고)`·발신자 정보·무료 수신거부·야간21~08시 별도동의·수신동의 2년 재확인)+**개인정보보호법**(처리방침 발송목적 고지·마케팅 선택동의 분리)을 갖춰 **2단계**로 얹는다. 순서 근거: 넛지를 컴플라이언스 없이 먼저 쏘면 위반(과태료)+미검증 주소 반송→발신 도메인 평판 하락으로 1단계 transactional 메일까지 스팸함 동반 사망(연쇄). §전략 레버 ① 설계 메모 + §이메일 인프라 체크리스트(1·2단계) 동기화 + 일반 교훈을 learning-notes **N-067**(재참여 메일=광고성 규제 vs transactional 규제 무관 → 발송 종류를 법적 부담으로 분리)로 박음. 문서만(코드·DB·테스트 무변경). (PR #277) |
| 2026-06-10 | **독서 정원 — 잔디를 게임화한 수집형 정원으로 (plan.md 아이디어 정리)** — 독서 잔디(GitHub 컨트리뷰션 복제)를 "정원"으로 재정의하는 비전을 plan.md에 캡처(미설계·아이디어 단계). ① **명칭 전환**: 잔디→정원(Garden). ② **식물 획득 2트랙**: ⓐ 타이머 할당량(시간 기반) ⓑ 완독(`BookStatus.FINISHED`)×읽고싶음(`WANT_TO_READ`) 책 **조합**의 *숨은 레시피*(어떤 조합이 어떤 식물인지 사용자에게 안내 안 함=발견이 재미). ③ **매핑 3축=출판사·작가·장르** — `Book.author/publisher/category`에 이미 존재(알라딘 자동 채움), 단 category가 계층 문자열·수동 등록 책 null이라 정규화가 난제로 기록. ④ **도입 순서**: 기존 잔디(53×7) 공존→정원 완성도 오르면 흡수(단계적 대체). ⑤ **리치 UI + 프론트 선후 결정[A]**: 식물 도감(현 SSR+htmx+Alpine 스택 가능)·드래그 커스텀 꾸미기(레이아웃 영속화=JSON API 필요)→ 드래그는 "두 번 짜기" 회피 위해 **프론트 전환 스코프에 묶어 한 번만**, 도감/읽기전용 미니 정원은 먼저 출하 가능. §기능 로드맵 「독서 정원」 신설 + §프론트엔드 전환에 "정원=인터랙션 요구↑ 구체 동인" 한 줄 연결. 문서만(코드·DB·테스트 무변경). (PR #278) |
| 2026-06-10 | **책장 검색 결과 페이지네이션 → 고정높이 스크롤 박스** — `/books` "책 추가" 카드의 검색 결과를 "← 이전 / N / M 페이지 / 다음 →" 페이저(전체 페이지 리로드, 스크롤 리셋)에서 **한 번에 ~40개 받아 ~3행 고정높이 박스 안에서만 스크롤**되게 교체. "모기" 같은 ~180건 결과를 18페이지에 걸쳐 "다음" 연타하던 비용 제거(서버 왕복 0). `BookSearchClient.PAGE_SIZE` 10→40(알라딘 MaxResults 상한 100 이내), `books.html` 결과 `<ul>`에 `.search-scroll` 클래스 + 커버 `loading="lazy"`(40장 중 ~3장만 즉시) + `<nav class="pager">` 통째 제거, `app.css`에 `.search-scroll{max-height:430px; overflow-y:auto; scrollbar-gutter:stable; overscroll-behavior:contain; …}` 신설(`.shelf-scroll` 작은 높이판) + 사용처 사라진 전역 `.pager`/`.pager .btn-ghost`/`.pager-info` 규칙 제거(admin-users.html은 자체 scoped style). 트레이드오프: 40 초과 결과는 잘림(더보기 없음) — 책 추가 용도엔 상위 40 안에 거의 다 들고, 못 찾으면 검색어 정제가 18페이지 스크롤보다 빠름(의식적). 기존 테스트 그대로 green(알라딘 URL 테스트는 `MaxResults` 무단언). 설계 핸드오프 `claude-docs/plans/2026-06-10-book-search-scroll.md`(작업용·비커밋). (PR #279) |
| 2026-06-10 | **책BTI 태그 — 규칙 기반 8종족 닫힌 어휘 (Phase 6b 1단계)** — `ReadingTribe`(8종족 enum + 알라딘 대분류 매핑), `ReadingTagger`(3축 결정적 태그: 장르 정체성·폭·저자 충성도), `PersonalityView.from` 배선(LLM 태그→결정적), `personality.html` 태그 칩 행 + `app.css` pill 스타일. Flyway·엔티티·서비스 시그니처 무변경(온더플라이). TDD Red→Green 3 Increment(`ReadingTribeTest`·`ReadingTaggerTest`·`PersonalityViewTest`). (PR #281) |
| 2026-06-10 | **계획 우선 규칙 보강 — 계획 md의 TDD 절에 RED → GREEN 명시 의무화** — 구현 세션이 핸드오프 계획을 읽고 TDD를 건너뛰는 것을 막기 위해, CLAUDE.md 「계획 우선」§계획에 담을 것의 'TDD 계획' 항목을 강화. **계획의 TDD 절은 평소의 RED → GREEN 순서(① 실패 테스트 먼저 → ② Red 확인 → ③ 최소 구현 → ④ Green 확인)를 명시한다(필수)** — 테스트 가능한 로직·동작이 있으면 생략·약화 금지; 순수 시각 변경만 사유와 함께 preview/수동 검증 게이트로 명시하되 TDD 전체 건너뛰기의 핑계가 되지 않게. 「계획에 담을 것」은 세션 분리 핸드오프 md도 그대로 참조하므로 단일 세션·md 양쪽에 전파. 배경: #279(책장 검색 스크롤) 계획 md가 'view/CSS라 새 테스트 안 만듦'으로 RED→GREEN을 약화해 구현 세션이 절차를 안 밟은 사례(이번 PR의 직접 동인). 인코딩 위치는 사용자 선택(소프트 CLAUDE.md 규칙 vs 하드 settings.json 훅 중 CLAUDE.md). 문서만(코드·DB·테스트 무변경). (PR #280) |
| 2026-06-10 | **책BTI 태그 — 책방 헤더 노출 (Phase 6b 2단계)** — #281 결정적 태거(`ReadingTagger`) 출력을 본인 화면 `/personality` 외에도 **남에게 보이는 책방 `/u/{loginId}` 상단 박스**(닉네임/@핸들/팔로우 카운트 묶음)에 칩 행으로 노출. `ProfileView`에 `personalityTags: List<String>` 추가, `ProfileService`에 `ReadingProfileService` 주입 후 `ReadingTagger.tagsOf(publicProfileOf(target))` 호출(공개 완독 책만 입력 → 비공개 취향 누출 차단), `ProfileController`가 모델에 적재, `profile.html`은 기존 `.tag-chip-row`/`.tag-chip` 클래스 재사용(CSS 신규 0). 서술 캐시(`personality`)와 독립이라 LLM 서술이 없어도 사실에서 직접 태그가 뜬다(견고성↑). 빈 목록이면 `th:if`로 행 자체 숨김(공개 완독 0권/매핑 장르 없음 콜드스타트). Flyway·엔티티·태거 무변경(배선만). TDD Red→Green 3테스트: 공개 완독→채워짐, PRIVATE 완독→누출 차단(N-055 정신), 0권→빈 목록. 설계 핸드오프 `claude-docs/plans/2026-06-10-profile-bti-tags.md`. (PR #282) |
| 2026-06-10 | **책BTI 태그 클릭 → 근거 책 드릴다운 (Phase 6b 3단계)** — 책방 헤더의 책BTI 태그 칩을 **클릭하면 그 태그를 만든 공개+완독 책 목록**을 칩 아래에 htmx로 인라인 펼친다. 클릭 대상은 **장르 종족 + 한우물형 칩만**(폭 태그는 부분집합 없음 — 비클릭). 신규: 순수 도메인 `PersonalityTagAttribution.booksForTag`(공개 책 → 완독 + 태그 필터, 장르 라벨/`AUTHOR_LOYAL_TAG` 상수 매핑), `profile/ProfileTag`(라벨+클릭가능 record), `GET /u/{loginId}/personality-tag?tag=…` 핸들러(`profile :: tagBooksPanel` 프래그먼트 반환). 수정: `ReadingTribe.ofLabel` 역매핑, `ReadingTagger.AUTHOR_LOYAL_TAG` 상수, `ReadingProfileAggregator.primaryGenre` public화, `ProfileView.personalityTags : List<String>` → `List<ProfileTag>`, `ProfileService.resolveVisibleTarget`로 가드 체인 추출(운영자·차단·없는 아이디 — `profileOf`와 `booksForPersonalityTag` 공유 → PRIVATE 책 누출 차단 보장 일관). `profile.html` 칩이 clickable이면 `<a hx-get>`(폭 태그는 `<span>`), 헤더 카드 직후 `#tag-books-panel` 컨테이너 + `tagBooksPanel` 프래그먼트(닫기 버튼·"없어요"·`.book-row` 재사용), `app.css` `.tag-chip-click`/`.tag-books-panel` 신설. Flyway·태거 규칙·LLM 무관. TDD Red→Green: `PersonalityTagAttributionTest`(장르 종족 합산·완독 필터·미매핑 제외·한우물형 동률 결정성·null/blank·알 수 없는 태그) + `ProfileControllerTest`(공개만 노출·PRIVATE 누출 차단 N-055·운영자/차단/없는 아이디 404·비로그인 리다이렉트·한우물형 라우팅·알 수 없는 태그 빈 목록·칩 clickable 분류). 설계 핸드오프 `claude-docs/plans/2026-06-10-tag-evidence-drilldown.md`. (PR #283) |
| 2026-06-10 | **plan.md 갱신 이력 발췌 폐지(changelog 단일 출처) + 본문 책BTI 태그 Phase 6b 반영** — ① **롤링 발췌 폐지**: plan.md의 `🔄 갱신 이력 (최근 10개)` 섹션을 제거(995→975행)하고 changelog.md를 변경 일지 **단일 출처**로 확정 — 매 PR '두 곳(plan.md 발췌 + changelog) 동기화'의 마찰·드리프트를 없앰. CLAUDE.md 4곳(Git워크플로 4번 발췌-갱신 의무 줄 삭제·작업종료 §최신화 1번·자가점검 문장 `plan.md 갱신이력`→`changelog 갱신이력`·폐지 이력 한 줄)과 changelog.md 헤더를 '발췌 안 둠'으로 동기화. ② **본문 반영**: 구현 세션들이 changelog·발췌엔 기록했으나 비어 있던 plan.md **본문 책BTI 로드맵**(§기능)에 Phase 6b 마일스톤(8종족 결정적 태거 #281·책방 헤더 노출 #282·근거 책 드릴다운 #283)을 섹션 헤더+진행 문단으로 추가 → 본문이 출하 상태와 일치(닫힌 어휘=매칭 키라 §3기능 #2 사람 추천 입력으로 직결 명시). 문서만(코드·DB·테스트·Flyway 무변경). (PR #284) |
| 2026-06-10 | **책방 책BTI 태그칩 클릭 시 스크롤 점프 제거** — 책방 헤더의 클릭형 태그칩을 누르면 패널은 펼쳐지지만 화면이 `#tag-books-panel`로 앵커 점프(+ URL에 `#tag-books-panel` 박힘)하던 문제 해결. 원인: 칩이 `<a href="#tag-books-panel" hx-get>`인데 htmx `shouldCancel`은 fragment-only href(`#`로 시작하되 `#` 단독 아님)엔 `preventDefault`를 안 걸어, AJAX swap과 동시에 브라우저 앵커 점프가 그대로 실행(books 필터칩은 실제 URL href라 점프 없음). 해법: 패널 펼치기는 navigation이 아니라 action이므로 칩을 `<a>`→`<button type="button">`(href 자체가 없어 앵커 점프 구조적 불가, 키보드 Enter/Space 활성화 유지). `app.css` `.tag-chip-click`에 button 기본값 리셋(`border:none`·`font-family:inherit`·`line-height:inherit`) 추가 — preview computed-style 비교로 span 칩과 픽셀 동일(높이 26px·padding·font·radius·color 일치) 확인. 서버·엔드포인트·모델·프래그먼트 무변경. TDD Red→Green 회귀 가드 1개(`ProfileControllerTest.profile_clickableTag_hasNoFragmentHrefThatJumps` — 클릭형 칩에 `href="#tag-books-panel"` 부재 + htmx 트리거·엔드포인트 보존 단언). 설계 핸드오프 `claude-docs/plans/2026-06-10-책방-태그칩-스크롤-점프.md`. (PR #286) |
| 2026-06-10 | **책BTI 태그 드릴다운 책 목록 → 고정높이 스크롤 박스** — 태그 칩 클릭 시 펼쳐지는 "그 태그를 만든 책" 목록이 길어 아래 팔로우·잔디·탭·공개 책장을 밀어내던 문제를, 전용 `.tag-books-scroll`(50vh) 신설로 박스 안 스크롤화(shelf-scroll 60vh·search-scroll 430px와 동일 관성·거터·번짐방지 패턴, 맥락 전용 명명). 드릴다운 헤드(제목·× 닫기)는 `<ul>` 밖이라 스크롤 중에도 고정. CSS 1블록 + `profile.html` 드릴다운 `<ul>` 클래스 한 단어. 코드·DB·테스트 무변경(어트리뷰션 불변식은 기존 `PersonalityTagAttributionTest`가 커버). 설계 핸드오프 `claude-docs/plans/2026-06-10-tag-books-scroll.md`. (PR #285) |
| 2026-06-10 | **랜딩 디자인 리프레시 — 종이톤 + 세이지 토큰 시스템 + 고운돋움 폰트 (입구 디자인 트랙 1번)** — 입구 첫인상 미감 이탈 차단 디자인 트랙의 첫 화면(landing). `app.css` `:root` 색 토큰을 범용 인디고(`--accent:#4f46e5`·흰 카드·회색 배경) → **종이톤+세이지** 팔레트로 교체: `--bg #F3EEE4`(크림)·`--card-bg #FCFAF5`·`--text #2C2A24`(잉크)·`--muted #6F6A5E`·`--accent #6E8A6A`(세이지)·`--accent-hover #4F6B4C`(딥세이지)·`--border #E4DDD0`. **토큰 이름 유지=무파괴** → `var` 참조하는 25개 화면 전역이 한 번에 리프레시(의도된 시스템 교체). 본문 폰트 **'고운돋움'**(둥근 한글 산세리프, Google Fonts `@import`)을 `body` 최우선으로 전역 적용(사용자 선택: 둥글둥글 무드). 보조 토큰 `--sage-soft #E7EEE2`·`--radius-sm 8px` 추가, 그림자 종이톤 따뜻하게 조정. 달성색 `--ok`는 세이지와 변별되게 `#059669`→`#2F8F6B`(더 선명한 초록)로 톤 조정(잔디 셀은 GitHub 스타일 고정 hex라 무영향). landing 미감: 태그라인 1.35rem→1.95rem(페이크볼드 제거, 크기로 위계)·`.brand h1`·`.card h2` 딥세이지·`.btn-oauth-google` 종이 표면(흰→`--card-bg`, hover `--sage-soft`)·기능 항목 strong 1.05rem. `landing.html` 마크업·카피·링크 전부 보존(CTA href 2개·title·meta·login/privacy 그대로). **회귀 0 검증**: 스크린샷 도구가 이 환경에서 타임아웃(렌더러는 정상) → 정적 목업(landing·dashboard-footer·history) + `preview_inspect`/`eval` computed-style로 종이톤·세이지·고운돋움 적용 + 책장 필터(세이지 배경+흰글자 대비)·달성색 변별 스폿체크. CSS만 변경(`.java`·템플릿·DB·Flyway 0 → 테스트 게이트 통과 대상 아님). 설계 핸드오프 `claude-docs/plans/2026-06-10-landing-design-refresh.md`. (PR #287) |
| 2026-06-10 | **책방 책BTI 태그칩 디자인 — full-width 버그 수정 + 책 기반/복합 색 구분** — #286의 `<a>`→`<button>` 교체로 클릭형 칩이 전역 `button{width:100%}`를 받아 가로로 늘어나던 버그를 `.tag-chip-click`에 `width:auto`로 잡고, 책 기반(근거 책 드릴다운 가능) 칩을 진한 채움(`var(--accent)`)+흰 글자로 강조해 복합 칩(옅은 베이스)과 색 구분(안 A). hover도 `filter+underline`(채움엔 과함)→`var(--accent-hover)` 배경만 진하게로 통일. **드리프트 반영**: 계획 md는 `--accent` 인디고(#4f46e5) 전제였으나 그 후 머지된 #287(종이톤+세이지 리프레시)이 토큰을 세이지(#6E8A6A)로 교체 → 계획이 토큰을 참조해 둔 덕에 코드 변경은 그대로, 채움색만 자동으로 세이지가 됨(전역 일관·의도 유지). `app.css` 1파일 CSS만(`.tag-chip-click` 두 셀렉터), 마크업·서버·DB·테스트 무변경. 설계 핸드오프 `claude-docs/plans/2026-06-10-tag-chip-design.md`. (PR #288) |
| 2026-06-10 | **인디고 잔재 토큰화 + signup 환영 헤더 (입구 디자인 트랙 2번 1차)** — #287 종이톤+세이지 토큰 교체가 못 건드린 **`var()` 아닌 raw 옛 인디고(`rgba(79,70,229,…)`)** 하드코딩 2곳을 세이지로 토큰화: `:root`에 공유 토큰 `--focus-ring: rgba(110,138,106,.30)`(세이지 #6E8A6A 기반) 신설 → `.field input/select:focus` box-shadow를 `var(--focus-ring)`로(폼 입력칸 포커스 글로우 인디고→세이지), `.highlight-tile:hover` 그림자도 raw 세이지 rgba로 교체. **전역 grep 0건 확인**(app.css·템플릿 전 범위에서 `79,70,229`·인디고 hex 잔재 부재 — 토큰 교체는 raw 하드코딩을 못 잡으니 grep 동반, N-068 보강). focus 링·타일 그림자는 전 화면 공통이라 입구뿐 아니라 사이트 전역 동시 수선. signup 마감 1차: `.greeting "회원가입"` 한 줄 → `.entry-hero`(환영 헤드라인 "책 읽는 습관, 오늘부터" + 서브카피, landing 히어로 축소판·크기로 위계) 교체, `.field-hint`(브라우저 기본 small만 먹던 입력 보조 안내)를 종이톤에 맞게 명시 정의. 데이터 바인딩 보존(`th:object`·각 `th:field`·`th:errors`·`timezones` 루프·`/signup`·`/login`·`/privacy`·`<title>`). 순수 시각(컨트롤러·폼·DB·Flyway 무변경) → 잔재 grep 0건 + `./gradlew test` 그린 + 바인딩 보존 확인이 게이트. onboarding·dashboard는 후속 분리 PR. 설계 핸드오프 `claude-docs/plans/2026-06-10-entry-screens-polish.md`. (PR #289) |
| 2026-06-10 | **onboarding 디자인 마감 (입구 디자인 트랙 2번 2차)** — signup(#289)이 확립한 `.entry-hero`(환영 헤드라인+서브카피) 패턴을 onboarding에도 통일: `.greeting` 동적 한 줄("환영합니다! 시작 설정을…") → `.entry-hero`(h2 "환영합니다! 🎉" + `needsLoginId` 분기 서브카피). 하루목표·7일 윈도우 안내 문단을 전역 공유 `.status-line muted` 대신 **입구 전용 `.entry-note`**(연한 세이지 박스 `--sage-soft` + `--radius-sm` + strong 딥세이지 `--accent-hover`)로 띄워 **가치 전달 위계 강화** — `.status-line`은 대시보드 라이브 등 전역 공유라 분리한 새 클래스(다른 화면 영향 차단). `needsLoginId` 두 형태(소셜=loginId 칸 보임 / 로컬=없음) + loginId 중복 에러 보존(`th:object`·`th:if`·각 `th:field`·`th:errors`·prefill 기본값·action `@{/onboarding}`). 순수 시각(컨트롤러·폼·DB·Flyway 무변경) → 기존 `OnboardingController` 테스트 그린 + 정적 목업 `.preview/onboarding-mock.html`(두 분기+에러) computed-style 검증(세이지 박스 `#E7EEE2`·strong 딥세이지 `#4F6B4C` 700·모바일 무깨짐). 스크린샷은 이 환경 타임아웃(렌더러 정상, snapshot/inspect로 대체 — #287과 동일). 설계 핸드오프 `claude-docs/plans/2026-06-10-entry-screens-polish.md`. (PR #290) |
| 2026-06-10 | **dashboard 빈상태 마감 (입구 디자인 트랙 2번 3차·완료)** — 입구 트랙 마지막·고위험 화면. 신규 첫 진입(`readingBooks`·`finishedBooks` 모두 empty)에서 타이머 카드가 측정 폼 대신 보이던 `.status-line muted` 안내 한 줄("책장에 '읽는 중' 책을 추가하면…")을 **`.timer-empty`**(환영 문구 "아직 책장이 비어 있어요. 첫 책을 추가하면…" + 풀폭 세이지 CTA "첫 책 추가하기" → `/books`)로 강화 — "텅 빈 화면"을 환영+첫 행동 유도로(첫 로그인 인상). **고위험 라이브 영역 구조 절대 보존**: `#dashboard-live` id·`th:fragment="live"`(`ReadingSessionController`가 htmx swap으로 공유)·`x-data="readingTimer()"`·`data-active/started/remaining/floor`·`x-text/x-show/:class`·htmx `hx-post/hx-target/hx-swap`(start/stop)·잔디 `th:each` 루프 무변경, 빈상태 `th:if` 조건식도 글자 단위 유지(시각·카피만). `a.btn`이 inline이라 `.btn`의 `width:100%`가 안 먹어 글자폭으로 나오던 것을 `display:block`으로 측정 시작 버튼처럼 풀폭화(landing은 flex 컨테이너라 먹었지만 여기선 아님 — computed-style로 발견·수정). 순수 시각(컨트롤러·서비스·DB·Flyway·JS 무변경) → **전체 테스트 그린**(라이브 프래그먼트·빈상태 분기·공유 swap 회귀 가드) + 정적 목업 `.preview/dashboard-empty-mock.html` computed-style·모바일 검증(CTA 세이지 `#6E8A6A` 흰글자 풀폭·밑줄 없음·strong 딥세이지 `#4F6B4C`). 입구 디자인 트랙(landing #287·인디고+signup #289·onboarding #290·dashboard #291) **완료**. 설계 핸드오프 `claude-docs/plans/2026-06-10-entry-screens-polish.md`. (PR #291) |
| 2026-06-19 | **마을 SPA S2 — 게임 위젯 Vue 이전(GardenGame.vue)** — 정원 SPA 분리 S2. ① `GardenApiResponse`에 `owned`(식물 팔레트)·`characters`(배회 캐릭터) 최상위 필드 추가 + TDD Red→Green(`GardenApiControllerTest`). ② Vue 빌드 셋업: `package.json`에 `vue@^3.5`·`@vitejs/plugin-vue@^5` + `vite.config.ts`에 `vue()` 플러그인. ③ `GardenGame.vue` 신설 — Alpine `myGarden` 컴포넌트를 Vue SFC로 이전; **N-082 필수**: `scene`·`game`·`placed`·`chars`는 setup 클로저 변수 + `markRaw()` 적용(Vue Proxy 순환참조 차단). `onMounted`에서 `fetch('/api/garden')` 직접 호출, 저장은 `POST /api/garden/layout`. ④ `main.ts`: `Alpine.start()`(도감 탭 유지) + `createApp(GardenGame).mount('#village-game')` 공존. ⑤ `garden.html`: 게임 섹션을 Vue 마운트 포인트(`#village-game`)로 전환, `window.__garden` Thymeleaf 주입 제거, Alpine 도감 섹션 유지. ⑥ `npm --prefix frontend run build` 번들 재생성 + git add(T-063). Chrome 확장 실 브라우저 검증: Phaser v3.90.0 로드·N-082 에러 0·편집 모드·저장·Alpine 탭 공존 확인. 브랜치 `feat/garden-spa-s2-vue-game`. (PR #403) |
| 2026-06-10 | **회원 탈퇴 500 수정 — purge에 `reading_goal_change` 정리 추가** — 설정 화면 회원 탈퇴(LOCAL 비번 경로·소셜 @핸들 경로 둘 다)가 500으로 실패하던 버그 수정. 원인: `AccountService.purge()`가 user를 FK 참조하는 자식 테이블 중 **`reading_goal_change`만 빠뜨린 채** 부모 `users`를 삭제 → 트랜잭션 flush 시 `fk_reading_goal_change_user` 위반(`DataIntegrityViolationException`). 실유저는 온보딩(`goalService.record`)·설정 변경·V21 백필로 거의 항상 이 행을 가져 사실상 전원 탈퇴 불가. 메서드(`ReadingGoalChangeRepository.deleteByUser`)는 이미 있었고 주입·호출 배선만 누락. 수정: `AccountService`에 `ReadingGoalChangeRepository` 주입 + `purge()`의 `timerRepository.deleteByUser` 다음 줄에 `goalChangeRepository.deleteByUser(user)` 한 줄(다른 테이블의 부모 아니라 user 삭제 전이면 순서 무관) + 클래스 javadoc 삭제 순서에 목표 이력 반영. TDD Red→Green: `AccountDeletionIntegrationTest.deleteAccount_withGoalChangeHistory_succeeds`(실 H2 FK, `record`로 행 생성 후 탈퇴 — 수정 전 FK 위반 RED 확인) + 단위 `AccountServiceTest` 두 `inOrder`에 배선 회귀 가드. Flyway·엔티티·컨트롤러 무변경(배선 1줄). learning-notes **N-055** 자매 실사례 한 줄 보강(완성 픽스처만 만들면 못 잡는 함정의 삭제 방향 판본). 설계 핸드오프 `claude-docs/plans/2026-06-10-account-deletion-goal-change-fk.md`. (PR #292) |
| 2026-06-11 | **이메일 발송 포트 토대 (이메일 인프라 1단계 PR-A)** — transactional 메일(가입 인증·비번 재설정·열거 통지)의 토대. 발송 수단을 포트로 추상화해 인프라(SES SMTP·발신 도메인 검증·SPF/DKIM/DMARC) 준비 전에도 호출부 코드를 개발·테스트하고, 준비되면 토글만 켠다(`ReadingPersonalityNarrator` 포트 + Gemini `isEnabled` 게이트와 동형). 신규 패키지 `com.booktimer.email`: `EmailSender` 포트(`send(to,subject,htmlBody)`) + `LoggingEmailSender`(기본/dev/test — `booktimer.email.enabled=false`·미설정 시 선택, 실발송 0·로그만, 본문은 토큰 링크 등 민감정보라 미로깅) + `SmtpEmailSender`(prod — `enabled=true` 시 `JavaMailSender` 위임, 실패는 `EmailSendException` 래핑) + `EmailSendException`. 두 어댑터는 `@ConditionalOnProperty`로 상호배타(정확히 하나만 빈). `build.gradle`에 `spring-boot-starter-mail`, 토글 `booktimer.email.enabled`(기본 false)·`booktimer.email.from` + `application-prod.properties` SES SMTP placeholder(비밀 미커밋). TDD: `SmtpEmailSenderTest`(발송 위임 to/subject/from MIME 단언 + 실패 시 래핑) — 어댑터 선택은 프레임워크 동작이라 누름. 전체 그린·회귀 0. 설계 핸드오프 `claude-docs/plans/2026-06-10-email-infra-phase1.md`. (PR #293) |
| 2026-06-11 | **운영 배포(CD) 무한 타임아웃 핫픽스 — mail 헬스체크 제외** — #293(`spring-boot-starter-mail`)이 들어간 첫 배포부터 ECS 안정화가 ~30분 타임아웃(`wait-for-service-stability` → `{"state":"TIMEOUT","observedResponses":{"200: OK":19}}`)하고 re-run(attempt 2)도 동일 실패. **원인**: starter-mail이 자동 등록한 Spring Boot `MailHealthIndicator`가 운영의 **빈 SMTP host**(`spring.mail.host=${SPRING_MAIL_HOST:}` — SES 미가동이라 미주입)로 연결을 시도해 health를 `DOWN`으로 만들고 → ALB 헬스체크 경로 `/actuator/health`(전체 그룹, mail 포함)가 503 → 새 태스크 unhealthy → ECS 무한 replace → 배포 안정화 실패. `minimumHealthyPercent=100`이라 옛 태스크(#292) 유지 → **사이트 자체는 무중단**, 배포만 못 끝남. 메일은 `booktimer.email.enabled=false`라 **실발송도 안 하는데 헬스체크만 새던** 케이스. **수정**: `application-prod.properties`에 `management.health.mail.enabled=false` 한 줄(SES 실가동 — `SPRING_MAIL_HOST` 주입 + `booktimer.email.enabled=true` — 시 제거해 헬스체크 되살림). 운영 프로필 전용 설정이라 단위테스트로 못 잡음(test는 H2·default 프로필) → **재배포 CD GREEN이 검증 게이트**. T-009(콜드스타트 grace)와 같은 "헬스 실패 → 태스크 replace 루프 → 안정화 타임아웃" 계열. (PR #295) |
| 2026-06-11 | **가입 이메일 인증 + pre-hijacking 차단 (이메일 인프라 1단계 PR-B)** — 보안 '높음' 갭(피해자 이메일을 미검증 LOCAL로 선점해 OAuth 계정을 탈취하는 벡터, N-053) 차단. 미검증 정책 "허용+핵심만 게이트"(사용자 승인): 미검증이어도 로그인·사용은 허용(thesis 입문자 마찰 최소), OAuth 자동연결만 막음. 기존 가입자는 Flyway `email_verified=true` 백필(grandfather). **도메인**: `User.emailVerified`(기본 false)+`verifyEmail()`(멱등)+`isEmailVerified()`, Flyway `V31`(컬럼+백필+`email_token` 테이블). **토큰 모델**(`com.booktimer.email`): `EmailToken`(평문 미저장·SHA-256 `token_hash`만·type·`expires_at`·`used_at` 일회용)+`EmailTokenType`(VERIFICATION 24h/PASSWORD_RESET 1h)+`EmailTokenService.issue/consume`(`Clock` 주입 — 만료 경계·일회용·type일치·재발급 무효화). **가입 인증**: `EmailVerificationService.sendVerification/verify`+`EmailVerificationController`(GET `/verify-email` 공개, POST `/verify-email/resend` 로그인)+`verify-email-result.html`, `SignupController`가 가입 직후 인증 메일 트리거(발송 실패 격리 — 메일 실패해도 가입 성공), `SecurityConfig` `/verify-email` permitAll. **pre-hijacking**: `OAuthUserProvisioningService.provision`이 미검증 LOCAL 선점 계정을 폐기 후 OAuth 신규(검증 LOCAL·기존 OAuth는 그대로 연결), `AccountService.purgeUnverifiedLocalAccount` 재사용. ⚠️ **flush 필수**(계획 §3-3 미묘 지점): 폐기 후 같은 이메일 INSERT 전 DELETE를 flush 안 하면 Hibernate가 INSERT를 먼저 실행해 `uk_users_email` 위반 → `flush()`로 순서 강제(실DB 통합 테스트가 회귀 가드). `AccountService.purge`에 `email_token` 정리 추가(FK). + code-review 반영: `booktimer.base-url` prod 설정 추가(미설정 시 인증 링크가 localhost로 깨지는 갭). TDD RED→GREEN: 토큰 경계 전수·가입 미검증·verify 멱등·pre-hijacking 단위(Mockito)+통합(실DB flush)·컨트롤러 공개/일회용/resend·Flyway 백필·검증, 기존 OAuth 회귀(N-026) 보존. 전체 그린·회귀 0. 설계 핸드오프 `claude-docs/plans/2026-06-10-email-infra-phase1.md`. (PR #294) |
| 2026-06-11 | **이메일 인증 하드닝 — GET 확인 페이지+POST 소비 + 비동기 발송 (SMTP 켜기 전)** — PR-B(#294) code-review에서 나온, 실발송(enabled=true)을 켜는 순간 활성화되는 두 문제 차단. **① GET 토큰 프리페치 차단**: `GET /verify-email`이 일회용 토큰을 즉시 소비하면 메일 클라이언트 링크 프리페치(Outlook SafeLinks·백신·메신저 언펄링)가 사용자 클릭 전에 토큰을 소진→"만료" 페이지 노출. → GET은 소비 없이 확인 페이지(`verify-email-confirm.html`, 토큰 hidden 폼+버튼)만 렌더하고, 사람이 버튼 클릭해야 발생하는 **`POST /verify-email`(CSRF)에서만 소비**. `SecurityConfig`는 path 매처라 POST도 이미 permitAll(무변경). **② 비동기 발송**: `sendVerification`이 토큰 발급+SMTP를 요청 스레드·트랜잭션 안에서 동기 실행 → 블로킹·DB커넥션 점유·가입 응답시간 차이(열거 타이밍 사이드채널). → 토큰 발급(DB)은 동기 유지(가입 시 확정), SMTP `send`만 **`EmailDispatcher.dispatch(@Async)`**(별 빈 — self-invocation 회피)로 분리, `AsyncConfig`(`@EnableAsync`+크기 제한 `ThreadPoolTaskExecutor`). 요청 스레드 블로킹·트랜잭션 내 I/O·응답시간 차이 제거, 발송 실패는 비동기 스레드라 dispatcher에서 격리. TDD RED→GREEN: GET 미소비·프리페치 안전(GET 2회 후 POST 검증)·POST 소비·일회용(컨트롤러)·dispatcher 위임(서비스)·위임+실패 격리(EmailDispatcher). 범위 밖(차후): SHA-256 공용 util·HtmlUtils 전환·resend UI 배너. 전체 그린·회귀 0. (PR #296) |
| 2026-06-11 | **비밀번호 재설정 (이메일 인프라 1단계 PR-C)** — 보안 '높음' 갭 마지막 하나(로컬 계정이 비번을 잊으면 복구 경로 없음) 닫음. PR-B의 토큰 인프라(`EmailToken`·`EmailTokenService`·`EmailDispatcher`)를 그대로 재사용 — Flyway·`User` 변경 0. **요청**(`PasswordResetService.requestReset`): 계정 존재/부재/소셜 여부와 **무관하게 동일 응답**(열거완화 N-052) — 차이는 내부 발송 여부뿐. `PASSWORD_RESET` 토큰(1h·SHA-256 해시저장·일회용)을 **LOCAL 계정에만** 발급해 재설정 링크(`/password/reset?token=`) 메일 발송(소셜=재설정할 비번 없음·부재=계정 없음 → 무발송). **재설정**(`reset`): 토큰 consume→`passwordEncoder.encode`→`User.changePassword`→save. **컨트롤러**(`PasswordResetController`, 공개): `GET /password/forgot` 이메일 폼 → `POST`는 항상 동일 안내 페이지(`password-forgot-sent`). `GET /password/reset`은 **토큰을 소비하지 않고** 새 비번 폼만 렌더(메일 프리페치가 토큰 소진 못 하게 — #296과 동일 패턴), 새 비번 검증(8~72자·일치) 통과한 `POST`에서만 소비. 검증 실패 시 토큰 미소비·폼 재렌더(링크 유지). `SecurityConfig` `/password/**` permitAll(토큰=자격, CSRF는 POST 유지) + `login.html` "비밀번호를 잊으셨나요?" 링크. TDD RED→GREEN: 서비스(LOCAL 발급·부재/소셜 무발송·유효 교체·무효 무저장) + 컨트롤러(forgot 동일 응답 3종·reset GET 미소비/프리페치 안전·POST 유효·무효·일회용·불일치/짧은 비번 미소비 재렌더). 전체 그린·회귀 0. 설계 핸드오프 `claude-docs/plans/2026-06-10-email-infra-phase1.md`. (PR #297) |
| 2026-06-11 | **PR CI 게이트(ci.yml) + main branch protection 도입 — 안전망 계층** — test가 `push:[main]`에만 걸려 PR에선 CI가 안 돌고(빨강 모른 채 30초 self-merge → main 오염 시 배포 정지), branch protection 전무(force-push·직접 push 방어가 전부 로컬 훅)였던 갭을 닫음. **① `.github/workflows/ci.yml` 신설**: `pull_request → main`에서 `./gradlew test`(H2라 Docker 불필요). `paths-ignore` 의도적 부재 — required check 대상 job이 문서-only PR에서 스킵되면 체크가 pending으로 남아 머지 영구 블록되는 함정 회피. deploy.yml의 push-time test는 이중 안전망으로 유지. **② main branch protection**(`gh api` PUT, 서버 설정): `required_status_checks {strict, contexts:["test"]}`(빨강이면 머지 버튼 잠금·out-of-date 차단) + `required_pull_request_reviews(승인 0)`(직접 push 차단=PR 강제, 1인이라 self-approve 불가로 0) + `enforce_admins=true`(본인도 우회 불가) + force-push/삭제 차단. 검증: ci.yml PR에서 test GREEN+체크 이름 실측 후 protection 적용 → main 직접 push 거부(RED)·readback 확인. 설계 핸드오프 `claude-docs/plans/2026-06-11-ci-pr-gate-and-branch-protection.md`. (PR #298) |
| 2026-06-11 | **learning-notes N-069 추가 — 소셜(OAuth)-only 인증의 트레이드오프** — "차라리 로컬 회원가입 없애고 구글·카카오·애플만 박으면 보안·법적 부담이 사라지나?" 설계 토론을 노트화. 결론: 비밀번호 관련 부담(해싱·재설정·credential stuffing·brute-force·가입 이메일 인증·pre-hijacking N-053)은 IdP에 *위임*돼 소멸하나 **책임 이전이지 소멸 아님** — 개인정보보호법 의무(이메일·닉네임 PII → 동의·처리방침·파기, 제3자 제공 고지 추가 N-027)·OAuth 콜백 보안(state·PKCE·redirect_uri)·세션·IdP 간 계정 병합은 잔존, 카카오 이메일 *선택* 동의(이메일 없는 계정 → `email` 유니크 식별자 깨짐)·애플 강제+Private Relay·단일 장애점은 새 함정. "다 박으면 단순"은 역설 → 하나(구글)로 좁게·식별자를 이메일 아닌 IdP subject ID로(N-046). 코드·DB 무변경(learning-notes 문서만). (PR #299) |
| 2026-06-11 | **CI 게이트 sweep — T-044 + N-070 문서화** — PR #298(CI 게이트+branch protection) 작업에서 나온 함정/개념 누적. **T-044**(troubleshooting): GitHub branch protection PUT은 4개 최상위 키(`required_status_checks`/`enforce_admins`/`required_pull_request_reviews`/`restrictions`)를 null이라도 모두 보내야 함(누락 시 422), PowerShell 5.1 here-string 파이프로 JSON 넘기면 인코딩 깨져 400 `Problems parsing JSON` → UTF-8 파일+`--input`(T-026 자매), `contexts` 체크 이름은 check-runs로 실측·ci.yml 머지 후 protection 켜는 닭-달걀 순서. **N-070**(learning-notes): required status check + `paths-ignore`는 머지 영구 블록 — 머지 필수 체크가 스킵되면 success 아니라 pending으로 영영 안 끝나 머지 잠김, "GitHub가 스킵을 통과로 쳐줄 것"이 핵심 오해, 회피는 required CI를 paths-ignore 없이 전 PR 실행(가벼우면 최선). (PR #302) |
| 2026-06-11 | **열거 통지 (이메일 인프라 1단계 PR-D)** — N-052 "조용히 수락 + 통지"의 정석 완성. 지금까진 이미 가입된 이메일로 재가입 시 응답만 가입 성공과 동일하게 흡수(`redirect:/login?registered`·계정 미생성)해 열거를 막되, 잊고 재가입한 정직한 주인은 로그인 단계에서야 알게 되는 비용이 있었다. 메일 인프라(PR-A~C)가 깔린 지금, 흡수는 그대로 두고 **그 이메일의 실소유자(기존 계정주)에게 통지 메일**을 보낸다. 신규 `email/SignupNotificationService.notifyExistingAccount(email)`: `findByEmail` → 존재하면 dispatch(LOCAL=로그인+비밀번호 재설정 링크, 소셜=Google 로그인 안내·재설정 링크 없음), 부재면 무발송(login_id 레이스 등 보낼 주인 없음). **핵심 보안 포인트**: 통지는 가입을 *시도한* 사람이 아니라 *기존 주인 메일함*으로만 가므로 열거가 아니다 — 시도자에겐 여전히 동일 응답. `SignupController`의 중복 흡수 catch(`EmailAlreadyExistsException`/`DataIntegrityViolationException`)에서 호출, 발송 실패 격리(`EmailDispatcher` @Async — 응답·계정 미생성 불변). 토큰·Flyway·`User`·템플릿 변경 0(메일은 문자열 빌더). TDD RED→GREEN: 서비스 3(LOCAL 통지·소셜 통지·부재 무발송) + `SignupControllerTest`에 `@MockitoBean`으로 중복 시 통지 호출·정상 가입 시 미호출 단언. learning-notes **N-052 실현 표시**. 전체 그린·회귀 0. 설계 핸드오프 `claude-docs/plans/2026-06-10-email-infra-phase1.md`. (PR #301) |
| 2026-06-11 | **개인정보처리방침 — 이메일 발송 목적 명시 (이메일 인프라 1단계 §5.5 누락 보충)** — 설계 md §5.5가 "PR-B에 묶어 처리"하라 한 처리방침 갱신이 PR-B(#294)에서 누락된 것을 보충. transactional 메일(가입 인증·비밀번호 재설정·보안 안내)도 개인정보(이메일) 이용이라 처리방침에 발송 목적 명시가 개인정보보호법 투명성 의무다(단 서비스 이행 목적이라 추가 동의·옵트인 불필요 — 2단계 마케팅 넛지의 선택동의와 구분). `privacy.html` §2 이용 목적에 한 줄 추가("가입 이메일 인증, 비밀번호 재설정, 보안·서비스 관련 중요 안내 메일 발송 — 광고성·마케팅 메일 미발송" 명시로 1단계/2단계 경계 분명히) + 개정일 2026-06-05→2026-06-11 갱신. 순수 문서(템플릿 텍스트만, `.java`·DB 무변경). (PR #303) |
| 2026-06-11 | **미검증 이메일 인증 배너 (이메일 인프라 1단계 — 정책 ③·코드 완료)** — 설계 md §3 정책③의 "대시보드/설정 배너로 인증 유도"를 구현. 입구 디자인 마감(#287~#291)과 파일이 겹쳐 미뤘던 마지막 1단계 코드 항목. `emailVerified=false`인 사용자에게 대시보드(`/`)·설정(`/settings`) 화면에 인증 유도 배너(앰버 `alert-warn`)를 띄우고, "인증 메일 다시 받기" 버튼이 `POST /verify-email/resend`(PR-B에서 추가)로 재발송한다. 미검증이어도 로그인·사용은 막지 않고 권유만 한다(thesis 마찰 최소 — 정책 A). 설정 화면은 재발송 결과 플래시(`verifyResendResult`: sent/already/failed)도 안내. 기존 가입자는 V31 백필(emailVerified=true)이라 배너가 안 뜬다(grandfather). **변경**: `DashboardController`·`SettingsController` 모델에 `emailVerified` 추가, `dashboard.html`(라이브 영역 밖 — htmx swap 무영향)·`settings.html`에 배너 마크업 + settings 플래시 표시, `app.css` `.alert-warn`+`.verify-banner`+`.btn-inline-link`(전역 `button{width:100%}` 회피 `width:auto`, #291 교훈). 토큰·Flyway·`User` 무변경. TDD RED→GREEN: Dashboard(미검증 노출/검증 숨김)·Settings(미검증 노출/검증 숨김) — `emailVerified` 모델 + `/verify-email/resend` 마커로 단언(플래시 텍스트 표시는 트리비얼이라 누름 — resend가 플래시 남기는 행동은 `EmailVerificationControllerTest`가 커버). ⚠️ 함정: Thymeleaf는 일반 HTML 주석(`<!-- -->`)을 출력에 보존 → 주석에 적은 `/verify-email/resend`가 검증 사용자 화면에도 새어 "배너 숨김" 단언이 깨짐 → 주석 문구에서 경로 제거로 해결. 전체 그린·회귀 0. **이메일 인프라 1단계 코드(가입 인증·pre-hijacking·하드닝·비번 재설정·열거 통지·처리방침·배너) 완료** — 남은 건 인프라(SES·DNS)뿐. (PR #304) |
| 2026-06-11 | **마케팅 수신동의 토대 (이메일 인프라 2단계 PR-1)** — 재참여 넛지(2단계)의 선결. 마케팅 수신 동의 수집·철회 경로 + 넛지 멱등 상태 컬럼을 깐다(실발송은 PR-2 이후·점등 PR-4, `booktimer.email.enabled` 기본 false라 회귀 0). **User 3컬럼**(캡슐화 메서드로만): `marketingEmailConsent`(기본 false — 정보통신망법 §50 opt-in 불변식)·`marketingConsentAt`(동의시각·2년 재동의·감사)·`lastNudgeSentAt`(비활동 구간당 1회 멱등) + `consentToMarketing(Clock)`·`withdrawMarketingConsent()`(시각 보존)·`recordNudgeSent`. **Flyway V32**(3컬럼, 동의는 `not null default false` — email_verified와 달리 **백필 없음**: 동의한 적 없는 사용자를 동의로 채우는 건 위조라 금지). **가입 동의**: `SignupForm.marketingEmailConsent`(선택·기본 OFF) + `UserRegistrationService.register` 오버로드(동의 시 트랜잭션 안 `consentToMarketing`·`Clock` 주입) + `signup.html` 체크박스(끼워팔기 금지 — 미체크도 가입). **철회**: `UserSettingsService.updateMarketingConsent` + `SettingsController` `POST /settings/marketing` + `settings.html` "소식·알림" 토글. TDD RED→GREEN: User 기본 OFF·동의시각·철회 시 보존 / register 동의·미동의·하위호환 / 설정 토글 ON·OFF / Flyway 기본 미동의(validate 드리프트 가드). 전체 그린·회귀 0. 설계 핸드오프 `claude-docs/plans/2026-06-11-email-infra-phase2-nudge.md`. (PR #305) |
| 2026-06-11 | **재참여 넛지 발송 로직 + 스케줄 (이메일 인프라 2단계 PR-2)** — 7일 비활동 + 동의·검증 사용자에게 "다시 읽어볼까요?" 메일을 비활동 구간당 1회 보낸다. 실발송은 `booktimer.email.enabled` 토글(기본 false=`LoggingEmailSender`)이 가르므로 머지해도 메일 0·회귀 0(점등은 PR-4 법무 게이트). **대상 선정**: `ReadingSessionRepository.findNudgeTargets(cutoff)` — §3-1 5조건 AND(한 번이라도 읽음 / 마지막 활동 ≤ cutoff / 동의 / 검증 / 이 구간 미발송). `max(startedAt)` 서브쿼리라 한 번도 안 읽은 사용자 자동 제외(null-state 누출 가드 N-055). **발송**: `RetentionNudgeService` — cutoff=now-7일(Clock 주입), per-recipient try-catch로 한 명 실패가 배치 안 멈춤, 성공 시에만 `recordNudgeSent`(멱등). 제목 `(광고)` 접두·발신정보 + 무료 구독해지 링크(`EmailTokenType.UNSUBSCRIBE` 30일)로 정보통신망법 §50 형태 충족. **스케줄**: `RetentionNudgeScheduler` `@Scheduled`(매일 KST 10시 — 야간 발송 제한 자연 회피) + `SchedulingConfig`(`@EnableScheduling`). TDD RED→GREEN: 7일 경계 전수(포함/이후/한번도안읽음/미동의/미검증/이미발송/재이탈="1회는 구간당") + 발송 격리(한 명 실패해도 나머지)·(광고)접두·구독해지 링크. 전체 그린·회귀 0. 설계 핸드오프 `claude-docs/plans/2026-06-11-email-infra-phase2-nudge.md`. (PR #306) |
| 2026-06-11 | **마케팅 구독해지 원클릭 (이메일 인프라 2단계 PR-3)** — 넛지 메일 수신거부 링크가 타는 경로. 토큰이 신원 증명이라 로그인 없이 해지(PR-2가 메일에 실은 `UNSUBSCRIBE` 토큰 소비). **`UnsubscribeService.unsubscribe(token)`**: `consume` → 유효하면 바인딩된 사용자만 `withdrawMarketingConsent`(IDOR 방어 — consume가 토큰 주인만 반환), 무효/만료/사용/위조면 불변. **`UnsubscribeController`**: `GET /unsubscribe`는 토큰 소비 없이 확인 폼만(메일 프리페치가 클릭 전 해지·토큰 소진 못 하게 — verify-email·password-reset과 동일 패턴), 사람이 버튼 누른 `POST`에서만 소비·해지. `unsubscribe-confirm.html`/`unsubscribe-done.html`. `SecurityConfig` `/unsubscribe` permitAll(CSRF는 POST 유지). 계획 §3-3 "GET one-click 소비"에서 한 곳 보정(프리페치 방어 컨벤션 준수). TDD RED→GREEN: 유효 해지·무효 거부·IDOR(서비스) + GET 미소비·POST 해지·일회용·무효(컨트롤러 MockMvc). 전체 그린·회귀 0. **phase2 코드(PR-1~3) 완료** — 남은 건 privacy.html(phase1 반영 별도)·PR-4 점등(법무 9박스·실발송 켜기). 설계 핸드오프 `claude-docs/plans/2026-06-11-email-infra-phase2-nudge.md`. (PR #307) |
| 2026-06-11 | **OAuth 사용자 emailVerified 정합 + 마케팅 처리방침 (이메일 인프라 2단계 보강)** — phase2 발송(#306) 머지 뒤 발견된 갭 보정 + 법무 게이트 처리방침. **3-A 정합**: 구글 가입 사용자가 `emailVerified=false`로 남아 재참여 넛지 대상에서 빠지고(`findNudgeTargets`가 `emailVerified=true` 필수) 인증 배너가 오노출되던 문제. `verifyEmail()`이 인증 메일 소비 경로에만 있고 `registerOAuth → User.ofOAuth`는 안 불러 기본 false로 가입되던 게 원인. `registerOAuth`에서 `verifyEmail()` 호출(provider가 소유 보증 = OAuth 게이트가 `email_verified=true`만 통과, N-026 — 그 거부 게이트는 위에 그대로) + Flyway **V33** 기존 소셜 백필(`auth_provider <> 'LOCAL'`, 로컬 무영향). **3-B privacy.html**: phase1 #303의 "광고성·마케팅 미발송"이 넛지와 모순 → §2를 transactional(동의 무관)·마케팅(선택동의·철회) 2항목으로 분리 + 끼워팔기 금지("동의 안 해도 서비스 이용 제한 없음") + §1 수집항목에 동의여부·동의시각 + 개정일 갱신. TDD RED→GREEN: `registerOAuth_marksEmailVerified`(verified 생성), 회귀 0(구글 픽스처 컨트롤러 테스트 그린), V33 유효성은 `FlywayMigrationTest` 자동(이 코드베이스는 백필 직접 미검증 — 테스트 DB는 마이그레이션 시점 데이터 0). 부수효과(배너·넛지)는 registerOAuth verified가 `isEmailVerified()`/`findNudgeTargets`로 자동 보장(중복 테스트 생략). 남은 건 phase2 점등(법무 9박스·SES/DNS·토글, phase2 완료 후 일괄). (PR #308) |
| 2026-06-11 | **쿠팡 파트너스 구매 링크 병행 (제휴 다변화)** — 알라딘 "구매" 버튼 옆에 쿠팡 "구매" 버튼을 나란히 추가(검색·등록은 계속 알라딘 API, 쿠팡은 구매 링크만). **핵심 설계 분기**: 알라딘은 검색 API가 통째로 준 링크를 DB(`Book.purchaseLink`)에 저장하지만, 쿠팡 검색 링크는 `f(ISBN·제목, 추적코드, URL템플릿)`로 결정적이라 **저장 없이 런타임 생성**(`CoupangLinkBuilder` — 알라딘 클라이언트의 enabled 게이트+정적 순수 URL 패턴 모방). 덕분에 마이그레이션·백필 0(기존 책 전부 즉시 버튼), 추적코드/템플릿 교체는 환경변수만으로 즉시 반영. **점진 출시**: 추적코드 `not-configured`(가입 전 기본값)면 `isEnabled()=false` → `coupangEnabled` 게이트로 쿠팡 버튼·공정위 고지문구를 화면에서 숨김(알라딘만). 쿠팡 파트너스 가입 후 `BOOKTIMER_COUPANG_TRACKING_CODE`/`_SEARCH_URL_TEMPLATE` 환경변수 주입만으로 활성화(알라딘 ttb-key처럼 relaxed binding, properties 미기재). 실제 추적 URL 형식 불확실성은 **URL 템플릿 환경변수**(`{query}`·`{trackingCode}` 치환)로 흡수 — 가입 후 코드 수정 없이 실형식에 맞춤. **변경**: `CoupangLinkBuilder`(@Component, ISBN13 우선·제목 폴백·URL 인코딩) / `Book.coupangClickCount`+`recordCoupangClick`(알라딘 `clickCount`와 분리 집계, Flyway **V34**) / `BookService.coupangEnabled`·`recordCoupangClick`(IDOR)·`recordPublicCoupangClick`(공개 게이트·책 주인 행 집계, 알라딘 메서드와 대칭) / `BookController` `/books/{id}/buy/coupang`·`/u/{loginId}/books/{id}/buy/coupang`(알라딘과 별도 경로 — 회귀 0) / `CoupangModelAdvice`(전역 `@ControllerAdvice`로 `coupangEnabled` 주입 — `AdsModelAdvice` 패턴, 3뷰 중복 제거) / 템플릿 3개(books·book-detail·profile에 쿠팡 버튼+고지문구). TDD RED→GREEN: CoupangLinkBuilderTest(enabled 게이트·ISBN/제목 검색어·추적코드 포함·인코딩) / CoupangBookServiceTest(소유·IDOR·비활성 null·공개 게이트·비공개 거부·없는 책) / CoupangBuyControllerTest(엔드포인트 4 + 뷰 게이트 렌더 2: 활성 시 버튼·고지 노출/비활성 시 숨김). 전체 그린·회귀 0. ⚠️ 미결(가입 후): 추적 URL 실형식 확정·ISBN 검색 품질 재평가·API 키는 판매 15만원 실적 게이트 뒤(초기 검색 링크 방식)·공정위 문구 노출 최종 확인. (PR #309) |
| 2026-06-11 | **도메인 TLD 이전 `.click`→`.app` — 코드/문서 일관성 (이메일 점등 선결)** — `.click`은 평판이 근본적으로 낮아 Chrome Safe Browsing "위험한 사이트" 차단 전력(N-036·T-027)이 있고, 이메일 2단계 마케팅 넛지를 평판 낮은 도메인으로 발송하면 딜리버러빌리티가 깎여 transactional 메일까지 동반 스팸처리되는 리스크 → 점등 전 `.app`(레지스트리 HTTPS 강제·평판 양호)으로 이전. **인프라(사용자 콘솔·이 PR 밖)**: `booktimer.app` 구매·ACM(서울 apex+www DNS검증)·ALB 443 SNI 인증서 추가·Route53 alias(apex·www)·Google OAuth redirect URI 병행 등록 완료, `https://booktimer.app` 가동(health UP) 확인. **이 PR(코드 일관성)**: base-url은 `BOOKTIMER_BASE_URL` env 주입이고 OAuth redirect는 `ForwardedHeaderFilter`가 `X-Forwarded-Host`로 동적 생성이라 **앱 동작 코드 0 변경** — 운영 환경변수만 새 도메인으로 바꾸면 메일 링크·redirect_uri가 전환됨. 하드코딩 3곳만 `.app`으로: `application-prod.properties` BASE_URL 주석 예시·`README`(Live·DNS)·`ForwardedHeadersHttpsTest`(샘플 호스트·redirect_uri 단언). 동작 로직 무변경이라 기존 테스트 전체 그린 유지가 게이트(`ForwardedHeadersHttpsTest`가 `.app` redirect_uri로 통과). **남음(사용자)**: 운영 `BOOKTIMER_BASE_URL=https://booktimer.app` 전환·재배포 → 로그인 검증 → 동의화면 privacy `.app` → `.click` 301. 실행 런북 `claude-docs/plans/2026-06-11-domain-tld-migration.md`. (PR #310) |
| 2026-06-11 | **운영 base-url `.app` 주입 (도메인 TLD 이전 ⑦)** — `.click`→`.app` 이전의 환경변수 전환. `BOOKTIMER_BASE_URL`이 그동안 `task-definition.json`에 없어 운영이 `application-prod.properties` 기본값 `http://localhost:8080`로 떨어져 있던 잠복 갭(메일 `enabled=false`라 발송 0이라 미발현)을 도메인 이전과 함께 해소. base-url은 공개 URL이라 SSM secret이 아닌 평문 `environment`로 주입(`{"name":"BOOKTIMER_BASE_URL","value":"https://booktimer.app"}`). 이 값은 메일 링크(가입 인증·비번 재설정·구독해지·열거 통지)의 절대 URL 베이스 전용 — OAuth redirect는 `ForwardedHeaderFilter`의 `X-Forwarded-Host` 동적 생성이라 무관(이미 `.app` 구글 로그인 검증 완료). `task-definition.json`은 `deploy.yml`이 register 소스로 쓰는 IaC라 main push 시 자동 배포로 주입. 코드·테스트 무변경(배포 설정만). (PR #311) |
| 2026-06-12 | **이메일 1단계 점등 — SES SMTP 주입 + 넛지 분리 게이트 (transactional 실발송 ON)** — SES 발송 인프라 셋업 후 운영 점등(1단계 transactional). **인프라(사용자 콘솔)**: `booktimer.app` 도메인 검증(Easy DKIM — SES가 Route53에 CNAME×3 자동 게시·확인)·프로덕션 액세스 신청·SMTP 자격증명 발급 → SSM `/booktimer/SPRING_MAIL_USERNAME`·`SPRING_MAIL_PASSWORD`(SecureString) 보관. **task def**: `SPRING_MAIL_HOST`(email-smtp.ap-northeast-2)·`SPRING_MAIL_PORT`(587)·`BOOKTIMER_EMAIL_ENABLED=true` 평문 environment + 자격증명 SSM secret 주입 → `SmtpEmailSender` 활성(가입 인증·비번 재설정·열거 통지 실발송). **핵심 보정(드리프트 포착)**: 발송 토글이 `booktimer.email.enabled` 하나라 켜면 마케팅 재참여 넛지도 함께 살아나 법무 9박스(정보통신망법 §50) 전 발송되는 갭 발견 → `RetentionNudgeScheduler`에 독립 게이트 `@ConditionalOnProperty(booktimer.nudge.enabled, 기본 OFF)` 추가로 분리. email.enabled=true여도 넛지 OFF 유지 → transactional만 점등, 법무 충족 후 `BOOKTIMER_NUDGE_ENABLED=true`로 2단계. TDD RED→GREEN: `RetentionNudgeSchedulerGateTest`(ApplicationContextRunner — OFF/미설정 빈 미등록·ON 등록, 2 Red→3 Green). mail 헬스체크는 첫 가동 SMTP 연결 실패 시 태스크 무한 replace 위험(T-009 계열)이라 `management.health.mail.enabled=false` 유지 — 실발송 검증 후 E단계 별도 PR로 부활. 전체 그린·회귀 0. (PR #312) |
| 2026-06-12 | **mail 헬스체크 부활 (이메일 1단계 점등 E단계)** — #312에서 SMTP 점등·실발송 검증(가입 인증 메일 실수신)을 마친 뒤, 첫 가동 안전을 위해 막아뒀던 mail 헬스체크를 되살린다. **배경**: #295가 `spring-boot-starter-mail`의 `MailHealthIndicator`가 빈 SMTP host(SES 미가동)로 연결을 시도해 health DOWN→ALB unhealthy→ECS 무한 replace→배포 타임아웃(T-009 계열)을 유발하자 `management.health.mail.enabled=false`로 껐던 것. 이제 #312로 SMTP가 실가동(자격증명·연결·발송 검증 완료)이라 제외를 풀어도 health가 UP으로 뜬다. **변경**: `application-prod.properties`에서 `management.health.mail.enabled=false`(+제외 주석)를 제거하고 history 주석만 남김 — 헬스체크가 기본 활성으로 복귀해, 이제 mail DOWN은 가짜가 아니라 진짜 SMTP 장애를 잡는 신호다(되려 막혀야 할 배포를 잡아줌). 운영 프로필 전용이라 단위테스트로 못 잡음(test는 H2·default라 mail 헬스 비활성) → **재배포 CD GREEN + `/actuator/health` mail UP이 검증 게이트**(#295 핫픽스의 정상화·대칭). `.java` 무변경. (PR #313) |
| 2026-06-12 | **admin에 이메일 인증 상태(emailVerified) 노출** — 운영자가 admin 화면에서 가입 메타는 봤지만 이메일 인증 여부는 못 봐 인증 독려(재발송) 판단 근거가 없던 갭을 닫음. 데이터(`User.emailVerified`)는 이미 존재 — **보여주기만**(Flyway·엔티티·Repository 무변경). `onboarded` 선례를 복제해 **목록 + 상세** 두 표면에 노출(통계 카운트는 범위 밖 — 후속 수요 시 `onboardedUsers`처럼). **변경**: `AdminUserRow`·`AdminUserDetail` record에 `boolean emailVerified` 추가(`from()`·`assemble()`에서 `u.isEmailVerified()` 매핑) / `admin-users.html` 표에 '인증' 컬럼(email 뒤, 미인증만 옅은 빨강 배지 `#ffcdd2`로 강조 — 후속 독려 대상 식별) / `admin-user-detail.html` 계정 KV에 '이메일 인증' 행(`인증 완료/미인증`). TDD RED→GREEN: `AdminUserServiceTest`에 목록·상세 각각 **인증·미인증 두 픽스처**를 만들어(`verifyEmail()` 호출, setter 없음) 각 `emailVerified` boolean이 정확히 실리는지 단언(N-055 정신 — 미인증 누락·오표시 가드, record 인자 순서 회귀 포착). 뷰는 단순 `th:text` 바인딩이라 UI 문자열 단언은 누름(브리틀 회피). 전체 그린·회귀 0. 설계 핸드오프 `claude-docs/plans/2026-06-12-admin-email-verified-display.md`. (PR #314) |
| 2026-06-12 | **도메인 TLD 이전 마무리 — `.click`→`.app` 301 전환·동의화면·자동갱신 (cleanup)** — 이메일 점등 선결로 시작한 `.app` 이전(#310/#311)의 마지막 cleanup. 콘솔 작업이라 코드 0, 문서만 갱신. **① ALB 443 301**: 호스트 헤더 `booktimer.click`·`www.booktimer.click` → `https://booktimer.app` 301(경로·쿼리 `/#{path}`·`#{query}` 보존, 우선순위 1, 기본값은 `.app` forward 유지). curl 검증 — apex·www·경로쿼리 전부 301, `.app`은 200 대조. **② Google OAuth 동의화면**: 홈페이지·개인정보처리방침 URL `.click`→`.app`, 승인도메인 `.click`·`.app` 병행 유지(redirect URI 병행 기간이라 아직 안 뺌). **③ 자동갱신**(Route53): `.app` ON(만료 2027-06-11)/`.click` OFF(만료 2027-06-02 자연 폐기). **잔여**(plan.md에 기록): ⓐ 2027-06 `.click` 만료 직전 ALB 443 기본 SSL 인증서 `.click`→`.app` 교체(현재 기본 인증서가 `.click`이라 `.click` TLS→301이 살아있고 섣불리 바꾸면 깨짐 — 만료 임박 시) ⓑ (선택) Search Console `.app` 등록 ⓒ `.click` 폐기 시 승인도메인·redirect URI에서 `.click` 제거. plan.md §도메인 TLD 이전 "완료 ✅"로 갱신. (PR #315) |
| 2026-06-12 | **독서 도구 제휴 아이디어 캡처 (plan.md 백로그)** — 책뿐 아니라 독서 보조 용품(독서대·책받침대·책갈피·북커버·리딩 라이트 등)을 제휴 검색 링크로 노출·홍보하는 아이디어를 §비즈니스 모델/수익화 "기존 제휴 축 확장"에 신설(eBook 제휴 형제, `🔧 독서 도구 제휴`, 아이디어 📝·우선순위 낮음·기록만). 핵심: `CoupangLinkBuilder.buildSearchLink(template,query,trackingCode)`가 임의 검색어를 받게 일반화돼 있어 인프라 8할(추적코드·공정위 고지·`coupangEnabled`·클릭추적) 재사용 가능 → 새로 짤 건 노출 화면 + 큐레이션 목록 정도. thesis 정합(마찰 낮추는 도구지만 부수 컨텐츠)·경계(전면 배너 ✗/맥락 있는 작은 섹션 ○, 제휴 중개 한정·직접 커머스 금지)·우선순위는 retention(이메일 넛지)·이메일 점등 뒤. 코드 무변경(문서만). (PR #316) |
| 2026-06-12 | **SPF·DMARC 딜리버러빌리티 보강 (이메일 1단계 — transactional 수신함 안착)** — #312 점등 후 발신 도메인이 기본 MAIL FROM(`amazonses.com`)이라 DKIM 단독 정렬·DMARC 미게시였던 갭을 닫음. **콘솔·DNS 작업이라 코드 0, 문서만.** **① 사용자 지정 MAIL FROM**: SES에서 `booktimer.app`의 MAIL FROM을 `mail.booktimer.app`으로(MX 실패 시 `USE_DEFAULT_VALUE` 폴백 — 전파 전 발송 안 끊김). **② DNS(Route53 자동 게시)**: `mail.booktimer.app` MX(`feedback-smtp.ap-northeast-2.amazonses.com`)·SPF(`v=spf1 include:amazonses.com ~all`) + `_dmarc.booktimer.app`(`v=DMARC1; p=none;` — 모니터링만·정상 메일 거부 0·rua 생략) → DKIM 단독→**SPF·DKIM 이중 정렬**. **검증(게이트)**: 8.8.8.8로 3레코드 전파 확인 + Gmail·호서대(`vision.hoseo.edu`=Google Workspace) 실발송 `Authentication-Results` 셋 다 `pass`(`smtp.mailfrom=…@mail.booktimer.app`·`header.i=@booktimer.app`·dmarc)·`Return-Path:<…@mail.booktimer.app>`(custom MAIL FROM 적용 증거)·받은편지함 안착. prod-only·앱 코드 무변경이라 단위테스트 불가 → DNS 실측+실발송 헤더가 게이트(SES "Custom MAIL FROM Setup SUCCESS" Health 알림도 수신). 잔여: 프로덕션 액세스(샌드박스 해제) 대기·DMARC `p=none`→`quarantine` 상향은 정렬 안정 후. (PR #317) |
| 2026-06-12 | **법무 9박스 명문화 + 전송자 연락처 보완 (이메일 2단계 넛지 §50 컴플라이언스 마감)** — 마케팅 넛지 점등(`BOOKTIMER_NUDGE_ENABLED=true`)의 법적 선결 "법무 9박스"가 약칭만 있고(`application.properties`·스케줄러 주석에 4개 예시) 명시 점검표가 없던 것을 plan.md 2단계에 **§50 9개 의무 × 충족 증거 감사표**로 명문화. 감사 결과 코드는 사실상 충족(opt-in 기본 OFF·(광고)접두·수신거부 one-click·야간 KST10시 회피·동의증빙 보존)이고 **실질 갭은 #4 전송자 연락처 하나** — 시행령은 명칭+연락처를 요구하나 본문엔 "발신: BookTimer"(명칭)만 있어 `RetentionNudgeService.buildBody`에 문의 이메일(`tlatldhs0504@naver.com`, 처리방침 §7과 동일) 한 줄 추가. #9 2년 재확인은 동의 2년 후 도래라 운영 항목(점등 비차단). **점등 runbook**(전제 ①SES 샌드박스 해제 케이스 178123901400162 ②9박스 → task def `BOOKTIMER_NUDGE_ENABLED=true` 주입 절차)도 박음 — 단 **점등은 샌드박스 해제 후**라 이번엔 실행 안 함. TDD RED→GREEN: `RetentionNudgeServiceTest.sendNudges_bodyHasSenderContact`(본문 연락처 포함 단언, §50 ④) 1 Red→6 Green·기존 5 회귀 0. 넛지 `enabled=false`라 실발송 0. (PR #318) |
| 2026-06-12 | **쿠팡 파트너스 환경변수 자리 추가 (배포 설정 — 점등 준비)** — #309에서 쿠팡 링크 코드·뷰·테스트를 머지한 뒤, 가입(개인)·추적코드(`AF…`) 발급 완료에 맞춰 운영 환경변수 자리를 깐다(계획 §3.7 "가입 후 별도 작업"). **실측**: 링크 생성 도구 도착 URL에서 추적 형식 확인 — 추적 키는 **`lptag` 파라미터**(쿠팡 URL에 `&channel=user&lptag=AF…` 부착 시 수익 귀속, 단축 링크 `link.coupang.com/a/…`는 편의 껍데기·실제 귀속은 도착 URL의 `lptag`). 우리 코드의 "검색 URL + `{trackingCode}`를 `lptag`에 런타임 치환"과 일치 확인. **변경**: `deploy/task-definition.json` secrets에 `BOOKTIMER_COUPANG_TRACKING_CODE`·`BOOKTIMER_COUPANG_SEARCH_URL_TEMPLATE` 두 자리(알라딘 ttb-key와 동일 SSM `valueFrom` 패턴). **회귀 0**: 자리만 깔 뿐, SSM 값 미주입이면 `CoupangLinkBuilder`가 not-configured 기본값으로 비활성(`coupangEnabled=false`)이라 쿠팡 버튼·고지문구 숨김. `.java` 무변경(JSON만)이라 테스트 게이트 자동 스킵. **남은 점등(사용자)**: SSM에 `COUPANG_TRACKING_CODE=AF…`·`COUPANG_SEARCH_URL_TEMPLATE` 주입 + 재배포 → 버튼·고지 노출 → 도착 검색 URL에 `lptag` 확인. 잔여: ISBN 검색 품질 재평가·API 딥링크 키 15만원 실적 게이트·`lptag` 직접 부착 약관 범위 확인. (PR #319) |
| 2026-06-12 | **랜딩 히어로 카피 줄바꿈 — 첫인상 가독성** — 비로그인 첫 화면(`landing.html`)의 문구가 화면 폭에 따라 의미와 무관하게 끊겨("…독서 / 습관.") 가독성이 떨어지던 것을, 문장·의미 단위 `<br>`로 의도적 줄바꿈. ① 히어로 태그라인 `하루 최소 시간만.` / `끊기지 않는 독서 습관.` 두 줄 ② 리드 단락 세 문장을 줄 단위로 분리(각 문장이 새 줄에서 시작) ③ 하단 CTA `오늘 딱 한 페이지부터.` / `작게 시작해 보세요.` 분리. CSS(`word-break: keep-all`)는 그대로라 더 좁은 폭에선 단어 단위로만 추가 접힘. 기능 목록·동작 단계는 이미 제목(`strong` block)·설명이 줄로 나뉘어 그대로 둠. 순수 뷰(텍스트 줄바꿈) 변경이라 단위테스트 무의미 — 기존 `LandingPageTest` `containsString` 단언은 텍스트 토큰 유지로 회귀 0, 시각 확인으로 갈음. `.java` 무변경. (PR #320) |
| 2026-06-12 | **구매처 선택 토글 통합 — '구매' 클릭 시 알라딘·쿠팡 펼침** — 점등(#319)으로 알라딘·쿠팡 '구매' 버튼이 한 행에 둘 나란히 뜨던 것을, 구매처가 **2개일 때만 '구매' 토글**(`<details>` 순수 CSS·JS 0)로 묶어 펼쳐 고르게 함. **1개뿐이면 단일 버튼 유지**(불필요한 클릭 제거). **동적 분기**: 조건(`#strings.isEmpty(purchaseLink)`·`coupangEnabled`)이 매 렌더 그 책 데이터로 평가 → 나중에 구매처가 2개로 늘면 자동 토글 전환(코드 변경 0). **변경**: 뷰 3곳(books/book-detail/profile) 두 `<a>`→3분기 + `app.css` `.buy-menu`(기존 `manual-add` `<details>` 패턴 재사용). 백엔드(엔드포인트·집계·추적) 무변경 — 마크업만. TDD RED→GREEN: `CoupangBuyControllerTest`에 "2개면 토글+두 링크 / 1개뿐이면 단일 버튼" 경계 2개 추가(첫 RED 확인 → web 패키지 전체 그린·회귀 0). preview 정적 mock으로 세 분기 시각·펼침(접힘→펼침) 검증. **함정**: Thymeleaf `and`/`or`에 String을 직접 쓰면 단독 `th:if`의 truthy 규칙이 안 통해 뷰 500 → `#strings.isEmpty()`로 명시적 boolean화. (PR #321) |
| 2026-06-12 | **홍보 전 서버 용량 게이트 — ECS 오토스케일링 + 부하 테스트 (1차 스코프)** — 단일 Fargate 태스크(desired=1)가 CPU 100%면 그대로 장애·자동 확장 없던 것을, 홍보(이메일 넛지 점등 임박) 전 필수 게이트로 보강. **워크플로 IaC**: `.github/workflows/autoscaling-config.yml`(workflow_dispatch, zero-downtime-config 패턴 복제) — `register-scalable-target`(ecs:service:DesiredCount, **Min=2/Max=4** = 단일 장애점 제거+자동 확장) + `put-scaling-policy`(TargetTracking, **ECSServiceAverageCPUUtilization=70**) + before/after describe readback. **부하 테스트**: `load-test/booktimer-load.js`(k6) — GET /login에서 CSRF 추출 → POST /login → 대시보드·기록 GET 읽기 루프(VU당 1회 로그인·이후 읽기; 가입은 DB 오염이라 제외), ramping-vus 1→60·p95<1.5s·실패율<5% 임계. **deploy-aws.md §12-1b** 신설: 절차 + **OIDC 역할 권한 추가 스니펫**(`application-autoscaling:*`·`cloudwatch:*Alarm*` — target-tracking이 CloudWatch 알람 자동 생성해 `ecs:UpdateService`만으론 부족). ⚠️ 코드 로직(`.java`) 0 → 커밋 테스트 게이트 자동 스킵, 검증은 실측 게이트(워크플로 describe readback + k6 scale-out/latency)로 대체. **남은 사용자 작업**: CloudShell 권한 추가 → 워크플로 실행 → k6 부하 실측. 세션 Redis·RDS 업그레이드는 실측 후 후속. (PR #322) |
| 2026-06-12 | **구매 토글 펼침 — 흐름 분리 오버레이로 형제 안 흔들리게 (UX 다듬기)** — #321 구매처 토글(`<details>`)을 펼치면 알라딘·쿠팡 목록이 일반 흐름에 쌓여 `.buy-menu` 박스가 세로로 커지고, 부모 `.book-actions`가 `align-items:center` flex라 "구매" summary는 위로·형제 "삭제"는 중앙 높이로 밀려 부자연스럽던 것을 수정. **원인**: 펼친 목록이 흐름 안에 있어 박스 높이가 증가 → flex 형제 재정렬. **해법**: `.buy-menu-items`를 `position:absolute; top:100%`로 흐름에서 빼내 "구매" 버튼 바로 아래에 드롭다운으로 띄움(+`.buy-menu{position:relative}` 기준점, `z-index:10`·흰 배경·그림자·`min-width:max-content`). 펼쳐도 `.buy-menu` 박스는 summary 높이(34px) 그대로 유지 → 형제 버튼 위치 고정. `app.css` 두 규칙만 변경, 템플릿·서버 무변경. 순수 CSS라 단위테스트 무의미 — preview 정적 mock에 실제 `app.css` 링크해 inspect 기하로 검증(펼침 시 `.buy-menu` 높이=summary만·items position=absolute 확인). (PR #323) |
| 2026-06-12 | **오토스케일링 service-linked role 함정 문서화 (PR #322 후속)** — #322 오토스케일링 워크플로를 사용자가 첫 실행하자 `register-scalable-target`이 `ValidationException: missing iam:CreateServiceLinkedRole`로 실패한 실사례를 문서에 반영. AWS가 오토스케일링 첫 점등 시 service-linked role(`AWSServiceRoleForApplicationAutoScaling_ECSService`)을 자동 생성하려는데 OIDC 역할에 그 권한이 없어 거부된 것(AccessDenied 아닌 ValidationException이라 헷갈림). **해결=CloudShell에서 `aws iam create-service-linked-role --aws-service-name ecs.application-autoscaling.amazonaws.com` 직접 1회 생성**(워크플로 역할에 IAM 권한 더하기보다 최소권한 — 이후 존재하니 Re-run 통과). troubleshooting **T-045** 신설(목차+본문+누적표) + deploy-aws.md §12-1b를 "직접 생성 우선"으로 보강(기존 "권한 추가" 약술 대체). 문서만(`.java`·코드 0). (PR #324) |
| 2026-06-12 | **오토스케일링 적용 완료 반영 (plan.md 상태·핸드오프 갱신)** — #322/#324로 ECS 오토스케일링(Min2/Max4/CPU70 target-tracking)이 운영에 **실제 적용·검증**된 것을 plan.md §홍보 전 선수과정 체크리스트에 반영: ① 오토스케일링·② desired=2 `[x]` 완료(적용 함정 T-045 한 줄) / ④ 부하 테스트는 스크립트 준비됨·**실행만 남음**(선택·미완, 실행 명령 명시) / ③ Redis·⑤ RDS는 ④ 실측 결과에 종속(보류, N-064). "🔧 준비" 블록을 "✅ 적용 완료 + 🧭 남은 선택 핸드오프"로 다시 써 다음 세션이 이어받게. 문서만. (PR #325) |
| 2026-06-12 | **책 검색에 '출판사' 기준 추가** — 알라딘 도서 검색 기준이 제목/저자뿐이라 출판사로는 못 찾던 것을 확장(plan §실사용 후속 후보 중 하나). `BookSearchType`에 `PUBLISHER("출판사","Publisher")` 한 값 추가 → 검색 기준 라디오는 `searchTypes=BookSearchType.values()`+`th:each`라 **컨트롤러·템플릿 0 변경**으로 자동 노출, `from()` 폴백·결과 행 출판사 표시(`book-pub`)도 기존대로. 핵심 구현은 `BookService.filterToSearchType` 후필터(알라딘 `QueryType`이 문서와 달리 다른 필드를 섞어 반환하는 걸 방어)의 `(type==AUTHOR)?author:title` 삼항을 `switch`로 확장해 PUBLISHER면 `publisher` 필드로 거르게 함. TDD RED→GREEN: `BookServiceTest`에 "출판사 매칭만 남김"·"publisher=null 결과 제외(N-055 미완성 메타 누수 가드)" 2개 + `BookSearchTypeTest` 파싱·매핑 단언 → enum만 추가한 상태로 후필터 2개 Red 확인 → switch 수정 후 Green(회귀 0). *검색 기준*만 — 내 책장 목록을 출판사로 거르는 필터는 범위 밖. (PR #326) |
| 2026-06-12 | **내 책장에 '공개여부' 필터 추가** — 내 책장 필터가 상태(읽고싶음/읽는중/완독) 1차원뿐이라 공개/비공개로는 못 거르던 것을 확장(plan §실사용 후속 후보). 상태 × 공개여부 **직교 2차원 AND 필터**. `BookController.books`에 `visibility` 파라미터 + `parseVisibility`(parseStatus 쌍둥이, 잘못된 값→null 폴백) + 기존 status와 동일한 **메모리 stream 필터** 체인(myBooks 전체는 소유판정·인기집계에 이미 쓰여 재사용 → Repository·서비스 무변경). `books.html`엔 상태 칩 줄 아래 **🌍공개/🔒비공개 칩 줄** 추가, 두 칩 줄이 서로의 현재 차원값을 링크에 실어 **상호 보존**(한 차원 클릭이 다른 차원을 안 풂)·htmx 부분 swap(shelfPanel) 유지·빈 메시지 "이 조건의 책이 없습니다"로 확장. CSS는 기존 `.shelf-filter` pill 재사용(두 줄 14px 간격, 추가 0). TDD RED→GREEN: `BookControllerTest`에 PUBLIC 단일·PRIVATE 양방향·status×visibility 교차 3개 → visFilter 미구현으로 3 Red 확인 → 컨트롤러 구현 후 Green, 전체 회귀 0. + 부산물로 troubleshooting **T-046**(MockMvc `model().attribute(name, nullValue())`가 속성 부재여도 통과 → 폴백은 nullValue() 말고 실제 반대값으로 RED 단언) 신설. (PR #327) |
| 2026-06-12 | **내 책장 공개여부 필터를 세그먼티드 토글로 — 서브 필터 위계 (UX 다듬기)** — #327이 공개여부 필터를 상태와 똑같은 `.shelf-filter` 칩 줄로 달아 두 줄이 동급으로 보이고 '전체'가 중복되던 것을, 공개여부가 상태의 **하위(서브) 차원**임이 한눈에 드러나게 재디자인. **해법**: 각 책 행의 공개 토글(`.vis-toggle`: 연한 트랙 알약 + 흰 knob) **비주얼 언어를 빌린** 세그먼티드 토글 `.vis-filter`(전체/🌍공개/🔒비공개 한 묶음, active=흰 알약, 우측 정렬)로 분리 — 칩(채움)과 형태가 달라 서브로 읽히고 '전체' 중복도 해소(토글 첫 칸=해제). **동작 불변**: `<a>`의 `th:href`/`hx-get`/`th:classappend`·htmx 타깃(`#shelf-panel`)·상태↔공개여부 상호 보존 그대로 — `books.html` nav 래퍼 교체 + `app.css` `.vis-filter` 신설(상태 `.shelf-filter`와 셀렉터 분리)뿐, 백엔드 0줄. 순수 마크업·CSS라 새 단위테스트 무의미 → 동작은 기존 `BookControllerTest`(PUBLIC/PRIVATE·status×visibility 교차) 3개가 회귀 가드(GREEN 재확인), 시각은 승인 목업 기준. (PR #328) |
| 2026-06-12 | **운영 도서 검색이 전부 0건이던 버그 수정 — 알라딘 엔드포인트 http→https** — 운영(`booktimer.app`)에서 제목·저자·출판사 모든 기준 검색이 "검색 결과가 없습니다"만 뜨던 장애. 원인: 알라딘이 앞단 **CloudFront로 `http`→`https` 301 리다이렉트**를 강제하는데 앱이 `http://`로 호출하자 `RestClient`가 301을 안 따라가 **응답 본문이 리다이렉트 HTML(`<html>…`)** → `parse()`가 JSON으로 읽다 `JsonParseException('<')` → 빈 결과 → 0건. 브라우저는 301을 자동 추적해 정상이라 "내 PC는 되고 서버만 0건"으로 보였고, 코드·키·#326과 무관한 **알라딘 측 변경**(시기만 겹침). 진단: CloudWatch(`AladinBookSearchClient` JsonParseException) + CloudShell `curl -D -`로 `HTTP 301 Location: https://`·CloudFront HTML 본문 확인. **해법**: `AladinBookSearchClient`의 `ENDPOINT`·`LOOKUP_ENDPOINT`를 `https://`로(301 제거 + 평문→TLS). TDD RED→GREEN: `buildSearchUrl`/`buildLookupUrl`이 `https://`로 시작 단언(http라 Red→https로 Green). 더해 검색 ON 경로(`searchEnabled=true`)가 통합테스트 사각지대였음을 확인해 `BookSearchOnRenderingTest`(검색결과·추가폼·출판사 라디오 렌더)로 메움. 회귀 0. (PR #329) |
| 2026-06-12 | **영미권(글로벌) 진출 백로그 등재 + 헤더 분리 학습노트(N-075)** — 영미권 홍보를 위한 검색·구매 소스 region 분리 아이디어를 논의·정리(구현은 나중/백로그). 알라딘·쿠팡=한국 전용이라 영미권은 **Google Books 검색 + Amazon 구매**로 교체해야 한다는 결론. **plan.md**: 기존 §다국어(i18n)를 **§영미권(글로벌) 진출** 우산으로 흡수·승격 — 확정 결정(단일 앱 + 같은 도메인 언어전환 + Google Books·Amazon + i18n 후순위) 표 + 아키텍처 골자(`Market.from(Locale)`·`RoutingBookSearchClient`(@Primary)·`AmazonLinkBuilder`=쿠팡 복제) + PR 3분할 + 미결정 3개, 기존 i18n 메모는 4단계(PR3)로 보존. **learning-notes**: N-075 — "헤더로 지역 분리"는 라우팅만 푼다(Host=확실·Accept-Language/GeoIP=보조), 진짜 병목은 데이터 소스·제휴. 상세 설계 초안 `claude-docs/plans/2026-06-12-en-region-split.md`(.gitignore·로컬). 문서만(`.java` 0). (PR #330) |
| 2026-06-12 | **troubleshooting T-048 — gh squash 제목 함정** — #330 머지에서 PR 제목을 `N-074`→`N-075`로 정정했으나 `gh pr merge --squash`가 PR 제목 대신 브랜치 첫 커밋 메시지(N-074)를 squash subject로 써 main 커밋 제목이 어긋난 사례. 해결=`--subject`/`--body` 명시 또는 커밋 메시지 동기화(사후엔 main force push 금지라 불가). troubleshooting **T-048** 신설(목차+본문+누적표). 문서만(`.java` 0). (PR #331) |
| 2026-06-13 | **독서 정원 MVP 1단계(트랙 A) 출하** — 잔디를 게임화한 "수집형 정원"의 첫 실행분(plan §독서 정원). **타이머 할당량 기반 식물 수집의 읽기전용 MVP**. 핵심 설계 = **보유 식물을 저장하지 않고 독서 실적의 함수로 유도**(부채 모델 N-058·Lazy accrual N-001과 같은 철학) → 데이터 모델은 **식물 카탈로그 정적 시드 1장(V35·14종 🌱→🌳)뿐**, 보유 이력 테이블 없음. 보유 = "누적 목표 달성일 ≥ 식물 임계", 해금일·NEW(최근 7일)도 달성 날짜 정렬로 유도. 달성 판정은 **그날 목표를 채운 날**(잔디 level4·`GoalSchedule` 소급 방어 N-059 재사용, baseline 이전 가입 전 날 제외). 신규 패키지 `com.booktimer.garden`: `Plant`/`PlantRepository`/`PlantUnlockCalculator`(순수 계산)/`GardenView`/`PlantState`/`GardenService`. 화면 = **대시보드 잔디 카드 안 접힌 `<details>` 베타 패널**(순수 CSS 토글 JS 0, summary 화살표는 카드 우측 상단 absolute, 별도 `/garden` 페이지 없음) — 🚧 베타 배너 + 미니 정원(보유 이모지) + 도감(보유=이모지+이름/+NEW, 미보유=🔒+목표일) + 진척·다음 해금. fragment `fragments/garden.html :: panel`로 분리(후속 재사용). 잔디와 **공존**(잔디 무변경·기존 데이터 무변경 → 회귀 낮음). TDD RED→GREEN: `PlantUnlockCalculatorTest`(순수 13개 — 달성 경계 이상포함·소급 방어·baseline 컷오프·빈 카탈로그 누수 가드 N-055) + `GardenServiceTest`(H2 배선 4개 — 자체 시드 카탈로그로 prod 14종과 디커플) + `DashboardControllerTest` 정원 모델 배선 1개. 전체 회귀 0, FlywayMigrationTest로 V35 이모지 시드 검증. (PR #332) |
| 2026-06-13 | **독서 정원 베타 토글이 페이지 우측 최상단으로 새던 버그 수정 — `.garden-host` positioning 기준 누락** — #332 정원 베타 토글(잔디 카드 안 접힌 `<details>`의 `summary.garden-toggle`)이 `position:absolute; top:18px; right:18px`인데 기준이 될 **positioned 조상이 없어**(잔디 카드에 `garden-host` 클래스만 붙고 정작 `.garden-host{position:relative}` 규칙은 누락, `.card`도 position 없음) absolute가 viewport(initial containing block)를 기준 삼아 **잔디 카드 우측 상단이 아니라 페이지 전체 우측 최상단으로 떠버리던** 버그. 원 설계 주석은 '카드 우측 상단에 absolute로 띄운다'였으나, 토글이 viewport로 날아간 탓에 실제 카드 배치를 못 본 채 머지된 것으로 보임. **해법**: `app.css`에 `.garden-host { position: relative; }` 한 줄 신설 → absolute 기준이 잔디 카드로 확정돼 의도대로 카드 우측 상단(`top/right:18px`)에 붙음. HTML(`class="card garden-host"`)은 이미 클래스가 있어 무변경. 순수 CSS 위치 수정이라 단위테스트 무의미(앱 로직·DB·Flyway·마크업 0) → 시각 검증 게이트. (PR #333) |
| 2026-06-13 | **독서 정원 베타 토글을 제목 옆(좌측)으로 이동 — 연속일수 이모지와 겹침 해소** — #333이 토글을 카드 우측 상단(`top/right:18px`)에 고정했는데 **거기가 연속일수 뱃지(`.grass-streak`)가 이미 앉은 자리**라 토글(정원 ▸)과 streak 이모지가 겹쳐 보이던 문제(#333은 '페이지 우측 최상단으로 샘'은 고쳤으나 '카드 우측'=streak 자리라 겹침이 남음). **해법**: 토글 absolute 기준을 `right:18px`→`left:24px`(카드 콘텐츠 좌단=제목 시작점)로 옮겨 제목("독서 잔디") 옆에 두고, 토글이 absolute(흐름 밖)라 헤더가 자리를 안 비우므로 `.garden-host .grass-head h2`에 `padding-left:3.4rem`로 토글 폭만큼 들여써 제목 글자와 안 겹치게 함(`.garden-host` 스코프라 정원 없는 `/history` 잔디 헤더 무영향). 정원 패널은 `<details>`가 흐름 끝(잔디 뒤)에 그대로라 **'잔디 아래 펼침' 동작 유지**(원래 검증된 동작 — display:contents+order 재배치는 이 프리뷰 엔진에서 불안정해 폐기). `app.css`(토글 위치·h2 들여쓰기·주석)+`dashboard.html` 주석 2줄뿐, 앱 로직·DB·Flyway·마크업 0 → 시각 검증 게이트. **검증**: `.preview` 정적 하니스+실 app.css로 데스크탑(1280)·모바일(375) geometry 측정 — 닫힘: 토글이 제목 옆(텍스트와 13px 간격)·streak과 안 겹침·세로 중앙 정렬(vAlign 0); 열림: 패널 잔디 아래 전체폭(grass-summary와 좌우 정렬). 기존 `DashboardControllerTest`(정원 렌더) GREEN 유지. (PR #334) |
| 2026-06-13 | **독서 정원 트랙 A 수집 확장 — 장르 매핑 식물(베타)** — "무슨 책을 읽든 같은 14종"이던 시간축의 동기 부재를 풀기 위해 **장르 수집축**을 추가(매핑 3축 중 **장르** 먼저, plan §독서 정원). 핵심 = **별도 카탈로그 `genre_plant`(V36·12장르 + 폴백 들꽃 1종)** 신설 → 시간축 `plant`/`PlantUnlockCalculator`/V35는 **완전 무변경**(회귀 0, 설계 옵션 B — 두 축은 해금 의미가 근본 다름: 날짜 누적 vs 장르 완독). 해금 = **그 장르 책 1권 완독(`FINISHED`)**(완독만 집계 → 파밍 방지·책BTI와 신호 일치, 사용자 결정). 장르 대분류는 `ReadingProfileAggregator.primaryGenre` 재사용(garden→personality 의존 신설). 보유 장르가 시드 어디에도 안 맞으면 **폴백 식물(들꽃)** 보유 → "읽었는데 0개" 허무 방지. category null(수동 등록) 완독책은 제외(N-055). 보유는 저장 안 하고 유도(부채 모델 N-058, 시간축과 동일 철학). 신규 `GenrePlant`/`GenrePlantRepository`/`GenrePlantState`/`GenreUnlockCalculator`(순수 계산) + `GardenService`(`BookRepository` 주입)·`GardenView`(+genrePlants/ownedGenreCount/totalGenreCount) 확장. 화면 = 같은 베타 패널에 "수집 식물(장르)" 섹션 추가(보유=이모지+장르명 / 미보유=🔒+장르명, 기존 `.garden-grid` CSS 재사용). 이모지는 시간축 14종과 안 겹치는 풀(🪷🌴🍀🌾🌰🍄🌺🪻🏵️🍇💐🍓🍃). TDD RED→GREEN: `GenreUnlockCalculatorTest`(순수 8개 — 같은 장르 흡수·다장르·null/빈 category 제외 N-055·빈 입력·시드 매칭·폴백 발동·시드+미지 동시) + `GardenServiceTest` 장르 5개(H2 배선 — 완독만·중복 흡수·null 누수 0·null-state·폴백) + `DashboardControllerTest` genrePlants 슬롯 단언. 전체 회귀 0. 시간축·잔디와 **공존**. (PR #336) |
| 2026-06-13 | **독서 정원 트랙 B 메커닉 — 숨은 레시피 발견(베타)** — 완독+읽고싶음 책의 **조합**을 발견하면 식물을 얻는 트랙 B 메커닉 출하(plan §독서 정원 「트랙 B」). 트랙 A와 **근본적으로 다른 두 축**: ① **발견을 저장**(`user_discovered_plant`·V37) — 발견은 "처음 만족된 순간" 이벤트라 박아야 하고(NEW·토스트) 책장이 바뀌어도 보유 유지(트랙 A는 저장 안 하고 유도). ② **조건 숨김** — 어떤 조합인지 안내 안 하고 미발견은 "???" 미스터리 슬롯으로만 노출(발견 자체가 재미, §2.5). **메커닉**: 조회(`GardenService.view`, 대시보드 진입) 시 책장 스냅샷으로 레시피 평가 → 새 발견 저장. 조회가 쓰기를 유발하는 패턴이라 `view`를 **읽기-쓰기 트랜잭션**으로 승격, 중복 발견은 `uk(user_id,plant_code)`+`existsByUserAndPlantCode`로 멱등(동시성 안전망). **파밍 가드(§2.4)**: 모든 레시피 **완독 ≥1 필수** — 읽고싶음(저비용 행동)만으론 해금 불가. 레시피 정의는 **코드 상수**(`RecipeDefinition`, 임의 술어라 SQL화 부자연), DB(`recipe_plant`/V37)엔 결과 식물 표시 메타(이름·이모지·스토리)만 시드(이모지는 트랙 A 시간축·장르축과 비충돌). 신규 `RecipePlant`/`RecipePlantRepository`/`RecipeDefinition`/`RecipeEvaluator`(순수·전수 TDD)/`ShelfSnapshot`/`UserDiscoveredPlant`/`UserDiscoveredPlantRepository`/`DiscoveredPlantState` + `GardenService`·`GardenView` 확장 + 같은 베타 패널에 "숨은 레시피" 섹션(발견 토스트·??? 슬롯). TDD RED→GREEN: `RecipeEvaluatorTest`(순수 9개 — 양쪽 조건·한쪽만·완독0 파밍가드·null 제외 N-055·장르 흡수·작가축·복수 만족·ALL 기본) + `GardenServiceRecipeTest`(H2 배선 6개 — 첫 발견 저장·멱등·**책장 바뀌어도 영속**·사용자 격리·미스터리 슬롯·null-state) + `DashboardControllerTest` 트랙 B 배선 단언. **⚠️ 레시피 8종은 베타 예시** — 메커닉 검증용, 식물 카탈로그를 직접 커스텀(관리자 입력)으로 관리하는 단계에서 식물·레시피 함께 교체(사용자 결정 2026-06-13). 트랙 A·잔디와 **공존**. 전체 회귀 0. (PR #337) |
| 2026-06-13 | **GA4 방문 통계 도입 — config-gated, AdSense 패턴 복제** — "접근 시작조차 안 하나(유통) vs 들어와 바로 나가나(랜딩)"를 분리 측정하려 Google Analytics 4를 심음(획득/첫 진입 가시성, CloudWatch raw 로그로는 사용자 단위 행동 구분 불가). AdSense가 푼 "공유 레이아웃 없는 head에 외부 스크립트를 config-gated로" 패턴을 그대로 복제: `AnalyticsProperties`(ga-id 비면 isEnabled=false)+`AnalyticsModelAdvice`(전역 @ModelAttribute)+`analytics.html :: gtag` fragment. head 보유 33개 템플릿 전수에 `~{analytics :: gtag}` 한 줄(charset 다음, ads.html·fragments 제외, error.html 포함). privacy.html에 GA4·쿠키·Google 전송·옵트아웃 고지. `GA_MEASUREMENT_ID` 미주입 시 기존 동작 완전 불변(전부 추가·config-gated). TDD RED->GREEN: `AnalyticsPropertiesTest`(경계 3) + `AnalyticsRenderingTest`(활성: 랜딩에 gtag.js+ID) + `AnalyticsDisabledRenderingTest`(비활성 안전 불변식). 전체 회귀 0. (PR #338) |
| 2026-06-13 | **/personality 500(IllegalStateException) 핫픽스 — CSRF 토큰 선확정** — #338(GA4) head 스크립트가 personality.html 응답 버퍼를 임계 너머로 밀어, 맨 아래 '다시 분석' 폼(`th:action`) 렌더 시 CSRF 토큰이 세션을 생성(`getSession(true)`)하려다 "response already committed"로 깨지던 회귀(`PersonalityControllerTest` 2개 red, main red). 근본 원인은 GA4가 아니라 **CSRF 세션 생성이 렌더(버퍼 커밋) 이후로 밀린 것** — GA4 몇백 바이트가 방아쇠일 뿐. `DashboardController`가 이미 쓰던 방어(렌더 전 `CsrfToken.getToken()`으로 세션 선확정)를 `PersonalityController` GET에 적용해 세션 생성을 응답 커밋 전으로 당김(컨트롤러 1곳·11줄). Spring Session(`SessionRepositoryFilter`) 경로라 테스트의 `.session(MockHttpSession)` 우회가 불완전했던 것도 함께 해소(세션 없이 GET해도 통과 = 진짜 방어). #337+#338의 semantic 충돌(텍스트 충돌 아님)을 #339 머지 준비 중 발견·분리. RED→GREEN, 전체 회귀 0. (PR #340) |
| 2026-06-13 | **독서 정원 도감 전용 페이지(`/garden`) 출하 — 대시보드는 요약+링크로 경량화** — 정원 3트랙 메커닉(시간·장르·레시피)은 다 베타 출하됐으나 담는 그릇이 대시보드 잔디 카드 패널 하나라 수집 재미가 시각적으로 안 살고 패널이 4섹션으로 길어 대시보드가 비대하던 것을, **별도 `/garden` 전용 페이지(도감 본진)** 로 키우고 대시보드 카드는 **요약+링크로 경량화**(plan §독서 정원). ① 신규 `web.GardenController`(`@GetMapping("/garden")`) — 대시보드와 **같은 `GardenService.view` 재사용**(서비스·뷰모델·계산기·엔티티·마이그레이션 전부 무변경 → 회귀 0), permitAll 목록에 없어 default-deny가 자동 인증 보호. ② 신규 `templates/garden.html` — brand 헤더 + 전체 진척 한 줄 + **축 탭(전체/시간/장르/숨은 레시피) + 보유/미보유 필터**(데이터가 이미 다 내려와 있어 Alpine `x-show` 클라이언트 토글, 서버 왕복 0; no-JS면 x-show 무시돼 전부 보이는 **점진 향상** 폴백) + 3축 그리드 + 대시보드 링크. ③ fragment `fragments/garden.html :: panel`→`:: summary` 경량화 — 대시보드 잔디 카드엔 미니정원 + 3축 진척 한 줄 + "🌱 정원 도감 →" 링크만 남기고 전체 도감 그리드는 페이지로 이전, 잔디 토글 인프라(`.garden-host`/`.garden-toggle`)·발견 토스트는 그대로 유지(**잔디 회귀 0**), `dashboard.html`은 fragment 참조 한 줄만 교체. ④ **잔디 흡수(53×7 히트맵 제거)는 이번 범위 밖** — 정원 완성도가 더 오른 뒤 후속(plan 도입 순서와 정렬). ⑤ 클릭 리치 상세는 `Plant`/`GenrePlant`에 설명 컬럼이 없어 1차 생략(hover title 대체) — 식물 커스텀 카탈로그 단계에서 메타와 함께 보강. `app.css`에 `/garden` 탭·필터·축 스타일 + 요약 링크 스타일 추가(기존 `.garden-grid`/`.plant-cell` 재사용). TDD RED→GREEN: `GardenControllerTest`(MockMvc — 미인증→`/login` 보호·인증 200+뷰`garden`+모델 GardenView·현재 유저 해소 3개) Red(컨트롤러 미구현 404) 확인 → 컨트롤러+페이지 구현 후 Green, 탭·필터 등 순수 시각은 preview 게이트(서버 렌더는 MockMvc 200으로 검증). 전체 회귀 0. (PR #339) |
| 2026-06-13 | **GA4 측정 ID 운영 주입 — 통계 수집 개시** — #338에서 심은 config-gate를 실제로 켬: ECS `task-definition.json`의 `environment`에 `GA_MEASUREMENT_ID=G-JFVDPJ036E` 한 줄 추가. 측정 ID는 클라이언트 HTML에 그대로 노출되는 **공개 식별자**라 `secrets`(SSM) 아닌 평문 `environment`에 둠(OAuth client-secret과 구분 — 숨길 값이 아님). 재배포하면 `analytics.html :: gtag` 로더가 렌더돼 방문 통계 수집이 시작된다(GA4 속성 `BookTimerWeb`, 웹 스트림 https://booktimer.app). 인프라 설정만(`.java` 0 → 테스트 게이트 자동 skip). 후속(운영, 코드 밖): GA4 실시간 보고서로 수집 검증 → 내부 트래픽(본인 IP) 제외 필터. (PR #342) |
| 2026-06-13 | QueryDSL/동적 쿼리 도입 트리거를 설계문서·메모에 고정 — 현재 동적 쿼리(QueryDSL/Specification/Criteria) 0건이 의도적 회피가 아니라 (1) 주요 조회가 사용자 단위 소량 데이터라 전건 로딩+`stream().filter()`로 충분(예: `BookController` 책장의 status×visibility 필터를 메모리에서 거름) (2) 다중 필터를 YAGNI로 명시적 유예 (3) SNS 검색 절제(login_id Top20·email 검색 제외)의 귀결임을 확인. **admin 다중 필터(전체 사용자 스코프 + 조건 임의 조합)가 도입을 처음 검토할 지점**임을 `admin-data-lookup-design.md` §7 주석 + auto-memory(`querydsl-trigger`)에 2겹으로 박음(미래 세션이 그 작업 맥락에서 떠올리도록). 코드·테스트 무변경(문서 + repo 밖 메모). (PR #343) |
| 2026-06-14 | **독서 정원 작가·출판사 다양성 축 — 트랙 A 매핑 3축 완성** — 시간·장르축에 더해 매핑 마지막 축인 **작가·출판사 다양성**을 추가(plan §독서 정원 「매핑 3축」 완성). 해금 = **완독(`FINISHED`) 책의 서로 다른(distinct) 작가/출판사 수 ≥ 임계**(시간축처럼 누적 수 기반·완독만 집계해 파밍 방지, 부채 모델 N-058). 장르축이 라벨 1:1 매칭이었던 것과 달리 작가/출판사는 **열린 집합(수천)**이라 1:1 시드가 불가하고 표기도 미정규화라 "다양성 카운트"로 설계(사용자 결정). 작가·출판사가 **같은 카운트 메커닉**이라 한 카탈로그에 `kind`로 구분(`diversity_plant`/V38 — 작가 7종 임계 1·3·5·10·15·20·30 + 출판사 5종 1·3·5·8·12). `Book.author`/`publisher`는 적재 시 정규화 안 됨(category와 달리 `blankToNull` 없음)이라 `distinctCount`가 strip+빈/null 제외를 맡음(N-055 누수 가드). 시간축 `plant`·장르축 `genre_plant`·레시피축 `recipe_plant` 전부 **무변경**(회귀 0). 신규 `DiversityKind`/`DiversityPlant`/`DiversityPlantRepository`/`DiversityPlantState`/`DiversityUnlockCalculator`(순수 계산) + `GardenService`(`DiversityPlantRepository` 주입, 완독책 작가/출판사 distinct — 책장 추가 조회 0)·`GardenView`(+diversityPlants/owned·totalAuthorCount/owned·totalPublisherCount) 확장. 화면 = `/garden`에 "🖋️ 작가·출판사" 탭 + 작가/출판사 두 소그리드(`kind` 분리)·전체 진척 줄·대시보드 요약(묶음 표기)에 추가, 기존 `.garden-axis`/`.garden-grid`/`.plant-cell` 재사용(새 CSS 0). 이모지는 4개 축과 비충돌(작가=과일 🫐🍒🍑🍎🍐🍊🍍, 출판사=채소 🫑🥦🥬🍠🍆). TDD RED→GREEN: `DiversityUnlockCalculatorTest`(순수 8개 — distinct 흡수·null/빈/공백 제외 N-055·strip·빈 입력·임계 경계 N-1/N/N+1·kind 교차 없음·카운트 0) + `GardenServiceTest` 다양성 5개(H2 배선 — 완독만·중복 흡수·null 누수 0·null-state·작가출판사축 독립) + `GardenControllerTest` diversityPlants 슬롯 단언. 전체 회귀 0. 시각은 기존 정원 패턴 복제라 preview 게이트(코드·Thymeleaf 렌더 GREEN). (PR #344) |
| 2026-06-14 | **독서 정원 식물 배치(꾸미기) — 경량 SSR MVP** — "나만의 정원 꾸미기"(보유 식물을 캔버스에 배치)를 plan의 "프론트 전환 스코프에 묶음" 결정을 **뒤집어 경량 SSR로 선출하**(설계 `2026-06-13-garden-placement.md` §0). 근거: 배치 하나는 작은 JSON API + 좌표 스키마면 충분하고 그것들은 **SPA 전환 시 재활용**(재작업은 UI 레이어뿐 = "절반만 두 번"), 전환 트리거 미정이라 무기한 미루느니 핵심 재미를 일찍 준다. **데이터**: 신규 `garden_placement`(V39 — `user_id`+`axis`+`plant_code`+`cell_index`, 두 uk: 한 식물 한 번·한 셀 하나). 식물 `code`가 **축 간 비유니크**(각 카탈로그 테이블 안에서만 유니크)라 배치 키는 **(axis, code) 복합** — `PlacementAxis` enum{TIME,GENRE,DIVERSITY,RECIPE}. 도감 보유는 유도/저장이지만 배치는 사용자 의도라 별도 저장(생명주기 다름). **유령 방지**: `GardenLayoutService.layoutOf`가 저장된 배치 ∩ 현재 보유만 렌더(완독책 삭제 등으로 보유 잃은 식물은 화면에서 빠짐). **위조 방어**: `save`가 미보유 식물·격자 범위 밖·중복 셀/식물을 거부(보유 집합은 `GardenView.ownedPlants()` = 도감과 같은 한 소스 재사용). 신규 `GardenPlacement`/`GardenPlacementRepository`/`PlacementAxis`/`OwnedPlant`/`PlacedPlant`/`PlacementRequest`/`GardenLayoutService` + `GardenView.ownedPlants()` + `GardenController`(`POST /garden/layout` JSON·인증·CSRF, 검증 실패→400) + `garden.html` 상단 "🌱 내 정원" 캔버스(8×6 격자) + "✏️ 꾸미기" 편집 토글. **편집 UX = 탭-투-플레이스**(팔레트에서 고르고 칸 탭해 배치·놓인 식물 탭해 거둠 — 모바일 터치 안정·HTML5 drag 회피, 설계 §2.6 폴백 채택). 보기 모드는 서버 SSR(no-JS 폴백), 편집은 Alpine. 저장 성공 시 새로고침으로 SSR 캔버스 최신화. TDD RED→GREEN: `GardenLayoutServiceTest`(H2 8개 — happy·미보유 거부·axis 분별(같은 code 다른 축)·격자 범위·중복 셀/식물·유령 교집합(보유 박탈)·IDOR·교체 저장) + `GardenControllerTest`(미인증 차단·빈 배치 200·미보유 400·배치 모델 적재). 캔버스·탭-투-플레이스 인터랙션은 `.preview` 정적 하니스(실 app.css+Alpine)로 검증(배치/거두기/이동/교체·"한 식물 한 번" 불변식). 도감 4축·계산기·시드 전부 무변경(회귀 0). 좌표 API·스키마는 SPA 전환 시 재활용(UI만 재작업 — 수용된 트레이드오프). (PR #345) |
| 2026-06-14 | **독서 정원 무대화(A0) — 이모지 격자를 '정원'으로 연출** — 정원 캔버스가 "단색 위 이모지 격자" 인상이라 미니게임 퀄리티를 막던 천장을, **순수 CSS/HTML 무대 연출**로 끌어올림(미니게임 로드맵 A 단계 첫 PR, 계획 `2026-06-14-garden-stage-a.md`). 사용자 결정(AskUserQuestion): **에셋 0·이모지 유지**, 배경·화분·그림자·울타리로 무대만 꾸며 게임감(SVG 승격은 후속 A2). **5요소**: ① 하늘→잔디 그라데이션 무대 프레임, ② 흙 화단(planter bed — 캔버스를 흙색+inset 그림자로 '패인 땅'), ③ 식물 발밑 타원 그림자(`occupied::after`, `pointer-events:none`로 탭 클릭 비간섭), ④ 나무 울타리 프레임(`inset box-shadow`), ⑤ 빈 정원도 '심을 밭'으로 읽히는 옅은 이랑(furrow) 패턴(`repeating-linear-gradient`). **백엔드/JS 로직 0** — 보기/편집 두 캔버스를 각각 `.garden-stage`로 **순수 래핑**, 보기 무대는 `x-show="!editing"`을 래퍼로 옮겨 동작 동일(no-JS면 무시돼 폴백 유지). 신규 토큰 `--garden-sky/grass/soil/fence`(다크모드 미도입 프로젝트라 단일값). **2.5D perspective는 비채택** — 셀 클릭 좌표가 원근 변환과 어긋나 탭-투-플레이스가 깨지므로 그라데이션+그림자+inset으로 깊이를 '암시'(좌표계 불변, 진짜 아이소메트릭은 후속 B 캔버스). 검증: A0는 로직 변화 0이라 새 Java 테스트 없이 **회귀 가드(`./gradlew test` 전체 GREEN — 정원 모델·라우팅 불변 증명)** + **preview 시각 게이트**(`.preview` 정적 하니스·실 app.css — 무대 그라데이션/흙 이랑/발밑 그림자 computed-style 단언, 편집 모드 탭이 정확한 셀에 배치돼 좌표 무결성 확인, 모바일 8열 유지). 파일 2개(`garden.html` 구조 최소 보강 + `app.css` 무대 블록). 후속 A1(생명감 모션)~A2(SVG 승격)~A3(드래그)~B(PixiJS 전환)는 방향·트리거만 고정. (PR #346) |
| 2026-06-14 | **독서 정원 잔디 카드 헤더 — 정원 ▸ 토글을 제목 왼쪽→오른쪽으로 스왑** — #334가 토글(`정원 ▸`)을 제목("독서 잔디") **왼쪽**(`.garden-toggle{left:24px}` + `.garden-host .grass-head h2{padding-left:3.4rem}`)에 뒀던 것을, **제목이 먼저 오고 토글이 그 옆**에 오도록 제목 오른쪽으로 옮김(사용자 요청). **해법**: h2 `padding-left:3.4rem` 제거(제목을 카드 콘텐츠 좌단에 정렬) + `.garden-toggle left:24px`→`6.4rem`(제목 "독서 잔디" 폭만큼 우측 = 제목 바로 옆). ⚠️ `left`는 제목 문구·폰트에 맞춘 매직넘버라 둘 중 하나가 바뀌면 재조정(주석으로 방어). 우측 끝 연속일수 뱃지(`.grass-streak`)와는 여유가 충분해 #333 겹침 회귀와 무관. `app.css`(토글 위치·h2·주석)+`dashboard.html` 주석 2줄뿐, 앱 로직·DB·Flyway·마크업 0 → 시각 검증 게이트. **검증**: `.preview` 정적 하니스+실 app.css 데스크탑(1280px) geometry 측정 — 좌→우 순서 [독서 잔디 405–460][정원 ▸ 483–524][🌱 2일 연속 790–875]·겹침 0(제목–토글 23px·토글–뱃지 266px)·같은 라인(top 65–67), 토글 펼침 시 패널은 잔디 아래 유지(panel_top 247 ≥ grass_bottom 231 = 위치 회귀 0). (PR #347) |
| 2026-06-14 | **troubleshooting T-051 신설 + T-050 본문·목차 복원** — #347 머지 sweep 산출물. **T-051**: 워크트리에서 연 세션이 `gh pr merge --delete-branch`를 하면 로컬 정리가 `fatal: 'main' is already used by worktree`로 실패하는 트랩(원격 머지는 성공·정리 후처리만 실패 — main이 메인 워크트리에 체크아웃돼 브랜치 전환 거부) → `gh pr view`로 MERGED 확인 → `git push origin --delete`로 원격 브랜치 삭제 → 메인 워크트리서 `merge --ff-only` → 세션 종료 후 `worktree remove`+`branch -d`(자기 발밑 폴더는 세션 중 제거 불가). 더해 **#346이 T-050(perspective 격자 클릭 좌표 어긋남)을 누적표에만 넣고 본문·목차를 누락**한 것을 발견해 함께 복원(목차+본문 섹션). 문서만(`.java` 0). (PR #348) |
| 2026-06-14 | **독서 정원 생명감(A1) — 식물이 살아 움직이는 은은한 상시 모션** — A0가 깐 정적 무대 위에 "정원이 살아 있다"는 체감을 얹음(미니게임 로드맵 A 단계, 계획 `2026-06-14-garden-stage-a1.md`). 사용자 결정(AskUserQuestion) = **은은한 상시 생명감**. **4가지 모션(전부 CSS + 선언적 Alpine 1상태·백엔드 0)**: ① **sway** — `.canvas-plant`에 무한 미세 흔들림(`@keyframes garden-sway` ±2.5deg, `transform-origin:bottom center`로 발밑 뿌리 고정·윗부분만 살랑), 발밑 그림자(`::after`)는 안 흔들려 식물만 그림자 위에서 움직임. ② **rise** — 입장 솟아오름(`garden-rise` 1회 translateY+scale+opacity, `backwards`로 시작 전 첫 프레임 유지) → .5s 뒤 sway 시작(시간 비중첩이라 transform 충돌 0). ③ **pop** — 편집에서 **방금 놓은 셀만** 1회 팝(`garden-pop` scale 오버슈트), `myGarden()`에 `justPlaced` 1상태 + 식물 span `:class="{'plant-pop':justPlaced===i}"`/`@animationend`로 해제→sway 복귀. `x-transition:enter`을 안 쓰는 이유 = 나타날 때마다 재생돼 **편집 진입 시 기존 식물 전부 팝**(특정 셀만 겨냥하려 상태 플래그). ④ **glow** — pop과 함께 `::before` radial-gradient 옅은 반짝 1회(은은 기조, 풍부한 파티클은 '화사'/B로 보류). **stagger** = `.canvas-cell:nth-child(...)`로 rise·sway delay/duration 차등(전부 동시 흔들림 어색 회피, 마크업 불변=순수 CSS). **reduced-motion 가드** = 정원 블록에 `@media (prefers-reduced-motion:reduce){ .canvas-plant{animation:none} }` 신설(모션 끄면 즉시 최종 상태·기능 손실 0). 도감 그리드(`.plant-emoji`)는 선택자 스코프(`.garden-canvas .canvas-plant`)로 무관·오염 0. **검증**: 로직 변화 0이라 새 Java 테스트 없이 **회귀 가드 `./gradlew test` 전체 GREEN**(정원 모델·라우팅·저장 계약 불변 증명, `justPlaced`는 저장 payload에 안 섞임) + `.preview` 정적 하니스(실 app.css)로 rise+sway 적용·`transform-origin` 발밑·nth-child stagger 차등·plant-pop=garden-pop(sway 덮음)·glow `::before`=garden-glow·키프레임 4종·**방금 심은 칸만 plant-pop**(편집 진입 일제 팝 없음)·reduced-motion CSSOM 규칙을 computed-style/CSSOM으로 단언. ⚠️ **pop 실제 재생·animationend→sway 복귀는 헤드리스 preview 렌더러가 애니 타임라인을 프레임 진행 안 시켜 자동 검증 불가**(reflow 강제 시에만 running 잡힘 = 실 Chrome/FF와 다른 렌더러 아티팩트로 판단) → 계획 §2.3대로 **실 브라우저 수동 시각 게이트**로 남김. 파일 2개(`garden.html` 최소 + `app.css` 정원 캔버스 블록). 후속 A2(SVG 승격·메타 1필드·정식 TDD)~A3(드래그)~B(PixiJS)는 방향·트리거만 고정. (PR #349) |
| 2026-06-15 | **로그인·가입·비번재설정 화면 빈 화면(500) 핫픽스 — 익명 폼 페이지 CSRF 토큰 선확정** — 로그아웃 후 `/login?logout`이 **빈 화면(배경색만)** 으로 깨지던 운영 장애. **원인 = T-033(dashboard)·T-049(personality)와 동일 패턴의 4번째 재발**: 로그인 폼(`th:action`)의 CSRF 숨김필드가 세션을 lazy 생성하는데, GA4 head 스크립트(#338)로 응답 앞부분이 커져 폼 렌더 전에 응답이 **커밋(flush)** 돼 `IllegalStateException: Cannot create a session after the response has been committed` → Spring이 500 error.html로 깨끗이 못 바꾸고 **login 앞부분 + error.html이 중첩·잘린 깨진 HTML**을 chunked로 흘려, 브라우저가 불완전 응답을 받아 빈 화면. 운영 raw HTML(`curl`)에서 두 문서 중첩·`transfer closed`로 확정. **로그인 화면은 익명이라 세션이 늘 없어** dashboard/personality(로그인=세션 有)보다 더 취약 — login·signup·`password/forgot`·`password/reset` 익명 폼 페이지가 전부 같은 잠복 버그(선확정 누락 확인). "로그아웃하면 깨진다"로 체감된 건 평소 세션이 살아 `/login`에 갈 일이 없었기 때문(=`/login` 자체가 깨진 상태). **해법**: `DashboardController`가 쓰던 방어(렌더 전 `CsrfToken.getToken()`으로 세션 선확정)를 `LoginController`·`SignupController`·`PasswordResetController`(GET 2곳)에 일괄 적용 — 추가만, 기존 동작 불변. TDD RED→GREEN: MockMvc로는 작은 페이지의 commit-후-500을 재현 못 해(폼이 어차피 세션 생성 — T-049가 기록한 한계), **GET 핸들러가 렌더 전 토큰을 선확정(`getToken()` 호출)하는가**를 검증하는 단위 테스트 4개(`WantedButNotInvoked` red → green). 전체 회귀 0. sweep: T-049 보강(익명 폼 페이지는 크기 무관 취약 — 4번째 재발). (PR #350) |
| 2026-06-15 | **독서 정원 SVG 식물 승격(A2) — 시간축 14종 코드 벡터** — A0(무대)·A1(모션)이 깐 위에서 "무대는 좋은데 배우가 이모지"인 마지막 간극을 메움(미니게임 로드맵 A 단계, 계획 `2026-06-14-garden-stage-a2.md`). 사용자 결정: **코드 벡터로 직접 그리기**(SVG `path`/도형, 외부 파일·수급·라이선스 0 — A0 "에셋 0" 기조 연속). **파이프라인**: `plant.sprite_id`(V40 — nullable 신규 컬럼, 기존 행 null=이모지 폴백으로 무중단) → `Plant.spriteId`(엔티티, `of()` 끝 인자) → `OwnedPlant`/`PlacedPlant` record(+`spriteId`) → `GardenView.ownedPlants()`(시간축만 `getSpriteId()`, **타 축은 null**) → `GardenLayoutService.layoutOf()`(보유 메타에서 `spriteId` 결합). **뷰 = 인라인 `<symbol>`+`<use href>`**: `fragments/garden-sprites.html`에 시간축 14종을 `<symbol id="sprite-{code}">`로 1회 정의(정의 1·참조 N·중복 0·외부 요청 0), 캔버스(보기/편집)·도감·팔레트가 `<use href="#sprite-{code}">`로 참조. **폴백 불변식(N-055)**: `spriteId==null`이 정상(47종 중 33종 타 축)이라 **있으면 SVG·없으면 이모지** 분기를 양쪽 모드 동일 적용 — 깨지면 식물이 안 보이므로 null-state를 일부러 테스트. **저장 계약 방어**: `PlacementRequest`(클라→서버)는 여전히 `axis/code/cell`만 — `spriteId`는 서버가 카탈로그에서 결합(위조 차단), `window.__garden` 부트스트랩(서버→클라)에만 실림(Alpine `initial()`/`selectPalette`에 `spriteId` 보존). 모션은 A1 상속(`.canvas-plant` sway/pop을 자식 `<svg>`가 transform 상속·추가 0). 팔레트 = 무대 토큰(잔디 세이지·흙 갈색)과 어울리는 채도 낮은 톤·다색 `fill` 명시. TDD **정식 RED→GREEN**(도메인 데이터·뷰모델 전파 변경이라 CSS 면제 아님): RED=`spriteId()`/`getSpriteId()` 미존재 컴파일 실패 5건 → GREEN=전파(시간축 spriteId→ownedPlants)·**null-state 누수 가드**(미지정 종이 빠지지 않고 spriteId만 null)·배치 결합(`PlacedPlant.spriteId`) 3종 신규 + `GardenControllerTest`에 스프라이트 정의 주입(`th:block` replace·14종 `<symbol>` 파싱) 배선 단언 1종. 좌표값·fill 색·마크업 문자열은 비검증(브리틀)→preview 시각 게이트. 타 축(장르·다양성·레시피) SVG화는 후속 PR(같은 파이프라인 복제). `Plant.java:19` "비주얼 SVG 후속" 예고 실현. (PR #351) |
| 2026-06-15 | **CLAUDE.md Git 워크플로 — 커밋 제목에 PR 번호 직접 넣지 않기 규칙 추가** — #351 머지 시 커밋 제목의 `(#351)`과 squash 머지가 자동 부착한 `(#351)`이 겹쳐 최종 커밋이 `… (#351) (#351)`로 중복된 것을 계기로, 재발 방지 규칙을 `🔀 Git 워크플로` 절차 2(작업+커밋)에 한 줄 명시. 이 repo는 squash 머지라 GitHub가 머지 시 제목 끝에 `(#391)`을 자동 부착하므로 커밋 제목엔 번호를 넣지 않는다(changelog 행은 PR 번호를 계속 기재 — 별개). 문서만(`CLAUDE.md`+`changelog.md`, `.java` 0). (PR #352) |
| 2026-06-15 | **독서 정원 인터랙션(A3) — 부드러운 드래그 + 휠 줌** — A0(무대)·A1(모션)·A2(SVG)가 깐 위에서 정원 꾸미기를 "탭-투-플레이스"에서 **직접 끌어다 놓는 미니게임감**으로 올림(미니게임 로드맵 A 단계, 계획 `2026-06-15-garden-stage-a3.md`). 사용자 결정 = **첫 PR = 드래그 코어 + 휠 줌**(핀치·팬·스왑 제외), **점유 칸 드롭 = 무효 복귀**. **입력 모델 = Pointer Events 단일 제스처**(마우스·터치·펜 통합): 한 `pointerdown`에서 **이동거리 임계(6px²=`DRAG_THRESHOLD2`)**로 탭/드래그를 가른다 — 임계 미만은 **기존 `tapCell`/`selectPalette`로 위임**(폴백·키보드 경로·회귀 0), 이상이면 드래그 승격. `setPointerCapture`로 셀 밖에서도 추적, 드래그 직후 발생하는 click은 `suppressClick` 1회 가드로 삼켜 탭과 충돌 0. **두 드래그 소스** = 팔레트→칸·칸→칸(빈 칸 이동만). **고스트** = 식물 시각 복제를 `position:fixed`(화면좌표)·`pointer-events:none`·정지로 손가락에 붙여 따라오게 함(줌과 무관하게 정확). **드롭 예고** = `pointermove`마다 `closestCellIndex`로 역산한 셀에 `.cell-drop-target` 하이라이트. **스냅 판정 = 셀 `getBoundingClientRect` 순회**(순수 함수 `closestCellIndex`): ① 포인터 포함 셀(반열린 구간) ② 없으면 중심 거리 최소 셀, 셀 크기 비례 마진 밖이면 -1. **줌 자동 정합**: `getBoundingClientRect`가 transform 적용 화면좌표를 줘 `clientX/Y`와 같은 좌표계 → **scale 보정 0**(고정 px 임계였으면 줌에서 깨짐 — 마진도 셀 크기 비례로 정합). **줌 = 휠 제자리 확대**(`clampScale` 1~2.5배, `.garden-canvas.editing`에 `transform:scale`·origin center, 무대 `overflow:hidden`이 확대분 클립, 팬 없음). **점유 칸 드롭 = 무효 복귀**(`applyDrop`: -1·점유 칸(출발셀 제외)이면 변화 없음, 빈 칸이면 출발칸 delete+타깃 set+`justPlaced` 드롭 pop) — 교체는 기존 탭이 담당해 두 모델 역할 분담. `touch-action:none`은 **편집 캔버스에만** 걺(보기 모드·페이지 나머지 스크롤 보존). **저장 계약 불변·백엔드 0** — 드래그가 정하는 건 `cell`(격자 인덱스)뿐, `PlacementRequest(axis,code,cell)`·`GardenLayoutService`·Flyway 전부 무변경(격자 스냅 유지로 B 트리거 `x,y` 의도적 회피). **TDD**: 순수 코어(`closestCellIndex`/`clampScale`)는 백엔드 변화 0이라 Java 테스트 면제지만 CLAUDE.md TDD 약화 금지 — `.preview` 노드 하니스(`a3-pure.test.mjs`)가 **garden.html 인라인 `@a3-pure-core` 마커 블록을 추출해 실제 실행**(사본 아님=단일 출처)으로 **RED→GREEN**(셀 중앙·경계 반열린·갭 최근접·밖 -1·줌 rects 정합·클램프 범위/경계 전수 19단언). 회귀 `./gradlew test` 전체 GREEN(저장 계약·검증 불변 증명, `.java` 무변경) + 인라인 스크립트 문법 검증 + **정적 Alpine 하니스에 실 PointerEvent/WheelEvent 시뮬**로 드래그(고스트·드롭예고·스냅·pop·placements 갱신)·칸→칸 이동·점유 칸 무효 복귀·휠 줌 클램프(1.3/max2.5/min1)를 preview MCP로 검증. 제스처 품질(실기기 멀티터치·`setPointerCapture` 브라우저 차이)은 헤드리스 한계라 실 브라우저 수동 게이트로 남김(계획 §4.3 ⚠️). 파일 2개(`garden.html` 순수 코어+드래그/줌 상태·핸들러+마크업 / `app.css` `touch-action`·드롭 하이라이트·고스트·줌 래퍼·grab 커서·reduced-motion 줌 transition off). 핀치·팬·스왑·캔버스 밖 드롭=거두기는 후속. (PR #353) |
| 2026-06-15 | **독서 정원 타 축 33종 SVG 승격(A2 후속) — 도감 4축 SVG 완결·이모지 혼재 해소** — A2(#351)가 시간축 14종에 깐 `sprite_id` 파이프라인을 **장르 13 + 다양성 12 + 레시피 8 = 33종**에 1:1 복제해, 도감을 열면 시간축만 SVG·나머지는 이모지로 섞이던 마지막 비주얼 갭을 메움(미니게임 로드맵 A 단계, 계획 `2026-06-15-garden-stage-a2-followup.md`). 사용자 결정 = **한 PR에 33종 완결**(축별 분리 미채택 — 같은 화면 혼재를 한 번에 해소). 새 설계가 아니라 **검증된 파이프라인의 N축 복제**(리스크는 기술이 아니라 33종 벡터 제작 노동). **파이프라인(A2 동형)**: `genre_plant`/`diversity_plant`/`recipe_plant`에 `sprite_id` 컬럼(V41 — 3 테이블 각 nullable 신규 컬럼 + `update set sprite_id = code` 전 행, 미래 행 null=이모지 폴백 무중단, H2/MySQL 공통) → 3 엔티티 `spriteId` 필드 + `getSpriteId()` + `of()` 끝 인자 → **`GardenView.ownedPlants()` GENRE/DIVERSITY/RECIPE 3줄 `null` → `getSpriteId()`(핵심 변경점)**. **자동 전파(추가 마크업 0)**: A2가 `OwnedPlant.spriteId` 분기를 캔버스(보기/편집)·팔레트 양쪽에 이미 깔아둬, 타 축 `spriteId`만 채우면 그 식물이 정원에 자동 SVG 렌더. **도감 그리드 4곳**(장르·작가·출판사·레시피)은 시간축 패턴 복제로 "spriteId 있으면 `<use href="#sprite-{spriteId}">`·없으면 이모지" 분기 추가(미보유 🔒·미발견 ❔ 슬롯 무변경). **벡터 = 코드 path 33 `<symbol>`**(`fragments/garden-sprites.html` 14→47종, viewBox 0 0 32 32·발밑 y≈31·중앙 x=16으로 sway 뿌리 고정·다색 채도 낮은 무대 톤, 과일·채소도 줄기/잎 한 가닥으로 "땅에 선 식물"감 — 외부 에셋·요청 0). 47종 code 전역 유니크라 `sprite-{code}` 충돌 0(A2 `spriteId=code` 관례 계승). **저장 계약 방어(A2 계승)**: `spriteId`는 서버가 카탈로그에서 결합 — `PlacementRequest`(클라→서버)는 여전히 `axis/code/cell`만(위조 표면 불변). TDD **정식 RED→GREEN**(도메인 데이터·뷰모델 전파 변경이라 CSS 면제 아님): RED=`of()` 시그니처 변경으로 호출처 컴파일 실패 + 타 축 전파 기대 테스트 3개(genre/diversity/recipe spriteId→ownedPlants) FAILED 확인 → GREEN=엔티티 3종 + `GardenView` 3줄 구현 → 통과. 더해 **null-state 누수 가드(N-055)** — spriteId 미지정 종(econ 등)이 `ownedPlants()`에서 빠지지 않고 spriteId만 null(이모지 폴백 보존)을 일부러 미지정 픽스처로 단언. SVG path 좌표·fill·마크업 문자열은 비검증(브리틀)→preview 시각 게이트(도감 4축 SVG 렌더·톤 일관·혼재 해소). 회귀 `./gradlew test` 전체 GREEN. 파일: V41(신규) + 엔티티 3 + `GardenView` + `garden-sprites.html` + `garden.html`(4곳) + 테스트 5파일 `of()` 일괄. `app.css` 무변경(A2가 `.plant-svg` 도감 크기까지 정의). 이로써 정원 A 단계의 비주얼(무대·모션·SVG·인터랙션)이 완결. (PR #354) |
| 2026-06-15 | **독서 정원 인터랙션 풍부화(A3 후속) — 점유 칸 스왑 + 캔버스 밖 드롭=거두기** — A3(#353)가 깐 드래그를 "빈 칸 이동만"에서 **드래그 모델 완성**으로 풍부화(계획 `2026-06-15-garden-stage-a3-followup.md`, 사용자 결정 = 스왑 + 밖-드롭 거두기, **핀치·팬·part-sway는 후속 제외**). **핵심 = 드롭 결정 로직을 순수 함수 `resolveDrop(source, target, placements)`로 추출** — 상태 변경(`this.placements` 대입)과 결정을 분리해 하니스가 결정만 단독 단언(`applyDrop`은 그 결과 맵을 적용만, justPlaced pop만 부수). **4가지 결정**: ① 빈 칸 이동(기존 보존) ② **셀→점유칸 = 스왑**(출발칸↔target 교환, 둘 다 "한 식물 한 번" 유지) ③ **팔레트→점유칸 = 교체**(점유자 거둠 + 같은 식물 중복 떼기 — 기존 탭 교체와 정합) ④ **밖(-1) = 거두기**(셀 소스만 fromCell 제거, 팔레트 소스는 무변경) + 제자리(target===fromCell) 무변경. **밖-드롭 오발 방지(R2)**: `closestCellIndex`의 셀 크기 비례 마진(`outOfBoundsMargin=1.0`)이 "확실히 밖"일 때만 -1 → 갭·근접은 안 거둠. 더해 셀 소스를 밖으로 끌 때 캔버스에 `.drag-removing` 붉은 점선 톤으로 "여기 놓으면 거둠" 경고(`:class` 바인딩, 가벼운 시각 피드백). **저장 계약 불변·백엔드 0**(A3 계승) — 스왑/거두기/이동 다 `placements` 맵 조작일 뿐 `save` payload는 여전히 `{axis,code,cell}`(위조 표면·Flyway 무변경, 격자 스냅 유지 = B 트리거 회피 지속). **TDD 정식 RED→GREEN**: `.preview/a3-pure.test.mjs`가 garden.html `@a3-pure-core` 마커 블록을 추출 실행(단일 출처) — `resolveDrop` 미구현 시 `Export not defined` RED 확인 → 마커 블록에 `resolveDrop`+`removeSamePlant` 추가 후 GREEN(빈칸이동·셀스왑·팔레트교체·밖거두기·제자리·입력 불변 + 기존 `closestCellIndex`/`clampScale` 회귀 가드 = **28단언**). 회귀 `./gradlew test` 전체 GREEN(`.java` 무변경). 제스처 품질(실기기 멀티터치·`setPointerCapture`)은 헤드리스 한계라 실 브라우저 수동 게이트(계획 §4.3). 파일 2개(`garden.html` 순수 함수 2개+`applyDrop` 리팩터+거두기 힌트 바인딩 / `app.css` `.drag-removing` 톤). 핀치 줌·팬·part-sway(모션)는 후속. (PR #355) |
| 2026-06-15 | **독서 정원 자유 위치 전환(살아있는 정원 게임 Phase 1) — 격자 폐지·정규화 좌표·Phaser 도입** — "정해진 칸에 스냅"하던 배치를 **어디든 놓는 자유 꾸미기**로 바꾸고, 최종 비전(성장·물주기·낮밤·파티클이 있는 살아있는 정원 게임)의 기반 단계를 깖(계획 `2026-06-15-garden-living-game-phase1-freeplacement.md`, 사용자 결정 = 본격 게임 비전 + 자유 위치 첫 출발점 + 모던 플랫 톤, **길 A = Phaser 처음부터 도입**으로 재작업 0). **위치 모델 전체를 격자 정수 → 정규화 좌표로 풀스택 교체**: ① **V42**(`drop table`+`create table` — `cell_index INT`·`uk(user,cell_index)` 폐지 → `pos_x/pos_y DOUBLE`(0~1)+`z_order INT`, 베타라 기존 배치 비우고 재생성, drop/recreate라 H2↔MySQL `drop constraint` 문법차·constraint명 의존 회피·`FlywayMigrationTest`가 H2 MySQL 모드서 실증) ② `GardenPlacement`(`int cellIndex` → `double posX,posY; int zOrder`, `uk_..._cell` 제거·`uk_..._plant` 유지=한 식물 한 번) ③ `PlacementRequest`(`int cell` → `double x,y; int z`) ④ `PlacedPlant`(+`x,y,z`) ⑤ `GardenLayoutService`(`GRID_*` → `WORLD_WIDTH/HEIGHT` 1000×640 상수, 검증 `cell 범위·셀 중복` → **좌표 범위 0~1**·**겹침 허용**(셀 중복 거부 삭제)·한 식물 한 번 유지, `layoutOf` 정렬 `cell` → `zOrder` 오름차순) ⑥ `GardenController`(`placedByCell`/`grid*` 모델 제거 → `worldWidth/Height`). **프론트 = Thymeleaf 셸 + Phaser 위젯**(SPA 전환 X): 보기 모드는 **서버 렌더 `.garden-view`에 정규화 좌표 절대배치**(`left/top`=정규화×100%·`z-index`=zOrder, no-JS 폴백·sway/그림자 A1·A2 상속), 편집은 **Phaser 3 게임 위젯**(garden 페이지 한정 `defer` 로드 — 타 페이지 영향 0): 월드 배경(하늘/잔디/흙 graphics·CSS 변수 톤 결합)·식물 스프라이트 드래그 자유 이동·**월드 밖 드롭=거두기**·팔레트 탭으로 월드에 추가(이미 놓인 종 비활성). **SVG→Phaser 텍스처 POC 성공**(최대 리스크 해소): `<symbol id="sprite-{code}">`(self-contained path·viewBox 0 0 32 32)의 `innerHTML`을 독립 SVG로 직렬화 → Blob URL → `load.image` 텍스처(spriteId 있으면 `Image`·없으면 이모지 `Text` 폴백, A2 폴백 불변식 계승). 저장 = 정규화 좌표 배열을 `POST /garden/layout`(서버 결합 spriteId 위조 표면 불변). **순수 코어 분리**(픽셀↔정규화·월드밖 판정)를 `@free-pure-core` 마커로 빼 DOM·Phaser 의존 0. **TDD 정식 RED→GREEN**: 서버 — 테스트를 좌표 시그니처로 갱신해 컴파일 RED 확인 → 프로덕션 구현 GREEN(좌표 범위 거부·**겹침 허용**·zOrder 정렬·미보유/위조/축분별/유령 교집합/IDOR/교체저장/spriteId 결합 회귀 유지, N-055 정신). 프론트 — `.preview/free-pure.test.mjs`가 마커 블록 추출 실행(clampNorm 경계·픽셀↔정규화 왕복·isOutsideWorld 마진 = **23단언**) + Phaser 씬은 preview MCP eval로 텍스처 4종 96×96 디코드·Image/Text 분기·좌표 복원·팔레트 추가·중복 거부·월드밖 거두기·팔레트 동기화 실증. 회귀 `./gradlew test` 전체 GREEN(`GardenControllerTest`·`FlywayMigrationTest` 포함). 실제 드래그 제스처·픽셀 시각은 헤드리스 WebGL 렌더백 한계라 실 브라우저 수동 게이트(계획 §5). 파일: V42(신규) + 엔티티/DTO/뷰/서비스/컨트롤러 + `garden.html`(내 정원 섹션·부트스트랩·Phaser 씬) + `app.css`(자유배치 CSS) + 테스트 2파일. Phase 2~5(변형·레이어 / 데코 / 살아있는 연출 / 게임 루프)는 후속 계획 세션에서 상세화. (PR #356) |
| 2026-06-15 | **정원 '꾸미기' 편집 식물이 태블릿에서 작던 문제 — 큰 화면에서 정원 넓히기(반응형)** — 컴퓨터에선 멀쩡한데 태블릿에서 정원 편집(꾸미기) 식물이 작아 식별·터치가 어렵던 실사용 버그. **원인 = 앱이 모바일-우선 `.container{max-width:460px}` 단일 컬럼인데 정원에 태블릿+ 반응형 미디어쿼리가 없어**, 큰 화면에서도 정원이 460px에 갇힘. 마침 #356이 정원을 Phaser 자유배치로 전환(격자 폐지)해 편집은 `.garden-phaser canvas{width:100%}` FIT 스케일 — **컨테이너가 460에 갇히면 캔버스도 460이라 식물이 작게 렌더**(데스크톱은 마우스·모니터로 덜 거슬렸을 뿐, 태블릿은 터치라 체감 ↑). **해결 = 정원 페이지만 큰 화면에서 넓힘**(원래 #357은 8열 격자 기준이었으나 #356 Phaser 전환에 맞춰 재작업) — `garden.html` `.container`에 전용 클래스 `garden-wide` + `app.css` `@media (min-width:600px)`로 `.container.garden-wide` max-width 460→720px. **편집 Phaser 캔버스는 컨테이너 폭을 따라 커지고 식물이 FIT 스케일로 비례 확대(CSS 추가 불필요 — '꾸미기' 식물이 커지는 핵심)**, 보기 모드 절대배치 식물·팔레트·도감은 DOM이라 SVG(rem 고정)를 함께 키움. **정원 페이지 한정 클래스라 다른 화면 회귀 0**, `min-width` 가드라 모바일(<600px) 불변. 검증 = 프리뷰 정적 하니스(`.preview/garden-edit-mock.html`, 실 app.css·Phaser 구조 재현)로 폭별 computed-style 실측 — 태블릿에서 컨테이너 460→720·편집 캔버스(`.garden-phaser`) 283→660px(2.3배=식물 비례 확대)·보기 식물 30.4→41.6px·팔레트 44→60px ↑, 모바일 전부 불변. 회귀 `./gradlew test` 전체 GREEN(`.java` 무변경, CSS+템플릿 클래스만). 파일 2개(`garden.html`·`app.css`). (PR #357) |
| 2026-06-15 | **독서 정원 '꾸미기' 팔레트 추가 먹통 핫픽스 — Alpine 반응 상태의 Phaser 인스턴스 reactive Proxy 오염** — Phase 1(#356) 자유배치 배포 후 실사용에서 ✏️ 꾸미기 진입·팔레트 식물을 클릭해도 정원에 안 들어가던(드래그·거두기 포함 편집 전체 먹통, 에러도 없음) 버그. **원인 = `garden.html` Alpine 컴포넌트가 Phaser `scene`/`game`을 `x-data` 반응 속성(`this.scene`)에 저장** → Alpine 3(Vue reactivity)가 인스턴스를 reactive Proxy로 깊게 감싸(`this.scene===s`가 false), Proxy scene을 `new Phaser.Game({scene})`에 넘기니 Phaser 내부 순환참조(`scene.sys↔scene`·`this.add`/`this.load` 플러그인)가 깨져 부팅 폭주·`mountPhaser` 중단·이후 줄 미실행. **mock(`garden-free-mock.html`)은 Phaser를 평범한 `const`에 담아 Proxy 0이라 정상** → 구현 세션의 헤드리스 POC·`free-pure.test.mjs`는 통과하고 실 Alpine 페이지만 깸(수동 게이트로 미룬 실클릭 경로에 버그 = 헤드리스 검증 사각). **해결 = `scene`/`game`을 컴포넌트 팩토리의 클로저 변수(`let scene, game`)로 빼 반응 밖에 두고**(반응 표시 상태 `placedKeys`만 `this.*` 유지), `mountPhaser`/`addFromPalette`/`destroyPhaser`/`save` 5곳 참조 변경. **검증 RED→GREEN = `.preview/garden-alpine-repro.html`**(실 `myGarden`+`GardenScene` 구조에 reactive/closure 모드 토글)을 preview MCP로 — reactive: `this.scene===s` false·Phaser 부팅 24회 폭주·`mountPhaser` 중단(`__mg` 미설정)·추가 무반응(RED), closure: `mountPhaser` 완주·`sceneReady=true`·팔레트 추가 0→1→2·중복 거부 2 유지(GREEN). `.java` 무변경(템플릿만)이라 커밋 게이트 `./gradlew test` 스킵·도메인 회귀 0. learning-notes **N-082**(반응성 프레임워크에 게임엔진·차트·맵 같은 라이브러리 인스턴스 저장 금지 — 인스턴스는 ref 밖·이벤트만 반응으로)·troubleshooting **T-053**(증상·재현·실클릭 자동검증) 박음. 파일 1개(`garden.html`) + 문서 3(changelog·learning-notes·troubleshooting). (PR #358) |
| 2026-06-15 | **독서 기록 일자별 보기 — 월별(◀▶)+스크롤로 개편** — 기록이 쌓일수록 `/history`의 일자별 목록이 페이지에 그대로 주르륵 늘어지던 것을 **한 번에 한 달만 보고 그 달 안에서 스크롤**하도록 개편(사용자 요청 = ① 스크롤로 ② 달별 보기, AskUserQuestion = 한 번에 한 달 + ◀▶ 이전/다음). **서버 = 월별 묶음 뷰모델 신설**: `MonthlyReadingSection(YearMonth month, long totalSeconds, List<DailyReadingRecord> days)` + `ReadingHistoryService.monthlyHistory(user)`(기존 `dailyHistory`의 최신순 결과를 `LinkedHashMap`으로 `YearMonth` 그룹핑 → 월·일 모두 최신 먼저, 월 합계 동봉) → `HistoryController`가 `records` → `months`로 교체. **뷰 = `.panel-records` 월별 개편**(잔디·이번 주 빠뜨린날 탭 무손): `◀ 2026년 6월 · N시간 M분 ▶` 네비 + 월 섹션을 모두 렌더하되 최신만 `th:hidden`으로 보이고, **외부 의존(Alpine/htmx) 없는 바닐라 인라인 JS**가 ◀▶로 hidden 토글·라벨 갱신·경계 버튼 disabled(JS가 꺼져도 최신 달은 보이는 폴백). **CSS** = `.record-scroll`(max-height 360 + overflow-y:auto + overscroll-contain) 스크롤 박스 + `.month-nav*` 버튼/라벨(history 전용 셀렉터라 정원 작업과 무충돌). **TDD 정식 RED→GREEN**: 서비스 단위(월별 그룹·최신월 먼저·각 월 최신 일·월 합계·월 경계 6/30·7/1 분리·빈 목록 4종) 컴파일 RED → 구현 GREEN, 컨트롤러 `records`→`months` 단언 전환 RED → GREEN(MockMvc가 Thymeleaf 렌더까지 = SpEL 검증). preview 정적 하니스(실 app.css·월 3개)로 스크롤 박스(360/710 내부 스크롤)·◀▶ 토글(6월↔5월↔4월·라벨·경계 disabled)·모바일 375 nav 단일행·행 fit 실측 GREEN. 회귀 `./gradlew test` 전체 GREEN. 파일: `MonthlyReadingSection`(신규) + `ReadingHistoryService` + `HistoryController` + `history.html` + `app.css` + 테스트 2파일. (PR #359) |
| 2026-06-15 | **남의 책방 '구매' 드롭다운이 화면 밖으로 잘려 가로 스크롤해야 보이던 문제 — 메뉴를 안쪽으로 펼침** — 공개 책방(`/u/{id}`)에서 '구매'를 누르면 알라딘·쿠팡 드롭다운이 우측으로 넘쳐 잘리고 가로 스크롤해야 보이던 실사용 버그. **원인 = `.buy-menu-items`가 `position:absolute; left:0`(버튼 왼쪽 기준 펼침)인데 `.book-actions`가 `margin-left:auto`라 구매 버튼이 행 우측 끝** → 드롭다운이 우측 경계를 넘고, `.shelf-scroll`이 `overflow-y:auto`(한 축이 auto면 가로도 클리핑)라 잘리고 가로 스크롤 발생. 화면을 넓혀도 펼침 방향은 그대로라 근본 해결이 안 됨. **해결 = 펼침 방향을 안쪽으로**: `.book-actions .buy-menu-items{left:auto; right:0}`로 행 우측 버튼의 메뉴를 버튼 우측 기준 **왼쪽(안쪽)** 으로 펼쳐 박스 경계 안에 가둠 + `.buy-menu-items` `flex-direction: column→row`(알라딘·쿠팡 가로 배치로 세로 높이↓=마지막 행 하단 클리핑 완화). **book-detail.html은 buy-menu가 `.book-actions` 밖 `<p>` 직속이라 우측정렬 규칙 비적용·기본 `left:0` 유지**(좌측이라 원래 안 잘림) — `.book-actions` 한정 셀렉터로 회귀 0. profile·books·book-detail 3곳 공유 CSS지만 분기로 안전. **검증 = preview 정적 하니스**(실 app.css·긴 제목 행 재현)로 모바일 375에서 펼친 메뉴 우측 경계(328) < 스크롤 박스 우측(334)·가로 스크롤 0(scrollWidth=clientWidth=293)·`flex-direction:row`·book-detail식 `<p>` 직속 buy-menu는 `left:0` 유지를 computed-style 실측. **순수 CSS 변경(`.java` 0)이라 Java 회귀 표면 0**(렌더 로직 불변), 마크업·컨트롤러 무변경. 파일 1개(`app.css` 2곳). (PR #360) |
| 2026-06-15 | **독서 정원 모바일에서 작고 답답하던 문제 — 월드 종횡비 완화(640→800, 5:4)** — Phase 1(#356) 자유배치·#357 태블릿 반응형 뒤, 모바일(<600px)에서 정원이 265×170px로 작고 납작해(종횡비 1000:640=세로 64%가 좁은 폭에서 세로 과압축) 식물 놓을 공간이 부족·답답하던 실사용 문제. 태블릿+는 #357로 720px까지 펴졌으나 모바일은 혜택 0. **해결 = 월드 세로 상수 `WORLD_HEIGHT` 640→800(5:4)으로 전 화면 정원을 세로로 키움** — 단일 출처(`GardenLayoutService` 상수) → `GardenController` 주입 → `garden.html` aspect-ratio·`__garden`·Phaser height 자동 전파(코드 수정은 상수 1곳 + 폴백 2곳 정리). 검증·저장은 좌표를 0~1 정규화로만 다뤄 비율 무관 → **서버 로직·기존 저장 위치 영향 0**(정규화라 위치 의미 보존, `save` 검증에 640/비율 리터럴 없음). **검증 = preview inspect 실측**(screenshot은 WebGL 컨텍스트 공유로 막혀 수치 게이트): 모바일 정원 세로 170→212px(+25%)·식물 평면대비 18→14%, 태블릿 411→513px(`garden-wide` 720·식물 2.3rem 유지=회귀 0), 데스크톱 동일. 좌표 코어 `free-pure.test.mjs` 23/0(경로가 이전 워크트리 절대경로로 stale했던 것을 현재 garden.html 상대경로로 수정) + `./gradlew test` 전체 GREEN. 식물 절대 크기(보기 rem·편집 Phaser `PLANT_PX`)는 실기기 확인 후 방향 정해 별도 미세조정 여지. 파일 2개(`GardenLayoutService.java`·`garden.html`). (PR #361) |
| 2026-06-15 | **독서 정원 식물이 모바일에서 작던 문제 — 모바일 식물 크기 상향 + 편집 캔버스 식물 동적화** — #361 종횡비 완화 배포 후에도 모바일(<600px)에서 팔레트·정원 식물 SVG가 점처럼 작던 실사용 문제(태블릿·PC는 정상). **원인 = #357이 garden-wide(≥600px)에서만 식물을 키우고 모바일 기본 식물 크기를 작은 채로 둠** — #361은 캔버스 종횡비만 바꿔 식물 크기와 무관(빈 정원이라 캔버스 확대도 체감 안 됨). **해결 2갈래**: ① **팔레트·보기·도감 식물 CSS 모바일 기본 상향**(`app.css`) — 팔레트 svg 1.5→2.1rem·박스 44→54px, 보기 svg 1.9→2.4rem·이모지 1.7→2.1rem, 도감 svg 1.7→2rem. `garden-wide`(≥600px)가 더 큰 값이라 override → **태블릿/PC 회귀 0**. ② **편집 Phaser 식물 동적화**(`garden.html`) — `PLANT_PX 56` 상수 → `plantWorldPx(targetCss, canvasCss, worldW)`(캔버스 표시 폭 기준 역산). 단순 상수 up은 데스크톱 과대라, 캔버스 폭에 반비례로 월드px를 정해 **모바일·데스크톱 모두 화면에서 ~36px 일정**(`GardenScene.create`에서 캔버스 폭 측정 → 식물 `setDisplaySize`). `@free-pure-core` 순수 함수로 분리. **TDD RED→GREEN**: `plantWorldPx` 단언 추가→미구현 RED(`ReferenceError`)→구현 GREEN(`free-pure.test.mjs` 28단언, 캔버스=월드폭·절반·폴백·min/max 클램프). **검증 = preview inspect 실측**: 모바일 팔레트 식물 24→33.6px·박스 44→54·보기 30.4→38.4px(식물이 박스의 ~20%→62%), 태블릿 회귀 0(palette 60·보기 41.6 유지). 인라인 스크립트 문법 OK(N-082 전례 방지) + `./gradlew test` 전체 GREEN. 편집 Phaser 식물 실제 크기는 WebGL이라 preview 측정 불가 → 배포 후 실물 확인. 파일 2개(`app.css`·`garden.html`). (PR #362) |
| 2026-06-15 | **독서 정원 변형·레이어(살아있는 정원 게임 Phase 2) — 식물 회전·크기·앞뒤 정렬** — 자유 위치(Phase 1, #356) 식물이 고정 크기·고정 각도였던 것을 **회전·크기 조절·z 정렬**로 "내 마음대로 꾸민다"의 표현력을 깊게 함(미니게임 로드맵 B 단계 Phase 2, 계획 md 흡수). **서버**: V43 `garden_placement`에 `rotation`/`scale` ALTER ADD(Phase 1 데이터 보존, 기본 0·1=무변형, H2/MySQL 공통) → `GardenPlacement`·`PlacementRequest`·`PlacedPlant` 필드 + `GardenLayoutService.save` 검증(rotation 0~360·scale 0.5~2.0 밖 거부, 위조 방어 일관)·`layoutOf` 왕복 보존. **편집(Phaser)**: `spawnPlant`가 `setAngle`/scale 적용, 식물 **탭=선택**(드래그와 이동량으로 구분·테두리 graphics), 캔버스 아래 **변형 툴바**(⟲⟳ ±15°·－＋ ±0.1·⬆⬇ 맨앞/맨뒤·🗑 거두기)가 scene 위임으로 `setAngle`/`setDisplaySize`/z정렬(`plantObjs` 순서+`bringToTop`/`moveAbove`)/삭제, `exportPlacements`가 rotation/scale 저장. **보기(서버 렌더) wrapper 분리**: 한 요소면 sway transform이 사용자 변형을 덮으므로 **outer=위치+변형(translate/rotate/scale 인라인)·inner=`.canvas-plant`(sway/rise)**로 나눠 부모-자식 합성, nth-child stagger도 자식 타겟(sway가 translate를 덮던 미세 어긋남 해소). **순수 코어**: `clampRotation`(0~360 wrap)·`clampScale`([0.5,2.0]) `@free-pure-core`. **TDD 정식 RED→GREEN**: `GardenLayoutServiceTest` 변형 범위·왕복·기본값 4종 + 기존 11종 시그니처 갱신 → 컴파일 RED → 구현 GREEN, `free-pure.test.mjs` clamp 10단언(38/0, 미구현 `ReferenceError` RED→GREEN), `GardenControllerTest` 미보유 JSON에 rotation/scale 추가(scale 검증 충돌 회피). 회귀 `./gradlew test` 전체 GREEN(FlywayMigrationTest V43 H2 포함) + 인라인 스크립트 문법 OK(N-082 전례) + 보기 wrapper 변형+sway 합성 preview 실측(outer `matrix(rotate·scale)`·inner `garden-sway`). Phaser 선택·변형·툴바 실조작은 WebGL이라 preview 측정 불가 → 배포 후 실기기 게이트. 파일: V43 + 엔티티/DTO 3 + 서비스 + `garden.html` + `app.css` + 테스트 2. (PR #363) |
| 2026-06-15 | **독서 정원 '꾸미기' 식물 배치가 실배포에서 줄곧 먹통이던 근본 원인 — Phaser `defer` 로드 순서로 GardenScene이 파싱 시점 TDZ에 빠짐** — Phase 1(#356)부터 ✏️ 꾸미기에서 팔레트 식물을 눌러도 정원에 안 들어가던(캔버스 자체가 빈 채) 실배포 버그. #358은 reactive Proxy 오염(N-082)을 잡았지만 **그건 별개 결함이었고 실제 출하 차단은 이것** — 사용자 "아직도 안 됨"의 정체. **원인 = `garden.html`이 htmx·Alpine·Phaser를 모두 `defer`로 로드하는데, 본문 인라인 `<script>`(defer 아님)는 파싱 중 즉시 실행돼 `defer` 스크립트들보다 먼저 돈다** → 그 안 `class GardenScene extends Phaser.Scene`가 아직 로드 안 된 `Phaser`를 참조해 `Phaser is not defined`로 던지고, 그 줄이 던져 `GardenScene` 바인딩이 TDZ(초기화 안 됨)로 남음. `myGarden`은 함수 선언이라 호이스팅돼 살아 팔레트·버튼은 떠 보이지만, ✏️ 클릭 시 `mountPhaser`의 `new GardenScene()`이 "Cannot access 'GardenScene' before initialization"로 죽어 **Phaser 게임·캔버스 0개 생성 → 추가 무반응**. **왜 그동안 못 잡았나 = mock·헤드리스 repro가 Phaser를 `<head>`에 동기 로드해 이 순서를 가림**(#358 closure 검증도 동기 로드라 통과). **해결(A안) = 클래스 정의를 `ensureGardenScene()` 함수로 감싸 파싱 시점이 아니라 `mountPhaser`(사용자 클릭 = `defer` 로드 완료 후) 시점에 1회 평가** — Phaser는 `defer` 유지(비차단 로드, 보기 전용 방문 성능 무손)하고 `myGarden`이 Alpine 전에 준비되는 기존 의도 보존. **진단 = Chrome 확장으로 실계정 `booktimer.app/garden` 직접 조작** → 콘솔 두 에러(`Phaser is not defined`·TDZ)·`canvasExists:false`로 확정(Phaser 자체는 그 브라우저서 WebGL 정상 렌더 = 순수 로드 순서). **검증 RED→GREEN = `.preview` 충실 하니스를 production처럼 Phaser `defer`로 로드**(기존 mock이 동기 로드라 가린 지점 교정): RED(미수정) 캔버스 미마운트·팔레트 클릭 placedKeys 0→0, GREEN(수정) 캔버스 마운트·0→1·버튼 disabled·placed. `.java` 무변경(템플릿만)이라 커밋 게이트 `./gradlew test` 스킵·도메인 회귀 0, `free-pure.test.mjs` 38/0 그대로. troubleshooting **T-054** 박음. 파일 1개(`garden.html`) + 문서 2(changelog·troubleshooting). (PR #364) |
| 2026-06-15 | **독서 정원 '꾸미기' 식물 앞뒤(z-order) ⬇ "맨 뒤로" 먹통 — `children.moveAbove` no-op을 `setDepth`로 교체** — #364로 식물 배치가 처음 동작한 뒤, 실사용에서 변형 툴바의 **⬇(맨 뒤로)를 눌러도 아무 변화가 없고, 맨뒤로 보낸 식물이 계속 위에 남아 탭하면 또 그게 선택**되던 버그(사용자 보고). **원인 = `GardenScene.sendToBack`의 핵심 `this.children.moveAbove(obj, this.bg)`가 완전한 no-op**. Phaser `DisplayList.moveAbove(A, B)`는 "A를 B 위로"인데, 식물(A)이 이미 배경(B) 위에 있어 **아무것도 안 함**(실서버 측정: 식물 display index 5→5 불변) → 맨뒤로가 동작 안 함. ⬆(`bringToTop`)는 우연히 동작했지만 ⬇와 비대칭. **진단 = Chrome 확장으로 실계정 scene 내부 상태 측정** — `sendSelectedToBack` 후에도 그 식물이 최상위 display index·`tapWouldSelect`로 남음을 객관 확인. **해결 = z-order를 `plantObjs` 순서를 단일 출처로 `setDepth`로 직접 박는다** — `restack()`(배경 `setDepth(0)`·식물 `setDepth(1..n)`·선택테두리 최상단)을 신설하고 `bringToFront`/`sendToBack`은 `plantObjs` 재배열 + `restack()`만(취약한 `children.bringToTop`/`moveAbove` 제거), `spawnPlant`/`removePlant`도 `restack()`, `highlightSelected`는 `_selBox.setDepth(1e6)`. depth가 렌더·입력 순서를 그대로 끌어감을 실서버서 실측(`matches:true`). export z(=plantObjs index)·보기모드 z-index와 의미 일치. **검증 RED→GREEN = `.preview` 충실 하니스(Phaser `defer`)**: 식물 2개 겹쳐 추가 → ⬇한 식물의 "탭 top"이 RED(미수정)에선 안 바뀜(`topUnchanged:true`), GREEN(수정)에선 다른 식물로 바뀜(`sendBack_works:true`)·⬆ 대칭(`bringFront_works:true`). 헤드리스 rAF throttle로 `getIndex`가 늦어 `.depth`/`depthSort()`로 측정. `.java` 무변경이라 도메인 회귀 0, `free-pure.test.mjs` 38/0. troubleshooting **T-055** 박음. 파일 1개(`garden.html`) + 문서 2. (PR #365) |
| 2026-06-15 | **프론트 테스트·정원 빌드+모듈 전환 트리거 문서화 (학습 정리, 코드 무변경)** — #364/#365(정원 꾸미기 핫픽스)가 헤드리스/mock으론 안 잡히고 **실 브라우저 연결로만 잡힌** 경험을 재발 방지로 정착시킨 문서 작업. **learning-notes**: **N-083**(defer로 늦게 뜨는 라이브러리를 최상위 `class extends`가 참조 → 본문 인라인 스크립트가 defer보다 먼저 실행돼 ReferenceError로 클래스가 TDZ에 갇히고 형제 function은 hoisting으로 살아 "UI 멀쩡·`new X()`만 터지는" 부분 실패로 위장; 해법=정의 지연 `ensureGardenScene`·또는 인라인도 defer) · **N-084**(프론트 테스트 지형도 — 순수 로직=Node 단위(백엔드와 동일)·컴포넌트=jsdom·로드순서/canvas/타이밍=실 브라우저 E2E로만, "프레임워크=테스트 해결"은 거짓(jsdom으론 Phaser canvas 불가), Testing Trophy로 통합층 비중↑, BookTimer 권장 믹스) · **N-017 보강**("프레임워크 도입?"의 분해 — "프레임워크"는 빌드+모듈+컴포넌트+반응성+라우팅 묶음이고 테스트 가능성은 컴포넌트가 아니라 빌드+모듈에서 나옴; 풀 SPA는 여전히 보류, 정원 섬은 빌드+모듈 임계 근접). **CLAUDE.md** 새 섹션 **`🖥️ 프론트 검증`**(soft 규칙) — 로드순서·타이밍·반응성이 얽힌 클라이언트 변경은 헤드리스/mock으로 끝내지 말고 실 브라우저(Chrome 확장) 콘솔까지 검증, 재현 하니스는 production `defer`까지 복제(T-053), TDD 게이트가 못 보는 사각을 메우는 역할 분담. **plan.md** 「프론트엔드 전환」 섹션에 조건부 결정 — *헤드리스-블라인드 정원 런타임 버그가 다음 1건 더* 나오면 **정원부터** ①Vite+Vitest 도입+정원 위젯 모듈 추출(섬 아키텍처)·②프레임워크 스왑은 그 다음(프로젝트 메모리 `garden-frontend-build-trigger`에도 기록). 코드·`.java` 무변경(문서만)이라 커밋 테스트 게이트 자동 스킵·회귀 0. 파일: `learning-notes.md`·`CLAUDE.md`·`plan.md`·`changelog.md`. (PR #366) |
| 2026-06-15 | **독서 정원 꾸미기 소품(데코 카탈로그) — 살아있는 정원 게임 Phase 3** — 식물만 놓던 정원에 **길·연못·울타리·벤치 등 보유 무관 장식 오브젝트 13종**을 더해 조경을 열었다(plan §독서 정원 로드맵 Phase 3). **소품은 식물과 근본이 다름**(설계 §1): ① 보유 무관(해금 없음·카탈로그 존재만 검증) ② 같은 소품 여러 개(식물 "한 종 한 번"의 반전 — `uk(user,code)` 없음). 그래서 검증된 식물 배치 도메인(`garden_placement`/`GardenLayoutService.save`/`layoutOf`)을 **0줄 건드리지 않고** 소품 전용 테이블·검증을 평행 신설(회귀 0, genre/diversity/recipe와 같은 분리 패턴). **DB(V44)**: `decoration`(카탈로그 13종 시드·해금 없음·`category`·`sprite_id`) + `garden_decoration_placement`(중복 허용 → `uk` 없음·각 행=한 인스턴스, 좌표/변형은 식물과 동일). **저장 계약**: `POST /garden/layout` 바디를 맨 배열 → **`LayoutSaveRequest{plants,decorations}` 래퍼**로 — "저장" 한 번에 식물+소품 **원자적 교체**(`saveLayout` 한 트랜잭션). **z 통합**: 연못은 식물 뒤·벤치는 식물 앞이라 식물·소품을 **단일 z 스케일로 병합 정렬**(`layoutItemsOf`→`PlacedItem` 통합 record, 보기·부트스트랩 단일 출처). 소품 검증 = 카탈로그 존재·좌표(0~1)·회전(0~360)·크기(0.5~2.0)·**개수 cap(200)**·고아 제외(카탈로그 교집합) — 보유 검증·중복 거부 **없음**. **신규**: `Decoration`/`DecorationRepository`/`GardenDecorationPlacement`/`GardenDecorationPlacementRepository`/`DecorationPlacementRequest`/`DecorationOption`/`PlacedItem`/`LayoutSaveRequest` + `GardenLayoutService` 확장(`saveLayout`/`saveDecorations`/`layoutItemsOf`/`decorationCatalog`). **프론트**: 보기 루프 `placedItems` 단일화(kind별 `.canvas-plant` sway / `.canvas-decor` 정적), 편집 팔레트에 **"소품" 섹션**(항상 활성=다중), Phaser 씬 식물 전용 `plantObjs`→통합 `objs`(kind 데이터)·`spawnObject`(2-종 공통, 텍스처 키 `tex-`)·`addDecoration`(중복 가드 없음)·`exportPlacements` 종별 분리, 부트스트랩에 소품 카탈로그·통합 배치. 소품 SVG 13종 신규 fragment `garden-decor-sprites.html`(코드 벡터·외부 에셋 0, code 식물 47종과 전역 유니크). **TDD RED→GREEN**: `GardenDecorationLayoutServiceTest` 11개(미지 코드 거부·좌표/변형 범위·**중복 허용**(불변식 반전)·교체+IDOR·cap·**z 병합 교차**·고아 제외·래퍼 오케스트레이션·카탈로그) RED 확인 후 구현 GREEN + `GardenControllerTest`(래퍼 바디·`placedItems`/`decorations` 모델·소품 400·소품 스프라이트 주입) + `FlywayMigrationTest`(V44 H2 MySQL 실행·`ddl-auto=validate` 드리프트) + **식물 배치 회귀 0**(`GardenLayoutServiceTest` 불변) + 전체 스위트 GREEN. **실 브라우저 검증(CLAUDE.md 🖥️·#366 게이트)**: `.preview` 충실 하니스(production처럼 Phaser `defer`·실 CDN, T-053)에 연결된 Chrome으로 — 콘솔 에러 0(defer/TDZ 사각), Phaser 2-종 마운트, 소품 팔레트 13종 SVG, 소품 배치·**중복(연못 2개)**, 식물 한-종-한-번 비활성, 저장 export `{plants:[axis 보유],decorations:[axis 없음],전역 z 교차}` 실증. 파일: `garden.html`·`app.css`·신규 fragment + 도메인 8 + 테스트 2 + `V44` + 문서. (PR #367) |
| 2026-06-15 | **독서 정원 필터 버튼이 태블릿/PC에서 세로로 꽉 차게 쌓이던 문제 — 전역 `button{width:100%}` 누수 상쇄** — `/garden` 도감 필터 탭(전체·⏳시간·🌸장르·🖋️작가·출판사·🔍숨은 레시피)이 넓은 화면에서 한 줄 pill이 아니라 풀폭 버튼으로 세로 스택돼 늘어지던 실사용 버그(모바일은 좁아 자연스러워 안 보였음). **원인 = 전역 `button,.btn{width:100%}`가 `.garden-tab`으로 새고, `.garden-tab`은 padding·border-radius만 덮고 width를 안 덮어 100% 상속** → `.garden-tabs`가 `flex-wrap`이라 풀폭 pill이 각자 한 줄씩 세로로 쌓임(`app.css` 233·421줄 주석·#286 회귀와 동일 함정). **해결 = `.garden-tab{width:auto}` 1줄로 누수 상쇄** — 내용 너비 pill 복원. 넓은 화면=한 줄 가로(넘치면 wrap)·모바일=2줄 접힘. **검증 = preview 정적 하니스**(실 `.garden-controls` 마크업 + 진짜 app.css·`.container.garden-wide` 720 캡 재현)로 데스크톱 1280/태블릿 768=탭 5개 한 줄(offsetTop 동일)·모바일 375=2줄 wrap·풀폭 0(`anyFullWidth:false`)·`.garden-tab` computed width 49px(내용 너비)·활성 탭 초록 채움 유지를 computed-style 실측. **순수 CSS(`.java` 0)**라 Java 회귀 0, 마크업·JS 무변경. 파일 1개(`app.css` 1줄). (PR #368) |
| 2026-06-16 | **독서 정원 Phase B1 — 작가 캐릭터 축(AUTHOR) 신설** — 완독책 작가(알라딘 원문 `"한강 (지은이)"` 형식)를 normalize·contains 매칭으로 20종 시드 카탈로그와 연결해 캐릭터 해금. `AuthorCharacterUnlockCalculator`(normalize·빈 matchName 가드·N-055 누수 가드)·`AuthorCharacter`/Repository/State 신설, `PlacementAxis.AUTHOR` 추가, `GardenView` record 확장(+3 파라미터), `GardenService.view()` 작가 캐릭터 블록 추가. V45 Flyway 마이그레이션(테이블+시드 20종). `garden.html` 도감 섹션·필터 탭 "🧑 작가캐릭터" 추가. TDD RED→GREEN: `AuthorCharacterUnlockCalculatorTest` 18개·`GardenServiceTest` 3개 추가·`GardenLayoutServiceTest` 3개 추가. 실 브라우저 검증: 배치→저장→리로드 유지·콘솔 에러 0. (PR #372) |
| 2026-06-17 | **AdSense 심사 거부 → 보류 상태로 plan.md 정정 (문서, 코드 무변경)** — Google AdSense가 `booktimer.click` 사이트를 **"가치가 별로 없는 콘텐츠(최소 콘텐츠 요건 미달)"**로 거부(2026-06-17). 공개 랜딩(#230)을 뒀어도 실콘텐츠가 전부 로그인 뒤라 크롤러가 보는 공개 면이 랜딩 1장+폼 수준 → 기준 미달. **도메인/리다이렉트가 아니라 공개 콘텐츠 부족이 사유**라 콘텐츠 확충 전 재심사는 동일 거부 → **보류 결정**(연동 코드·ads.txt·랜딩 유지). plan.md 「비즈니스 모델」 애드센스 절을 `완료 ✅` → `연동 완료 / 심사 거부 → 보류 ⏸️`로 정정(헤더·수익 토대 줄·남은 외부 작업 줄 + 심사 결과 blockquote 신설). 코드·`.java` 무변경(문서만)이라 커밋 테스트 게이트 자동 스킵·회귀 0. learning-notes **N-073** 후보(애드센스는 로그인 게이트형 도구 앱을 저가치 콘텐츠로 거부 — 공개 크롤 콘텐츠가 승인 전제). 파일: `plan.md`·`changelog.md`. (PR #373) |
| 2026-06-17 | **독서 정원 CoC 방향 확정 학습 정리 — N-089 신설 + N-087 본문 복원 (문서, 코드 무변경)** — 정원 방향을 "CoC와 거의 동일(아이소 격자 좌표계+스냅 배치)"로 확정한 개념 논의를 learning-notes로 정착. **N-089 신설**: 타일 게임 "배경"은 3책임(지형 렌더=시각·격자 좌표계=공유 수학·배치 오브젝트=상태+입력)의 시각적 융합이고, **건물 배치는 "배경 기능"이 아니라 격자 좌표계의 기능**(체스판=나무 아닌 8×8 좌표계·모눈종이 비유) / **"보이는 타일" ⊥ "격자 로직" 직교**(4조합)라 CoC는 둘을 정렬해 한 몸처럼 보이게 한 특수 케이스 → "배치=배경" 착시의 근원 / **"CoC화"의 본질=타일 아트가 아니라 배치 모델 전환**(자유좌표 0~1 연속→이산 격자 셀+스냅), "JS로 되나"는 쉬운 질문(좌표 변환·depth sort `col+row` ~20줄·이미 Phaser 적재)·비용은 모델 전환+타일 아트. **N-087 본문 복원**: B1 구현 세션(#372)이 TOC·누적 갱신 행만 넣고 본문 `## N-087` 섹션을 빠뜨려 TOC 앵커가 깨져 있던 것을 `AuthorCharacterUnlockCalculator` 실코드 기반으로 복원(외부 API 원문 정규화·contains 부분매칭·빈 matchName 누수 가드·매칭 방향). 코드·`.java` 무변경(문서만)이라 커밋 테스트 게이트 자동 스킵·회귀 0. 파일: `learning-notes.md`(N-089 본문+TOC+누적행, N-087 본문)·`changelog.md`. (PR #374) |
| 2026-06-17 | **독서 정원 편집 모드 카메라 줌·팬** — Phaser 편집 씬에 `clampZoom`(순수 함수·`@free-pure-core` 확장, ZOOM_MIN=1·ZOOM_MAX=2.5) + 카메라 `setBounds/setZoom/addPointer(1)` + 빈 공간 드래그=팬(`hitTestPointer` 분기·줌 보정 scrollX/Y) + 휠 줌(커서 포컬·`getWorldPoint` 전후 scroll 보정) + 핀치 줌(2 포인터 거리비·중점 포컬). 기존 오브젝트 드래그(이동·snap)·탭 선택·거두기 회귀 0(dragX/dragY가 월드 좌표라 줌/팬 무관). 저장·서버·보기 모드 무변경. TDD: `clampZoom` RED→GREEN 경계값 7단언 추가(`free-pure.test.mjs` 총 45 통과). learning-notes **N-090 신설**(격자 밀도=줌의 함수). 파일 1개(`garden.html`). (PR #376) |
| 2026-06-17 | **독서 정원 격자 좌표계 1차 — 파생 격자·스냅(직교 사각 20×16)** — N-089 CoC 방향 확정 후 첫 격자 슬라이스. **저장 모델 = A(파생 격자·스냅만)**: 0~1 저장 유지, 격자는 편집기 스냅+화면 비주얼에만 존재 — **서버·DB·마이그레이션 0, 가역적**(되돌리기 = 스냅 코드 제거, 데이터 손실 0). 직교 사각 격자 먼저(아이소는 2차). **`@free-pure-core` 확장**: `GRID_COLS=20·GRID_ROWS=16`(1000/20=800/16=50px 정사각 셀) + `cellOf`(정규화→셀, 경계·범위 밖 클램프)·`cellCenter`(셀→정규화 중심)·`snapToCell`(cellOf∘cellCenter 합성) 3 순수 함수 추가. **Phaser 편집 씬**: `drawGrid()`(Phaser Graphics, depth 0.5, 옅은 녹색 격자선) + `drag` 핸들러를 `pixelToNorm→snapToCell→normToPixel` 라이브 스냅으로 교체 + `addPlant`/`addDecoration` 스폰도 셀 중심으로(export/저장 계약 불변). **TDD RED→GREEN**: 순수 함수 단언 22개 추가(`cellOf` 경계·클램프, `cellCenter` 값·왕복 20×16 전수, `snapToCell` 멱등·모서리) — `free-pure.test.mjs` 총 60 단언 통과. **실 브라우저 검증(CLAUDE.md 🖥️ 게이트)**: `.preview/garden-grid-snap-repro.html`(production처럼 Phaser `defer`·실 CDN·`ensureGardenScene` T-054 패턴) — 콘솔 에러 0·Phaser WebGL 정상·격자선 비주얼·식물 셀 중심 스폰 실증. learning-notes **N-090** 후보(파생 격자·스냅 가역성 설계 — 저장 모델 A→B 승격 청사진). 파일 1개(`garden.html`) + 문서 2(`plan.md`·`changelog.md`). (PR #375) |
| 2026-06-17 | **독서 정원 식물-격자 비율 수정 — 식물 크기 셀 고정 + 초기 줌 기기 보정** — 격자(#375)+줌팬(#376) 도입 후 `plantWorldPx`(화면폭-의존 크기)가 고정 격자와 충돌해 모바일에서 식물이 2~4셀로 발산하던 버그 수정. **B안**: `plantWorldPx` 제거 → `plantWorldSize(worldW, cols, ratio=1.0)` = 셀 고정(기기 무관), `initialZoomFor(targetCss, plantPx, canvasCss, worldW)` = 초기 줌으로 기기 보정(데스크톱≈1.0, 모바일≈2.1, canvasCss≤0 폴백). `cam.centerOn(worldW/2, worldH/2)` 추가(줌인 시 중앙 정렬). TDD RED→GREEN: `plantWorldSize`·`initialZoomFor` 단언 9개 추가·`plantWorldPx` 단언 5개 제거(`free-pure.test.mjs` 총 49 통과). `TARGET_PLANT_CSS` 주석 갱신. learning-notes **N-085 갱신**(전환 트리거 도달·실제 해법 적용 — 격자+줌팬 도입 후의 진화). 파일 1개(`garden.html`) + 문서(`changelog.md`·`learning-notes.md`). (PR #377) |
| 2026-06-17 | **SES 프로덕션 액세스 1차 거부 → 재요청 + 학습 정리 (문서/운영, 코드 무변경)** — AWS Support 케이스 178123901400162(SES 샌드박스 해제)가 **1차 거부**(2026-06-17, "보안상 사유 비공개"). 진단: AWS의 추가정보 요청(6/12, 4가지)에 우리가 상세 없이 "검토만 해달라"(6/13) 회신한 게 거부의 큰 원인 → 발송 빈도·수신자 목록 관리·반송/불만/수신거부 처리·메일 예시를 실제 구현(SES SMTP·검증 도메인 N-071·이중 옵트인·기본 OFF 마케팅·원클릭 서명토큰 수신거부·SES suppression 의존) 기반으로 담아 **케이스 다시 열기로 재요청**. learning-notes **N-091 신설**(SES 프로덕션 액세스는 거부 가능·사유 비공개·상세 사용 사례로 재요청해야 뚫림, 토글 점등≠실발송, 검증 도메인은 전제일 뿐), troubleshooting **T-058 신설**("사례 해결"=요청 포기지 승인 아님·거부돼도 "사례 다시 열기"로 재요청·진짜 승인 신호는 SES 콘솔 Account dashboard). plan.md 「2단계 점등 runbook」 ① 전제를 "케이스 대기" → "1차 거부 후 상세 재요청 중"으로 정정. 코드·`.java` 무변경(문서만)이라 커밋 테스트 게이트 자동 스킵·회귀 0. 파일: `learning-notes.md`·`troubleshooting.md`·`plan.md`·`changelog.md`. (PR #378) |
| 2026-06-17 | **독서 부채 backward-only catch-up 뱅킹(과거 메우기) — 선납 없는 순수 유도 계산** — 오늘 할당량 채우고 더 읽으면 그 초과분이 가장 오래된 과거 부채부터 갚는 backward-only 재분배(WeeklyDebtCalculator 코어 재작성). 저장·서비스·표시·DB·프론트 0 변경(순수 계산 1파일+테스트만). **선납 불가 불변식**: 오늘 부채를 갚아줄 나중 날이 없어 오늘 카운트다운은 항상 원시값 — thesis('매일' 습관)·유도·무상태 모두 유지. TDD RED(5개 경계 실패)→GREEN(22개 전부 통과)→전체 BUILD SUCCESSFUL. (PR #379) |
| 2026-06-18 | **독서 정원 아이소메트릭 전환 — 직교 사각 격자 → CoC식 아이소 다이아몬드** — #375(직교 격자) 예고한 "아이소 2차" 출하. **저장 모델 불변(옵션 A 계속)**: 0~1 정규화·서버·DB·마이그레이션 0, 투영 레이어 교체만. **JS 순수 4함수** 추가(`normToIso/isoToNorm/normToIsoPixel/isoPixelToNorm`, ISO_FLATTEN=0.5). **Java** 신규 `GardenIsoProjection.screenX/Y/Percent` + `PlacedItem.isoLeftPct/isoTopPct` 위임. **Phaser 6개 투영 호출지점** 아이소 교체(`create` 복원·`drag`·`addPlant`·`addDecoration`·`exportPlacements`·`drawBackground` 다이아몬드·`drawGrid` 사선 격자). **SSR `th:style`** `isoLeftPct()/isoTopPct()` 적용(view=edit 투영식 동기화). **`app.css`** `.garden-view::before` CSS `clip-path` 다이아몬드 바닥. TDD: JS 96단언 RED→GREEN(`free-pure.test.mjs`) + Java `GardenIsoProjectionTest` 9단언 RED→GREEN + 전체 1009 BUILD SUCCESSFUL. 실 브라우저: 다이아 격자·바닥 ✅·콘솔 0. Thymeleaf `[[...]]` 인라인 충돌 픽스(object배열). (PR #380) |
| 2026-06-18 | **독서 정원 식물 접지 패스 — 발밑 앵커+접지 그림자** — kind='plant' 아이템을 발밑 앵커(`setOrigin(0.5,1)` Phaser / `translate(-50%,-100%)` SSR)로 땅에 박고, `shadowLayer`(depth 0.6) 접지 타원 그림자로 아이소 접지감 완성. 데코는 현행 중심 앵커·CSS 그림자 유지. `app.css` `.gv-plant { transform-origin: bottom center }` + `.gv-plant::after` 발밑 그림자(CSS specificity 2-class 덮어쓰기, 데코 공통 `::after` 현행 보존). Java 0·스키마 0. 실 브라우저: 데코 중심 앵커·CSS 그림자 보존 ✅. (PR #382) |
| 2026-06-18 | **admin 부채 진단 뷰 — 재계산 per-day 윈도우 추적** — /admin/users/{loginId}/debt?asOf= 엔드포인트 신설. WeeklyDebtCalculator.computeTrace+WeeklyDebtTrace/DayDebtTrace 추가(단일 출처, 기존 compute() 동치 보장). ReadingDebtService.weeklyDebtTrace(user, asOf) as-of 재계산. admin 진단 표(원시부채·초과·1분미만용서·잔여·초과소모) + 유저 상세 링크. TDD RED→GREEN, 스키마·마이그레이션 0. (PR #381) |
| 2026-06-18 | **대시보드 정원 요약 → 바로가기 버튼 교체** — 잔디 카드 안 접힘 `<details>` 요약 패널(`.garden-host`·`.garden-toggle`·`fragments/garden.html :: summary`)을 제거하고 잔디 아래 전체폭 "🌱 정원" 바로가기 타일 버튼(`/garden`)으로 교체. `fragments/garden.html` 삭제, `app.css` 프래그먼트 전용 죽은 규칙 제거 + `.garden-link-tile { margin-top: 14px }` 추가. `gardenService.view()` 발견 평가 부수효과는 컨트롤러 무변경으로 보존. Java 0·스키마 0·BUILD SUCCESSFUL. (PR #383) |
| 2026-06-18 | **독서 정원 겹침 금지 + 깊이 자동 정렬** — 아이소 격자 한 칸=하나 불변식: `occupiedCells`·`nearestFreeCell`(BFS 빈 칸)·`resolveDrop` 순수 함수 + Phaser drag raw 좌표 isOutside 판정·점유 칸 빨간 프리뷰·dragend 3분기. `restack()` y-sort 자동 깊이 정렬. 수동 앞/뒤 ⬆⬇ 버튼 제거. `PlacedItem.depthZ()` + SSR z-index y파생 교체. `.preview/free-pure.test.mjs` 109단언 GREEN. 힌트 텍스트 "회전·크기를 바꿀 수 있어요"로 수정. T-059(Thymeleaf `[[` 이중 대괄호 object 배열 우회), N-095(겹침 금지 vs 깊이 정렬 별개 개념). (PR #384) |
| 2026-06-18 | **로컬 dev 테스트 계정 시드 + 구현 세션 8080 포트 반납 규칙** — `LocalTestAccountSeeder(@Profile("local") ApplicationRunner)` 신규: `./gradlew bootRun` 시 testid/1234qwer!! USER 계정을 멱등하게 시드해 실 브라우저 검증 게이트 살림. `build.gradle bootRun → spring.profiles.active=local` 배선. CLAUDE.md: 🛠️ local 프로파일·시드 계정 문서화 + 🪢 8080 반납 의무 추가. Flyway 0. TDD RED→GREEN(@DataJpaTest 수동배선, 기능성+멱등). (PR #385) |
| 2026-06-18 | **독서 정원 크기 변경 제거 — CoC식 고정 크기(회전만)** — scale UI(툴바 -/+ 버튼)·SSR scale() 렌더 제거. spawnObject 고정 크기, exportPlacements scale:1 상수. Java scale 잔재·DB 마이그레이션 0(#384 z_order 잔재 유지 선례). clampScale 순수함수 삭제 → free-pure.test.mjs 44단언 PASS. 실 브라우저: 툴바 크기 버튼 없음·SSR transform scale 없음·콘솔 0. (PR #387) |
| 2026-06-18 | **정원 프론트 빌드+모듈화 1차(A) — `.preview` 표류 하니스를 Vitest 커밋 그물로 승격·npm 토대** — `.preview/free-pure.test.mjs`(gitignore·표류)를 `frontend/test/garden-pure.test.mjs`(커밋·CI)로 승격. 정규식 추출+`new Function` 단일출처 방식 유지(garden.html 0줄 변경·브라우저 무변경). Vitest 398단언 RED→GREEN(그물 실증: clampNorm 깨서 21 RED·복원 후 전부 GREEN). `frontend/package.json`(vitest ^3·"type":"module") 신설·`package-lock.json` 커밋(CI 재현). CI `test` job에 `setup-node@v4`(node 20·npm cache)+`npm --prefix frontend ci/test` 스텝 추가(gradle 앞). 커밋 게이트 `.claude/hooks/require-tests-before-commit.ps1`에 `garden.html`/`frontend/**` 변경 시 `npm --prefix frontend test` 게이트(node 없으면 경고+스킵). `.gitignore`에 `frontend/node_modules/`. plan.md §프론트엔드 전환 "트리거 발동·1차 A 출하·B 후속"으로 갱신. Java·마이그레이션·build.gradle 0 변경. (PR #388) |
| 2026-06-18 | **정원 프론트 빌드+모듈화 2차(B) — Vite+TS 번들·Phaser npm import로 정원 편집 클라 모듈 추출(T-054/N-082 구조 제거)** — `garden.html` 540줄 인라인 스크립트 → `frontend/src/garden/` 4 TS 모듈(`pure.ts`·`scene.ts`·`component.ts`·`main.ts`). Phaser CDN defer 제거·npm import→번들 정적 포함으로 T-054(defer×extends TDZ) 원인 소멸. `ensureGardenScene()` 래핑 패턴 삭제·`class GardenScene extends Phaser.Scene` 직접 선언. `x-data="myGarden()"→"myGarden"`(Alpine.data 등록명). SSR·보기모드·저장계약·Docker·build.gradle 불변. `static/garden/garden.js` 번들 커밋·CI stale 게이트(`git diff --exit-code`). `frontend/test/garden-pure.test.ts`로 ESM import 승격(정규식 추출 삭제). TDD RED→GREEN(clampNorm 깨 21 RED→복원 398 GREEN)·Gradle BUILD SUCCESSFUL. (PR #391) |
| 2026-06-18 | **랜딩 페이지 리디자인 — 와이드 마케팅 랜딩(1080px, 세리프, 3단 반응형)** — 기존 460px 단일 컬럼 랜딩을 전면 교체. `landing.css` 신규(랜딩 전용 스코프 격리): sticky 네비바 / 히어로 2단(.85fr 1.15fr) / 잔디 섹션 CSS 목업(7×13 칸) / 정원 섹션 / 4컬럼 기능 그리드 / 3단계 how-it-works / 하단 CTA / 푸터. 반응형 860/480px. Gowun Batang 세리프 헤드라인. `app.css`: `@import Gowun Batang` 추가·새 음영 토큰 15개 추가·기존 `.landing-*` 블록 제거(이관). `landing.html` 전면 재작성(7섹션, Thymeleaf 링크 보존). `LandingPageTest` 신규 2건(garden 동선·핵심 키워드 Red→Green) + 기존 3건 Green 유지 = 5/5. 실 브라우저 bootRun 확인(콘솔 에러 0). (PR #392) |
| 2026-06-18 | **독서 정원 회귀 핫픽스 — CDN Alpine 번들 흡수(#391 로드순서 버그)** — #391 모듈화가 Alpine을 CDN(`defer`)으로 남겨, `defer` 스크립트 실행 직후 마이크로태스크 체크포인트의 `queueMicrotask(Alpine.start)`가 `type=module` 번들보다 먼저 실행 → `alpine:init`이 번들의 리스너 등록 전에 끝남 → `x-data="myGarden"` 미등록 `ReferenceError`로 '내 정원' 섹션 전체 사망(운영 배포 후 노출). 해결: Alpine도 Phaser처럼 npm import해 번들이 소유 — `main.ts`가 `Alpine.data('myGarden', myGarden)` 등록 후 `Alpine.start()` 직접 호출, `garden.html` CDN Alpine `<script>` 제거(로드순서가 코드로 보장돼 스크립트 태그 순서 무의존; Phaser로 T-054 없앤 것과 동일 논리). `alpinejs ^3.14.1` 의존 추가·번들 재빌드(`static/garden/garden.js`). 실 브라우저 검증(로컬 bootRun+Chrome): 콘솔 에러 0·보기/편집(Phaser iso 캔버스) 마운트·`alpinejs` CDN 요청 없음. `.java`·스키마·저장계약·Docker 0. (PR #393) |
| 2026-06-18 | **랜딩 독서 잔디 모바일 반응형 — 13열 비율 유지 축소** — #392 잔디 CSS 목업(91칸=7행×13열)이 모바일(≤480px)에서 `grid-template-columns:repeat(7,1fr)`로 열을 13→7로 줄이며 91칸이 13행으로 넘침(셀 `aspect-ratio:1`이라 폭↑→높이↑) → 세로로 길쭉해지던 버그. 모바일 그리드 오버라이드 제거로 데스크톱과 동일 13×7 유지 → 셀이 카드 폭에 맞춰 비율째 축소. 정적 하니스+preview eval 검증: 모바일 375px 13×7 293×156(비율 1.88) ≈ 데스크톱 750px 637×341(비율 1.87) 동형. `.css`만(테스트 게이트 N/A). (PR #394) |
| 2026-06-18 | **랜딩 CTA 마이크로카피 제거 — "신용카드 없이 · 10초면 충분해요" 삭제** — Google 로그인 버튼 아래 `<p class="lp-cta-micro">` 한 줄 제거(피싱 사이트 인상 제거 요청). 더 이상 참조 없는 `landing.css` `.lp-cta-micro` 규칙도 정리. 순수 마크업/텍스트 삭제, `.java`·스키마·로직 0(테스트 게이트 N/A). (PR #395) |
| 2026-06-18 | **정원 부트스트랩 로드순서 회귀 자동 그물 — CI 정적 가드 + N-097 정정** — #391→#393 로드순서 회귀(CDN `defer` Alpine × `type=module` 번들의 `alpine:init` race)가 vitest 순수함수 그물(`garden-pure`)·헤드리스 사각을 빠져나가 운영을 깬 것을, 부트스트랩 **구조**를 박는 정적 가드로 자동 차단. `frontend/test/garden-bootstrap.test.ts` 신설 — `htmlHasNoAlpineCdn`(Alpine CDN `<script>` 부활 차단)·`bundleOwnsAlpine`(main.ts가 `import Alpine`+`Alpine.data`→`Alpine.start` 순서)로 8단언, 깨진 픽스처(CDN 부활·순서 역전·import 없음)와 실제 파일 양쪽 단언. TDD: stub 가드로 4 RED→본 구현 406 GREEN. CI 0변경(`npm test`가 새 `.test.ts` 자동 포함)·production 코드 0변경. `learning-notes` N-097 정정 — "CDN defer 번들 늦어도 `alpine:init` 보장"이 #393으로 반증된 분석이라 해법(번들 흡수)·이 가드 링크로 화해(근거 보존). 실 브라우저 충실 검증은 CLAUDE.md soft 게이트 유지(역할 분담). (PR #396) |
| 2026-06-18 | **출판사 건물 도입 — CoC 베이스 B단계 완성** — 정원 수집축 6번째: `Building` 엔티티+`BuildingRepository`+`BuildingState`+`BuildingUnlockCalculator`(출판사명 정규화·contains·권수 임계) 신설. `PlacementAxis.BUILDING` 추가→`ownedPlants()` 경유 배치 팔레트·검증 자동 편입. `GardenView`·`GardenService`에 건물 3필드 추가(5.7절). 다양성축 `PUBLISHER` 제거(건물로 흡수) — `DiversityKind`·`DiversityUnlockCalculator.resolve` 인자 축소·`GardenView` publisher 필드 제거. V46 마이그레이션: `building` 테이블+10개 국내 출판사 시드+`DELETE diversity_plant WHERE kind='PUBLISHER'`. `garden.html`: 🏢 건물 탭 신설·다양성 탭 출판사 소그리드 제거·진행요약 건물 N/M. TDD: `BuildingUnlockCalculatorTest`(14건 RED→GREEN) + `GardenServiceTest` 건물 배선 4건 신설·기존 publisher 단언 정정 + `DiversityUnlockCalculatorTest` AUTHOR 전용으로 정리. 실 브라우저 게이트 필요(건물 탭·팔레트). (PR #397) |
| 2026-06-18 | **정원 진화 컨셉 백로그 + 완료 계획 md 정리** — 사용자 구상 기록(향후·구현 X): 수집 해금을 *이진(보유/미보유)* → *누적 권수 단계적 진화*로 확장(작가 캐릭터 외형 발전·출판사 건물 성장, tier별 `spriteId`). `plan.md` 정원 §핵심 메커닉에 백로그 항목 + 비전 메모리 `garden-vision-coc-zoo` 기록. 완료된 계획 md 3개(garden-build step-a/b·publisher-building) 정리(gitignore라 git diff 무관). 더불어 위 #397 행 PR번호 누락 보충. `.java`·스키마 0(문서만). (PR #398) |
| 2026-06-18 | **정원 C1 — 보기 엔진 SSR→Phaser 전환(C2 배회 AI 토대)** — 보기 모드를 서버 렌더 정적 DOM에서 Phaser 렌더로 전환. `scene.ts` `GardenSceneConfig.readonly` 플래그 추가 → create()에서 드래그/팬/줌/선택 핸들러 조건부 skip, spawnObject에서 setInteractive skip, drawGrid skip(보기엔 격자 불필요). `component.ts` `phaserReady` 상태·`init()` Alpine 훅·`mountView()`(readonly:true Phaser 마운트)·`startEdit()` 수정(보기 Phaser 먼저 destroy 후 편집 마운트)·`cancel()` 수정(편집 destroy 후 mountView 재마운트). `garden.html` SSR 스테이지 `x-show="!editing && !phaserReady"`(no-JS 폴백 보존)·Phaser 보기 컨테이너 `#garden-phaser-view` 추가. 번들 재빌드. 실 브라우저 5항목 통과(콘솔 0·캔버스 1000×800·SSR→Phaser 인계·꾸미기↔취소 재마운트·FIT 스케일 5:4 비율 유지). 백엔드 0변경. (PR #399) |
| 2026-06-18 | **정원 C2 — 작가 캐릭터 자유 배회 + 풀어놓기** — 배회 AI(프론트)와 풀어놓기(좌표→컬렉션, 백엔드)를 한 PR로. 프론트: `pure.ts`에 `WanderState`·`wanderStep` 순수 상태머신 추가(idle↔walk, rand 주입으로 결정성 보장), `scene.ts`에 `spawnCharacter()`·`update()` 루프(매 프레임 kind='character'만 wanderStep 적용 후 restack()) 추가, `component.ts`·`GardenSceneConfig`에 `characters` 필드 연결. 백엔드: `OwnedCharacter` 레코드 신설, `GardenView.ownedPlants()`에서 AUTHOR 루프 제거(→ save 거부·팔레트 제거·유령방지 자동), `ownedCharacters()` 추가, `GardenController`·`garden.html` 부트스트랩 JSON에 `characters` 주입. V47 마이그레이션: `DELETE FROM garden_placement WHERE axis = 'AUTHOR'`. TDD: wanderStep 경계 7종(idle만료→walk, 미만료, walk미도달, 도달→idle, overshoot clamp, dt=0, rand결정성) RED→GREEN, `GardenViewC2Test` 3건(ownedPlants AUTHOR 제외·ownedCharacters 보유만·N-055 미보유 가드), `GardenLayoutServiceTest` 2건 repurpose(AUTHOR save 거부·잔존 DB 배제). 번들 재빌드. 브라우저: 에러 0·캔버스 정상·캐릭터 주입 경로 확인. (PR #400) |
| 2026-06-18 | **마을 SPA S1 — 백엔드 JSON API 레이어 신설** — `GardenApiController`(`@RestController /api/garden`) + `GardenApiResponse`(DTO 평탄화 record 계층) 신설. `GET /api/garden` → 200 JSON(world·nickname·placed·decorationCatalog·catalog 6축). `POST /api/garden/layout` → 기존 `GardenLayoutService.saveLayout` 재사용. DTO 평탄화: `PlantState` 등 JPA 엔티티 포함 State 타입을 `PlantDto/GenrePlantDto/DiversityPlantDto/RecipePlantDto/AuthorCharacterDto/BuildingDto/OwnedCharacterDto` 7종 nested record로 매핑(엔티티 직렬화 금지). `PlacedItem`·`DecorationOption` safe record 직접 재사용. Security: default-deny `/api/**` 자동 인증. TDD: `GardenApiControllerTest` 9건(미인증 302×2, 인증 200+JSON 구조 키, DTO 직렬화 에러 없음, 닉네임 유저 격리, CSRF 403, 빈 배치 200, 미보유 400, 미지소품 400) RED→GREEN. 기존 테스트 회귀 0. (PR #402) |
| 2026-06-18 | **정원→마을 리네임 — 사용자 대면 텍스트·URL** — "정원"→"마을", `/garden`→`/village`로 사용자 대면 층만 리네임(코드·DB·CSS·번들 경로는 garden 유지 — 의도적 혼재). `GardenController`: `/village`·`/village/layout` 매핑 신설 + `GET /garden` 레거시 302 리다이렉트(옛 북마크 호환). `garden.html` 한국어 텍스트(제목·인사·꾸미기 안내), `dashboard.html` 링크+라벨, `landing.html` nav·메타·섹션·슬로건 전수. `component.ts` fetch `/village/layout`, `scene.ts` "마을이 가득 찼어요 🌿". 번들 재빌드. TDD: "GET /garden → 302 /village" RED→GREEN + 기존 `/garden`·`/garden/layout` 단언 → `/village`·`/village/layout` 갱신 + `LandingPageTest` 링크·키워드 갱신. `./gradlew test` 통과. 실 브라우저 5항목 게이트(§5). (PR #401) |
| 2026-06-19 | **마을 SPA S3 — 도감(6축) Vue 이전 + 잠금 라벨 API 보강** — `GardenApiResponse` 6축 DTO에 잠금 라벨 필드 추가(`PlantDto.unlockThresholdDays`, `GenrePlantDto.genreLabel`, `DiversityPlantDto.thresholdCount`, `BuildingDto.matchName·thresholdCount`, `AuthorCharacterDto.matchName`, `RecipePlantDto.story`). 레시피 미발견 story null 마스킹(백엔드). `GardenDex.vue`(도감 셸: 진척바+탭7개+보유필터+6축) + `DexCell.vue`(owned/locked/mystery 분기) 신설. `main.ts` Alpine 제거 + Vue 도감 마운트. `package.json` alpinejs devDep 제거. `garden.html` 도감 섹션 `#village-dex` 마운트 포인트화(SSR 폴백 유지). `garden-bootstrap.test.ts` `bundleHasNoAlpine` 가드 추가(Alpine 재도입 방지). TDD: `GardenApiControllerTest` 8건 RED→GREEN(6축 잠금 필드 직렬화 + N-055 미발견 story null·발견 story 노출). vitest 416건, `./gradlew test` 전체 통과. 실 브라우저 게이트(6축 탭전환·잠금라벨·Alpine 제거·콘솔 에러 0). (PR #405) |
| 2026-06-19 | **다중 세션 워크트리·브랜치 잔재 청소 + 트러블슈팅 T-064** — 이전 다중 세션들이 쌓은 워크트리·브랜치 잔재 청소: `claude/*` 머지 자동브랜치 19개 `git branch -d` + `.claude/worktrees/` 고아 폴더 19개(활성 워크트리 0·미커밋 0·`HEAD@origin/main` 포함 점검 후 `rm`). 그 과정에서 만난 함정을 `T-064`로 기록 — ① squash 머지로 `git branch --merged`가 `#399~#403`으로 머지된 `feat/garden-*`를 '미머지'로 오판 ② 고아 워크트리 폴더는 `git worktree prune` 미포착(메타만 청소) ③ 청소 절차(`claude/*` `-d` / `feat/*` PR 대조 후 `-D` / 고아 폴더 점검 후 `rm`)+예방(머지 후 `worktree remove`+`branch -d`). 문서만(`.java`·스키마 0). (PR #404) |
| 2026-06-19 | **마을 SPA S4 — /village Vue 셸 풀 컷오버** — `VillageApp.vue` 신설(셸 컴포넌트, `/api/garden` 1회 fetch + 에러 핸들링), `GardenGame.vue`·`GardenDex.vue` self-fetch 제거→defineProps 수신(`GameData`·`CatalogDto`), `main.ts` 두 마운트→단일 VillageApp 마운트, `garden.html` SSR 폴백 253줄 제거·`#village-app` div만(얇은 셸), `GardenController.village()` nickname만 모델·`GardenService` 의존성 완전 제거. TDD: `village_spaShell_hasNicknameOnly`·`village_spaShell_hasVillageAppMount` RED→GREEN, `GardenControllerTest` 전체 통과. 브라우저 게이트: `/api/garden` 1회/페이지로드·콘솔 에러 0·6축 도감·게임 캔버스 정상. S5 폐지(S4에서 완전 교체 완료). (PR #406) |
| 2026-06-19 | **마을 캐릭터·건물 SVG 승격 — 파일럿(작가 1·건물 1)** — 작가 캐릭터·출판사 건물을 식물 A2 패턴(이모지→코드 벡터 SVG)으로 승격 착수. 렌더 파이프라인(`scene.ts` `spawnCharacter`/`spawnObject`·`DexCell.vue`)은 이미 `spriteId` 분기 완비라 작업 = **`<symbol>` 그리기 + `sprite_id` 시드**(엔티티·scene.ts·Vue 0변경). 파일럿 2종(`han_gang`·`minumsa`)만 승격해 32px 가독성·접지·배회 룩을 실 게임 선검증(N-080). 신설 fragment `garden-character-sprites.html`(한강, 발밑 작은 타원 그림자로 접지)·`garden-building-sprites.html`(민음사 아이소 박스). **건물 접지(실 게임 피드백 보정)** = 밑면 footprint를 타일 다이아 각도(≈0.4)·앞코너 발밑 앵커로 정합 + `scene.ts drawShadows`가 건물(axis=BUILDING)만 식물용 캐스트 그림자 생략 → CoC식 flush 접지(식물·소품 그림자 유지). 사유 = 게임 Phaser 그림자가 얇은 식물용(`plantPx*0.55`)이라 넓은 박스 바닥보다 좁아 "그림자 위에 뜬" 인상 + footprint 각도 불일치. scene.ts 변경이라 번들 재빌드(T-063). + `garden.html` `<th:block>` 2줄 + V48 `sprite_id` UPDATE(파일럿 2행만 — V47 선점으로 V48 조율). 회귀 가드 = `FlywayMigrationTest`에 부분 승격 불변식(파일럿 2종 code 채움·나머지 null 폴백 유지, N-055) RED→GREEN, `./gradlew test` 전체 통과. 실 브라우저 게이트(T-053/054): 배회 작가 SVG·발밑 접지·배회 / 건물 배치 SVG·발밑 앵커 / 도감 보유 셀 SVG·미보유 🔒 / 콘솔 0. 후속 = 대표 고유 + 제네릭 확장. |
| 2026-06-19 | **마을 캐릭터·건물 SVG 승격 — 나머지 전종(작가 19·건물 9)** — 파일럿(#407)이 확정한 룩·구조를 복제 확장해 **전종 SVG 승격 완료**. `garden-character-sprites.html`에 작가 19종 `<symbol>`(N-080 = 대표 고유[무라카미 고양이·카뮈 태양·톨스토이 흰 수염·카프카 갑충 등] + 베이스 아바타[머리·옷색·소품] 변형) + `garden-building-sprites.html`에 건물 9종 `<symbol>`(민음사 아이소 footprint 기하 복제 + 지붕형·벽색·층수 변형 — 오피스/성/누각/공사장 등) 추가. V49 `sprite_id = code` UPDATE(나머지 28행). 회귀 가드 = `FlywayMigrationTest` 불변식을 **부분 승격→전종 승격**으로 전환(author_character·building 전 행 `sprite_id=code`·비null, 새 시드 누수 가드 N-055) RED→GREEN, `./gradlew test` 전체 통과. `scene.ts`·Vue·엔티티 변경 0(파일럿 접지 보정 계승) → 번들 재빌드 불요. 30종 게임 렌더 크기 자가 검증(32px 가독성 게이트) + 실 브라우저(T-053/054). 절차 걷기 애니·프로 아트는 같은 `spriteId` 위 후속. |
| 2026-06-19 | **마을 캐릭터 절차 걷기 애니메이션 (D 폴리시 — 통짜 transform)** — SVG 승격(#407·#408)으로 깔린 통짜 스프라이트 위에 **코드만의 절차 걷기 애니** 적용(그림 0, 30종 즉시). 배회 작가가 걸을 때 **통통(bob)·좌우 흔들(tilt)·납작늘임(squash)·진행방향 바라봄(flipX)**, 멈추면 **잔잔한 숨쉬기(idle breathing)**. 기법 = **단일 Image transform 변형**(파츠 분리/스프라이트시트 아님 — 사용자 결정; 파츠·시트는 프로 아트 졸업 후속). `pure.ts` 순수함수 `walkPose(phase,clockMs,dx,prevFlipX)→WalkPose`(θ=2π·clock/STEP, bob=−A·\|sinθ\| 위로만, tilt=B·sinθ, squash 부피보존 scaleX·scaleY=1∓WALK_SQUASH·\|sinθ\|, flip 데드존) + 상수 6종(`WALK_BOB_PX`·`WALK_TILT_DEG`·`WALK_SQUASH`·`WALK_STEP_MS`·`IDLE_BREATH*`·`FLIP_DEADZONE`), DOM·Date·random 0(결정성). `scene.ts` 3곳: ① `update()`가 wanderStep 뒤 walkPose 적용 — **시각 bob ⊥ 논리 footY 분리**(bob은 `o.y`에만, depth·접지는 `footY` setData → restack 정렬키 footY로 = depth 떨림 차단) + `setDisplaySize` scale을 base로 곱한 squash(크기 보존) + setFlipX. ② `spawnCharacter` baseScale 저장 + animClock 위상 오프셋 시드(걸음 군무 방지). ③ `restack` 정렬키 footY 우선(식물·건물은 `o.y` 폴백 = 무변경). 엔티티·SVG·Vue 도감·백엔드·저장 무변경. TDD: `garden-pure.test.ts` walkPose 경계 11종(idle 정지 / bob 범위·부호[발 안 뚫음] / 주기성·극값 / flip+데드존 / squash 위상·부피보존 / 위상 연속성 / idle facing·breathing) RED→GREEN(vitest 429). 번들 재빌드(T-063). 실 브라우저 게이트(T-053/054): 배회 작가 walk/idle·접지 유지·발 안 뚫음·depth 겹침·위상 분산·콘솔 0·편집↔뷰·도감 회귀 0. |
| 2026-06-19 | **마을 모바일 세로 진입 자동 가로 S2 — Phaser 카메라 회전** — 세로(portrait) 화면에서 `cam.setRotation(Math.PI/2)` + 줌 기준축 전환(portrait 시 캔버스 높이 축)으로 게임을 가로로 꽉 채움. DOM CSS rotate(Phaser#7175 입력 깨짐) 대신 카메라 회전으로 입력↔렌더 정렬 유지. 팬 제스처를 `getWorldPoint` 경유로 교체해 회전 축 어긋남 제거. `applyOrientation(w,h)` 메서드로 초기화·resize 핸들러 통합(모바일≤900px 한정, 데스크톱·태블릿 회전 0). vitest 429 Green, 번들 재빌드. 실 기기(iOS Safari·안드 크롬) 스파이크 게이트 선행 필요. |
| 2026-06-19 | **마을 풀스크린 단일 게임 셸 재편 S1** — `/village` 를 카드 2분할에서 **뷰포트 전체 단일 게임 스테이지**로 재편. `garden.html` `.container.garden-wide`·브랜드 헤더 제거, `#village-app` `data-nickname` 전달, `viewport-fit=cover` 추가. `VillageApp.vue` HUD 오버레이(브랜드·닉네임·꾸미기·도감·대시보드 버튼) + 도감 전체 오버레이(ESC·백드롭 닫힘). `GardenGame.vue` 헤더 제거·편집 패널 하단 오버레이(`village-edit-panel`)·`Scale.RESIZE`·`defineExpose({startEdit})`. `scene.ts` `scale.on('resize')` 리사이즈 핸들러(줌·센터 재계산). `app.css` 마을 풀스크린 레이아웃 288줄 추가(`position:fixed` 루트·HUD·도감 오버레이·편집 패널·데스크톱 ≥900px 액자·`dvh`/safe-area). `component.ts` 참조 0 확인 후 삭제. vitest 429 Green, 번들 재빌드. S2(모바일 세로 자동 가로 회전) 후속. |
| 2026-06-19 | **마을 세로폰 HUD·도감·편집 패널 가로 회전 동반** — portrait(<=900px+h>w)에서 게임 Phaser 카메라 setRotation(+pi/2)에 맞춰 DOM UI(VillageApp.vue HUD+도감 오버레이·GardenGame.vue 편집 패널)를 .village-ui-wrap 래퍼로 묶고 rotate(-90deg) + 치수 스왑(width:100dvh;height:100dvw) + translate(-50%,-50%)로 동일 시각 방향 회전. portrait·max-width:900px 미디어 쿼리로 데스크톱·태블릿 회귀 0. 번들 재빌드. 실 기기(Chrome 확장) 방향·버튼 클릭 게이트 필요. |
| 2026-06-19 | **마을 강제 회전 폐기·순수 반응형+보기 줌** — #411·#412 강제 가로 회전 폐기. `scene.ts` `fitCamera(w,h)` — `containZoomFor` 신설(contain 줌), 초기줌=min(식물36px, contain) 절충, `ZOOM_MIN=0.25`로 완화, 회전 없음(기기 방향 그대로). 팬·핀치·휠 보기/편집 공통 분리(보기 모드 줌·팬 가능). `VillageApp.vue` `.village-ui-wrap` 래퍼 제거→HUD 직배치. portrait 미디어 쿼리 삭제. `pure.ts containZoomFor` + TDD 435 Green. 번들 재빌드. |
| 2026-06-19 | **마을 카메라 중앙 정렬 + 줌아웃 하한 containZoom 동적화** — Phaser Camera.centerOn의 zoom 미보정 결함으로 좌상단 쏠림 → cameraCenterScroll(scrollX=worldW/2-viewW/(2·zoom)) 공식으로 대체. 줌아웃 하한을 고정 ZOOM_MIN=0.25에서 iewZoomBounds() containZoom(월드 전체 딱 들어오는 줌)으로 동적화 — 사용자가 줌아웃 시 마을 전체가 항상 화면에 보임. pure.ts iewZoomBounds·cameraCenterScroll 신설 + TDD 8종 RED→GREEN(vitest 443). scene.ts fitCamera·pinch·wheel 핸들러 갱신. 번들 재빌드. |
| 2026-06-19 | **마을 모바일 초기 중앙 정렬 버그 픽스** — `scene.ts create()` 첫 측정을 `getBoundingClientRect`에서 `this.scale.gameSize`로 통일, `delayedCall(0)` 재보정 추가, `initH: 0` 폴백 제거(T-069). 번들 재빌드. |
| 2026-06-20 | **마을 모바일 가로 중앙 정렬 진짜 원인 수정** — `cam.setBounds(0,0,W,H)`가 containZoom 상태에서 centering 음수 scrollX를 클램핑해 왼쪽 치우침 발생(T-069). `fitCamera()`에서 centering offset 기반 동적 bounds 확장으로 수정. 번들 재빌드. |
| 2026-06-20 | **README.md 최신화** — 독서 마을(수집형 게임화) 섹션 신설, 부채 모델을 cap → 7일 윈도우 per-day + backward-only catch-up으로 갱신(2.1 타이머·6 도메인 규칙·7 의사코드), 기술 스택에 Vue 3·Vite·TS·Phaser 3 추가, 로드맵에 수동입력·7일 윈도우 부채·작가 격언·마을·랜딩 리디자인 반영, 잔디 섹션에 수동입력·연속일 성장 식물 추가. 문서만(`.java`·스키마 0). |
| 2026-06-20 | **PWA L1 풀스크린** — `manifest.json`(standalone·start_url=/dashboard·세이지 테마)·임시 아이콘 4종(192·512·maskable·apple-touch, sharp SVG→PNG)·`pwa-head` 공통 fragment(garden·dashboard·landing·login)·SecurityConfig `/manifest.json`·`/icons/**` 공개. vitest 8종 + MockMvc `PwaStaticAccessTest` 2종 RED→GREEN. "홈 화면에 추가" 시 브라우저 UI 없는 풀스크린 실행 기반 완성. |
| 2026-06-20 | **PWA L2 오프라인/설치** — `static/sw.js` 신설(install precache·activate 구 캐시 삭제·fetch: HTML network-first·garden.js network-first·정적자산 cache-first·`CACHE='shell-v1'`)·`pwa-head` fragment에 `serviceWorker.register('/sw.js')` 추가·SecurityConfig `/sw.js` permitAll. vitest `pwa-sw.test.ts` 7종 + MockMvc `PwaStaticAccessTest.swJs_isPublic` RED→GREEN. 안드 크롬 설치 배너 자격·오프라인 셸 기반 완성. learning-notes N-100·N-101 TOC 갱신·N-101 본문 추가. |
| 2026-06-20 | **PWA L3a 독서 리마인더 푸시** — VAPID·PushSubscription(1:N 멀티 디바이스)·PushApiController(subscribe upsert/unsubscribe IDOR차단/public-key)·PushReminderService(매일 KST 20시 배치·하루 1회 멱등·404/410 만료 구독 삭제)·PushReminderScheduler(@ConditionalOnProperty push.enabled)·sw.js push+notificationclick 핸들러·VillageApp HUD 알림 토글(iOS 미설치 게이트)·V50 마이그레이션. TDD 19종 RED→GREEN. VAPID 키 환경변수 주입, 실키 커밋 0. |
| 2026-06-20 | **learning-notes N-102 추가** — VAPID(Web Push 서버 신원 증명 키쌍): EC P-256 공개키/비공개키 역할, 왜 필요한가(endpoint 스팸 차단), JWT 서명·검증 흐름, 키 변경 시 전원 재구독, `npx web-push generate-vapid-keys`, 도장/인감증명서 비유. TOC·본문 추가, N-101 링크. 문서만. |
| 2026-06-20 | **PWA L3b 비활동 복귀 nudge 푸시** — `marketingPushConsent`·`marketingPushConsentAt`·`marketingPushNudgeSentAt` 필드 신설(이메일 동의·L3a와 채널별 독립, §50 매체별 동의) + V51 Flyway + `RetentionPushService`(findRetentionPushTargets JPQL: exists 구독·7일 비활동·채널별 멱등, null-state 가드 N-055) + `RetentionPushScheduler`(매일 KST 19시, `@ConditionalOnProperty nudge-push.enabled`) + `PushApiController#setMarketingConsent`(POST /api/push/marketing-consent) + VillageApp 복귀 알림 토글(VAPID 체크는 ON 경로만, OFF는 즉시 철회). TDD 20종 RED→GREEN(§50 payload "(광고)" 포함·emailVerified=false 포함 단언). VAPID 미설정 브라우저 검증 완료. |
| 2026-06-20 | **문서 정비 (md 점검)** — changelog 표 형식 복구(표 중간 빈 줄 6개·잘린 중복 행 1개 제거 → GitHub 표 렌더 정상화) · DEVLOG에 "Phase 10 이후는 changelog 단일출처" 아카이브 명시 · design 문서 stale 갱신(domain-design `User` 표를 login_id 식별모델·passwordHash nullable로 최신화, admin-data-lookup·sns-design 상단 "코드 없음"→"구현 완료", reading-personality §8 공개 생성 트리거를 PR #241 해소로 표시). HELP.md는 이미 .gitignore라 무조치 확인. 문서만(코드·DB·테스트 무변경). |
| 2026-06-20 | **PWA L3c 설치 유도 칩** — `static/pwa-install.js` ESM 신설(빌드 없음): 우하단 알약 칩 + × 7일 침묵. 크로미움(beforeinstallprompt 디퍼드·`appinstalled` 자동 제거)·iOS Safari(공유→홈 화면 안내 오버레이)·standalone/기타 숨김. `pwa-head.html`에 `<script type="module">` 전역 주입. SecurityConfig `/pwa-install.js` permitAll 추가. vitest 순수 함수 21종(isDismissalActive·detectPlatform·decideChip) RED→GREEN. 실 브라우저 게이트 필요(안드 크롬·iOS Safari·× 7일·타이머 비침범). |
| 2026-06-20 | **책 상세 페이지 개편** — ① 누적 시간 소수점 제거(Thymeleaf `${x} / n`은 ${} 밖 산술이라 소수로 평가 → 전체를 한 `${...}` SpEL로 감싸 정수 나눗셈, 일자별·history와 통일) ② **책별 잔디 완전 제거**(`BookContributionService`에서 `ContributionGraphBuilder`·목표 스케줄·Clock 의존 들어냄 — 잔디 폐지 방향; 전체·프로필 잔디는 유지) ③ 일자별 기록을 독서 기록과 동일한 **월별 ◀▶ 스크롤**로 통일(`MonthlyReadingSection.groupByMonth` 공유 헬퍼 추출). TDD: 누적 소수 버그 RED("1.5시간")→GREEN, 컨트롤러 렌더·서비스 월 묶음. |
| 2026-06-20 | **책 상세 헤더 빈 공간 수정** — `.book-detail-head` 과다 여백 버그 수정(#425 회귀). 원인: `<p>` 안에 블록 요소 `<details class="buy-menu">`를 넣어 브라우저 파서가 빈 `<p>` 2개 자동 생성 → `book-detail.html` 구매 래퍼 `<p>` → `<div class="book-buy">`로 교체(세 분기 모두 안전). `app.css`에 `.book-detail-head .book-meta > p:not(.affiliate-note) { margin: 0; }` 추가해 status 행 기본 margin 제거. 실 브라우저 검증: 빈 p 0개·status p margin 0px 확인. |
| 2026-06-20 | **PWA 설치 칩 라벨 줄바꿈 수정** — `pwa-install.js` `_showChip` 내 label 버튼에 `white-space:nowrap` 추가. 320px 소형폰 우하단 코너에서 "📲 홈 화면에 추가"가 단어 중간("홈 화"/‌"면에 추가")으로 줄바꿈되던 것을 한 줄 고정. 순수 CSS 1줄. vitest 480개 green(순수 함수 계층 무변경, 실 브라우저 검증 필요). |
| 2026-06-20 | **마을 HUD 액자 안 배치(데스크톱)** — `app.css` 마을 섹션에 `@media (min-width: 900px) .village-hud` 블록 추가: `inset:auto; top/left:50%; transform:translate(-50%,-50%); width:min(100%,1000px); height:min(100dvh,800px)`. 기존엔 HUD가 풀 뷰포트(inset:0)에 펼쳐져 로고·버튼이 액자 밖에 뜨던 것을 `.village-game-root`와 동일 식으로 액자에 정확히 포갬. 모바일(<900px) 무변경. CSS만(VillageApp.vue·garden.js 무변경). 실 브라우저 CSS 주입 검증: hudWithinFrame=true·로고·버튼 모두 액자 안 확인. |
| 2026-06-20 | **SW cache-first stale 수정 — shell-v4·NETWORK_FIRST 일반화** — `sw.js` 캐시 버전 `shell-v3`→`shell-v4`(기존 사용자 캐시 무효화), 파일명 고정 자산(garden.js·app.css·pwa-install.js)을 `NETWORK_FIRST` 배열로 묶어 network-first 전략 일반화. garden.js 전용 단일 분기 → `NETWORK_FIRST.includes()` 일반 분기. 근본 원인: #427(`white-space:nowrap`)이 배포 후에도 stale SW가 구 pwa-install.js를 cache-first 서빙해 두 줄 유지. 아이콘·manifest cache-first·HTML navigate·API 분기 무변경. |
| 2026-06-21 | **learning-notes N-105** — 사파리/iOS 캐시 3계층(① HTTP 캐시 ② SW Cache Storage ③ SW 스크립트 갱신)의 수명 차이 정리. SW Cache Storage는 시간만료 없이 코드 주도(`caches.match` 히트 즉시 반환·TTL 개념 없음), iOS는 7일 미사용 시 ②③ 자동 삭제하되 홈 화면 PWA(standalone)는 면제·탭과 스토리지 분리 → "Safari 탭 옛 버전 / 홈 PWA 최신" 현상의 메커니즘. #430(SW stale 수정) 후속 개념 정리. 문서만(코드·DB·테스트 무변경). |
| 2026-06-21 | **알림 설정 통합** — 마을 HUD 푸시 버튼 2개 완전 제거, 설정 페이지 "알림" 섹션으로 통합(① 매일 독서 알림·② 복귀 알림(광고)·③ 복귀 메일). `notification-settings.js`(빌드 없는 ESM, readyState 타이밍 보호·iOS 게이트 포함)·`SettingsController`(vapidPublicKey·marketingPushConsent 모델 추가)·`VillageApp.vue`(푸시 로직 전면 제거)·`GardenController`(vapid/consent 모델 제거)·`garden.html`(vapid 메타·data-consent 제거)·CSS(btn-push-toggle 스타일). garden.js 재빌드. |
| 2026-06-21 | **chore: .gitignore node_modules 전역화** — 루트에 잘못 생긴 `node_modules`(루트 `package.json` 없이 `.vite/vitest` 캐시만 든 잔재) 삭제 + `.gitignore`의 `frontend/node_modules/`를 전역 `node_modules/`로 일반화(모든 깊이 차단). `git status` 깔끔화. |
| 2026-06-21 | **정적 자산 콘텐츠 해시 도입 — 캐시 stale 근본 해결** — `application.properties`에 `spring.web.resources.chain.strategy.content.*` 켜기: Spring이 정적 자산에 내용 MD5 해시 URL 부여(`app.css`→`app-<md5>.css` 등), Thymeleaf `@{}`이 `ResourceUrlProvider`로 자동 변환(빌드 파이프라인 무변경). `sw.js` NETWORK_FIRST 임시방편 졸업(해시 URL은 cache-first 안전) + PRECACHE_URLS에서 해시 자산 제거(아이콘·manifest만 유지) + 버전 `shell-v4`→`shell-v5`. `pwa-head.html`의 `/pwa-install.js` 하드코딩→`th:src="@{/pwa-install.js}"`. vitest 4종 RED→GREEN. 실 브라우저 검증: 해시 URL 서빙 확인·콘솔 에러 0·SW 활성·자산 변경 시 해시 변경 확인. |
| 2026-06-21 | **선별 SPA 단계 1a — search 페이지 Vue 섬 전환 + 페이지별 멀티빌드 인프라** — Vite 페이지별 독립 멀티빌드(`cross-env APP=search vite build` → `search.js` 단일 파일), Spring chain 해시 URL 자동 적용. `SearchApiController`(검색+추천+`myLoginId`+`rateLimited`)·`FollowApiController`(팔로우/언팔·CSRF·IDOR·멱등·자기팔로우 400·없는 loginId 404)·`SearchController` SSR 슬림화. `search.html` Vue 셸로 교체. `SearchApp.vue`(onMounted 즉시 호출·팔로우 토글·rateLimited 안내). 백엔드 TDD 13종 + vitest 5종 RED→GREEN. 실 브라우저 게이트: 검색 동작·콘솔 에러 0·해시 URL 변경 확인. |
| 2026-06-21 | **선별 SPA 단계 1b — history 페이지 Vue 섬 전환** — `HistoryApiController`(`GET /api/history` — 통합 한 방, `GrowthStage` enum→`GraphDto`에서 `growthEmoji`·`growthLabel` 문자열로 평탄화)·`HistoryController` SSR 슬림화(nickname만)·`history.html` Vue 얇은 셸 교체. `HistoryApp.vue`·`ContributionGraph.vue`·`MonthlyRecords.vue`·`WeeklyShortfall.vue` 신설. Vite 멀티빌드에 history 1줄 추가(`build:history` → `static/history/history.js`). vitest 프론트 8종 + 백엔드 TDD 4종 RED→GREEN. 실 브라우저 게이트: 잔디 377셀·탭 전환·콘솔 에러 0·해시 URL 자동 적용 확인. |
| 2026-06-21 | **선별 SPA 단계 1c — personality Vue 섬 전환(단계 1 읽기전용 3페이지 완료)** — `PersonalityApiController`(`GET /api/personality` + `POST /refresh`(429 한도초과) + `POST /select/{id}`(IDOR 가드)), DTO 평탄화(state=enum name, `generatedAtLabel` 서버 포맷, zone 미노출). `PersonalityController` POST 매핑 제거·GET 셸만. `personality.html` Vue 셸로 교체. `PersonalityApp.vue`+`PersonalityCarousel.vue`(상태 3종 v-if·캐러셀 scroll-snap JS 이관·refresh 동기 LLM 30초 타임아웃·로딩 스피너·CSRF 헤더·응답 view 교체). `static/js/personality.js` 삭제·`static/personality/personality.js` 신규 빌드. 백엔드 TDD 8종+vitest 9종 RED→GREEN. 실 브라우저 게이트: COLD_START 렌더·Vue 마운트·CSRF 메타·zone 미노출 확인. |
| 2026-06-21 | **fix: 책BTI 카드 CSS 복원 — 단계 1c Vue 전환 시 인라인 style 유실 회귀** — `personality.html` 삭제 시 `<style>` 블록 내 `.personality-entry`·`.personality-entry-selected`·`.personality-entry-head`·`.personality-badge`·`.personality-time`·`.personality-stale` 6개 규칙이 `app.css`로 이관되지 않아 카드 박스·badge 양끝 정렬·화살표 비겹침이 깨진 순수 시각 회귀 복원. `app.css` L1516 뒤에 원본 값 그대로 추가. CSS 규칙 브라우저 로드 확인·콘솔 에러 0 확인. |
| 2026-06-22 | **선별 SPA 단계 2a — follow-list Vue 섬 전환 + 공유 컴포넌트 첫 추출 + N-055 필터** — `FollowListApiController`(`GET /api/follow-list?type=`) 신설·`UserSearchResult` 직접 반환. `FollowListService` N-055 필터(login_id=null 팔로워/팔로잉 양방향 제외). `FollowListController` 얇은 셸(myLoginId+initialTab만). `follow-list.html` Vue 셸 교체. `shared/follow.ts`(getCsrfToken·UserRowData·toggleFollow)·`shared/UserRow.vue`(슬롯 방식) 첫 추출. `follow-list/` 앱(탭 pushState 동기화·popstate 뒤로가기 복원). Vite 멀티빌드 follow-list 추가. 백엔드 TDD 8종 RED→GREEN. 실 브라우저: 탭 전환·URL 동기화·뒤로가기·콘솔 에러 0 확인. |
| 2026-06-22 | **선별 SPA 단계 2b — book-readers Vue 섬 전환 + FollowAction 공용 추출** — `BookReadersApiController`(`GET /api/book-readers?isbn=`) 신설·`BookReadersResponse(reading,wanting)` 직접 반환. `FollowScopeReadersService.rows()` N-055 필터 추가. `BookController.readers()` 얇은 셸(isbn+title만). `book-readers.html` Vue 셸 교체. `shared/FollowAction.vue` 추출(self 배지/팔로우 토글 공용화) — `FollowListApp.vue`·`BookReadersApp.vue` 모두 사용. `book-readers/` 앱(reading/wanting 두 버킷, 탭 없음). Vite 멀티빌드 book-readers 추가. 백엔드 TDD 8종 RED→GREEN. 실 브라우저: book-readers 렌더·follow-list 회귀 재확인·콘솔 에러 0. |
| 2026-06-22 | **docs: git/gradle 무한 hang 대처를 CLAUDE.md·troubleshooting에 명시** — 구현 세션에서 반복된 commit 무한 로딩(커밋 훅 `gradlew test`가 멀티세션 gradle 데몬·빌드 락 경합으로 hang, Claude Code 코어 아님)의 진단·강제 정리(`Stop-Process java,git`·`index.lock` 삭제·`gradlew --stop`)를 항상 로드되는 `CLAUDE.md`에 승격(🧪 TDD "커밋이 무한 hang 하면" 소절 신설·🪢 다중세션 gradle 경합 cross-ref·🧯 "2회+ 재발 트랩은 CLAUDE.md 승격" 메타룰). `troubleshooting.md` T-078 추가 + T-076(inlineDynamicImports 멀티input)·T-077(jsdom scroll-snap 계측 0) 본문·목차 보충(누락 정합). |
| 2026-06-22 | **선별 SPA 단계 2c — block-list Vue 섬 전환 + BlockApiController 신설 + UserRow linkProfile prop** — `BlockApiController`(`GET /api/blocks`→`BlockListResponse{blocked,myLoginId}`, `POST /api/block·/api/unblock`→`BlockResult{blocked}`) 신설. `BlockResult.blocked`는 `hasBlocked(me,target)`(단방향 `existsByBlockerAndBlocked`) — `isBlockedBetween` 양방향 쓰지 않아 상대 차단 노출 방지. `BlockService.hasBlocked` 헬퍼 추가. `BlockController` GET /me/blocks 얇은 셸화(POST /block·/unblock 폼은 2d까지 유지). `shared/UserRow.vue` `linkProfile` prop 추가(기본 true — 2a/2b 회귀 0, block-list는 `:link-profile="false"` 닉네임 비링크). `shared/block.ts`(`setBlock` 유틸). `block-list/` 앱(onMounted fetch·차단 해제만·해제 후 목록 즉시 제거). Vite 멀티빌드 block-list 추가 + `static/block-list/block-list.js` 빌드 산출물. 백엔드 TDD 9종 RED→GREEN(미인증 302×2·조회 구조·차단→true·해제→false·자기차단 400·미존재 404·단방향 비노출·null loginId 가드). 실 브라우저: /me/blocks 렌더·API 정상·follow-list 회귀 0·콘솔 에러 0. `RateLimitService.clearForTest()` + `FollowApiControllerTest @BeforeEach` 테스트 격리 버그 수정. |
| 2026-06-22 | **선별 SPA 단계 2d — profile(/u/{loginId}) Vue 섬 전환 + htmx 제거** — `ProfileApiController`(`GET /api/profile`·`/api/profile/books`·`/api/profile/personality-tag`) + `ReportApiController`(`POST /api/report`·CSRF·차단 404·자기신고 400·RateLimit REPORT 무음) 신설. `ProfileController` 얇은 셸(loginId·myLoginId만). `profile.html` Vue 셸 교체·htmx 스크립트 제거. `BookSummary` 화이트리스트 평탄 DTO(엔티티 직렬화 방지). `CoupangLinkBuilder.isEnabled()` 직접 주입(`@ModelAttribute`가 `@RestController`에 무시됨 대응). `shared/report.ts`(`report()` 유틸) 신설. `profile/ProfileApp.vue`(탭·상태필터·태그 드릴다운·팔로우/차단/신고·구매·popstate URL 동기화). Vite 멀티빌드 profile 추가. `ProfileControllerTest` 셸 테스트 7종으로 정리·이관. `ProfileApiControllerTest` 25종 + `ReportApiControllerTest` 13종 RED→GREEN(전체 45 테스트). 실 브라우저 게이트 완료. |

| 2026-06-22 | **선별 SPA 단계 3 — books(/books) Vue 섬 전환 + htmx 완전 제거** — `BookApiController`(`GET /api/books`·`GET /api/books/search`·`POST /api/books`·`/{id}/status`·`/{id}/visibility`·`/{id}/delete`) 신설. DTO 화이트리스트 `MyBookSummary` 평탄 record(status name+label·visibility name+label 공존). IDOR→404 `mutate()` 헬퍼. `BookController.books()` 얇은 셸(myLoginId만). `setVisibility` htmx 분기 제거(PRG-only). `books.html` Vue 셸 교체(CSRF null 가드·`#books-app`·`data-my-login-id`·htmx 제거). `BooksApp.vue`(검색·필터computed·추가 2종·상태select·공개토글·삭제·낙관 갱신+롤백·popularity). Vite 멀티빌드 books 추가 + `static/books/books.js` 빌드 산출물. 백엔드 TDD RED→GREEN(`BookApiControllerTest` 24종+`BookControllerTest` 17종). |
| 2026-06-22 | **선별 SPA 백로그 정리 — 옛 SSR POST 핸들러 제거** — `FollowController`·`ReportController` 삭제, `BlockController`·`BookController` POST 핸들러 6종 제거(GET·buy* 4종·`/me/blocks` 유지). dead CSS `#tag-books-panel` 제거, `garden.html` htmx `<script>` 태그 제거. `GlobalExceptionHandler`에 `HttpRequestMethodNotSupportedException` 405 상태 보존 추가(기존 catch-all이 500으로 삼키던 버그 수정). `SsrMutationRemovedTest` 9종 RED→GREEN. |
| 2026-06-22 | **fix: 프로필 탭 패널 미표시 버그 수정** — `ProfileApp.vue` 탭 패널 div에서 `tab-panel` 클래스 제거(Vue `v-if`가 직접 가시성 제어하므로 불필요). `app.css`에서 죽은 `#tab-bti:checked ~ .panel-bti`·`#tab-shelf:checked ~ .panel-shelf` 셀렉터 제거(라디오 인풋이 ProfileApp.vue에 없어 적용 불가)·`.panel-bti, .panel-shelf { padding-top: 18px }` 독립 규칙 추가. 배경: 선별 SPA 2d 전환으로 라디오-탭 패턴 제거 후 `.tab-panel { display:none }` 이 패널을 영구 숨겨 책BTI·공개 책장 탭 내용이 보이지 않던 B-3 latent bug. HistoryApp.vue 라디오 탭 CSS 무변경. |
| 2026-06-22 | **fix: 내 책장(/books) 무한 로딩 — 라우트 충돌 핫픽스** — `BookController` book-detail 매핑 `/books/{id}`→`/books/{id:\d+}`(숫자 제한). 미제한 `{id}`가 같은 prefix의 Vue 번들 요청 `/books/books-<hash>.js`(2세그먼트)를 가로채 `@PathVariable Long` 변환 실패→앱 500/503 에러페이지를 내, books 섬 셸이 "불러오는 중…"에서 멈춰 있던 버그. #425 book-detail + #447 books 섬 번들이 둘 다 `/books/` prefix를 쓰며 발생(머지 후 잠복 — 헤드리스/단위테스트는 `/api/books` 목으로 실 번들 URL이 라우팅을 안 타 사각). 숫자 제한으로 비숫자 경로는 기본 정적 리소스 핸들러로 폴백→200. 충돌은 books만(9섬 vite 디렉터리×`/<seg>/{var}` 매핑 교차 감사). 회귀 TDD 2종 RED→GREEN(`BookControllerTest`: `/books/books.js`→200·`/books/not-a-number`→404). |
| 2026-06-22 | **fix: Service Worker가 에러 응답 캐싱 → 서버 fix 후에도 stale (`sw.js`)** — `sw.js` 정적 cache-first가 `res.ok` 검사 없이 응답을 캐싱해, 라우트 충돌(#450) 시기에 번들 `/books/books-<hash>.js`의 500이 `shell-v5`에 캐싱됨 → 오리진을 고쳐도 cache-first가 그 500을 영구 서빙(self-heal 불가, 영향받은 사용자 셸 무한로딩 지속). 수정=① 캐시 `put`에 `if (res.ok)` 가드(에러 응답 캐싱 금지·재발 방지) ② `CACHE` `shell-v5`→`shell-v6`(activate가 옛 캐시 purge → 기존 피해자가 다음 방문 시 자동 복구). SW 동작이라 단위테스트(전역 목 하니스 과대) 대신 실 브라우저 게이트로 검증(shell-v6 활성·shell-v5 purge·번들 200·콘솔 0). N-112·T-071·T-075. |
| 2026-06-22 | **선별 SPA 단계 4 — 대시보드(/) Vue 섬 전환** — `DashboardApiController`(`GET /api/dashboard`·`POST /api/sessions/start|stop`) 신설 + `DashboardModel.computeLive(User)` 도메인 추출. `ContributionGraphDto`·`GardenApiResponse.catalogOf()`·`QuoteDto` REST DTO 화이트리스트. IDOR → 409/400. `DashboardController` 얇은 셸(인증·역할·온보딩 분기만). `dashboard.html` Vue 셸 교체. `ReadingSessionController` htmx 분기 제거(Vue REST API 흡수). 프론트: `useReadingTimer.ts`(wall-clock 자가보정 composable)·`DashboardApp.vue`·`TimerCard.vue`·`BookPickForm.vue`·`ContributionGraph.vue`·`GardenPanel.vue`·`QuoteCard.vue`·`EmailVerifyBanner.vue`·`QuickNav.vue`. Vite 멀티빌드 dashboard 추가. TDD RED→GREEN(`DashboardApiControllerTest` 15종·`DashboardControllerTest` 5종·`useReadingTimer.spec.ts` 5종·전체 1170 테스트 통과). |
| 2026-06-22 | **chore: 대시보드 cleanup — dead 자산 제거** — `static/js/dashboard.js` 삭제(구 SSR dashboard.js — Vue 섬 전환 #453으로 `static/dashboard/dashboard.js`가 대체). 템플릿 htmx·Alpine 잔재 0 확인. |
| 2026-06-22 | **perf: PWA 내비게이션 navigationPreload 도입 (`sw.js`)** — SW가 navigate 요청을 `respondWith`로 가로채면 SW 부팅(콜드스타트)이 네트워크 요청 앞에 직렬로 얹혀 PWA 페이지 이동이 브라우저보다 느렸다. `activate`에서 `navigationPreload.enable()`(feature-detect — 미지원 브라우저는 스킵), navigate 핸들러가 `event.preloadResponse` 우선 사용(없으면 기존 fetch→cache 폴백) → 브라우저가 SW 부팅과 네트워크를 병렬화해 콜드스타트 지연 제거. island 구조(클라 라우팅 0)라 모든 페이지 이동이 실 navigate로 SW를 거쳐 전역 적용. `CACHE` 버전 무변경(프리캐시·정적 캐싱 미변경). SW 동작이라 단위테스트(전역 목 과대) 대신 실 브라우저 게이트로 검증(navigationPreload 활성·navigate에 `Service-Worker-Navigation-Preload` 헤더·콘솔 0·오프라인 폴백). N-083. |
| 2026-06-22 | **perf(N+1 PR-A): `findByUser` lazy-Book N+1 제거 + 뮤테이션 단건 집계** — `ReadingSessionRepository`에 3 메서드 신설: `findByUserWithBook`(LEFT join fetch — null-book 세션 보존), `sumSecondsByBook`/`sumSecondsByPublicBook`(DB GROUP BY 집계·세션 엔티티 미로딩). `BookSecondsRow` projection record. `BookReadingStatsService`를 루프+필터 제거 → GROUP BY 집계 위임+`toMap` 매핑으로 교체. `ReadingHistoryService.aggregate`·`ReadingProfileService.publicProfileOf` 각 1줄 → `findByUserWithBook`. `BookReadingStatsService.secondsForBook` 추가 + `BookApiController` 뮤테이션 3곳(add/changeStatus/setVisibility)을 전체 재집계 → 단건 `secondsForBook`으로. TDD RED-1(repo @DataJpaTest 9종 — LEFT·eager·null-book·합산·진행중제외·PUBLIC필터 등)→GREEN·RED-2(stats 서비스 mock 재작성 3종)→GREEN·회귀(History 14 stub·Profile 공개경로·ApiController 단건 seconds 1종) 전체 그린. |
| 2026-06-23 | **fix: 책방(/u/{loginId}) Vue 탭 디자인 깨짐 3종 복합 — #449 회귀·빌드 stale (`app.css`·`profile.js`)** — 선별 SPA 2d 전환 후 공개 프로필 탭에서 ①책BTI·공개 책장 패널 미표시 ②하단 `.link-row`(대시보드/내 책장/차단) 우측 25px로 짓눌림 ③활성 탭 밑줄 누락이 겹쳤다. 원인: (1) #449가 `ProfileApp.vue`에서 `tab-panel` 클래스를 뺐으나 `npm build` 누락으로 `profile.js`가 stale → production은 `tab-panel` 잔존 → `.tab-panel{display:none}` 적중(T-063 재발). (2) #449가 죽은 `#tab-bti:checked ~ .panel-bti` 셀렉터를 지우며 거기 묶였던 `clear:both`를 함께 잃어, `.record-tab{float:left}`가 clear되지 않고 `.record-card`를 collapse → 다음 형제 `.link-row`(block grid)가 stretch 폭을 못 받아 min-content로 붕괴. (3) 라디오 `:checked + .record-tab` 활성 스타일을 Vue의 `.active` 클래스로 안 옮김. 수정=`profile.js` 재빌드(tab-panel 잔재 제거)·`.panel-bti,.panel-shelf{clear:both}` 복원·`.record-tab.active` 활성 셀렉터 추가. 파급=책방 단독(history는 라디오+`:checked` 유지로 정상, 나머지 페이지는 `record-card` 미사용). 실 브라우저 검증(link-row 25→460·패널 display none→block 617px·active 밑줄 accent). T-082·N-114. |
| 2026-06-22 | **fix: SPA 전환 후 리스트 행 액션 버튼 풀폭 깨짐 — `<form>` 래퍼 소실 회귀 (`app.css`)** — SSR(Thymeleaf) 시절 책장·프로필·팔로우 리스트의 액션은 각각 작은 `<form>`으로 감싸져 있어 전역 `button{width:100%}`가 그 form 폭에 갇혀 내용폭으로 렌더됐다. 선별 SPA 전환에서 fetch 방식으로 바뀌며 `<form>` 래퍼가 사라지자 `<button>`이 flex-row 컨테이너(`.book-actions`·`.profile-actions`)의 직접 자식이 되어 `width:100%`를 그대로 받아 풀폭으로 터지며 `flex-wrap`에서 한 줄씩 세로로 쌓였다(/books 책 행의 공개토글·삭제 버튼이 풀폭 세로 배열). 수정=`.book-actions > button`·`.profile-follow .profile-actions button`에 `width:auto`(248·761·1213행과 같은 상쇄 패턴). `.book-actions`는 books·follow-list·book-readers·block-list·search(UserRow 공용 셸) 전부 커버. dashboard `.timer-controls`는 block 컨테이너라 SSR·Vue 모두 풀폭이 의도 → 회귀 아님(무변경). 순수 CSS 시각 회귀라 실 렌더 검증 게이트. |
| 2026-06-23 | **perf(N+1 PR-B): Admin 신고함/문의함 행 조립 N+1 제거** — `ReportRepository.findAllByOrderByCreatedAtDescIdDesc`에 `@EntityGraph(attributePaths={"reporter","reported"})`, `FeedbackRepository.findAllByOrderByCreatedAtDesc`·`findByTypeOrderByCreatedAtDesc`에 `@EntityGraph(attributePaths={"author"})` 추가. 서비스·컨트롤러·Row record·템플릿 무변경. TDD: 신규 `ReportRepositoryTest`·`FeedbackRepositoryTest`(@DataJpaTest, flush+clear 후 `Hibernate.isInitialized` 단언 — 1+2N·1+N 각 RED→GREEN). 회귀(`ReportServiceTest`·`FeedbackServiceTest`) 전체 통과. |
| 2026-06-23 | **fix: 대시보드 세로 간격 리듬 일원화 — quick-actions↔잔디카드 0px 붙음 해소 (`app.css`)** — Vue 전환 후 대시보드 간격이 어색하다는 보고 → 조사·실측. `DashboardApp` 자식 컴포넌트들이 Vue fragment라 wrapper 없이 `#dashboard-app` 직계로 평탄히 놓이는데, 각 자식의 개별 수직 마진(`.greeting` 20·`.highlight-actions` 14·`.quick-actions` 0·`.card` 16·`.logout-form` 18)이 흩어져 quick-actions↔잔디카드가 **0px로 붙고** 리듬이 20·16·14·0·16·18로 불균일했다(SSR→Vue 분할로 "바깥 간격 책임" 소실 — #449·#457류와 동뿌리). 수정=`#dashboard-app{display:flex;flex-direction:column;gap:16px}` + `#dashboard-app>*{margin:0}`으로 간격 책임을 컨테이너 gap에 일원화 → 직계 간격 균일 16px. 스코프가 `#dashboard-app`이라 타 페이지 영향 0(마운트 id 전부 상이: books-app·profile-app·…). Vue 컴포넌트 무변경·빌드 불필요. 순수 CSS 시각 변경이라 빌드된 `dashboard.js`+`/api/dashboard` 더미 stub 정적 하니스로 RED(20·16·14·0·16·18)→GREEN(직계 전부 16) 측정 검증. |
| 2026-06-23 | **perf(N+1 PR-C): `UserSearchService.recommend()` DB 필터 전환 — N+1 감사 종결** — `UserRepository`에 `findRecommendCandidates(@Param("viewerId") Long, Pageable)` JPQL 추가: ADMIN/본인/null-loginId/차단(양방향 not exists) 제외, `order by function('rand')` + Pageable LIMIT. `UserSearchService.recommend()`을 `findRecommendCandidates` 단일 위임으로 축소(findAll 전체로드·existsBetween 후보당 N+1 제거). import 정리(ArrayList·Collections·Objects 제거). TDD: `UserRepositoryTest`에 7종 필터 단언(excludesAdmin·excludesSelf·excludesNullLoginId·excludesBlocked 양방향·includesEligible·respectsLimit, compile RED→GREEN), `UserSearchServiceTest`에 `recommend_mapsRowsAndExcludesBlocked` 배선 가드 추가. 풀 스위트 통과. N+1 감사(2026-06-22) PR-A/B/C 3건으로 5이슈 전부 종결. |
| 2026-06-23 | **fix: 전 페이지 세로 리듬 통일 — `.page-stack` 공통 스택 도입 + 검색 제목 줄 추가 (`app.css`·Vue 5)** — #460(대시보드)에서 확정한 "간격은 컨테이너 gap이 소유" 패턴을 전 페이지로 확장. 페이지 루트 구조가 두 부류라 분기 적용: ① fragment 페이지(history·books·personality, wrapper 없이 마운트 id 직계 평탄)는 `#history-app·#books-app·#personality-app`을 `#dashboard-app` 공통 규칙에 합류(CSS만, 마크업 무변경) ② div 래퍼 페이지(search·profile·follow-list·book-readers·block-list)는 루트 `<div>`에 `.page-stack` 클래스 부여. 공통 규칙=`{display:flex;flex-direction:column;gap:16px}` + 직계 `> *{margin:0}`. 추가로 검색(`SearchApp`)만 제목 줄이 없던 불일치를 `<p class="greeting">사용자 검색</p>`로 보완(프로필 헤더 카드는 @핸들·팔로워 수 담은 의도된 디자인이라 유지). 마크업 바뀐 5개 앱 재빌드(search·profile·follow-list·book-readers·block-list). 검증=빌드 산출물+더미 stub 하니스로 search·profile 직계 간격 전부 16 균일 + flex `align-items:stretch` 폭 회귀 0(카드 폭 343=컨테이너 343) 측정. fragment 페이지는 #460과 동일 메커니즘(CSS만)이라 동형 갈음. N-115. |
| 2026-06-23 | **docs: troubleshooting T-083 추가 — `gh pr checks --watch` CI 등록 전 "no checks" 즉시 종료** — #460·#462 머지 자동화에서 push 직후 `gh pr checks <PR> --watch`가 체크 런 등록 지연 사이에 돌아 `no checks reported`로 즉시 exit 1 → 체이닝한 `&& gh pr merge`가 무산된 트랩을 박음. 해결=watch 전 `gh pr checks`가 no checks를 안 뱉을 때까지 sleep 폴링 후 watch(run_in_background). 워크트리 `--delete-branch` 로컬정리 실패는 기존 T-051이 이미 커버(이번에 참고 못 하고 재발). |
| 2026-06-23 | **perf(N+1 후속): `UserRowAssembler` 목록 행 조립 N+1 일괄 제거 — 배치 `toRows` + `@EntityGraph`** — N+1 감사 PR-A·B·C의 잔여 항목(`toRow` 행당 2쿼리: 공개책수 count + isFollowing). 헤드라인 `FollowListService`는 `Follow.follower/followee` LAZY로 행당 3쿼리(unbounded). 두 기법으로 해소: ① 배치 — 신규 `UserPublicBookCount` 인터페이스 프로젝션 + `BookRepository.countPublicByUsers`(group by 1쿼리, 공개책 0권은 행 누락 → 호출부 0-디폴트) + `FollowRepository.findFollowedIdsAmong`/`FollowService.followingAmong`(팔로우 부분집합 1쿼리). `UserRowAssembler.toRow`→`toRows(viewer, List<User>)` 배치 교체. ② `@EntityGraph` — `findByFollowee/FollowerOrderByCreatedAtDesc`(follower/followee), `BlockRepository.findByBlockerOrderByCreatedAtDesc`(blocked) 즉시 로딩으로 lazy User ×N 제거. 소비처 5곳(search·recommend·followers·following·blockedUsers·readers) `toRows`로 전환(필터·정렬 보존). 범위 밖: search의 `existsBetween`(bounded Top20) 유지. TDD: 신규 `BookRepositoryTest`(group 집계·0권 행 누락·PRIVATE 제외)·`FollowRepositoryTest`(부분집합·방향 + flush/clear 후 `Hibernate.isInitialized` fetch RED→GREEN)·`BlockRepositoryTest`(fetch). `FollowListServiceTest`에 0-디폴트(publicBookCount==0) 단언 보강. 회귀(Follow·Search·Block·Readers) 풀 스위트 통과. |
| 2026-06-24 | **fix: 대시보드 바로가기 정리 — "직접 채우기" 타일 제거 + 타일 재배치 (Vue·`dashboard.js`)** — ① 대시보드 quick-actions에서 "직접 채우기"(`/sessions/manual`) 진입 타일 제거(독서 기록의 "빠뜨린 날" 채우기 흐름 하나로 일원화 — 라우트·컨트롤러·`V22` 마이그레이션은 유지). ② 사용자 요청대로 대시보드 세로 순서 재배치: `QuickNav.vue`를 정적 마크업 3개로 분해(`HighlightActions`=내 책장·내 책방 / `PrimaryActions`=독서기록·책BTI·사용자검색 / `UtilityActions`=설정·문의피드백) 후 `DashboardApp.vue`에서 타이머→내책장·내책방→독서 잔디→PrimaryActions→정원→UtilityActions→로그아웃 순으로 배치. 두 그룹 모두 기존 `.quick-actions` 클래스 재사용(`:last-child:nth-child(odd)` 풀폭 규칙 그대로 — PrimaryActions 3개라 사용자검색 풀폭). 마운트·API·타이머 로직 무변경. 검증=헤드리스 Chrome 캡처로 최종 순서·배치 + 직접채우기 부재 확인, 콘솔 에러 0. 순수 마크업 재배치라 `.java` 무변경. |
| 2026-06-24 | **chore: PR 머지 자동화 헛폴링 예방 — `.claude/scripts/pr-merge.sh` 신설 + CLAUDE.md·T-083 보강** — DIRTY 충돌 상태에서 `no checks reported`를 "CI 등록 중"으로 오인해 무한 폴링하는 재발 함정 구조적 차단. `pr-merge.sh`=mergeStateStatus 진단(DIRTY 즉시 exit)→CI 폴링(nochecks/pending sleep)→squash 머지→원격 브랜치 삭제, 하드 타임아웃 12분. CLAUDE.md Git 워크플로 5번에 머지 전 진단 필수·스크립트 안내 추가. T-083에 "DIRTY 오진 함정" 경고 단락 + `pr-merge.sh` 참조 추가. |
| 2026-06-24 | **feat: 마을 줌아웃 여백 장식 — 물 베이스 + void 앰비언트(섬·바위·나무·수련)** — 줌아웃(containZoom) 시 노출되는 마름모 밖 75% void가 단색 하늘로 휑했던 문제 해소. 옵션 A(물 베이스+정적 앰비언트) 채택. `drawBackground` 베이스색 `--garden-sky`→`--garden-water`(#6FA8C7), 신규 `drawAmbientDecor()`가 `AMBIENT_DECOR` 10개 큐레이션 좌표를 depth 0.1~0.4 전용 밴드에 스폰(`this.objs` 미포함 → 격자·저장·배회·드래그와 격리). `garden-ambient-sprites.html` 4종 SVG(섬·바위·나무·수련) 신설. CSS `--garden-water` 토큰 + village letterbox(`#village-app`·`.village-shell`·`.village-game-root`) 배경 물색 정합. vitest: `isInsideDiamond`·`AMBIENT_DECOR` 불변식 20종 RED→GREEN(453 tests 전체 통과). |

| 2026-06-24 | **refactor(프론트): 마을 가지치기 PR-1 — 식물·소품 제거, 작가·건물 2축만 렌더** — GardenGame.vue에서 `decorations`/소품 팔레트 제거, `buildingOwned` computed로 BUILDING 축만 팔레트 표시. GardenDex.vue 6축→작가·건물 2축(탭·진척·인터페이스 모두 축소). scene.ts `create()` 루프에 `axis !== 'BUILDING' → continue` 필터(고아 식물 행 렌더 차단), `exportPlacements()` BUILDING-only 반환(고아 재저장 차단). garden.js 재빌드 포함. 백엔드 무변경(프론트 소비자만 먼저 제거 — PR-2에서 백엔드 가지치기 예정). 실 브라우저 7체크포인트 통과(식물·소품 팔레트 사라짐·도감 2축·고아 배치 미렌더·건물·작가 정상·여백 회귀 0·콘솔 0). |
| 2026-06-24 | **chore: soft 규칙 4건 훅 승격 — 커밋제목 PR번호·인라인 한글·번들 stale·세션 잔재 경고** — `check-commit-message.ps1`(신규): 커밋 제목 끝 `(#NNN)` 차단 + `-m` 인라인 한글 차단(T-026). `require-bundle-build.ps1`(신규): `frontend/**` 스테이징 시 재빌드+10섬 diff 검사(CI garden-only 사각 보완, T-082). `warn-multi-session.ps1`(확장): 머지된 `claude/*` 브랜치 카운터(임계 10) + 고아 워크트리 폴더 경고. 전 훅 TDD RED→GREEN. `.claude/settings.json` PreToolUse 배선 + CLAUDE.md soft 규칙에 훅 참조 추가. |
| 2026-06-24 | **refactor(백엔드): 마을 가지치기 PR-2 — 식물 4축 + 소품 Java 코드 제거, 작가·건물만** — Plant/GenrePlant/DiversityPlant/RecipePlant/Decoration 등 Java 클래스 27개 삭제(DB 테이블 보존, 소프트 제거). GardenView 20-arg→6-arg(authorCharacters·buildings 2축). GardenService/GardenLayoutService 의존 정리(식물·소품 Repository 참조 제거). GardenApiResponse CatalogDto 22필드→7필드. GardenApiController decorationCatalog 파라미터 제거. LayoutSaveRequest decorations를 List<Object>로 역직렬화 호환 유지. PlacementAxis TIME·GENRE·DIVERSITY·RECIPE @Deprecated. layoutOf()에 명시 BUILDING 필터(defense-in-depth). garden-sprites.html·garden-decor-sprites.html 삭제. 테스트 파일 6개 삭제·5개 재작성(TDD: layoutOf_excludesOrphanTimePlacement·save_deprecatedAxesRejected RED→GREEN). 대시보드 types.ts CatalogDto 재정의 + GardenPanel.vue 식물 도감→작가·건물 카운트. garden.js·dashboard.js 재빌드. 1120 tests 100% 통과. |
| 2026-06-24 | **fix: PWA 콜드 런치 404 — `manifest.json` start_url `/dashboard` → `/`** — standalone PWA를 OS가 메모리에서 내린 뒤 다시 열면 마지막 라우트가 아니라 manifest의 `start_url`부터 콜드 로딩하는데, 그 값이 매핑 없는 `/dashboard`(대시보드는 `DashboardController`의 `@GetMapping("/")`)라 콜드 런치마다 404 → error.html("대시보드로" 링크)가 떴다(세션은 살아 있어 `/`로 가면 즉시 대시보드 — 로그인 무관). `start_url`을 실제 라우트 `/`로 수정(로그인 시 대시보드 분기 유지). 기존 설치 사용자의 구 manifest 캐시 무효화 위해 `sw.js` `CACHE` shell-v6→v7. TDD: `PwaStaticAccessTest`에 "manifest의 start_url 경로가 404 아님" 가드 추가 + `pwa-manifest.test.ts` start_url 단언 `/dashboard`→`/` 정정(잘못된 값을 고정하던 테스트) RED→GREEN. |
| 2026-06-24 | **ci: 번들 stale 검사를 garden 1섬 → `static` 전체(10섬)로 확장** — `ci.yml` "Verify bundle is not stale"의 `git diff --exit-code` 경로를 `src/main/resources/static/garden`에서 `src/main/resources/static`으로 넓힘. garden만 검사하면 profile 등 9섬 stale이 CI를 통과해 새어나간다(T-082가 그 사각으로 샌 사례). build 스텝이 이미 10섬 전부를 빌드하므로 검사 경로만 확장. 로컬 `require-bundle-build.ps1`(#469)이 같은 검사를 하지만 fail-open·우회 가능이라 우회 불가한 최종 게이트는 CI다. 사전 검증: 새 워크트리에서 `npm ci`+10섬 빌드 후 `git diff --exit-code static` 깨끗(자폭 없음) 확인. |
| 2026-06-24 | **feat(백엔드): 마을 먹이주기 루프 PR-1 — 백엔드 도메인+API** — `DailyQuotaCalculator`(달성일 유도), `AuthorAffection` 엔티티+`AuthorAffectionRepository`(V52 마이그레이션), `FeedingService`(foodBalance·affectionByCharacter·feed), `POST /api/garden/feed` 엔드포인트, `GardenApiResponse`에 `foodBalance`·`affection` 추가. 단위·레포지토리·서비스·컨트롤러 테스트 TDD RED→GREEN 전 통과. |
| 2026-06-24 | **feat: 마을 먹이주기 루프 PR-3 — 정(affection) 진화 v1 (5단계 레벨·칭호·레벨업 연출)** — `AffectionLevel` record(feedCount → level 0~5·title·nextThreshold 순수 유도, 단일 출처), `FeedResult` 3필드→6필드(+level·title·leveledUp), `FeedingService.feed()` before/after 레벨 비교로 leveledUp 산출, `GardenApiResponse` AuthorCharacterDto·OwnedCharacterDto에 level·title 추가. 프론트: DexCell 배지 "Lv{n} {칭호} ❤️{count}" 형식, GardenDex·GardenGame·VillageApp 타입 확장, scene.ts `playFeedReaction` leveledUp 시 ✨별+칭호 tween(T-084 회피: `this.objs` 제외). TDD: AffectionLevelTest 11개 경계값 + FeedingServiceTest 4개 + GardenApiControllerTest 2개 + FeedApiControllerTest 1개 추가. 161 Java 테스트·547 vitest 전부 GREEN. 브라우저: 배지 "Lv2 친해지는 중 ❤️6" 실 렌더링·콘솔 에러 0 확인. |
| 2026-06-24 | **feat(프론트): 마을 먹이주기 루프 PR-2 — 프론트 먹이 인터랙션** — VillageApp HUD 🍙 먹이 잔액 표시, 배회 작가 탭 → POST /api/garden/feed + 하트 떠오름 반응 애니, HUD 먹이 −1·도감 정(affection) ❤️ N 즉시 갱신. scene.ts 보기 모드 한정 setInteractive + gameobjectup 탭 핸들러(팬 오발 방지 _panMoved 재사용). 반응 애니는 update() 덮어쓰기 함정 회피를 위해 독립 Text 오브젝트 tween. DexCell affection prop, GardenDex AuthorCharacterDto.affection 추가. 547 기존 테스트 그린 유지. |
| 2026-06-24 | **fix: PWA 콜드 런치 404 후속 — 서버가 `/dashboard`를 `/` 별칭으로 수신** — #473이 `manifest.json` start_url을 `/dashboard→/`로 고쳤지만, **이미 설치된 PWA는 start_url을 설치 시점에 동결**해(특히 iOS Safari는 재설치 전 영영) 구 install이 여전히 콜드 런치마다 `/dashboard`를 쏴 404가 재발했다. 서버 측 방어로 `DashboardController` 매핑을 `@GetMapping({"/", "/dashboard"})`로 확장(분기 로직 그대로 공유) + `SecurityConfig` permitAll 매처에 `/dashboard` 추가(미인증 콜드 런치도 `/`처럼 landing 200, 로그인 튕김 없음). manifest는 미래 설치용으로 `/` 유지(무변경). TDD: `DashboardControllerTest`에 `/dashboard` 별칭 케이스(온보딩→dashboard·ADMIN→/admin), `PwaStaticAccessTest`에 미인증 `/dashboard` 200(permitAll 누락 가드) RED→GREEN. |
| 2026-06-24 | **fix: PWA 설치 이름 `독서 마을` → `BookTimer`** — 홈 화면 설치 시 자동 이름이 "독서 마을"로 떠 브랜드명과 불일치. `manifest.json` `name` "BookTimer 독서 마을"·`short_name` "독서 마을" → 둘 다 `BookTimer`, `pwa-head.html` `apple-mobile-web-app-title`(iOS 설치명) "독서 마을" → `BookTimer`. manifest는 `sw.js`에서 cache-first 프리캐시라 구 캐시 무효화 위해 `CACHE` shell-v7→v8(N-116 교훈). 단 OS-레벨 설치명도 설치 시점 동결이라 기존 설치는 재설치해야 새 이름 반영(신규 설치엔 즉효). |
| 2026-06-24 | **feat(프론트): 마을 D 폴리시 PR-A — idle 행동 다양화** — `pure.ts`에 `IdleAction`(stand/read/stretch/look) 타입·`pickIdleAction`(rand 주입, 분포 60/15/15/10%)·`idlePose`(action별 transform: stand=기존 breathing / read=30% 진폭 / stretch=느린 세로 늘임 / look=clock 기반 flipX 토글) 추가. `WanderState`에 `idleAction?` 필드, `wanderStep` walk→idle 전환 시 `pickIdleAction(rand)` 자동 부여(idle 동안 고정). `scene.ts` `update()`: walk는 `walkPose`, idle은 `idlePose` 사용. read/stretch idle 진입 시 📖/🙆 이모트 독립 Text tween 1회(T-084 회피: objs 미포함). TDD: `pickIdleAction` 분포 경계 8종·`idlePose` 각 action 불변식 11종·`wanderStep` idleAction 연동 4종 모두 RED→GREEN. 568 vitest GREEN. Phaser 초기화·콘솔 에러 0 확인. |
| 2026-06-24 | **feat(프론트): 마을 D 폴리시 PR-B — 근접 상호작용** — pure.ts에 INTERACT_DIST(0.13)·INTERACT_COOLDOWN_MS(12000ms)·isNear(ax,ay,bx,by,threshold)·aceEachOther(ax,bx,prevA,prevB)(a<b→a오른쪽/b왼쪽, a>b→반대, 동위→prevFlipX 유지) 추가. scene.ts: import 확장·_greetCooldowns:Map<string,number> 멤버·update(time,delta)(time rename) 끝에 캐릭터 쌍 순회 — isNear 시 idle 쌍 마주보기(flipX 강제, 메인 루프 뒤 배치로 look 토글 우선순위 확보)·쿨다운 보호 인사 이모트(👋 70%/❤️ 30%, T-084 독립 Text tween). TDD: isNear 5종·aceEachOther 4종 RED→GREEN. 577 vitest GREEN. 번들 재빌드(T-063). |
| 2026-06-24 | **feat: 대시보드 Claude Design 이식 PR-1 — todayGoalSeconds API + timerProgress.ts 순수함수 (UI 변경 0)** — 진행바 분모 단일출처: `ReadingDebtService.todayGoalSeconds(user)` 공개 메서드 추가(`GoalSchedule.goalFor(today)` 기반, getDailyIncrementSeconds 직접호출 차단), `weeklyDebtTrace`가 동일 `buildGoalSchedule` 헬퍼 공유. `DashboardModel.LiveState` + `DashboardApiController.DashboardResponse/TimerState` 두 DTO에 `todayGoalSeconds` 필드 추가. 프론트: `types.ts TimerState`에 `todayGoalSeconds` 추가, `timerProgress.ts` 순수함수 6종(computeProgress·fmtMSS·cellTone·visibleAuthors·showStreakChip·displayName) 신설. TDD: `DashboardApiControllerTest` 3건 RED→GREEN, `timerProgress.test.ts` 37건 경계전수 RED→GREEN. 614 vitest GREEN. 번들 재빌드. **+ `/code-review` 후속 교정 2건**: ① `computeLive`가 부채 계산이 만든 `WeeklyDebt.todayGoalSeconds`(trace 오늘 날 goal)를 재사용 → `buildGoalSchedule` 이중 호출 제거(대시보드 로드·start·stop마다 DB 쿼리 4→2), `todayGoalSeconds(user)`는 `weeklyDebt().todayGoalSeconds()`로 위임해 부채 분모와 단일 출처 코드 보장. ② `computeProgress`의 floor 차감 분기 입력 `carryover`(debtCarryover) 불리언을 `LiveState`·`DashboardResponse`·`TimerState`·`types.ts`에 노출(PR-2 진행바가 추측 없이 받게). TDD: `ReadingDebtServiceTest` 3건·`DashboardApiControllerTest` carryover 3건 RED→GREEN. |
| 2026-06-24 | **feat: 대시보드 Claude Design 이식 PR-2 — 타이머 히어로 2단·진행바·잔디 세이지·빠른이동·문장 카드** — `TimerCard` 2단 재구성(좌: 상태 pill+대형숫자+진행바+목표라벨 / 우: IDLE·MEASURING·ACHIEVED 3패널). 진행바·달성은 서버 스냅샷이 아니라 클라이언트 라이브 `remainingNow`에서 파생(`computeProgress`). 순수함수 2종 추가: `panelState`(측정 중이면 항상 measuring — 종료 보존, 달성은 좌측 pill·진행바로만 표시: 사용자 결정 2026-06-24)·`goalLabel`(분/시간 표기, 0→목표없음). `ContributionGraph` 세이지 5톤(`.dash-grass-cell.s1~s5`)으로 교체 — `/history`의 `.grass-cell.level-*`과 신규명 격리(전역색 회귀 0), `cellTone` 사용, 범례(목표 미달→달성)·manual·`cellTitle` 의미 보존, streak 칩. `QuickNav` 신설(7링크 전체 2열 타일) — `HighlightActions`/`PrimaryActions`/`UtilityActions` 흡수·삭제. `QuoteCard` 카드+pill 승격. `BookPickForm` optgroup·폴백·빈책장 보존하며 디자인 톤. `DashboardApp` `applyTimerState`에 `todayGoalSeconds`·`carryover` 보존(start/stop 후 stale 방지). app.css `.dash-*` dashboard 스코프 신규(전역 `.card`/`button{width:100%}`/`.grass-cell` 회귀 격리). TDD: `timerProgress.test.ts` panelState 4·goalLabel 5건 RED→GREEN, 623 vitest GREEN. 실 브라우저(static-preview+fetch mock) IDLE·MEASURING·ACHIEVED 3상태·진행바 라이브성(33→46→100%)·반응성(N-082)·콘솔 에러 0 검증. 번들 재빌드. |
| 2026-06-24 | **feat: 대시보드 Claude Design 이식 PR-3 — 헤더 사용자영역·정원/마을 재해석·로그아웃 알약·와이드 2단 반응형** — `DashHeader` 신설(`loginId` 님 + 이니셜 아바타) — brand 로고는 `dashboard.html` SSR 유지(로고 중복 0, §0 확정). `GardenPanel` 작가/건물 재해석(식물 폐기): 무대=보유 작가 이모지·요약 `작가 N/M · 건물 N/M`·방문 작가 아바타(`visibleAuthors` 상위 3 + `+N`), `avatarInitial` 이니셜, affection/level/title 참조 금지(0 고정). `types.ts CatalogDto`에 `ownedCharacters`(name·emoji) 추가 — 백엔드 무변경(`GardenApiResponse.catalogOf`가 이미 전송). 로그아웃 알약(`width:auto`로 전역 `button{width:100%}` 상쇄 — #286 회귀 방지). `QuickNav`+정원을 `.dash-grid-2col`(auto-fit) 2단으로, `body.dashboard-page` 와이드(640px+ `.container` max-width 760 — 전역 460 무변경). 순수함수 `avatarInitial`(빈값→?) TDD 5건 RED→GREEN, 628 vitest GREEN. 실 브라우저(static-preview) 헤더·정원 무대/방문작가·2단 그리드·로그아웃 알약·콘솔 에러 0 검증. 번들 재빌드. 3-PR 대시보드 리디자인 완료. |
| 2026-06-24 | **docs: learning-notes N-117 — 섬 프론트 실 브라우저 검증을 bootRun 없이** — SSR 섬(N-109)의 로드순서·반응성 검증에 무거운 `bootRun`+MySQL Docker 대신 정적 서버 + `window.fetch` 가로채기 패턴 기록. 실제 빌드 번들·CSS·`type=module` defer 로드는 production 그대로 두고 `/api`만 mock → N-083(헤드리스 블라인드) 회피 + gradle 데몬 경합(T-078)·Docker 누적 회피. 경계(서버 계약은 MockMvc 몫)·관련(N-082·N-084·N-040) 명시. 대시보드 PR-2/3 검증에서 실제 적용한 패턴의 일반화. |
| 2026-06-24 | **fix: 대시보드 UI 디테일 — '오늘 기록 보기' 버튼·'오늘의 문장' pill 제거 + 카드 간격 여유** — ACHIEVED 패널의 `오늘 기록 보기` 링크 삭제(독서 기록은 빠른이동에 이미 있음 — 중복 제거), 유일 사용처 사라진 dead CSS `.dash-btn-ok` 동반 정리. `QuoteCard`의 `오늘의 문장` pill 삭제(문장만으로 의미 전달 충분 — 마크업만, 공유 `.dash-pill` CSS 보존). 카드 사이 간격 16→20px: 공유 `#dashboard-app{gap:16px}`(history/books/personality 4페이지 공유, N-115) 미접촉하고 `body.dashboard-page #dashboard-app`(특이도 (1,1,1)>(1,0,0)) 스코프 오버라이드로 대시보드만 — 타 페이지 회귀 0. 순수 시각 변경(timerProgress.ts 로직 미접촉)이라 단위테스트 N/A, static-preview(N-117)로 idle/achieved/measuring 3상태·카드 간격 실측 20px 균일·콘솔 에러 0 검증. 번들 재빌드. |
| 2026-06-24 | **chore: 테스트/구현 종료 시 Docker 컨테이너 정리 규칙 + `docker-cleanup.sh`** — bootRun(spring-boot-docker-compose)이 워크트리별로 띄운 MySQL 컨테이너가 검증 후 미정리(Up 좀비)·워크트리 삭제 후 고아(Exited)로 누적되던 문제. `.claude/scripts/docker-cleanup.sh` 신설 — `com.docker.compose.project.working_dir` 라벨로 **BookTimer 소속(메인+모든 워크트리)만** 선별 제거, 타 프로젝트(hoseo/twitter 등) 보호. 기본 Exited만(멀티세션 안전)·`--all` Up 포함·`--dry-run` 미리보기. CLAUDE.md 「🪢 다중 세션」(포트 8080 옆 docker 정리 규칙 신설)·「🛠️ 빌드/실행 메모」(컨테이너는 테스트 아닌 bootRun이 생성 — 오해 정정) 반영. 누적 20개 → BookTimer 15개 정리(타 프로젝트 5개 보존). T-086. |
| 2026-06-25 | **fix: 대시보드 카드 묶음 구분 — `.dash-card` 부활(CSS 주석 `*/` 버그) + 테두리 강화 + pill 제목 간격** — 발견: PR-2부터 `.dash-card` 앞 주석의 `기존 .grass-cell/.timer-*/.quick-*`에서 `.timer-*/`의 `*/`가 CSS 주석을 조기 종료시켜, 뒤 `.quick-* … */ .dash-card { … }`가 invalid 셀렉터로 묶여 `.dash-card` 규칙(배경·테두리·그림자·padding·flex)이 통째 드랍되고 있었음(콘솔 에러 0, 잠복 — 카드가 배경색 유사로 어렴풋이 보여 안 들킴). 수정: 주석 슬래시→`·`로 `*/` 제거 → `.dash-card` 부활(카드가 의도된 디자인=흰 배경+테두리+그림자, 타이머 좌우 2단으로 복원). 요청 반영: 테두리 `var(--border)`→`#D8CDB9`(또렷한 연한 선, 묶음끼리 명시적 구분, 대시보드 전용) + pill 제목 아래 간격 4px(`.dash-nav > .dash-pill`·`.dash-grass-head`·`.dash-garden-head`). 순수 시각 변경이라 단위테스트 N/A, static-preview(N-117)로 5개 카드 배경 #FCFAF5·테두리 #D8CDB9 1px·radius 18px·shadow·padding 적용 + pill 4px + 콘솔 에러 0 검증, CSSOM `cssRules`로 `.dash-card` 파싱 확인. 노트: N-118·T-087. |
| 2026-06-25 | **chore: `pr-merge.sh --rebase` 옵션 — DIRTY 자동 rebase로 흐름 끊김(재실행 누락) 방지** — 배경: #488 머지가 DIRTY(머지 충돌)로 exit 3 후 수동 rebase·force push는 했지만 `pr-merge.sh` 재실행을 누락해 머지가 ~40분 방치됨("exit→수동→재실행"으로 흐름이 끊기는 게 누락의 뿌리). 보완: `--rebase` 플래그(opt-in) — DIRTY를 만나면 한 호출 안에서 `origin/main` 자동 rebase + `git push --force-with-lease` 후 머지 폴링을 이어간다. 안전장치=현재 브랜치==PR head(엉뚱한 브랜치 방지)·워킹트리 clean, 아니면 거부. 텍스트 충돌이면 `git rebase --abort` 후 exit 3(수동 필요). 플래그 없으면 기존대로 DIRTY 즉시 exit 3 — 파괴적 자동화는 opt-in이라 회귀 0. 스모크(DRYRUN/FAKE_STATE)로 구문·회귀(기본 DIRTY exit 3·CLEAN merge·사용법 exit 2)·신규(`--rebase` would-rebase)·안전장치(브랜치 불일치 거부) 검증. |
| 2026-06-25 | **docs: troubleshooting T-088 — 백그라운드 머지 태스크 후속 누락 방지** — #488의 ~40분 방치를 트랩으로 박음. 뿌리=`pr-merge.sh` exit 후 흐름이 끊겨 재실행을 잊음, "12분 하드 타임아웃"은 스크립트가 도는 동안만 유효(exit 후엔 안 걸림). 규칙=백그라운드 머지 태스크 완료 알림 시 반드시 output·exit 코드 확인 후 후속(0/3/4/5 분기) + `--rebase`로 흐름 끊김 제거(#489 보완과 짝). T-083·T-051 자매. |
| 2026-06-25 | **fix: 대시보드 타이머 히어로 2단 레이아웃 깨짐 — 데스크톱 겹침·모바일 공백** — `.dash-card` 부활(#488)로 `.dash-timer-hero`의 좌우 2단(`display:flex`)이 동작하기 시작하며 노출된 잠복 회귀. ① 데스크톱: 좌측 숫자 `clamp(56px,13vw,108px)`(8글자 `01:43:47` 텍스트 413px)가 좌측 컬럼(339px)을 넘쳐 우측 폼을 35px 침범 → `clamp(52px,8.5vw,76px)`로 축소(텍스트 339px, 우측 88px 여유). ② 모바일(≤520px): `flex-direction:column` 전환 시 `flex:1 1 320px`의 basis가 height로 잡혀 좌블록이 320px(콘텐츠 151px)로 169px 공백 → 미디어쿼리에 `.dash-timer-left,.dash-timer-right{flex:0 0 auto}` 추가해 콘텐츠 높이로(공백 0). app.css 2곳만, 로직 미접촉이라 단위테스트 N/A — static-preview(N-117)로 데스크톱·모바일 × idle/measuring/achieved 전수 RED→GREEN·콘솔 에러 0 실측. 계획의 "주석 오타 테두리 드랍"은 유령(모든 커밋 ref에 `/*` 정상)으로 판명돼 제외. |
| 2026-06-25 | **docs: 학습노트 N-119 + 트러블슈팅 T-089 — 타이머 2단 디버깅 sweep** — N-119: `flex-direction`을 `row→column`으로 바꾸면 `flex: 1 1 320px`의 basis가 main axis를 따라가 width→height로 뒤집혀 세로 전환 시 고정 높이(공백) 발생 → 방향 전환 미디어쿼리에서 `flex: 0 0 auto`로 basis도 리셋. T-089: 반응형 재현 mock이 worst-case(최대 글자수·최장 문자열)를 안 담으면 RED가 안 떠 레이아웃 버그를 놓침(타이머 mock `1200초`=5글자라 8글자 오버플로 미재현) → mock을 production 최악 케이스로 맞춘 뒤 RED 확인, N-055의 시각/레이아웃판. #491 디버깅 sweep. |
| 2026-06-25 | **feat: 대시보드 '내 정원' 카드 재설계 — 입주 작가 SVG 무대 통합 + 가로 스크롤** — ① 라벨 '방문한 작가'→'입주한 작가'. ② 상단 emoji 무대(`slice(0,5)`)와 하단 중복 아바타 리스트(같은 소유 작가를 위는 그림·아래는 이름으로 두 번 노출)를 **하나의 가로 스크롤 갤러리로 통합**(중복 제거). ③ 각 작가를 마을과 동일한 **SVG 캐릭터**(`<use href="#sprite-{spriteId}">`)로 렌더 — emoji만으론 어떤 작가인지 알 수 없던 문제 해소, `spriteId` 없으면 emoji 폴백. `dashboard.html`에 `garden-character-sprites` fragment를 `#dashboard-app` **앞**에 include(`<use>`가 같은 문서 심볼을 Vue 마운트 전부터 참조하도록). ④ `.dash-garden-stage` `overflow:hidden`→`auto`(양옆 잘림이 진짜 스크롤로) + 스크롤바 숨김 + 오른쪽 페이드 힌트. `visibleAuthors` 제네릭화로 `spriteId`·`code` 보존(필드-보존 회귀 가드 테스트 추가 — 런타임 동작 불변·타입 리팩터라 단위 RED 없음 명시, N-055류 누락 가드). 검증: `bootRun` 실 페이지+실 fragment 심볼 위에서 빈 상태(실 데이터 0명 "아직 입주한 작가가 없어요")·SVG `<use>` 8개 bbox>0(실제 캐릭터 렌더)·`scrollWidth>clientWidth`(scrollable)·라벨 정량 확인(스크린샷은 preview 합성 환경 이슈로 3회 타임아웃 → computed style·정량으로 대체). 번들 재빌드. |
| 2026-06-25 | **chore: `pr-merge.sh` 원격 브랜치 삭제에 30s 타임아웃 — 머지 성공 후 hang 차단(T-091)** — `do_merge`가 `gh pr merge`(성공) 후 `git push origin --delete`로 원격 브랜치를 지우는데, 백그라운드(비대화형) git이 credential/원격 단계에서 멈추면 `exit 0`에 도달 못 해 명령 끝 `\| tail`이 영원히 대기 → PR은 MERGED인데 백그라운드 머지 태스크가 안 끝남(#493 머지에서 실제 발생, git 좀비 2개 잔존·원격 브랜치 미삭제로 확인). `git push origin --delete`를 `timeout 30`으로 감싸 hang을 30s로 제한(실패해도 머지는 끝났으니 진행 + 수동 정리 안내 출력). troubleshooting **T-091** 추가(감별=MERGED인데 작업 실행 중·git 좀비·원격 미삭제 / T-088의 자매 — exit 후 후속 누락이 아니라 exit 자체를 못 함). |
| 2026-06-25 | **fix: 대시보드 '내 정원' 무대 데스크톱 가로 스크롤 — 마우스 휠·드래그 지원** — `.dash-garden-stage`의 `overflow-x:auto`는 **터치 네이티브 스크롤만** 제공해 데스크톱에선 휠·드래그 둘 다 안 됐다(휠=세로 deltaY라 가로 컨테이너 안 움직임, 드래그=브라우저 기본 기능 아님). GardenPanel.vue에 ① `@wheel`로 `deltaY`→`scrollLeft`(좌/우 끝 경계를 넘는 방향이면 `preventDefault` 안 해 페이지 세로 스크롤에 양보) ② `@pointerdown/move/up`으로 잡아끌기(grab, `pointerType==='touch'` 제외 — 모바일 네이티브 가로 스크롤 유지) 추가. 휠 경계 로직은 순수함수 `wheelScrollLeft(deltaY, scrollLeft, clientWidth, scrollWidth)`로 추출(스크롤 불가·시작/끝 경계 양보·중간 전수 8개 RED→GREEN). CSS `cursor:grab`/`.is-grabbing`(`user-select:none`), `scroll-behavior:smooth` 제거(프로그래매틱 scrollLeft 끈적임 방지). `setPointerCapture`는 try/catch(캡처 미지원 환경 방어). 검증: bootRun 실 페이지에서 합성 휠(deltaY120→scrollLeft120)·끝경계 양보(unchanged)·드래그(250→150=+100)·터치 제외·콘솔 0 확인. 번들 재빌드. |
| 2026-06-25 | **docs: learning-notes N-120 — overflow-x 가로 스크롤은 데스크톱에서 휠·드래그가 JS 필요** — `overflow-x:auto`는 모바일 터치 네이티브 스크롤만 제공하고 데스크톱은 마우스 휠(세로 deltaY)·클릭 드래그 둘 다 JS로 직접 붙여야 한다는 개념 정리(PR #495에서 실제 겪은 "모바일은 되는데 데스크톱은 안 됨" 함정). 휠→scrollLeft+경계 양보, pointer 드래그(touch 제외), 경계 로직 순수함수 단위테스트. N-117·N-083 관련. |
| 2026-06-25 | **feat: 대시보드 '내 정원' 무대 — 작가 이름 제거 + 시간 경과 자동 한 칸 스크롤** — ① **이름 텍스트 폐지**: 작가명이 길어 거의 다 잘려 보이던 `.dash-garden-resident-name` span 제거(캐릭터 그림만 남김) — 이름은 hover `title`·`aria-label`로만 보존(스크린리더·마우스오버 접근성 유지). ② **자동 스크롤**: `GardenPanel.vue` `onMounted`에서 2.5s 간격 인터벌로 한 작가(=한 칸)씩 `scrollTo({behavior:'smooth'})`, 끝(maxScroll)·시작(0)에 닿으면 방향 반전해 **왕복(ping-pong)** — 이미 경계면 그 자리 dwell 후 다음 틱에 복귀(stuck 방지). 다음 위치·방향 계산은 순수함수 `nextAutoScroll(scrollLeft, step, clientWidth, scrollWidth, dir)→{left,dir}`로 추출(스크롤 여지 없음·step≤0 가드·중간 전진/후진·양끝 clamp+반전 전수 9개 RED→GREEN). 한 칸 너비(step)는 첫 카드 `offsetWidth + columnGap` 실측. ③ **충돌 방지 일시정지**: hover(`mouseenter/leave`)·드래그(기존 flag)·터치(`touchstart/end`) 중엔 틱이 양보, `prefers-reduced-motion`이면 인터벌 아예 미기동(접근성), `onUnmounted` `clearInterval`. 기존 휠·드래그 수동 스크롤(#495)과 공존. 검증: bootRun 실 페이지에 작가 8명 주입(reactive prop 직접 변이) — 이름 span 0개·SVG 8개 심볼 참조 해결·한 칸씩 84px 전진→끝 clamp→0 복귀(왕복)·hover 시 정지(168 고정)→이탈 시 재개·콘솔 에러 0 실측. 순수함수 vitest 646 GREEN. 번들 재빌드. |
| 2026-06-25 | **feat: 대시보드 '내 정원' 무대 — 가운데 포커스 캐러셀 + 중앙 작가 이름 상단 표시** — ① **가운데 정렬**: `.dash-garden-stage` 좌우 패딩을 `max(16px, calc(50% - 30px))`(30px=한 칸 `.dash-garden-resident` 폭 60px의 절반)로 줘, 기존 한 칸 스크롤(`scrollLeft=i·step`)이 곧 **i번째 작가를 화면 정중앙**에 놓이게 함 — 자동 스크롤 엔진(`nextAutoScroll`) **무변경**으로 가운데 포커스 캐러셀 성립. 양끝 패딩이 사용자가 말한 "양 끝 빈칸"(첫·마지막 작가도 중앙 도달). `.dash-garden-resident{width:60px}` 고정으로 sprite/emoji 폭차에도 중앙 정확. ② **중앙 작가 이름 상단 표시**: 순수함수 `centeredIndex(scrollLeft, step, count)=clamp(round(scrollLeft/step),0,count-1)`로 "지금 중앙 작가" 역산(count·step≤0 가드·절반 경계·끝 clamp·음수 clamp 11개 RED→GREEN), `focusName` computed로 그 이름을 무대 상단 중앙(`.dash-garden-focus-name`, 캐릭터 바닥 정렬이라 상단 비어 무겹침)에 1줄 표시. `@scroll`이 자동·수동·드래그 모든 스크롤에서 갱신 → 작가가 중앙을 지날 때 이름도 함께 바뀜(한 명만 표시라 잘림 적음, `aria-live=polite`). ③ 수동 스크롤도 가운데 안착(`scroll-snap-type:x proximity` + `.dash-garden-resident{scroll-snap-align:center}`, 프로그래매틱 smooth와 충돌 없음 실측), 페이드 힌트 양쪽 대칭(`::before`/`::after`). 검증: bootRun+작가 8명 주입 — 로드 시 0번 중앙(offset 0px)·패딩 131px(=(323-60)/2)·이름 일치, 자동 7틱 전부 중앙 작가 offset≤3px·이름 일치(역방향 스윕 6→0 포함), 휠 300→snap 335(4번 중앙)·끝 586=마지막 작가 중앙·이름 동기, 콘솔 0. vitest 657 GREEN. 번들 재빌드. |
| 2026-06-25 | **chore: 대시보드 '내 정원' 카드 라벨 정리 — "입주한 작가" 제거 + pill "내 정원"→"정원"** — 중앙 작가 이름이 무대 상단에 따로 표시되면서 중복이 된 `.dash-garden-authors-label`("입주한 작가") 제거(미사용 CSS 규칙도 함께 삭제), `.dash-pill` 텍스트 "내 정원"→"정원". 빈 상태 메시지 "아직 입주한 작가가 없어요"는 유지(다른 문자열). `.dash-garden`이 `gap:16px` flex 컬럼이라 라벨(flex 아이템) 제거 시 간격 자연 병합(고아 margin 0). 순수 텍스트 변경이라 로직·테스트 무변경, bootRun 실 페이지로 pill="정원"·라벨 제거·중앙 이름 정상·카드 균형 확인. 번들 재빌드. |
| 2026-06-25 | **feat: 대시보드 명언 카드 슬롯머신 자동 로테이션 — 시간이 흐르면 아래→위 롤로 명언 교체** — 단일 명언 정적 표시를 6초마다 다른 명언으로 슬롯머신처럼(작가 캐러셀과 달리 세로 롤) 자동 교체. **백엔드(계약 변경)**: `quoteService.random()`(1개) → `randomList(max=10)`(셔플 N개), `QuotePicker.pickList(quotes, random, max)` 순수함수 신설(복사본 `Collections.shuffle`로 셔플 후 `min(max,size)`개, 빈/`max<=0`→빈 목록), `QuoteService.randomList`(빈→폴백 1개), `DashboardApiController`의 `QuoteDto quote` → `List<QuoteDto> quotes`(record·get()·DTO 매핑). DB 큐레이션(admin 관리) 그대로 재사용. **프론트**: `QuoteCard.vue`가 `quotes[]` 받아 `onMounted` 6s 인터벌로 `nextQuoteIndex=(i+1)%count` 순환, Vue `<Transition>`로 새 명언 `translateY(100%)→0`·옛 명언 `0→translateY(-100%)`(둘 다 absolute로 겹쳐 롤, reel `overflow:hidden` 클립). **가변 길이 대응**: 숨은 `.dash-quote-sizer`가 모든 명언을 CSS grid 한 칸(`grid-area:1/1`)에 겹쳐 쌓아 reel 높이=가장 긴 명언(클립·높이점프 없음, 화면폭 줄바꿈에 반응형). hover 시 정지(읽을 시간), `prefers-reduced-motion`이면 슬라이드만 끄고 교체는 유지(다양성 보존), `onUnmounted` 정리. **TDD RED→GREEN**: `QuotePicker.pickList`(size cap·전체·시드 결정성·빈·max0) + `QuoteService.randomList`(상한·전체·빈→폴백) + `DashboardApiControllerTest`(`$.quote`→`$.quotes` 배열·text/author) 컴파일 RED→구현→`./gradlew test` GREEN, 프론트 `nextQuoteIndex`(wrap·count1·count0) RED→GREEN(vitest 663). 검증: bootRun 실 페이지 — `/api/dashboard` quotes 10개·옛 quote 필드 제거, 6s마다 text+author 교체, 전환 중 두 줄 공존 + 한 줄 ty>0(아래)·한 줄 ty<0(위)=슬롯 롤 실측, reel=sizer 88px·클립 0, hover 정지→이탈 재개, 콘솔 0. 번들 재빌드. |
| 2026-06-25 | **fix: 대시보드 '내 정원' 무대 — 사용자 스크롤(휠·드래그) 제거, 시간 경과 자동 스크롤만 유지** — 무대를 자동 캐러셀로 단순화: 사용자가 직접 굴리는 기능(앞서 추가한 휠·드래그)을 폐지하고 시간 경과 자동 스크롤만 남김. ① CSS `.dash-garden-stage` `overflow-x:auto`→`hidden` — 사용자 스크롤(휠·드래그·터치·스크롤바)은 막되 **프로그래매틱 `scrollTo`는 그대로 동작**(overflow:hidden 컨테이너도 JS `scrollLeft` 가능)해 자동 스크롤은 생존. `cursor:grab`/`.is-grabbing`/`scroll-snap-*`(수동 스크롤 안착용) 제거. ② `GardenPanel.vue`에서 `onWheel`·`onPointerDown/Move/Up`(드래그 grab)·`onTouchStart/End`(터치 플래그)·관련 상태·템플릿 바인딩 삭제, `autoTick` 가드는 `hovering`만 유지(읽을 때 정지). 중앙 이름 갱신 `@scroll`은 유지(프로그래매틱 smooth도 scroll 이벤트 발생). ③ dead code된 순수함수 `wheelScrollLeft` + 그 테스트(8개) 제거. 검증: bootRun 실 페이지+작가 5명 주입 — 자동 스크롤이 시간 따라 172→258→344(끝)→268 **왕복**·중앙 이름 동기 갱신, 휠·드래그 입력 시 `scrollLeft` 불변(차단)·grab 클래스 0·스크롤바 0·`overflowX=hidden`, 콘솔 0 실측. vitest 655 GREEN. 번들 재빌드. |
| 2026-06-25 | **feat: 대시보드 우상단 프로필 아바타 클릭 → 설정·문의·로그아웃 드롭다운(일원화)** — 장식이던 아바타(`<div aria-hidden>`)를 클릭 가능한 사용자 메뉴 토글 버튼으로 만들고, 흩어져 있던 진입점을 한곳으로 모음. ① `DashHeader.vue`: `open` ref 토글 + 아바타 아래 절대배치 `role="menu"`(설정 `/settings`·문의 `/feedback` 링크 + 로그아웃 POST `/logout` form+CSRF). **바깥 클릭·Esc로 닫힘**(`onMounted` document 리스너 등록 / `onUnmounted` 해제, 메뉴 내부 클릭은 `@click.stop`으로 안 새게), a11y(`aria-haspopup`·`aria-expanded`·`aria-label="사용자 메뉴"`·`role=menuitem`). ② **일원화**: `QuickNav.vue`에서 설정·문의·피드백 타일 제거(5개로), `DashboardApp.vue` 하단 단독 로그아웃 form 제거 → 모두 드롭다운으로. 죽은 `.dash-logout-form`/`.dash-logout-btn` CSS 삭제. 전역 `button{width:100%}`는 아바타 `width:42px`(클래스 특이도)로 상쇄, 메뉴 항목은 풀폭 행이라 그대로 활용(#286 가드). ③ `plan.md` 문의 진입점 서술 갱신(빠른이동 타일→아바타 메뉴). **TDD RED→GREEN**: `dash-header.test.ts`(@vue/test-utils mount) — 초기 닫힘·`aria-expanded=false` / 클릭 시 열림+설정·문의·로그아웃(form+`_csrf`) / 토글 / Esc 닫힘 / 바깥클릭 닫힘 / 내부클릭 유지 6개 RED→구현→GREEN. 검증: bootRun 실 페이지 — 아바타=button·메뉴 항목 3개·실 CSRF 96자·아바타 아래 우측정렬·Esc·바깥클릭 닫힘·로그아웃 end-to-end(`POST /logout`→`/login?logout`)·QuickNav 5타일·옛 로그아웃 버튼 0·콘솔 0 실측. vitest 661 GREEN. 번들 재빌드. |
| 2026-06-25 | **feat: 독서 기록(/history) 리디자인 PR-1 — 인사말 + 독서 잔디 카드 리스킨** — Claude Design 시안(HistoryPage.dc.html)대로: ① 인사말을 한 줄 `.greeting`에서 헤드라인(고운바탕 serif 31px/700, 화면 전용 `.history-greeting`)+서브카피로 강화 — 전역 `.greeting` 무변경(다른 화면 영향 0), `@import`에 Gowun Batang `wght@400;700` 추가해 실 700 로드(faux-bold 제거). ② 잔디 카드: 연속일수 뱃지를 연한 세이지 **pill**(`#E7EEE2`/보더 `#D7E2CF`/radius 999px, streak 0이면 통째 숨김), 요약 강조색 `--ok(#2F8F6B)`+tabular-nums, placeholder(선행·미래) 셀 `transparent`→`#ECE7DB`(시안 future 톤), level-0 미세 inset 보더(`rgba(27,31,35,.04)`), manual inset `2px→1.6px`, 요일/월 라벨 `#9A917F`, 범례 상단 구분선(`#EFE9DC`)+12px·그룹 정렬. ③ 네이티브 `title` 대신 **커서 추적 다크 툴팁**(`.grass-tip`, `position:fixed`라 카드 overflow에 안 잘림): level>0=`날짜 · N분[ · 직접 채움]`, level0=`날짜 · 기록 없음`, placeholder=없음. **TDD RED→GREEN**: 툴팁 텍스트 조립 순수함수 `cellTooltip`(placeholder→null·level0·분 floor·manual·level0+manual 6개) RED→구현→GREEN, 프론트 vitest 667 전부 GREEN(기존 history mount 9 무회귀). 검증: static-preview(N-117)에 실 빌드 `history.js`+실 `app.css`를 mock `/api/history`로 띄워 시안 hex 전수 일치(pill/셀/inset/강조/serif 700)·툴팁 4분기(일반·manual·level0·placeholder)·`mouseleave` 소거·콘솔 에러 0 실측. 번들 재빌드. 기록 카드 split·하단 타일은 PR-2·3. |
| 2026-06-25 | **feat: 독서 기록(/history) 리디자인 PR-2 — 기록 카드 pill 탭 + 와이드 split 2단(반응형)** — 기록 카드를 폭에 따라 두 모드로: **좁으면 stacked**(pill 탭 2개[활성=세이지 채움 `#6E8A6A`/흰 글자·999px, 비활성=투명+보더 `#E4DDD0`] + 선택 패널 하나), **넓으면 split**(탭 없이 `grid 1.45fr 1fr`로 좌 일자별 / 우 빠뜨린날 나란히). ① **반응형 분기**: 순수함수 `chooseLayout(width)`(`layout.ts`, `SPLIT_MIN_WIDTH=880`)로 stacked/split 판정 — `HistoryApp.vue`가 `window.innerWidth`+resize로 reactive `layout` 갱신. split일 때 `document.body`에 `history-wide`를 **JS로 토글**해 `body.history-page.history-wide .container{max-width:940px}`로 컨테이너 확장(미디어쿼리 대신 JS — `innerWidth`와 `@media`의 스크롤바 폭 불일치로 경계서 어긋남 방지, 단일 출처=`chooseLayout`). ② **CSS-라디오 탭 → JS 탭**: 기존 `input[type=radio]`+`.tab-panel`(:checked~) 토글을 제거하고 `.hist-tab` 버튼+`activeTab` ref+`v-show`로 — N-083/T-053의 CSS :checked 가시성 사각 자체가 사라짐(전역 `button{width:100%}`는 `.hist-tab{width:auto}`로 상쇄, #286 가드). 기존 `.record-card`/`.record-tab` 라디오 CSS는 profile이 공유하므로 보존. ③ **행 시안 적용**: 일자별 시간색 `--accent-hover(#4F6B4C)`(책장 `.record-time`엔 영향 없게 `.hist-records`/`.hist-pane` 스코프), 달네비 ◀▶ `38→34px`, 빠뜨린날 행을 박스→`border-bottom` 리스트(날짜 위/`N분 부족`[`#B06A2C`] 아래 세로 + 우측 채우기 고스트). **TDD RED→GREEN**: `chooseLayout` 경계(임계 미만→stacked·정확/초과→split) 4개 RED→구현→GREEN, `history-app.test.ts` 갱신(좁은폭 pill 탭 active 전환·넓은폭 split 2단·탭 0·`history-wide` 부착), 프론트 vitest 672 전부 GREEN. 검증: static-preview에 실 빌드 번들, 1280폭=split(2단 543/375≈1.45:1·컨테이너 940·시간 `#4F6B4C`·부족 `#B06A2C`·탭 0), 375폭=stacked(pill 탭 활성 `#6E8A6A`/흰 글자/999px·비활성 보더 `#E4DDD0`·클릭 시 패널 토글·컨테이너 343·split 없음) 양 모드 colsole 에러 0·스크린샷 시안 일치. 번들 재빌드. 하단 타일·전체 반응형 점검은 PR-3. |
| 2026-06-25 | **docs: learning-notes N-124 — 반응형 분기를 JS·CSS @media로 이원화하면 스크롤바 폭만큼 경계가 어긋난다** — PR-2(독서 기록 split) 디버깅 sweep. 화면 폭으로 레이아웃을 가를 때 **분기 판단은 JS `window.innerWidth`(스크롤바 포함), 컨테이너 폭 변경은 CSS `@media (width)`(스크롤바 포함 여부 브라우저별 상이)**로 이원화하면 경계에서 ~15px 어긋나 split DOM이 좁은 컨테이너에 끼는 깜빡임 발생 → 분기·폭을 **단일 출처**(`chooseLayout(innerWidth)` + `body` 클래스 토글, `@media` 미사용)로 통일. 실측(static-preview 880px)으로 컨테이너 833 정상 확장 확인. PR-3은 하단 타일 표준 준수·반응형 검증 통과로 코드 변경 없이 종료(카드 간격은 N-115 균일 16px 존중, 죽은 라디오 CSS 제거는 T-082 profile 회귀 위험으로 보류). N-115·N-117·T-093 관련. |
| 2026-06-25 | **fix: 독서 기록(/history) 잔디를 대시보드 잔디와 시각 통일** — 같은 잔디인데 대시보드(`.dash-grass-*`)는 세이지 톤, /history(`.grass-*`)는 GitHub 초록(리디자인 시안 값)이라 두 페이지가 따로 놀던 것을 **대시보드 기준으로** 맞춤(사용자 요청, 시안의 초록 팔레트 의도 오버라이드). ① 셀 색 `#ebedf0~#216e39`→세이지 `level0~4 = #E9E3D6/#CFE0C2/#A7C597/#7BA46F/#4F6B4C`(대시보드 s1~s5 동치, level0 미세 inset 제거), ② 셀 `11px/radius2`→`13px/radius3`(요일/월 라벨·그리드 트랙 동반), ③ empty(선행·미래) `#ECE7DB`→투명, ④ manual inset `1.6px #f59e0b`→`2px #C99A3A`, ⑤ 연속 뱃지를 성장 이모지 pill(🌷+"N일 연속")→대시보드 칩(SVG 새싹 + `<strong>N</strong>일 연속 독서`, `#E7EEE2`/accent-hover). /history 전용인 **요일·월 라벨과 hover 다크 툴팁은 유지**(전용 페이지 정보량). 순수 시각이라 새 단위테스트 N/A — 클래스명(level-N/empty/manual) 불변으로 기존 vitest 672 GREEN(무회귀), static-preview computed style로 셀 색·크기·manual·뱃지가 대시보드와 hex 단위 일치·툴팁 유지·콘솔 에러 0 실측(스크린샷은 합성 환경 타임아웃이라 정량 검증으로 대체). 색 값은 `.dash-grass-cell`과 중복이라 주석으로 결합 표기(단일 출처 var 후보). 번들 재빌드. |
| 2026-06-25 | **fix: `pr-merge.sh` 원격 브랜치 삭제를 `git push`→gh API로 — 백그라운드 머지 40분+ 좀비 hang 근절(T-094)** — 머지 자동화가 반복적으로 머지는 됐는데 백그라운드 태스크가 안 끝나고 "출력 없음"으로 40분+ 매달리던 문제. 원인=`do_merge`의 `git push origin --delete`가 Windows Git Bash에서 git이 띄운 **자식**(git-remote-https·credential helper)이 SIGTERM을 안 받아 `timeout 30`(T-091 미봉책)으로도 안 죽고 매달림 → 스크립트 exit 불가·파이프 풀버퍼링이라 진행 로그도 flush 안 됨. **해결=git push를 버리고 `gh api -X DELETE repos/{owner}/{repo}/git/refs/heads/<head>`로 원격 ref를 HTTP 삭제**(gh는 자체 토큰·자식 프로세스 없어 hang 불가), `gh pr merge`·`gh pr view`도 `timeout 120/30`으로 감쌈. 스모크(`PR_MERGE_DRYRUN=1 PR_MERGE_FAKE_STATE=CLEAN/DIRTY`)로 머지·충돌차단 경로 + `bash -n` 문법 확인. 트러블슈팅 **T-094**(timeout이 Windows에서 git 손주를 못 죽임 / 좀비 응급=PR MERGED면 끝, TaskStop+gh api 삭제). 잔여=`--rebase`의 force-push는 gh 대체 불가라 git 유지(드물어 보류). |
| 2026-06-25 | **docs: 트러블슈팅 재발 카운팅 체계화 — 「🔁 재발·승격 트래커」 표 신설 + CLAUDE.md 규칙** — "2회 이상 재발 트랩은 CLAUDE.md/훅으로 승격" 규칙은 있었으나 **재발을 세는 방법이 없어**(모델이 디버깅 중 유사성 알아채기 + 상호참조 흔적 + 사용자 기억에만 의존) 승격 트리거를 놓칠 수 있던 구멍을 메움(사용자 지적, 실제 머지 hang T-091→T-094 같은 날 2회째인데 카운팅 미명시 사례). ① `troubleshooting.md` 상단에 **재발·승격 트래커 표**(트랩군 / 발생 T-### / 회차 / 승격상태) 신설 — 기존 재발군 6개 초기 채움(머지자동화 T-083·88·91·94, 번들 stale T-063·82, SW 캐시 T-071·75·80, git hang T-078, 클라 로드순서 T-053·54, 한글깨짐 T-026). ② CLAUDE.md 「🧯 트러블슈팅」에 **규칙 추가**: T-### 작성 시 재발이면 `N회차(이전 T-### 재발)` 명시 + 트래커 갱신, 2회+ 미승격이면 승격(하드픽스 우선), 답변에도 "N회차" 명시. 카운팅 단일 출처를 표로 못박아 승격 누락을 구조적으로 차단. |
| 2026-06-25 | **docs: 머지 절차에 GitHub auto-merge 우선 경로 명문화** — 레포 `allow_auto_merge=true` 활성화(사용자, 서버사이드 설정이라 워크트리 무관 전역)를 받아 CLAUDE.md Git 워크플로 「5. 머지」에 추가: 단일·마지막 PR은 `gh pr merge <PR> --auto --squash --delete-branch` **한 번**으로 GitHub가 필수체크(`test`) 통과 시 서버사이드 머지+원격 브랜치 삭제 → 로컬 폴링·백그라운드 불필요(머지 hang 클래스 구조적 소멸, T-094 후속). 연쇄 PR은 `--auto` 후 `state=MERGED`만 짧게 확인. `pr-merge.sh`는 폴백(auto 미허용·동기 머지 필요 시). 이 PR 자체를 `--auto`로 머지해 라이브 검증. |
| 2026-06-25 | **fix: footer "내 책장" 링크 이모지(📚)를 대시보드 책 아이콘으로 통일** — 독서기록 페이지의 "내 책장" 이동 버튼은 컬러 이모지 📚, 대시보드 QuickNav의 같은 버튼은 라인아트 SVG(책등 3권, `stroke=currentColor`)라 둘이 달라 보이던 것을 **대시보드 기준으로** 맞춤(사용자 요청). `📚 내 책장` footer 링크가 있던 **4곳**(독서기록 `HistoryApp.vue`·프로필 `ProfileApp.vue`·책BTI `PersonalityApp.vue`·수동기록 `manual-session.html`)의 이모지를 QuickNav와 동일한 인라인 책 SVG로 교체(16px, `aria-hidden`). 정렬·간격은 `.link-row a`가 이미 `flex(center, gap:8px)`라 그대로 따르고, CSS는 `.link-row a svg.link-ico{flex:0 0 auto}` 축소방지 한 줄만 추가. 순수 시각 교체라 단위테스트 N/A — 이모지 단언 테스트 없어 무회귀, static-preview computed-style로 이모지 제거·SVG 16px·stroke가 링크색(rgb 44,42,36) 상속·콘솔 에러 0 실측(스크린샷은 합성환경 타임아웃이라 DOM 정량검증으로 대체). 번들 재빌드(history·profile·personality.js 3개만 실변경, 나머지 7개는 CRLF 노이즈라 미스테이징=T-093). |
| 2026-06-25 | **fix: 네비 버튼 hover 효과를 대시보드 QuickNav 기준으로 통일 + 토큰화** — footer `.link-row a`(독서기록·프로필·책BTI·수동기록·책장·탐색·로그인 등 전 페이지가 공유하는 전역 클래스)의 hover가 "테두리 강조(`--accent`)+그림자(떠오름)"라, 대시보드 QuickNav 타일(`.dash-nav-tile`)의 "연녹 채움(#E7EEE2)+연녹 테두리(#CFE0C2)"와 달라 보이던 것을 **대시보드 기준으로** 맞춤(사용자 요청). 단일 출처를 위해 `:root`에 nav hover 시맨틱 토큰 신설(`--nav-hover-bg: var(--sage-soft)`=#E7EEE2, `--nav-hover-border`=#CFE0C2)하고 `.dash-nav-tile:hover`·`.link-row a:hover` **둘 다** 이 토큰을 참조 → 한쪽만 바뀌어 어긋나는 drift 차단. footer hover에서 box-shadow 제거, transition에 `background .2s ease` 추가(대시보드와 동일), `:active{translateY(1px)}` 누름 미세효과는 hover 휴지 모습과 무관해 유지. `.link-row a`가 이미 전역 공유 클래스라 **app.css 규칙 한 곳 수정으로 전 페이지 footer에 일괄 적용**(페이지별 작업 0 = 요청한 템플릿화). app.css는 정적 서빙이라 번들/Java 무관(재빌드·테스트 게이트 없음). 순수 시각이라 단위테스트 N/A — static-preview computed-style로 두 `:hover`가 동일 토큰 참조·토큰 해석(#E7EEE2/#CFE0C2)·강제 hover 시 bg rgb(231,238,226)+border rgb(207,224,194)+box-shadow none·콘솔 에러 0 실측(스크린샷은 합성환경 타임아웃이라 정량검증으로 대체). |
| 2026-06-25 | **docs: troubleshooting T-095 — 워크트리에서 `gh pr merge --delete-branch`는 로컬 main 체크아웃 충돌로 에러나지만 머지는 성공** — #511 머지 중 발견. 워크트리 세션에서 `gh pr merge --auto --squash --delete-branch`가 `fatal: 'main' is already used by worktree`로 비정상 종료하나, 서버사이드 머지·auto-merge 등록은 그 전에 끝나 영향 없고 **로컬 `--delete-branch` 단계만** 깨짐(gh가 머지 후 main으로 checkout 시도 → main은 주 워크트리 점유). 해결=`gh pr view`로 MERGED 확인 → 원격은 `gh api -X DELETE`(T-094)·로컬은 베이스 브랜치로 checkout 후 `git branch -D`. 예방=워크트리에선 `--delete-branch` 빼고 머지 후 수동 정리. T-094(worktree×Windows 머지 자동화 로컬 git 단계 깨짐 계열 — git 손주 hang vs main 체크아웃 충돌)·#509 관련. 1회 신규라 재발·승급 트래커엔 미등재(2회+ 시 등재). |
| 2026-06-25 | **feat: 내 책장(/books) 리디자인 PR-1 — 인사말 + 요약통계 + 표지 플레이스홀더** — Claude Design 시안(My Shelf.decoded.html)대로: ① 인사말을 한 줄 `.greeting`에서 헤드라인(고운바탕 serif 30px/700, 화면 전용 `.shelf-greeting`) + **요약통계 한 줄**(`총 N권 · 읽는 중 N · 완독 N · 읽고 싶음 N`, tabular-nums)로 강화 — 전역 `.greeting` 무변경(다른 화면 영향 0, `.history-greeting` 선례 계승). ② **무표지 표지 플레이스홀더**: `coverUrl`이 없으면 색 배경 + 이니셜(고운바탕 serif)로 표지 대체 — 책 행·검색 행 양쪽 적용. 색은 결정적 해시로 시안 12색 팔레트에서 골라(같은 책=항상 같은 색) 인라인 주입(`.book-cover-ph`). 팔레트는 `:root`가 이미 시안 종이톤+세이지와 일치해 추가 색 토큰 0. **TDD RED→GREEN**: 순수함수 `pure.ts` 신설 — `summarize`(빈/단일/혼합/미지상태 경계), `initialOf`(trim·빈·공백·null·특수문자→폴백 `?`), `coverColor`(결정성·팔레트 범위·분산·빈 seed) 16개 RED→구현→GREEN, 프론트 vitest 688 전부 GREEN(무회귀). 번들 재빌드(books.js). 실 브라우저 종합 검증·책 추가/내 책장 카드 리스킨·와이드 2열+사이드바 셸은 PR-2·3. |
| 2026-06-25 | **feat: 내 책장(/books) 리디자인 PR-2 — 책 추가 카드 리스킨(검색 칩·byline·직접추가)** — 시안 My Shelf대로 '책 추가' 카드를: ① **검색 기준 칩**: 라디오(제목/저자/출판사) → 토글 칩(`.search-chips`/`.search-chip`, 활성=세이지 채움 `--accent`/흰 글자·비활성 투명+보더, `role=radiogroup`·`aria-checked` 유지, 전역 `button{width:100%}`는 `width:auto`로 상쇄=#286 가드). 죽은 `.search-type` 라디오 CSS 제거. ② **검색 결과 byline**: 저자·출판사 두 span → `저자 · 출판사` 한 줄(`.book-byline`). 없는 값(null·빈·공백)은 빼고 남은 것만 ` · `로 결합 — 순수함수 `byline(author, publisher)`. ③ **카드 소제목**을 시안 `.lbl`(12px·자간 .13em·`--accent-hover`·600)로 — 전역 `.card h2`를 `#books-app` 스코프로만 오버라이드(다른 화면 영향 0, 책 추가·내 책장 카드 공통). ④ **직접 추가** summary를 흐린 안내문 + '직접 추가' CTA(세이지·밑줄, `.manual-add-cta`)로 분리. **TDD RED→GREEN**: `byline`(둘 다·저자만·출판사만·둘 다 없음·빈·공백 trim) 6개 RED→구현→GREEN, 프론트 vitest 694 전부 GREEN(무회귀). 번들 재빌드(books.js). 내 책장 카드(필터칩·책카드·상태배지색·액션행)·와이드 2열+사이드바 셸·실 브라우저 종합 검증은 PR-3. |
| 2026-06-25 | **feat: 내 책장(/books) 리디자인 PR-3 — 내 책장 카드 + 와이드 2열 셸 + 상태들** — 시안 My Shelf 완성: ① **필터 칩**: 상태(전체/읽고싶음/읽는중/완독)·공개여부(전체/🌍/🔒) 링크→칩(`.filter-chip`, 활성 세이지 채움). books 전용 새 클래스라 프로필 책방이 공유하는 `.shelf-filter`·`.vis-filter` 무영향(별개). ② **책카드**: 상태배지 색 구분(읽는중 `#E7EEE2`/`#4F6B4C`·완독 `#E0EFE6`/`#2F8F6B`·읽고싶음 `#F0E8DB`/`#8A6D3B`) — 순수함수 `statusBadge`로 인라인 `:style` 주입(프로필 공유 `.book-status-badge` 전역 CSS 보존), 공개토글을 트랙/노브 스위치→칩(`.vis-chip` 🌍 공개/🔒 비공개, books 전용이라 인디고 `#eef2ff`·`#cbd5e1` 잔재 제거). ③ **와이드 셸**(확정안): `@media(min-width:880px)` **단일 출처**(JS 분기 안 씀=N-124 회피)로 컨테이너 1180px·grid `340px 사이드바(책추가) | 1fr 본문(내책장)`·본문 2열 책카드·우상단 이동링크(좁으면 하단 `.link-row`). `.container.shelf-page` 스코프(books.html). ④ **상태들**: 빈 책장 🌱 점선 박스, 로드 실패 ⚠️ 박스+다시시도(`loadError` 추가 — 기존 `load()`는 실패 무처리였음), 로딩 스켈레톤(표지+텍스트 바), 공개 안내 세이지 박스(`#E7EEE2`). ⑤ 카드 소제목 `.lbl`. **TDD RED→GREEN**: `statusBadge`(읽는중/완독/읽고싶음/미지 폴백) 4개 RED→구현→GREEN, 프론트 vitest 698 전부 GREEN(무회귀). 번들 재빌드(books.js). **검증**: static-preview(실 books.js+실 app.css, mock `/api/books`)로 콘솔 0·집계(총6 읽는중2 완독2 읽고싶음2)·와이드(사이드바340+본문816 2열카드·우상단링크·하단숨김)·좁은폭(세로행·하단링크)·상태배지색·공개칩·표지플레이스홀더·공개박스·빈🌱/에러⚠️/로딩스켈레톤·필터동작(읽는중→2권) 실측. 프로필 책방 회귀 0. (필터 active 칩 배경은 v-for 칩만 합성환경 getComputedStyle 비결정으로 transparent 측정됐으나 CSS·규칙매칭·정적칩·probe로 accent 교차검증.) |
| 2026-06-25 | **docs: /books 리디자인 sweep — T-095 승격 + N-125 + T-096** — /books PR 시리즈에서 나온 트랩·개념 3건 정리. ① **T-095 2회째 승격(#511 → #515)**: 워크트리에서 `gh pr merge --delete-branch`가 로컬 main checkout 충돌(`fatal: 'main' is already used by worktree`)로 깨짐(머지·등록 자체는 성공) — troubleshooting 재발·승격 트래커 등재 + CLAUDE.md Git워크플로 auto-merge 절 caveat 승격(워크트리=`--delete-branch` 빼고 머지 → 원격 gh api·로컬 수동 정리). ② **N-125**: 합성 preview의 `getComputedStyle`가 Vue `v-for` 패치 노드에서 비결정적 stale(transparent) 반환 — CSS 소스·`el.matches()`·정적 probe·override 부재(grep)로 삼각측량(정적칩·probe는 accent라 실 렌더 정확 판정). ③ **T-096**: 연쇄 PR에서 머지 폴링 `TIMEOUT`/`OPEN`/`DIRTY`(미머지)를 머지 완료로 오인해 다음 브랜치를 `origin/main`에서 따 직전 PR 변경 유실(즉시 복귀로 무해) — 다음 브랜치 따기 전 `state=MERGED` 확인. 문서 전용(.md, 테스트·번들 게이트 무관). |
| 2026-06-25 | **fix: 대시보드 — 오늘 할당량을 채워도 타이머 시작 버튼이 사라지지 않게** — 대시보드 리디자인 후 측정 안 하는 상태에서 오늘 목표를 달성하면 타이머 우패널이 `achieved` 상태(`panelState` timerProgress.ts)로 바뀌어 격려 메시지만 렌더하고 **측정 시작 폼(`BookPickForm`)이 사라지던** 회귀 수정(사용자 보고). 시작 폼은 `idle`에서만 보였기 때문. 사용자는 목표를 채운 뒤에도 계속 시간을 측정할 수 있어야 함. **해법**(선택: 격려+폼 둘 다): `panelState` 로직은 불변, TimerCard.vue의 achieved 패널에 격려 메시지(🌱 오늘도 약속을 지켰어요 + 연속 N일)를 유지한 채 `dash-divider` + `BookPickForm`(@start emit)을 추가 — 측정 중 달성 시 `measuring` 유지(종료 보존, 2026-06-24)는 그대로. CSS: achieved 패널이 `align-items:center`라 폼 컨트롤이 좁아지는 것 방지로 폼 자식(divider·라벨·select-wrap·empty·btn-fill)에 `align-self:stretch;width:100%`만 추가. **TDD RED→GREEN**: 신규 `timer-card.test.ts`(@vue/test-utils) — 미측정+달성 시 격려 메시지와 '측정 시작' 폼 공존·start emit(책 id)·idle/measuring 회귀 가드 4개 RED(달성 패널에 버튼 없어 실패)→구현→GREEN, 프론트 vitest 702 전부 GREEN(무회귀). 번들 재빌드(dashboard.js). |
| 2026-06-25 | **fix: 내 책장(/books) 공개 안내 문구 모바일 줄바꿈 깨짐 수정** — `.shelf-public-hint`가 `display:flex`인데 그 안에 이모지·`<strong>`·텍스트·`<a>`가 한 줄로 섞여 있어, flex가 연속 텍스트 조각·인라인 요소를 각각 별도 flex 아이템으로 분리 → 좁은 폭에서 각 아이템을 min-content(글자 단위)까지 짜부려 "책방 공/개"·"내 책/방"처럼 세로로 깨지던 것 수정(사용자 제보, iPhone XR 414px). 해법=아이콘만 flex 아이템으로 두고 문구 전체를 `.shelf-public-hint-text` 한 span으로 감싸 그 안에서 정상 단어 단위 wrap이 일어나게(아이콘 위 정렬 `align-items:flex-start` 의도 보존). `.shelf-public-hint-icon{flex:0 0 auto}`·`.shelf-public-hint-text{flex:1 1 auto;min-width:0}` 추가, 데코 이모지에 `aria-hidden` 분리. 순수 마크업/CSS라 단위테스트 N/A(로드순서·반응성 무관) — static-preview 414px로 flex 자식 2개·strong "책방 공개" 1줄(51px)·link "내 책방" 1줄(38px)·문구블록 2줄(38px)·박스 60px·콘솔 0 실측(글자단위 세로깨짐 소거 확인). 번들 재빌드(books.js). |
| 2026-06-25 | **feat: 대시보드 타이머 — 측정 시작/종료 패널 전환을 부드럽게(크로스페이드 + 진행 중 피드백)** — 측정 시작 시 우패널이 "툭" 끊기던 것 개선(사용자 제보). 원인 2가지: ① 우패널 3상태(measuring/idle/achieved)가 `v-if`/`v-else`로 **즉시 DOM 교체**(Vue `<Transition>` 부재) → 순간 swap. ② `handleStart`/`handleStop`이 `await fetch` **응답이 와야** 패널 전환 → 버튼 누름~응답 사이 멈칫인데 피드백 없음. **해법(A+B)**: ⓐ 우패널을 `<Transition name="panel-fade" mode="out-in">`(key=state)로 감싸 opacity+translateY(6px) 크로스페이드(0.2s), `prefers-reduced-motion`이면 트랜지션 0초(기존 패턴). ⓑ `DashboardApp`에 `starting`/`stopping` ref 추가 — 버튼 누르는 즉시 `시작하는 중…`/`종료하는 중…`+비활성(`BookPickForm`에 `pending` prop, 종료 버튼 `:disabled`)으로 멈칫을 의도된 로딩으로 표시 + 중복 클릭(409) 방지(핸들러 진입 가드 `if(starting.value)return` + `finally`로 해제). 버튼 비활성 시각(`opacity:.6;cursor:progress`). 로직(`panelState`·API)은 불변, 표현 계층만. **TDD RED→GREEN**: `timer-card.test.ts`에 진행 중 피드백 4개 추가(starting→시작버튼 비활성+문구·기본 활성·달성패널도 비활성·stopping→종료버튼 비활성+문구) RED 3개(prop 미반영)→구현→GREEN, 패널 렌더 회귀 가드 그대로 통과, 프론트 vitest 706 전부 GREEN. 번들 재빌드(dashboard.js). 크로스페이드 부드러움 자체는 순수 시각(jsdom 무의미)이라 실 대시보드에서 측정 시작/종료 육안 확인이 최종 게이트. |
| 2026-06-25 | **feat: 내 책장(/books) 필터를 묶음별 세그먼트 컨트롤로 — 두 그룹 개별 구분** — 상태(전체/읽고싶음/읽는중/완독)·공개여부(전체/🌍/🔒) 필터가 둘 다 같은 칩 스타일 + 세로 간격만이라 7개 칩이 한 묶음처럼 보이던 것을, **묶음별 한 덩어리 세그먼트 토글 바**로 분리(사용자 요청, 시안 비교 후 B안 선택). app.css의 books 전용 `.shelf-filter-chips`(→ `display:flex`+`width:fit-content`로 block레벨 세로 적층·내용폭, 외곽 보더+`radius:999px`+`overflow:hidden`로 한 덩어리)·`.filter-chip`(개별 보더·라운드 제거, `.filter-chip + .filter-chip` border-left 셀 구분선, `active`만 세이지 채움, hover는 보더→`:not(.active)` 옅은 배경)만 교체 — 마크업·동작(`selectStatus`/`selectVis`)·접근성(`role=group`) 불변이라 **CSS-only(BooksApp.vue 무수정 → 번들 재빌드 불필요)**. 두 클래스는 books 전용(grep 확인)이라 프로필 책방 등 회귀 0. 순수 시각이라 단위테스트 N/A — static-preview로 모바일 414px·데스크탑 1280px 양쪽에서 두 묶음 한 덩어리(보더·라운드·셀 구분선)·active만 채움(v-for/정적 셀 동일 `#6E8A6A`=N-125 비결정 아님)·두 묶음 세로 분리(가로 안 붙음, 간격 8px)·가로 넘침 0(상태 252·공개 211px)·클릭 필터 동작 유지(읽는중→1권)·콘솔 0 실측. |
| 2026-06-25 | **style: 내 책장 필터 세그먼트 모서리를 각지게 (`999px`→`--radius-sm`)** — 세그먼트 토글 바가 완전 pill(`border-radius:999px`)이라 두 묶음을 붙여 놓으니 모서리가 과하게 둥글어 어색하던 것을, books 전용 `.shelf-filter-chips`의 `border-radius`를 토큰 `var(--radius-sm)`(8px)로 바꿔 각지게(사용자 요청). 컨테이너 값 한 줄만 변경(셀은 `overflow:hidden`으로 컨테이너 라운드를 따라 클립되므로 컨테이너 radius만으로 충분). CSS-only(books 전용 클래스라 회귀 0, 번들 무관). static-preview 414px로 두 묶음 모두 `border-radius` 4모서리 8px 적용·`overflow:hidden`·콘솔 0 실측. |
| 2026-06-25 | **feat: BookTimer 헤더 로고 — 폰트 Gowun Batang serif + 통일 책 SVG 아이콘** — 최상단 로고가 ① 폰트는 body 기본 sans(Gowun Dodum)라 책 느낌이 약하고 ② 아이콘은 기본 컬러 이모지 📚라, footer·QuickNav가 이미 통일한 라인아트 책 SVG 테마와 어긋나던 것을 정리(사용자 요청). **① 폰트**: `.brand h1`에 `font-family:'Gowun Batang',serif; font-weight:700` 추가 — 타이머 숫자(`.dash-timer-num`)와 동일 패밀리. CSS 한 곳이라 brand 헤더를 쓰는 전 페이지에 자동 적용. **② 아이콘**: `<span class="emoji">📚</span>`를 QuickNav·footer와 동일한 라인아트 책 SVG(책등 3권, `stroke=currentColor`)로 교체 — 📚 사용 25개 템플릿 일괄(PowerShell 리터럴 치환, bash grep이 멀티바이트 매칭 실패라 우회). admin 등 특수 이모지(🛠️·👥·🪪·📊·💬) 8개는 페이지 정체성이라 보존. CSS `.brand .brand-ico{width/height:1.5rem; align-self:center; color:var(--accent-hover)}` — `.brand-home`이 `align-items:baseline`이라 SVG가 떠 보이는 것 center로 광학정렬, `color`로 stroke를 로고색(글자와 동색)으로(미지정 시 아이콘만 기본 텍스트색이라 글자와 따로 노는 회귀 방지). **순수 시각**(SSR 템플릿+정적 CSS, Vue·번들·Java 무관)이라 단위테스트 N/A — static-preview(실 app.css)로 h1 font-family='Gowun Batang',serif·아이콘 color rgb(79,107,76)=#4F6B4C(=로고색)·24px·아이콘↔글자 수직중심 일치(263=263)·gap 8px·SVG 정상렌더(rect+path 3) 실측(스크린샷은 합성환경 타임아웃이라 DOM 정량검증으로 대체=선례). 치환 검증 grep으로 brand 📚 0·brand-ico 25 확인. |
| 2026-06-26 | **docs: troubleshooting T-097 — Git Bash 멀티바이트(이모지·한글) grep/sed 매칭 실패로 일괄 치환 누락** — 헤더 로고 📚→책 SVG 일괄 교체(#522) 중 발견. `grep -rl '<span class="emoji">📚</span>' | while read f; do sed -i ...; done`이 grep 0건이라 while이 안 돌아 **치환 0건인데 에러 없이 성공처럼 종료**. 원인=이 환경 Git Bash가 C 로케일이라 4바이트 이모지를 패턴으로 신뢰성 있게 못 잡음(Grep 도구=ripgrep은 정상 매칭이라 대비로 진단). 해결=일괄 치환은 PowerShell 리터럴 `.Contains`/`.Replace`(BOM 없는 UTF-8 `[IO.File]::WriteAllText`)로 우회, 치환 전후 카운트는 ripgrep으로. 예방=멀티바이트 검색·치환은 Grep 도구/PowerShell로(bash grep·sed에 이모지·한글 패턴 직접 넣지 말 것). T-026·T-003(PS5.1 인코딩·멀티바이트 처리 함정 군) 관련. 1회 신규라 재발·승급 트래커엔 미등재(2회+ 시 등재). |
| 2026-06-26 | **fix: 내 책장 필터 세그먼트 active 셀 모서리를 겉 박스와 일치 (전역 `button` 10px 누수 차단)** — 세그먼트 바를 각지게(컨테이너 `border-radius:8px`) 한 뒤에도 선택된 active 셀의 초록 하이라이트만 더 둥글어 겉 박스 모서리와 어긋나던 것 수정(사용자 제보). 원인=세그먼트화 때 `.filter-chip`의 명시적 `border-radius`를 제거하자 전역 `button{border-radius:10px}`가 셀에 누수 → active 셀이 컨테이너(8px)보다 둥근 10px라 첫/끝 셀 active 시 코너에 카드배경 틈(static-preview hit-test로 좌상단 곡선 안쪽이 셀 아닌 컨테이너로 잡힘 = 초록이 코너 못 채움 확인). 해법=`.filter-chip`에 `border-radius:0` 명시(전역 10px 상쇄, `width:auto`로 전역 `button{width:100%}` 상쇄하는 #286 가드의 radius판) → 셀 직각 + 컨테이너 `overflow:hidden`이 첫/끝 active를 8px로 클립해 겉 박스와 동심 일치. CSS 한 줄(books 전용, 회귀 0, 번들 무관). static-preview 414px로 첫 셀 active(왼쪽 8px 동심·스크린샷 육안)·끝 셀 active(우상단 코너 셀로 채워짐·`border-radius:0`)·콘솔 0 실측. |
| 2026-06-26 | **chore: 프로세스 sweep — changelog `merge=union` 하드픽스(T-098) + 전역 `button` radius 누수 트랩(T-099)** — 멀티세션 작업 중 반복된 두 마찰 정리. ① **changelog 머지 충돌 하드픽스**: 여러 세션이 전부 `changelog.md` 맨 아래에 행을 추가해 2026-06-26 하루 #516·#518·#520·#523에서 PR마다 같은 위치 충돌→rebase·force-push 왕복. `.gitattributes`에 `claude-docs/changelog.md merge=union` 추가 → git 내장 union 드라이버가 충돌 시 양쪽 새 행을 마커 없이 둘 다 자동 병합(append-only에 정확, 이 PR이 main에 든 다음 머지부터 충돌 자체 소멸). troubleshooting T-098 + 재발·승격 트래커 등재. ② **전역 button border-radius 누수 트랩**: 세그먼트화로 `.filter-chip` 명시 `border-radius` 제거 시 전역 `button{border-radius:10px}`가 새던 #523 건을 T-099로 등재 — 전역 button width 누수(T-056·T-081·#286·#368)의 radius판이라 트래커 「전역 button 속성 누수」 군으로 묶음(width 3+·radius 1). 문서·설정 전용(테스트·번들 게이트 무관). |
| 2026-06-26 | **feat: 책방(/u/{loginId}) 공개 프로필을 대시보드 디자인 언어로 재이식** — self/other 한 컴포넌트(ProfileApp.vue)의 **로직(fetch·팔로우·차단·신고·구매·드릴다운·popstate)은 불변**, 시각 스킨만 메인보드 통일(이모지 📚🚩🚫⏱←↗ 전면 제거→라인 SVG, 단색 잔재→3색 배지·줄무늬 표지 폴백, 세리프 절제). **와이드 2열 풀구현**: `matchMedia('(min-width:860px)')` 리액티브 `isWide`(JS·CSS 단일출처라 N-124 경계어긋남 회피)로 모바일=단일열 탭(책BTI↔공개책장)·와이드=좌 360px 사이드바(정체성+책BTI 상시+신고)/우 공개책장 2열 grid 분기. 신규 컴포넌트: `ShopIcon`(아이콘 사전 주입)·`ShopHeader`(정체성 헤더, §7 DRY 추출)·`BtiPanel`·`ShelfPanel`(모바일 탭·와이드 양쪽 공유), `icons.ts`(라인 SVG 사전)·`format.ts`(시간·배지클래스·표지이니셜·hasCover 순수함수). 제네릭 `.dash-*` 직접 재사용+고유는 `.shop-*` 신설(공유 `.book-*`/`.record-*` 미수정→타 페이지 회귀 0), 컨테이너 폭 확장은 `body.profile-page` 스코프, 전역 `button{width:100%}` 함정은 인라인 버튼/칩 `width:auto`로 방어. 주인 이니셜 아바타(`.dash-header-avatar` 42px 세이지 원) 추가. §4 데이터 불변식 보존: PUBLIC만·시간은 seconds≥60일 때만·배지 한글 라벨·구매 리다이렉트 경유+`rel="noopener nofollow"`·제휴 고지 법적 필수·신고 거짓 접수 금지·self엔 액션/구매/제휴 미렌더. **TDD**: `format.test.ts`(20)·`icons.test.ts`(3, .vue 참조 무결성 포함) RED→GREEN, 프론트 vitest 729 전부 GREEN. **실 브라우저 검증**(static-preview+fetch mock, N-117): other 모바일·와이드·self·404·빈책장·BTI콜드·드릴다운·표지폴백·860px 경계 전환 전수 + 콘솔 0 실측 — 이 과정에서 **N-118 버그**(주석 내 `.book-*` 슬래시 `.record-*` 표기의 `*/`가 주석 조기종료→`body.profile-page .container` 폭 규칙 침묵 드랍, 와이드 메인 오버플로) 발견·수정. 번들 재빌드(profile.js). |
| 2026-06-26 | **fix: 책방(/u/{loginId}) 데스크톱 공개 책장 카드 폭 확대 — 긴 제목 박스 길어짐 완화** — #526 이식 후 사용자 제보: 데스크톱에서 920px 컨테이너가 양옆을 비우고, 좁은 와이드 메인(532px)을 `1fr 1fr`로 쪼개 카드 폭 259px·제목 영역 169px라 긴 제목이 7~8줄로 접혀 박스가 세로로 길어짐. 해법 2줄(둘 다 `body.profile-page` 페이지 스코프, 타 페이지 회귀 0): ① 컨테이너 `max-width:920→1160px`(데스크톱 빈 옆 공간 활용, 메인 532→772px). ② `.shop-wide .shop-books`를 `1fr 1fr`→`repeat(auto-fit, minmax(300px,1fr))`(대시보드 `.dash-grid-2col` 관용구) — 와이드 2열(카드 259→352px·제목 줄수 절반↓) 유지하되 중간폭(메인<614px)에선 비좁은 2열 대신 1열 풀폭으로 우아하게 강등. CSS 전용(app.css 정적이라 번들·TDD 게이트 무관). static-preview(N-117) 1280px(2열·카드 352px·콘솔 0)·900px(1열 강등)·375px(max-width 캡 무관·탭 정상) 실측. |
| 2026-06-26 | **feat: 로그인·회원가입 페이지를 대시보드 디자인 언어로 재이식** — `login.html`·`signup.html`의 SSR 마크업·CSS만 시안대로 교체(Java·번들 변경 0, Thymeleaf 바인딩·Spring Security 계약 불변). **스코프 격리**: 공유 글로벌 클래스(`.card`/`.field`/`.btn-primary`/`.alert`/`.oauth-계열`/`.entry-hero` — 형제 인증 페이지 9개가 공유)는 미수정, `body.auth-page` 스코프 전용 `.auth-*` 블록 신설(컨테이너 폭 400px도 페이지 스코프 → 타 페이지 회귀 0). 색은 `:root` 토큰 재사용, 토큰 없는 에러톤(테라코타 #B0524A 계열)만 raw hex. **마크업**: 이모지 제거→책 라인 SVG 워드마크, 멀티컬러 G 로고, '또는' 구분선, 알림배너 3종(에러 #F6E7E4·성공/로그아웃 세이지, 아이콘 path 시안 그대로), 필드에러는 `th:classappend`로 입력 테두리 토글 + `th:errors`. **계약 보존**: `name=username/password`·`th:action`(CSRF 자동주입)·플래시 4종(registered·logout·deleted·error)·`th:object`/`th:field`·타임존 select·마케팅 동의. **검증**: LoginControllerTest·SignupControllerTest 그린 유지(Java 0이라 TDD 게이트 자동 skip → 수동 실행). 실 브라우저(Chrome, bootRun)로 7개 상태(로그인 기본/에러/가입완료/로그아웃/계정삭제 + 가입 기본/필드에러) 전수 렌더·콘솔0 실측 — 이 과정에서 **추가한 주석의 `.oauth-*/.entry-hero` 내 `*/`가 블록주석을 조기종료**해 바로 뒤 `.auth-page .auth-shell{max-width:400px}` 규칙이 침묵 드랍(양 페이지 폭 미적용)된 것을 발견·수정(슬래시→가운뎃점). **이 트랩은 #526 N-118의 2회차 재발**. |
| 2026-06-26 | **chore: CSS 주석 조기종료(`*/`) 트랩 하드픽스 — 커밋 훅 `require-css-comment-safe.ps1`** — CSS 주석 안에 `*/`가 끼면(보통 와일드카드 셀렉터 나열 `.foo-*/.bar-*`) 주석이 조기 종료돼 바로 뒤 규칙이 침묵 드랍되는 트랩이 **T-087 3회차**(#522 `.dash-card` → #526 `body.profile-page .container` → 직전 로그인 이식 `.auth-shell`)에 이르러, 예방 규칙·게이트만으론 못 막는다고 보고 prose→하드픽스로 승격. **훅**: `PreToolUse(Bash|PowerShell)` 체인에 추가, 스테이징 `.css` 블롭을 스캔해 주석 닫는 `*/`가 **양옆 모두 셀렉터문자 `[A-Za-z0-9._#*-]`에 붙은** 경우만 exit 2로 차단. "양옆 glued" 조건이 과거 보류 사유였던 FP를 해소한다 — ` */color`(앞 공백)·standalone `/*x*/`(뒤 공백/개행)는 통과, 잔여 FP `/*c*/.sel`류 희귀패턴만 우회 토큰 `SKIP_CSS_COMMENT_CHECK`로 처리. regex 사전필터로 무관 파일은 즉시 통과(실 app.css 무매칭 확인). **테스트**: `.claude/hooks/tests/test-require-css-comment-safe.sh` 9케이스(차단 2종·정상/문자열내`*/`/우회/비-css/비-commit/깨진JSON) 전부 GREEN. 트래커 회차 2→3·승급상태 갱신, T-087·N-118 본문 3회차·하드픽스 반영. .ps1/.sh/.json/.md만 변경(Java·CSS·번들 0). |
| 2026-06-26 | **feat: 전 페이지 하단 네비 버튼을 대시보드 디자인으로 통일 + 템플릿화** — 24곳에 흩어진 `.link-row`(Vue 8섬 + SSR 16곳)의 아이콘 혼재(이모지 📖🔍👥💬🚨📝·인라인 SVG·profile ShopIcon)와 마크업 중복 정리(사용자 제보: 대시보드 버튼과 통일성↓). 레이아웃(가로 타일)은 유지하고 **버튼 하나의 디자인**을 대시보드 타일과 통일 — 아이콘 전면 라인 SVG화 + 색을 `--accent-hover`(대시보드 타일과 동일, 라벨은 차콜 유지). **템플릿화(한 곳 수정=전체 반영)**: 두 런타임이라 Vue `shared/NavLinks.vue`+`NavIcon.vue`+`navIcons.ts`(라인 SVG 사전 20키) / Thymeleaf `fragments/nav-links.html`(navLink)+`nav-icons.html`(icon, `th:switch`)로 갈리되 같은 `.link-row` CSS·같은 아이콘 의미 공유. profile `icons.ts`는 navIcons 재export로 흡수(중복 0). 폼 보조 라벨 명사화(이미 계정이 있나요? 로그인→로그인, 비밀번호를 잊으셨나요?→비밀번호 찾기). **TDD**: `navIcons.test.ts` RED(필요 14키 중 7키 누락)→GREEN, `.vue`의 `icon:'X'` 리터럴 무결성 검사(오타 방어, N-055). vitest 732 GREEN. **검증**: Vue static-preview(NavLinks 2열·아이콘색 accent-hover·콘솔0) + SSR bootRun(login/signup/privacy 200·navLink→icon fragment 체인 실 렌더). 무관 번들 2개(dashboard/garden) phantom CRLF는 되돌림(T-093), 번들 훅은 SKIP_BUNDLE_CHECK로 우회(수동 빌드·검증 완료). |
| 2026-06-25 | **chore: 머지된 로컬 git 브랜치·고아 워크트리 정리 + troubleshooting T-087** — 머지 완료된 로컬 브랜치 30개 삭제(`gh pr list --state merged`의 headRefName과 대조해 검증, `claude/*` 6개·살아있는 워크트리 브랜치는 보존)·고아 워크트리 폴더 2개 제거. `tender-zhukovsky` 빈 폴더는 죽은 세션의 도구 셸 좀비 11개(bash 10·powershell 1)가 cwd로 점유해 `Device or resource busy` — `NtQueryInformationProcess`로 전체 프로세스 cwd를 읽어 그 폴더를 cwd로 가진 PID만 검증·종료 후 삭제(gradle 데몬·살아있는 세션·타 프로젝트는 cwd가 달라 무영향). T-087 기록(빈 워크트리 폴더 busy=좀비 셸 cwd 점유, 감별·해결). |
| 2026-06-26 | **docs: SES 프로덕션 액세스 2차 거부 결정 기록** — 1차 거부(6/17, N-091) 후 4가지 우려를 충실히 담은 2차 재요청도 거부(6/24). "상세로 재요청하면 통과"(N-091 초안) 가설 반증 → 근본은 정보 부족이 아니라 **신규·저트래픽의 발송 실적·평판 0**(닭-달걀, AdSense '콘텐츠 부족' 거부와 같은 뿌리). plan.md retention 레버 ①·§하드닝 메일 인프라에 보류 박스 추가(이메일 의존 기능=재참여 넛지·가입 인증·비번 재설정 보류, 마을 게임화 등 SES 비의존 retention 우선), learning-notes N-091에 2차 거부 후속 + 일반화 갱신("거부 가능한 외부 심사는 상세·정직이 통과율 — 단 사유가 '정보 부족'일 때만, 실적·실체 부재면 서류 대신 실체부터"). |
| 2026-06-26 | **fix: 측정 종료 시 독서 잔디 즉시 갱신 (새로고침 불필요)** — 대시보드 Vue 섬에서 측정 종료(`POST /api/sessions/stop`) 응답이 `TimerState`(타이머만)라 `handleStop`이 타이머 카드만 갱신하고 `data.graph`는 초기 `/api/dashboard` 값으로 stale → 잔디 색·연속일(streak)이 페이지 새로고침 전까지 안 변하던 버그(사용자 제보). 측정 종료가 잔디가 변하는 바로 그 순간이라 **stop 응답을 `StopResponse(timer, graph)` 중첩으로 확장**해 방금 확정된 세션이 반영된 잔디를 동봉(컨트롤러가 `sessionService.stop` 후 `contributionGraph(user)` 빌드, `toGraphDto`·`buildTimerState` 재사용). 프론트 `handleStop`이 `applyTimerState(r.timer)` + `data.value.graph = r.graph`로 갈아끼워 Vue deep ref가 `ContributionGraph`·`TimerCard`(streak) 재렌더. **start는 불변**(잔디는 stop 때만 변함 → `TimerState` 그대로, start 테스트·계약 무변경). **TDD**: 백엔드 `DashboardApiControllerTest` stop 응답 graph 동봉 RED→GREEN, 프론트 `dashboard-app.test.ts` 신설(DashboardApp 마운트·측정 종료 후 연속일 1→5 즉시 갱신) RED→GREEN. 백엔드 전체 + vitest 734 GREEN. 번들 `dashboard.js` 재빌드. |
| 2026-06-26 | **docs: troubleshooting T-100 — 워크트리 세션 프론트 node_modules 부재 트랩** — 워크트리는 git 추적 파일만 복제해 `frontend/node_modules`가 없어 vitest/vite 빌드가 `Cannot find package 'vite'`로 startup 실패. 메인 node_modules가 빈 껍데기면 junction 재사용도 무용(디렉토리 존재 ≠ 패키지 설치). 해결=워크트리 frontend에서 `npm ci`(메인도 비었으면 메인도), 판정은 `test -d node_modules/vite` 등 핵심 패키지 존재로. PR #533(잔디 즉시 갱신) 작업 중 만난 트랩 기록. |
| 2026-06-26 | **feat: Playwright E2E 표적 도입(로컬 수동, 로그인·정원 저장 2개) + content-hash 정적자산 인증누수 버그 수정** — N-084가 미뤄둔 E2E 트리거 충족(마을 S4 SPA 분리·정원 게임화로 #358·#364 헤드리스-블라인드 반복)에 따라 로컬 수동 Playwright 2개 도입(`frontend/e2e/`: ① 로그인 성공/실패 ② 정원 진입→꾸미기→저장 200·콘솔0). `@playwright/test`+chromium, `playwright.config.ts`(baseURL 8080·`storageState` 세션 재사용·webServer 없이 떠 있는 `bootRun`에 붙음), vitest는 `test/`만 잡게 `include: ['test/**/*.{test,spec}.ts']` 한정(기존 `useReadingTimer.spec.ts`와 패턴 겹쳐 e2e/ 분리 필요). **전면 아님**(커밋 게이트 미포함 — 느린 E2E가 `gradlew test` 루프를 안 막게, CI는 vitest·JUnit만). **도입 즉시 실버그 검출**: 첫 E2E(로그인 setup)가 로그인 성공(POST/login 302)인데 `#dashboard-app` 미도달 → 디버그로 최종 URL=`/pwa-install-<hash>.js?continue`(SavedRequest) 실측. 원인=`spring.web.resources.chain` content-hash가 `@{/pwa-install.js}`를 `/pwa-install-<md5>.js`로 렌더하는데 SecurityConfig permitAll이 정확 경로만 둬 해시 URL이 미인증 302→SavedRequest→로그인 후 그 자산으로 리다이렉트(캐시 빈 신규 세션에서만 재현, 실사용자 첫 로그인 영향). `manifest.json`도 동일. 해결=permitAll `/pwa-install*.js`·`/manifest*.json` 와일드카드. 회귀가드=`PwaStaticAccessTest`에 가짜 해시 미인증 `not(302)` 2케이스 추가, SecurityConfig 원복 시 RED(1 FAILED)→재수정 GREEN(기존이 정확경로만 단언해 은폐=N-055 변형판). E2E도 RED(버그)→수정→GREEN 4 passed. troubleshooting T-101·learning-notes N-126·N-084 갱신된 입장. |
| 2026-06-26 | **feat: 탐색(사용자 검색)을 대시보드 타일에서 내 책방으로 흡수 (A안)** — 탐색은 SNS 발견 기능인데 대시보드 QuickNav 5타일 중 하나로 핵심 독서기능과 동급 비중을 차지(사용자 제안: 책방 안 검색창으로 합치자). 탐색의 결과물(누른 사용자→`/u/<id>`)이 곧 책방이라 같은 소셜 흐름 → **대시보드 탐색 타일 제거(5→4)** + **내 책방(self) 상단에 '다른 책방 찾기' 사용자 검색 + 친구 추천 흡수**. DRY: SearchApp의 검색·추천 본체를 `shared/UserSearchPanel.vue`로 추출(heading/placeholder prop화, `shared/follow`의 `toggleFollow` 재사용) → SearchApp은 얇은 wrapper, ProfileApp self 뷰가 동일 패널 재사용. **남의 책방엔 안 뜸**(`v-if="profile.self"` — 누수 방지 불변식). **A 가역성(B 여지)**: `/search` 라우트·컨트롤러·`search.html`·book-readers 링크는 유지(삭제·리다이렉트는 B안에서 재결정). **TDD**: `profile-app.test.ts` 신설 RED(self=true 패널 부재)→GREEN + self=false 누수 가드, 기존 `search-app.test.ts` 5개 보존(리팩터 동작 보존). vitest 736 GREEN. 번들 search·profile·dashboard 재빌드. 백엔드 무변경(`/api/search` 재사용). |
| 2026-06-26 | **docs: troubleshooting T-102 — auto-merge 대기 워처의 DIRTY 미감지(머지 hang)** — 탐색→책방 PR #536을 auto-merge 등록한 직후 다른 세션 #535(E2E)가 머지되며 `plan.md`·`changelog.md`가 겹쳐 PR이 DIRTY/CONFLICTING로 사후 전환됐는데, 직접 짠 백그라운드 워처가 `MERGED`/`CLOSED`만 보고 `DIRTY`를 안 봐 헛폴링(사용자가 먼저 충돌 발견). 해결=`git rebase origin/main`(changelog `merge=union`이라 로컬 충돌 0, T-098)+`push --force-with-lease`로 MERGEABLE 복귀. 교훈=손수 워처 짜지 말고 `.claude/scripts/pr-merge.sh`(DIRTY 즉시 차단 제공)를 쓰거나, 워처에 `mergeStateStatus==DIRTY` 분기 필수. 머지 자동화 hang·DIRTY-blind 군 5회차(T-083·T-091·T-094 재발), 재발 트래커 갱신. |
| 2026-06-26 | **fix: 탐색→책방 흡수 보완 — 책방 비대화 해소, 슬림 검색 진입점(→/search)으로 교체** — A안(#536)이 `UserSearchPanel`(검색+친구추천 풀카드 2장)을 내 책방(self) 상단에 통째로 박아 책방 본체(헤더·책BTI·책장)를 밀어내며 비대화(사용자 피드백: "검색은 페이지 채울 큰 기능 아닌데 책방이 뚱뚱해졌다"). 책방=사용자 연결 장소·검색=그 도구라는 개념은 유지하되, **책방 상단엔 검색창처럼 생긴 슬림 진입 링크(`a.shop-search-entry`)만 두고 누르면 `/search`(검색+친구추천 본체)로 이동**(트위터 모바일 검색바 패턴 — 가짜 입력칸이 전용 검색 화면으로 라우팅). `/search`는 A안 때 이미 `UserSearchPanel` 그대로라 목적지 무변경 → ProfileApp에서 패널 제거+진입 링크 교체, app.css에 화면 전용 `.shop-search-entry` 신설(`.book-search-form` 라운드 + `.dash-nav-tile` 톤). **self-only 유지**(남의 책방 누수 방지 불변식). 책방은 더는 `/api/search`를 호출 안 함(라이브 검색 본체가 전용 페이지로 빠짐 = 비대화 근본 해소). **TDD**: `profile-app.test.ts` 계약을 '흡수 패널(input·`/api/search` 호출)'→'진입 링크(`a[href=/search]`)+`/api/search` 미호출'로 재작성 RED→GREEN, self=false 누수 가드 유지, `search-app.test.ts` 5개 보존. vitest 632 GREEN(전부). 번들 `profile.js` 재빌드. 백엔드 무변경. 자동 메모리 `explore-search-into-bookshop` 갱신(A 전체흡수 폐기→진입점 방식, B는 계속 보류). |
| 2026-06-26 | **fix: "차단 목록"을 책방 하단 버튼 → 아바타 드롭다운(사용자 메뉴)으로 이동** — 차단 목록은 자주 안 쓰는 계정 관리인데 내 책방(self) 하단 NavLinks에 셋째 타일로 상시 노출돼 거슬림(사용자 피드백: "자주 쓰지도 않는 버튼이 자주 보이는 게 더 불편"). 설정처럼 계정/개인 관리라 사용자 메뉴가 자연스러운 집 → `ProfileApp` NavLinks에서 차단 목록 제거(책방 self 하단 = 대시보드·내 책장 2타일), `DashHeader` 아바타 드롭다운에 차단 목록 항목 추가(`/me/blocks`, `block` 금지원 아이콘, 설정 다음·로그아웃 앞). 드롭다운은 대시보드 전용이라 진입 경로는 '대시보드→아바타→차단 목록'으로 한 단계 깊어지나, 드물게 쓰는 동선이라 의도된 트레이드오프. **TDD**: `dash-header.test.ts` 일원화 테스트에 `/me/blocks` 링크 단언 추가 RED→GREEN, 기존 5개 보존. vitest 632 GREEN. 번들 `dashboard.js`·`profile.js` 재빌드. 백엔드 무변경(`/me/blocks` 라우트 그대로). |
| 2026-06-26 | **docs: Playwright 사용법·결과 육안 확인(N-127) + E2E 자동화 위치 정책 명문화** — #535 도입 후속(코드 변경 0). learning-notes **N-127** 신설: 실행법(`bootRun` 띄운 뒤 `npm --prefix frontend run test:e2e`)과 **결과 육안 4계층** — `--headed`(실 브라우저 창)·`--ui`(스텝 DOM 스냅샷 타임트래블)·HTML 리포트·trace viewer, 우리 config가 `reporter:list`·headless·`trace:on-first-retry`+`retries:0`이라 평소 trace 미생성 → 옵션 명시 필요. **CLAUDE.md 🖥️ 프론트 검증 절**에 소프트 규칙 추가: E2E(Playwright)는 **커밋 게이트(훅)에 안 넣는다**(실서버·Docker·브라우저 전제라 매 커밋 수 분화·멀티세션 8080 경합·플래키로 `SKIP_TESTS` 우회 유발, per-commit 아닌 머지 전 레이어), **승격 트리거**(①E2E가 잡았어야 할 회귀 재발 ②스펙 2~3흐름↑) 충족 시 커밋 훅이 아니라 **CI 잡(머지 게이트)**으로. (오늘 `--headed`·`--ui`로 4 스펙 실 브라우저 통과 육안 확인 후 결정.) 문서 전용 — Java·번들·테스트 게이트 무관. |
| 2026-06-26 | **fix: 책방 공개 책장에 스크롤 박스 — 긴 책 목록이 하단 네비를 밀어내던 문제(PC·모바일 공통)** — 책방(`/u/<id>`)의 공개 책장(`ShelfPanel` `.shop-shelf > ul.shop-books`)이 높이 제한 없이 책 수만큼 페이지를 늘려, 하단 네비(`NavLinks` 대시보드·내 책장)가 페이지 맨 끝으로 밀려 책 많으면 한참 스크롤해야 닿던 문제(사용자 피드백: 모바일에서 특히 불편, 책 늘면 PC도 결국 동일). `/books`의 `.shelf-scroll`(60vh)·검색·태그드릴다운·독서기록에 이미 확립된 "리스트 ul을 스크롤 박스로 가둬 페이지가 안 길어지고 하단 네비가 늘 보이게" 패턴을 이식 → `app.css`에 `.shop-shelf > .shop-books { max-height: 60vh; overflow-y: auto; scrollbar-gutter: stable; -webkit-overflow-scrolling: touch; overscroll-behavior: contain; }` 추가. PC·모바일 분기 없음(60vh가 뷰포트 적응 — 데스크톱은 화면 대부분 채워 '꽉 찬 느낌' 유지하며 상한). 필터칩·제휴고지는 ul 밖이라 고정, 책 적으면 미도달=스크롤바 없음(자연 폴백). 직계(`>`)라 BTI 드릴다운(`.shop-drill > .shop-books.shop-drill-books`)엔 영향 0. **순수 CSS**(Vue·번들·백엔드 무변경) — 단위테스트 무의미(jsdom 레이아웃·스크롤 미계산, 전례 스크롤박스 4종도 무테스트), 검증=육안. |
| 2026-06-26 | **fix: 태그(책BTI) 드릴다운 리스트에도 스크롤 박스 복원 — 죽은 `.tag-books-scroll` 정리** — #541(공개 책장 스크롤) 후속. 책BTI 태그칩을 누르면 뜨는 근거책 리스트(`BtiPanel` `.shop-drill > ul.shop-books.shop-drill-books`)도 높이 제한 없이 길어져 같은 불편(사용자 제보). 조사 결과 **원래 이 드릴다운엔 50vh 스크롤 박스(`.tag-books-scroll`)가 설계돼 있었으나 마크업이 `.shop-drill-books`로 리네임되며 연결이 끊겨 사라지고, 클래스만 orphan(죽은 CSS)으로 잔존**(주석에 "헤더 카드 안 보조 미리보기라 shelf 60vh보다 짧게" 의도 명시). → `app.css` `.shop-drill .shop-books` 규칙에 50vh 스크롤 박스 레시피(거터·관성·번짐방지·padding-right) 합쳐 **잃어버린 설계 의도 복원**, 죽은 `.tag-books-scroll` 블록 제거. 헤드(제목·×닫기)는 ul 밖이라 고정, 책 적으면 미도달=스크롤바 없음. 셸프(60vh)보다 짧은 50vh는 드릴다운이 BTI 카드 안 보조 미리보기라는 원 설계 그대로. **순수 CSS**(Vue·번들·백엔드 무변경, `frontend/` 없어 번들 훅 skip) — 검증=육안. |
| 2026-06-26 | **docs: 세션 메타(모델·effort) 기록 규칙 신설 (반자동, soft)** — AI 협업 성숙도 진단 후속(어느 세션=모델·작업량이 낸 산출물인지 사후 추적). 각 세션이 **자기 모델·effort를 자기 산출물에 전진 기록**(세션 분리상 다른 세션 값은 알 수 없어 소급 불가). **모델**=환경 주입값 자동 인식(`claude-opus-4-8`), **effort**=사용자 고지값 그대로(척도 `high`·`medium`이든 모드 `ultracode`든; 내 컨텍스트에 effort 미노출이라 추측 금지, 미고지면 세션당 1회 질의·그래도 미정이면 `unknown`). **위치**: 계획 md 상단 blockquote `🧭 세션 메타: model=… · effort=…`(큰 작업만) + 커밋 trailer `Session-Model`/`Session-Effort`(모든 커밋, `Co-authored-by` 옆, squash 후 `main` 보존). soft 규칙(CLAUDE.md 「계획 우선→세션 메타 기록」 + Git 워크플로 커밋 항목 cross-ref) — 누락 2회+ 재발 시 hard 훅 승격. 문서 전용(코드·훅·테스트 0). |
| 2026-06-26 | **fix: 책방 책BTI 태그칩 클릭 시 BTI 탭 자동 전환 — 모바일 드릴다운 무반응 해소** — 모바일(좁은 화면)에서 책방 공개 책장(shelf) 탭을 보는 중 헤더의 책BTI 태그칩(이야기파 등)을 누르면 화면이 그대로던 문제(사용자 제보). 원인=`openTag`가 `tagPanel`/`tagLabel`을 채워도 그 드릴다운을 그리는 `BtiPanel`이 모바일에선 `v-if="activeTab==='bti'"`로만 렌더돼, shelf 탭이면 DOM에 없어 무변화 → 사용자가 직접 책BTI 탭으로 가야만 드릴다운이 보임(첫 사용자는 그 동선을 거의 발견 못 함). 와이드(≥860px)는 `BtiPanel`이 좌 사이드바 상시 렌더라 버그 없음(모바일 전용). → `openTag`가 데이터 로드 후 `activeTab='bti'`로 자동 전환(한 줄; 와이드는 탭이 렌더에 무영향이라 무해). **TDD**: `profile-app.test.ts`에 회귀 테스트(shelf 탭 진입→태그칩 클릭→`.shop-drill` 등장+근거책 표시) RED→GREEN, 기존 2개 보존. vitest 633 GREEN. 번들 `profile.js` 재빌드. 백엔드 무변경. |
| 2026-06-26 | **feat: troubleshooting.md 목차 자동생성 커밋 게이트 (L5 누수 메움)** — AI 협업 성숙도 진단 후속. troubleshooting.md 📑 목차를 손으로 관리하다 항목이 누락되는 drift(본문엔 `## T-###`가 있는데 목차에 빠진 9개: T-037·038·040·041·042·061·064·065·066)를 메모리 인덱스(`rebuild-memory-index`) 패턴으로 하드픽스. 단 repo 추적 파일이라 SessionStart 대신 **커밋 게이트**(PreToolUse): troubleshooting.md 스테이징+`git commit` 시 목차를 본문 `## T-###` 헤딩에서 재생성→변경됐으면 **자동 re-add**(자동수정형 — T 추가 커밋에 목차 동봉, git dirty·멀티세션 충돌 없음). anchor=GitHub 규칙(소문자화→`[^\p{L}\p{N} _-]` 제거→공백을 `-`로), EOL·끝개행·BOM 보존(phantom CRLF·BOM 제거 회피, T-093 정신), 목차 섹션 못 찾으면 fail-open(본문 불변). 신규: `.claude/scripts/rebuild-troubleshooting-toc.ps1`(코어, 수동 실행 가능)·`.claude/hooks/require-troubleshooting-toc.ps1`(게이트)·`tests/test-require-troubleshooting-toc.sh`(16케이스: anchor·재생성·no-op·BOM 보존·훅 트리거·fail-open, RED→GREEN). `settings.json` PreToolUse 배선. 최초 1회 실행해 누락 9개 메움. **범위 밖**: T-084~T-102는 정식 `## T-###` 헤딩 없는 누적표 행이라 anchor가 없어 별도 본문 정규화 작업으로 분리. |
| 2026-06-26 | **docs: trailer×squash 함정 기록(T-104·N-128) + learning-notes 죽은 누적표 폐지** — 세션 메타 trailer가 squash 후 `git %(trailers)` 구조 조회로 안 잡히는 원인을 scratch repo로 실증 규명: git trailer 파서는 커밋 메시지의 **맨 끝 문단 1개만** trailer로 인식하는데, GitHub squash가 `Co-authored-by`를 `---------` 구분선과 함께 맨 끝 블록으로 붙여 `Session-*`가 중간으로 밀림(텍스트는 보존돼 `git log --grep`으론 잡힘, 구조 조회만 깨짐). 트러블슈팅 **T-104**(우회=grep)·학습노트 **N-128**(git trailer는 위치 의존이라는 개념)로 박음. 더불어 learning-notes 「🔄 누적 갱신」 표가 N-106에서 멈춰 본문과 21개 드리프트 → 상단 목차(📑)와 중복인 죽은 표라 **폐지**(표 115줄 삭제, N-001~N-128 본문·목차는 전부 보존; BOM·CRLF 보존). 문서 전용. |
| 2026-06-27 | **feat: 대시보드 '독서 기록' 빠른이동 타일 → 잔디 박스로 흡수** — 잔디 박스(`ContributionGraph`)와 빠른 이동(`QuickNav`)의 `독서 기록` 진입점 중복 해소. 잔디 카드 헤더에 `전체 기록 →`(`/history`) 링크 추가(정원 카드 `정원 가기 →` 패턴 대칭), pill 라벨 `최근 독서 기록`→`독서 기록` 격상, 연속일 칩은 pill 옆(왼쪽 묶음)으로 이동. 빠른 이동에서 `/history` 타일 제거 → 3타일(내 책장·내 책방·책BTI). CSS: `.dash-grass-head-left` flex 묶음, `.dash-grass-link` hover 공유(`.dash-garden-link` 그룹 합산). **TDD**: `contribution-graph.test.ts`·`quicknav.test.ts` 신규 RED→GREEN, vitest 637 전체 통과. 번들 `dashboard.js` 재빌드. 백엔드·`/history` 무변경. |
| 2026-06-27 | **docs: 세션 메타 effort `unknown` 자동 폴백 폐지 — 미상이면 다시 묻기** — CLAUDE.md 「세션 메타 기록」에서 effort 미고지 시 `unknown`을 묵시적으로 박던 폴백을 제거. 기록 시점(커밋 trailer·계획 md 상단)에 effort가 미상이면 비워두거나 `unknown` 대신 **사용자에게 다시 물어 실제 값을 받고서 기록**, `unknown`은 사용자가 명시적으로 "모름/미정"이라 답한 경우에만 적는다. 배경=새 세션(compact 포함)이 시작 시 effort 미고지면 묻지 않고 `unknown`을 박아 추적값이 비던 사례. soft 규칙 강화(문서 전용, 코드·테스트 0). |
| 2026-06-27 | **docs: troubleshooting T-084~T-102 본문 정규화** — 누적표 행으로만 있던 19개에 정식 ## T-### 헤딩 부여(목차 자동생성 범위 밖 결번 해소, #545 꼬리) + T-087 번호 중복 해소(빈 워크트리 항목→T-105). 목차는 훅 자동 재생성. |
| 2026-06-27 | **docs: troubleshooting T-106 — auto-merge `--delete-branch` 원격 브랜치 미삭제 트랩** — `--auto`(비동기 서버 머지)와 `--delete-branch`(gh의 즉시 로컬 삭제)는 시점이 어긋나 머지 후 원격 브랜치가 잔존(#548 후속에서 실측). 해결=MERGED 확인 후 `gh api -X DELETE .../git/refs/heads/<branch>`(T-094)·근본=repo auto-delete 설정. 목차는 훅 자동. 문서 전용. |
| 2026-06-27 | **docs: troubleshooting T-107 — `git add && git commit` 묶음이 PreToolUse 자동수정 훅을 skip** — 묶음 커밋은 훅이 add 전(명령 실행 전) 스테이징을 못 봐 목차 자동생성·번들·테스트 게이트가 skip될 수 있음(직전 T-106 커밋이 실제로 목차 누락→스크립트 수동실행+amend로 보정). 해결=`git add`를 별도 호출로 먼저, `git commit` 단독 호출. 문서 전용. |
| 2026-06-27 | **chore: gap#3 스크립트·머지 자동배선 — deleteBranchOnMerge ON + docker-cleanup SessionEnd 훅** — AI 성숙도 진단 누수 gap#3 메움. ① repo 설정 `deleteBranchOnMerge=true`로 머지 후 원격 브랜치 **서버 자동삭제**(T-106 근본 해결, T-094·T-095 caveat 무력화). ② `SessionEnd` 훅 `.claude/hooks/cleanup-docker-on-session-end.ps1`이 bootRun 잔재 컨테이너를 세션 종료마다 **기본 모드(Exited만, Up 보존)**로 자동 정리 — fail-open(docker 없거나 실패해도 종료 무방해). 테스트 `test-cleanup-docker-on-session-end.sh`(기본모드·fail-open·부재허용 RED→GREEN). settings.json SessionEnd 배선, CLAUDE.md 머지·docker 절 화해, troubleshooting T-106 근본해결 표시. pr-merge 자동머지 훅은 고위험이라 제외. 설정·훅·문서(코드 .java 0). |
| 2026-06-27 | **chore: gap#4 Phase A — CI 보안 게이트(CodeQL + Dependabot) 도입** — AI 성숙도 진단 누수 gap#4(required check가 `test` 단일)의 1단계. ① `.github/workflows/codeql.yml` 신규 — 내 코드 SAST(`java-kotlin`+`javascript-typescript`, `build-mode: none`으로 Gradle/Java21 빌드 회피), PR+main push+주간, `paths-ignore` 없음(N-070 회피). 우선 **advisory**(머지 안 막음). ② `.github/dependabot.yml` 신규 — 의존성 SCA(gradle `/`·npm `/frontend`·github-actions `/`) 주간 업데이트 PR. ③ learning-notes **N-129**(SAST vs SCA + CodeQL/Dependabot/SpotBugs/OWASP 위치) + 목차. 설정·문서뿐(코드 .java 0). **Phase B(미착수)**: CodeQL green 확인 후 required check 승격 여부 별도 결정(문서 PR에도 도는 비용 trade-off). |
| 2026-06-27 | **chore: gap#4 Phase B — CodeQL 스마트 조건부 required 승격** — Phase A advisory였던 CodeQL을 required check로 올리되 문서-only PR엔 세금 0. `codeql.yml`을 "항상 실행하되 코드(java/kt/js/ts/vue/gradle/Dockerfile/package.json) 변경 PR만 CodeQL init·analyze, 문서-only PR은 수 초 success"로 개편(fail-safe=분석; merge-base diff·`fetch-depth:0`). job을 path-skip 하지 않아 required여도 pending 영구블록(N-070) 없음. branch protection required contexts에 `Analyze (java-kotlin)`·`Analyze (javascript-typescript)` 추가(집계 `CodeQL`은 doc PR에서 안 떠 미포함). gap#4 종결. 설정·문서뿐(코드 .java 0). |
| 2026-06-27 | **fix(deps): web-push 5.1.1→5.1.2 — runtime로 내려간 전이 의존(httpcore·jose4j) 명시 복원** — Dependabot #559가 web-push를 5.1.2로 올리자 컴파일 실패: 5.1.2가 httpasyncclient(→`org.apache.http`)·jose4j를 `compile`→`runtime` scope로 내려, `PushSenderService`가 직접 쓰는 `org.apache.http.HttpResponse`·`send()`의 `throws JoseException`이 컴파일 클래스패스에서 빠짐(API·버전 불변, scope만 변경). build.gradle에 `httpcore:4.4.16`·`jose4j:0.7.9` 명시 추가(코드 0줄, bcprov-jdk15on exclude 유지). 전이 의존 안티패턴 → learning-notes 후보(#557 Jackson 2→3와 같은 클래스). |
| 2026-06-27 | **refactor: Jackson 2→3 마이그레이션 (Spring Boot 4.1 대비, #557 선행)** — Boot 4.1.0이 Jackson 2(`com.fasterxml.jackson`)를 전이에서 빼고 Jackson 3(`tools.jackson`)을 기본화하므로, Jackson을 직접 쓰는 코드를 미리 3으로 옮긴다(현 Boot 4.0.6에 Jackson 2·3 공존 → 안정 Boot 위에서 마이그레이션·검증 후 bump 분리=de-risk). 7개 파일(`AladinBookSearchClient`·`GeminiReadingPersonalityNarrator` + 테스트 5): import `com.fasterxml.jackson`→`tools.jackson`, `new ObjectMapper()`→`JsonMapper.builder().build()`(불변 빌더), `findAndRegisterModules()`→`builder().findAndAddModules()`. 동작 불변(기존 파싱 테스트 전수 green), `asText()`는 deprecated로 남아 유지. learning-notes **N-130**(전이 의존 안티패턴 2메커니즘) + 목차. |
| 2026-06-27 | **chore(deps): spring-boot 4.0.6→4.1.0 (#557)** — Dependabot Boot minor bump. #563(Jackson 3 마이그레이션) 선행 머지 후 rebase하니 컴파일 통과 — Boot 4.1이 Jackson 2를 전이에서 제거한 게 유일 파괴점이었고, 그 외 동작 회귀는 전체 테스트 green으로 확인(로컬 `./gradlew test` 2분 23초 + CI). 곁들여 troubleshooting **T-108**(gradlew.bat phantom-modified로 rebase 차단 — `.gitattributes eol=crlf` 불일치, `--assume-unchanged` 우회) 추가. build.gradle 버전 1줄 + 문서. |
| 2026-06-27 | **chore: gradlew.bat EOL 정규화 (.gitattributes eol=crlf 일치, T-108 근본)** — #560 gradle-wrapper bump가 gradlew.bat을 CRLF 블롭(CR 82바이트)으로 커밋했는데 `.gitattributes`의 `*.bat text`는 LF 저장 규칙이라, 매 세션 `git status`에 phantom-modified로 뜨고 브랜치 전환·rebase를 방해했다(T-108). `git add --renormalize gradlew.bat`로 블롭을 LF(CR 0)로 바로잡음 — working tree는 `eol=crlf`라 CRLF로 체크아웃되고 정규화 비교 시 블롭과 일치해 clean. 코드 변경 아님(EOL 정규화). |
| 2026-06-27 | **docs: README 상단 AI 협업 성숙도(L5) 다이어그램 추가** — AI 협업 성숙도 진단 결과를 시각화한 자기완결 SVG(`docs/ai-collaboration-level.svg`)를 README 최상단에 히어로로 박음. L1(단발 질의)→L5(자가개선 메타시스템) 마일스톤 트랙 + L5 정점(헤일로·자가개선 순환 화살표·"현재 위치") + 하단 진단 배지 2줄. GitHub `<img>` 렌더 안전 설계(스크립트·외부참조 0, viewBox 전체 불투명 배경 rect, 한글 시스템폰트, marker 대신 polygon 화살촉). 후보 4개 병렬 생성→합성 워크플로로 제작. 문서·자산뿐(코드 .java 0). |
| 2026-06-27 | **fix: 부채 갚는 구간에서 타이머 '남은 시간'이 멈추던 버그** — 대시보드 라이브 카운트다운(`useReadingTimer`)이 `remainingNow`를 `max(carriedDebtSeconds, …)`로 clamp해, 오늘 목표를 다 채운 뒤 과거 빚을 갚는 구간에서 floor(과거 빚)에 고정돼 매초 감소가 멈췄다(측정 종료 시에만 갱신 → 통일성 깨짐). 서버 도메인(`WeeklyDebtCalculator` backward-only 재분배)은 **오늘 초과분으로 과거 빚을 갚으므로**, 라이브 잔여도 전체 빚이 0이 될 때까지 줄어야 일관된다 → clamp를 `max(0, …)`로. 미사용이 된 `baseFloor` 인자·`goalMet` 정리, `DashboardModel` floor 주석 정정. 곁들여 `vite.config` test include에 `src/**` 추가 — `timerProgress.test.ts` 등 소스 곁 테스트 4개(105개)가 조용히 미실행되던 사각 복원. |
| 2026-06-27 | **chore: 워크트리 node_modules 정션 공유 — 매 세션 npm install 회피** — git worktree로 폴더를 분리할 때마다 `frontend/node_modules`가 없어(node_modules는 git 미추적이라 어느 워킹트리에도 복제 안 됨) 매번 `npm install`을 다시 하던 마찰 제거. 커밋(.gitignore 해제)은 `sharp` 등 OS별 네이티브 바이너리 때문에 안티패턴 → 대신 main의 node_modules를 워크트리에서 **Windows 디렉토리 정션**으로 재사용(다운로드 0·디스크 0, frontend는 빌드 전용이라 읽기 공유 안전). 신규 `.claude/scripts/link-node-modules.ps1`(멱등·`-Force`·`-DryRun`, main 비면 먼저 `npm ci`로 채움) + CLAUDE.md 워크트리 절에 호출 한 줄 + learning-notes **N-132**. 설정·스크립트·문서뿐(코드 .java 0). |
| 2026-06-27 | **docs: learning-notes N-133 — CodeQL을 PR·main push 둘 다 트리거하는 이유** — 머지 후 CodeQL이 또 도는 게 중복 아니냐는 질문에서 정리. PR 스캔=머지 게이트(취약점 차단), main push 스캔=default branch baseline(Security 탭 alert 추적)으로 *역할이 다름*. 같은 `push: main`에 뜨는 deploy 워크플로는 CodeQL과 독립(배포가 부르는 게 아님). 비용=머지당 1회(public이면 무료) < baseline 추적 가치 → 현행유지 권장. [[N-129]] 연결. 문서뿐. |
| 2026-06-27 | **chore: 워크트리 node_modules 정션 자동 연결 — SessionStart 훅** — #567 정션 스크립트를 매 세션 수동 실행하던 마찰까지 제거(사용자 입력 0). 신규 훅 `link-node-modules-on-session-start.ps1`이 SessionStart마다 세션 cwd에서 `link-node-modules.ps1`을 돌려 워크트리 `frontend/node_modules`를 main 정션으로 자동 연결 — 멱등(이미 연결 시 즉시 통과)·main이면 no-op·**fail-open**(git 없음·스크립트 실패·정션 오류에도 `exit 0`으로 세션 시작 안 막음)·출력 ASCII(N-006). `settings.json` SessionStart 배선(timeout 120) + 테스트 `test-link-node-modules-on-session-start.sh`(git repo 호출됨·fail-open 3종 RED→GREEN) + 실 훅→스크립트 통합 실증(main 0→`npm ci` 137 자가복구·정션·타깃 보존). CLAUDE.md 워크트리 절: 수동→자동 안내 갱신 + ⚠️ 정리 시 정션 먼저 제거(worktree remove가 정션 타깃 삭제 위험 — 실증 중 발견). 설정·훅·문서뿐(코드 .java 0). |
| 2026-06-27 | **feat: 책방 헤더 액션 정리 — 차단·신고를 케밥 드롭다운으로, 신고 모달 팝업** — 남의 책방(`/u/{loginId}`) 헤더에서 차단 버튼을 제거하고, 헤더 카드 우상단에 케밥(⋯) 드롭다운을 추가해 차단·신고를 모았다. 신고는 오버레이 모달 팝업으로 표시(기존 details 펼침 박스 → 모달 교체, 모바일·와이드 양쪽 details 제거). 팔로우 버튼은 초록 강조 유지 + 차단 버튼 제거로 1개만 남아 크기 통일감↑. 신규 `ReportModal.vue`, `navIcons.ts`에 `more` 케밥 아이콘 추가, `app.css`에 드롭다운·모달 CSS. `shop-header.test.ts`(신규) + `profile-app.test.ts`(확장)으로 RED→GREEN TDD. |
| 2026-06-27 | **docs: troubleshooting T-110 — 정션 둔 워크트리의 worktree remove가 정션 타깃을 비운다** — #567/#569 정션 워크플로의 정리측 함정을 격리 재현으로 확정해 박음. 정션 건 워크트리를 `git worktree remove --force`로 지우면 정션을 따라가 타깃(main node_modules) **내용**을 삭제(폴더는 남고 0개 — #567에서 137→0이던 정체). 해결=remove **전에** `[IO.Directory]::Delete()`로 정션만 끊기(`Remove-Item -Recurse`는 타깃까지 지워 금물), #569 대조 입증. troubleshooting T-110 + 목차 + 누적갱신, CLAUDE.md 정리순서는 #569에서 이미 반영. 문서뿐(코드 .java 0). |
| 2026-06-27 | **feat: 책방 헤더 제목 닉네임만 + 폰트 18px 고정 + 한 줄 말줄임** — 책방(`/u/{loginId}`) 헤더 제목을 `{nickname}님의 책방` → 닉네임만으로 변경(인스타/트위터식). `font-size: clamp(22px, 3vw, 28px)` → 18px 고정(아바타 이니셜 동일 크기)으로 바꿔 데스크톱 넓은 화면에서 긴 닉네임이 2줄로 개행되던 문제 해결. `white-space: nowrap; overflow: hidden; text-overflow: ellipsis;`로 미래 긴 닉네임도 한 줄 말줄임 고정. flex 자식 ellipsis 함정 방지용 `.shop-id-text { min-width: 0; }` 추가. `shop-header.test.ts` 확장(닉네임만/책방 텍스트 제거 RED→GREEN TDD). |
| 2026-06-27 | **feat: 책BTI(/personality) 페이지 Claude Design 재스킨** — 시안(책BTI.decoded.html FRAME1~4)대로 4상태(READY·COLD_START·FALLBACK·한도소진)를 종이톤+세이지로 재스킨. **스코프 격리**: 신규 `.pbti-*` 클래스 + personality 전용 `.personality-*`/`.carousel-*` 재스킨만 — 공유 클래스(`.card`·`.greeting`·`.status-line`·`.tag-chip`·`.btn-*`·`.record-*`·`.link-row`) 정의 미접촉(검색·책방·history·대시보드 회귀 0), 간격은 `body.personality-page #personality-app{gap:18px}`로 N-115 공유 규칙 위에 페이지 스코프 덮어쓰기. **§4 정합 플래그**: 태그칩 균일 비클릭(클릭 배선 없음), COLD_START 진행바 제거·카피 보존(coldStartMinBooks=1), 모든 수치 실데이터 바인딩(완독/저자/장르 수·장르/저자 라벨 ` · ` join·남은 N/3), header.brand 유지(시안 앱바 미채택), 이모지(🧬⭐🌍🔄⏳📚)→라인아이콘(DNA·별·지구본·회전화살표·모래시계·책·셰브론·아래화살표), 캐러셀 도트 인디케이터 신규. 캐러셀 step/sync/scrollBy 스크롤 로직은 보존(시각만 재스킨, prev/next를 absolute 오버레이→인라인 34px 원형). **TDD RED→GREEN**: 순수함수 `personalityView.ts`(joinLabels 구분자·refreshState 3상태 우선순위·dotIndex 0나눗셈/반올림/clamp) 11개 RED→구현→GREEN, 기존 `personality-app.test.ts` 셀렉터 갱신(`.refresh-form`→`.pbti-refresh`, `'2/3'`→`'2 / 3'`)으로 동작 무회귀 입증, 프론트 vitest 764 전부 GREEN. **검증**: static-preview(N-117)로 실 빌드 `personality.js`+실 `app.css`를 mock `/api/personality`로 띄워 4상태 렌더·CSSOM 규칙 전수 파싱·gap 18px·캐러셀 scroll+도트 갱신·select/refresh POST+CSRF·반응성·한도소진 회색버튼(#ECE6D9/#A89F8C)·콘솔 에러 0 실측. 이 과정에서 추가 주석의 `.personality-*/.carousel-*`·`.btn-*/.record-*` 내 `*/`가 블록주석을 조기종료(바로 뒤 `body.personality-page` 규칙 침묵 드랍)하는 **N-118 3회차 재발**을 CSSOM 점검으로 잡아 수정(슬래시→가운뎃점). 번들 재빌드. |
| 2026-06-28 | **feat: 책BTI(/personality) 독서 카드 개별 구분 + 선택 카드 '선택됨' 풋터** — 캐러셀 슬라이드가 테두리·배경 없는 맨 본문이라 부모 카드(FCFAF5) 안에서 서로 구분이 안 되던 문제를, 각 슬라이드를 테두리(1px `--border`)+radius 16px+패딩+은은한 그림자로 "띄운 카드"화해 해결. 대표(선택) 카드는 sage 테두리(`--accent`)+sage 리프트 그림자로 한눈에 구분. 선택된 카드만 하단이 비던 비대칭(선택 버튼이 `v-if="!selected"`)을, 비클릭 "✓ 선택됨" 상태 풋터(선택 버튼과 같은 footprint, `margin-top:auto`로 하단 정렬)로 채워 카드 높이 균형+빈 느낌 해소. 캐러셀 트랙 `padding-block:6px`로 `overflow-x` 트랙의 세로 그림자 클리핑 방지. **스코프**: personality 전용 `.personality-entry`/`.pbti-select-rep`/신규 `.pbti-selected-mark` + Carousel `v-else`만 — 공유 클래스·스크롤 로직(step/sync/scrollBy) 미접촉. **TDD RED→GREEN**: `personality-app.test.ts`에 2-엔트리(선택1+후보1) 회귀 테스트(선택 카드 `.pbti-selected-mark` "선택됨" 노출·비버튼·선택버튼 후보 1개만) RED→`v-else`+CSS 구현→GREEN, 프론트 vitest 766 전부 통과. **검증**: static-preview(N-117)로 실 번들·실 CSS 로드 후 computed style 실측(후보 border #E4DDD0·radius 16px·shadow / 대표 border #6E8A6A·sage shadow / "선택됨" DIV·bg #E7EEE2 / 트랙 padding 6px)·DOM 3카드·콘솔 0에러. 번들 재빌드. learning-notes **N-134**(overflow-x:auto가 overflow-y를 auto로 끌어올려 가로 스크롤러 속 자식의 세로 그림자가 잘리는 함정·padding 회피) 추가. |
| 2026-06-27 | **chore: PR 머지 BEHIND 무한대기 자동 해소 — `pr-merge.sh` BEHIND 처리 + `--arm` 모드 (T-111)** — 레포 "머지 전 브랜치 최신화 필수" 정책에서 BEHIND인 PR에 `gh pr merge --auto`만 걸면 GitHub가 브랜치를 자동 갱신 안 해(auto-update off) **영영 안 머지**되던 사각(이번 #574에서 실제 발생, 수동 rebase로 풀었음)을 자동화로 막음. ① `pr-merge.sh`에 `try_update_branch`(BEHIND를 `gh pr update-branch` 비파괴 서버사이드 갱신으로 해소) 추가 — 폴링 루프의 `BEHIND` 케이스(이전엔 catch-all로 타임아웃까지 대기만) + 신규 **`--arm` 모드**(`--auto` 걸고 BEHIND/DIRTY만 1회 풀고 즉시 종료=「걸고 떠나기」, 머지는 서버가 마저). ② 스모크 테스트 `.claude/scripts/tests/test-pr-merge-behind.sh`(arm/sync BEHIND→update-branch·DIRTY no-rebase→exit3·CLEAN→arm, dryrun+FAKE_STATE 10단언). ③ CLAUDE.md 🔀 Git 워크플로 5번: L115 "폴링 불필요/hang 소멸" 오기 정정·표준 경로를 `pr-merge.sh <PR> --arm`으로 승격·bare `--auto` 단독 금지, BEHIND 상태 항목 추가. ④ troubleshooting **T-111**(머지 자동화 hang 군 6회차, T-083·T-091·T-094·T-102 후속) + 상단 재발 트래커 갱신. 이 PR 자체를 새 `pr-merge.sh --arm` 경로로 머지해 실전 검증. |
| 2026-06-28 | **chore: 프론트 번들 EOL 유령 변경 차단 — `.gitattributes`에 빌드 산출물 `eol=lf` 고정** — `core.autocrlf=true` 환경에서 vite/Node가 LF로 생성하는 번들(`static/<app>/<app>.js`)이 git의 CRLF 기대와 어긋나 빌드마다 `git status`에 영구 'modified(유령)'로 떠(=`git diff`는 0줄) 세션/워크트리 정리 때 "uncommitted" 경고가 반복되던 문제를 구조적으로 차단. `.gitattributes`에 `src/main/resources/static/**/*.js text eol=lf` 추가(autocrlf보다 우선·커밋돼 portable) + `git add --renormalize .` 1회로 인덱스 정합(블롭 이미 LF라 내용 churn 0). **실측 검증**: 규칙 전 9개 번들 유령 재현(RED) → 규칙+renormalize 후 전부 clean·새 유령 없음(GREEN) → 깨끗 상태 재빌드 후에도 clean(재발 방지 실증). 진단 핵심 도구 `git ls-files --eol`(유령=`i/lf w/lf` vs 정상=`i/lf w/crlf`). learning-notes **N-135** 추가. |
| 2026-06-28 | **feat: 책BTI(/personality) 분석 카드 데스크톱 와이드** — 데스크톱 넓은 화면에서 캐러셀 분석 카드가 전역 `.container` max-width:460px에 갇혀 긴 AI 서술이 세로로 길쭉하던 문제(모바일은 이 폭이 적절)를, `@media (min-width:600px){ body.personality-page .container{max-width:720px} }` 한 줄로 페이지-스코프 와이드 처리. 같은 글이 더 적은 줄로 흘러 narrative 실측 **17줄→9줄**(232→440px), 옆 카드 peek·도트·캐러셀 `step()`/`dotIndex` 스크롤 로직은 무변경(슬라이드 폭을 실측 역산하므로 JS 불필요). garden-wide·dashboard와 동일한 페이지-스코프 패턴이라 다른 페이지 회귀 0, min-width 가드로 모바일(<600px) 무손상. **검증**: 순수 시각 변경(`.java`·`frontend/**` 변경 0, 단위테스트 무의미)이라 프론트 검증 규칙대로 실 브라우저(booktimer.app/personality, viewport 1920)에 동일 규칙 주입해 줄 수 감소·요소 정렬·레이아웃 무손상 실측 + 회귀 위험 적대적 감사(스코프누수·캐러셀정합성·와이드요소). |
| 2026-06-28 | **feat: 책BTI(/personality) 상단 제목 제거(sr-only 보존) + 안내문 최하단 작은 고지로 이전** — 헤더의 가시 제목 행(DNA 아이콘 + "{닉}님의 책BTI")과 안내문(`.pbti-subtitle`)을 제거해 화면 최상단을 BookTimer 로고 → 바로 "내 독서 성향" 카드로 정리. 제목은 `h1.sr-only`(visually-hidden 유틸 신설, position:absolute·1px·clip)로 **보존**해 문서 제목·접근성·SEO 유지(시각만 숨김). 안내문("MBTI처럼…")은 페이지 **최하단**(NavLinks 아래)에 보험 약관식 작은 고지(`.pbti-disclaimer` 11px·muted·가운데, READY/COLD_START/FALLBACK 모든 상태 공통)로 이전. **스코프**: personality 전용(`.pbti-*` + 신규 전역 `.sr-only`는 기존 미사용처라 회귀 0). **TDD RED→GREEN**: `personality-app.test.ts`에 "가시 제목 행·`.pbti-subtitle` 제거 + `h1.sr-only` 책BTI 보존 + 최하단 `.pbti-disclaimer` 1개" 회귀 테스트 RED→`PersonalityApp.vue`/`app.css` 구현→GREEN(프론트 vitest 767 전부 통과). **검증**: N-117 static-preview로 실 번들 `personality.js` + 실 `app.css`를 mock `/api/personality`로 띄워 h1 시각숨김(rect≤2px)·상단 가시 제목 부재·하단 고지 11px/muted(rgb111,106,94)/가운데/최하단(lastChild)·콘솔 0에러 실측(데스크톱). 번들 재빌드. |
| 2026-06-28 | **feat: 책BTI(/personality) 두 안내 문구를 "내 독서 성향" 카드 우상단 ? 헬프 팝오버로 이전** — 상시 노출되던 두 문구(하단 공개 안내 박스 `.personality-public-note` "공개한 책만…" + 최하단 고지 `.pbti-disclaimer` "MBTI처럼…")가 READY 화면을 어수선하게 하던 걸, 카드 우상단 `?` 헬프 아이콘 클릭 토글 팝오버(범용 패턴)로 숨김. `?` 클릭 시에만 두 문구가 말풍선으로 노출, **밖 클릭(투명 백드롭)·Esc·재클릭** 3경로로 닫힘. `aria-expanded`/`role=dialog`/`aria-label`로 접근성 보존. **상태별**: READY만 `?` 팝오버(카드·공개 안내가 READY 전용) — COLD_START·FALLBACK엔 앵커할 카드가 없어 `.pbti-disclaimer`를 최하단에 그대로 노출(`v-if="state!=='READY'"`, 비-READY 무변경). 죽은 `.personality-public-note` CSS 제거(`.pbti-note-ico`는 팝오버 내 재사용 보존). **스코프**: personality 전용(`.pbti-card-head`/`.pbti-help-btn`/`.pbti-help-pop`/`.pbti-help-backdrop` 신규 + `PersonalityApp.vue`) — 공유 클래스·캐러셀 로직 미접촉. z-index: 백드롭50 < 헤더(is-open)60 → 버튼·팝오버가 백드롭 위라 열린 채 버튼 재클릭 가능. **TDD RED→GREEN**: `personality-app.test.ts`에 팝오버 토글(초기 닫힘·두 문구 미노출→클릭 시 노출→재클릭 닫힘)·백드롭 닫힘+aria 토글·COLD_START `?` 부재+고지 잔존 회귀 테스트 RED→구현→GREEN(프론트 vitest 770 전부 통과). **검증**: N-117 static-preview로 실 번들+실 app.css를 mock `/api/personality`로 띄워 — `?` 우상단 근접(gapRight 21·gapTop 24)·초기 닫힘·두 문구 미노출·클릭 시 팝오버(공개안내+고지+프로필링크, z61, width 300, 뷰포트 내)·`elementFromPoint`로 백드롭/버튼/팝오버 스택 순서·백드롭클릭/Esc/재클릭 닫힘·콘솔 0에러 실측 + 스크린샷 육안 확인. 번들 재빌드. |
| 2026-06-28 | **feat: 책BTI(/personality) 모바일 한정 — 긴 AI 서술 내부 스크롤(카드 높이 단축)** — 모바일(<600px) 좁은 폭에서 긴 성향 서술이 카드를 길쭉하게 늘이던 걸, 서술 `<p class="personality-narrative">`에만 `max-height:240px`(≈8줄) + `overflow-y:auto`를 줘 **본문만 내부 세로 스크롤**로 잘라 카드 높이를 일정하게 함. 배지·시간·"대표 선택" 버튼은 슬라이드의 형제라 고정(가운데 본문만 스크롤). `overscroll-behavior:contain`(끝까지 스크롤해도 페이지 체이닝 안 됨) + `scrollbar-width:thin`("더 있다" 힌트, 캐러셀의 전역 스크롤바 숨김과 달리 노출). **경계**: `@media (max-width:599px)` 한 블록 — task1 와이드(`min-width:600px` `.container` 720px)와 정확히 맞물려 데스크톱(≥600px)은 무변경(이미 9줄로 짧음). 캐러셀은 가로(`overflow-x`)라 세로 내부 스크롤과 축이 직교 → 가로 스와이프 충돌 없음, `step()`/`dotIndex`는 가로 로직이라 높이 무관(JS·번들 무변경). **스코프**: `app.css` 한 미디어 블록만(`.personality-narrative` 한정) — Vue·번들·공유 클래스 미접촉. **TDD**: 순수 CSS 미디어쿼리·스크롤이라 jsdom 단위테스트 무의미 → 실 브라우저 검증을 게이트로. `resize_window`가 이 환경서 뷰포트(innerWidth)를 못 바꾸는 한계를 **폭 390px iframe에 preview를 로드**(미디어쿼리는 iframe 자체 뷰포트를 봄)해 우회, 모바일 발동을 진짜로 측정: 서술 computed `max-height 240px`/`overflow-y auto`/`overscroll contain`·scrollH 977>clientH 240·`scrollTop` 이동 가능·배지 위/버튼 아래 고정·슬라이드 375px로 단축 vs 부모(1920) 캡 미적용(`max-height none`, 266px 무스크롤) 동시 실측 + 스크린샷 육안 확인(얇은 스크롤바·마지막 줄 컷). 적대적 회귀 감사(스코프/반응형 경계/캐러셀 정합)로 회귀 0 확인. |
| 2026-06-28 | **fix(인프라): `www.booktimer.app` 미정규화로 검색 유입자 로그인 불가 — ALB 301에 누락 호스트 추가** — 구글 검색 1위가 `www.booktimer.app`(canonical 신호 전무로 구글이 www를 독립 색인)인데, ALB 443 우선순위1 리디렉트 규칙이 `.click`·`www.click`만 apex로 보내고 신규 `www.app`을 빠뜨려(PR #315 누락) 기본 forward로 샜다 → 앱이 `X-Forwarded-Host: www…`로 redirect_uri를 `https://www.booktimer.app/login/oauth2/code/google`로 동적 생성(구글 콘솔 apex만 등록) → `400 redirect_uri_mismatch`, 동시에 host-only 세션 쿠키(apex 발급)가 www엔 안 실려 비로그인 랜딩. 주소창 apex 직접진입은 정상(검색 유입자만 깨짐). 해소=ALB 규칙 호스트 조건에 `www.booktimer.app`을 OR 값으로 추가(콘솔, 새 조건 추가는 AND라 .click 301 파손—금물), curl로 www 홈·OAuth 301→apex·최종 redirect_uri=apex·구글 로그인화면 200 + `.click`·apex 무회귀 실측. 진단=병렬 워크플로(OAuth·세션·도메인·프록시·랜딩 5스캔)+라이브 curl 호스트별 redirect_uri 실측. troubleshooting **T-113** + learning-notes **N-138** + plan.md §도메인 이전 정정. 인프라·문서뿐(코드 .java·frontend 0). |
| 2026-06-28 | **chore: 워크트리 안전 삭제 도구 — `remove-worktree.ps1` + `/remove-worktree` 스킬 (T-110 하드픽스)** — 워크트리의 `frontend/node_modules`는 메인을 가리키는 정션이라 `git worktree remove`·`rm -rf`가 정션을 따라가 메인 node_modules를 비우던 사고(T-110, 137→0)를 prose 주의문 대신 **도구**로 차단. 신규 `.claude/scripts/remove-worktree.ps1`(정션이면 `[IO.Directory]::Delete`로 링크만 끊고→`worktree remove`→브랜치 `-d` 정리; 가드=비워크트리·main·미등록·cwd-inside 거부 exit 3; `-DryRun`/`-Force`/`-KeepBranch`; 호출자 EAP=Stop에도 안전하게 자체 `Continue`(N-006)) + 이 repo **첫 스킬** `.claude/skills/remove-worktree/SKILL.md` 래퍼(`/remove-worktree`) + 스모크 테스트 `tests/test-remove-worktree.ps1`(임시 repo+실정션으로 ★타깃 보존(T-110)·dry-run 무변경·비워크트리/cwd 거부 4검 RED→GREEN). 실 워크트리 `-DryRun` 검증(정션 감지·무변경)까지 확인. CLAUDE.md 워크트리 절: 수동 2줄→도구 호출. 도구·문서뿐(코드 .java·frontend 0). |
| 2026-06-28 | **docs: troubleshooting T-112 — Chrome MCP `resize_window` 뷰포트 무변경 시 iframe으로 모바일 미디어쿼리 검증** — 모바일 한정 CSS 검증에서 `resize_window`가 렌더 뷰포트(`innerWidth`)를 못 바꿔(1920 고정) `@media(max-width:599px)`가 발동 안 하던 사각을, **폭 N px iframe에 같은 페이지를 로드**(미디어쿼리는 iframe 자체 뷰포트를 봄)해 모바일 규칙을 실발동시키고 같은 오리진 `contentDocument`로 측정·부모(1920)와 동시 대조하는 우회법으로 박음(직전 책BTI 모바일 스크롤 작업서 확립, 2회 봉착). troubleshooting T-112 추가 + 누적 갱신(목차는 훅 자동 재생성). 문서뿐(코드 0). |
| 2026-06-28 | **fix: 책BTI(/personality) 모바일 카드 본문 아래 큰 빈칸 제거 + 카드 길이 확대** — 모바일에서 일부 카드 본문 아래 큰 빈칸이 부자연스럽던 버그 수정. **원인**: 캐러셀(`display:flex`, align stretch)이 모든 슬라이드를 가장 높은 카드로 늘이는데, 좁은 폭에서 'stale 알림'(이 분석 이후 책장이 바뀌었어요…)이 여러 줄로 감겨(실측 168px) 그 카드가 제일 커짐 → 직전 PR의 `max-height:240px` 고정 본문 탓에 stale 없는 카드는 (고정 본문 + `margin-top:auto` 풋터) 사이에 ~196px 빈칸(데스크톱은 본문이 캡 없이 차 안 두드러져 정상). **해결**: 모바일 블록을 `.personality-entry{min-height:500px}` + `.personality-narrative{flex:1 1 0; min-height:0; overflow-y:auto…}`로 교체 — 본문이 남는 세로공간을 채워 빈칸 0, stale 없는 카드는 본문이 더 길게 보이고(365px≈12줄) stale 카드는 본문이 줄어 내부 스크롤(182px), 카드 높이 500px 일정(요청대로 더 길게). **스코프**: `app.css` 모바일 미디어 블록만 — Vue·번들·공유 클래스·데스크톱 무접촉. **검증**: iframe 390px로 RED(빈칸 196px·slideH 558) 재현(mock에 stale 카드 주입) → 수정 후 GREEN(빈칸 13px=정상 gap·slideH 500 균일·stale 카드 내부 스크롤 동작·데스크톱 `flex-basis:auto`/`overflow:visible` 무변경) 실측 + 스크린샷. 적대적 회귀 감사(flex 정합·반응형 경계·UX)로 회귀 0 확인. |
| 2026-06-28 | **fix: 책BTI(/personality) stale 알림 문구 축약** — 분석 카드의 stale 알림 문구가 너무 길어(모바일 좁은 폭에서 4줄·168px) 카드를 키우던 걸, "이 분석 이후 책장이 바뀌었어요 — '다시 분석'하면 지금 책장 기준으로 새로 봐요." → **"지난 책장 기준 분석이에요."** 한 줄로 축약(사용자 요청: 과거 책장 기준이라는 내용만). `PersonalityCarousel.vue` 문구 1개만(Java javadoc/주석은 무관). 부수효과: 모바일 stale 박스 168→64px로 줄어 그 카드 서술이 182→265px로 늘어 가독성↑(직전 빈칸 수정의 min-height:500 유지). 번들 재빌드, 프론트 vitest 16 통과, static-preview(iframe 390px)로 새 문구·레이아웃 실측. |
| 2026-06-28 | **fix: 책BTI(/personality) '자주 읽는 저자'에서 옮긴이 제외 — 글쓴이만 집계** — '책장 요약 > 자주 읽는 저자'에 알라딘 author 원문("레프 톨스토이 (지은이), 연진희 (옮긴이)")이 통째로 한 라벨이 돼 옮긴이가 섞여 노출되던 문제(사용자 요청: "옮긴이를 자주 읽는 저자 판단 요소로 쓰지 마")를 **글쓴이만 남기기**로 해결. 신규 순수 유틸 `WriterName.lead(author)`가 author를 쉼표로 토큰화해 옮긴이·그림·사진·엮음·감수 등 비저자 역할을 제외하고 글쓴이(지은이·글·저·원작·공저 등 또는 역할표기 없는 이름) 대표 1명만 추출(없으면 null=분포 제외). `ReadingProfileAggregator`(자주 읽는 저자·distinctAuthors)와 `PersonalityTagAttribution`(저자 충성도 태그 근거책) 두 집계가 이를 사용 — 같은 저자·다른 옮긴이 책이 합산되고(distinct 정확↑) 옮긴이만 있는 책은 제외. **범위는 책BTI 집계에만 국소**(검색·책상세의 author 표시는 옮긴이 정보가 유용해 원문 유지, 정원 작가 캐릭터 `AuthorCharacterUnlockCalculator.normalize`와도 목적 분리 — 그쪽은 옮긴이 남기고 공백 제거한 contains 매칭용). LLM 서술(`bookFactsJson`)도 정제된 topAuthors만 입력받아 옮긴이를 모르고(서술 누수 0), 캐시는 `ProfileSignature`가 topAuthors·distinctAuthors를 포함해 분포 변화 시 자동 재생성(stale 옮긴이 서술 자연 해소). **TDD RED→GREEN**: 신규 `WriterNameTest`(지은이+옮긴이/순수이름/옮긴이만/비저자역할만/복합·옮긴이선행/공저/공저역할/전각괄호/null·blank) RED→구현→GREEN + `ReadingProfileAggregatorTest`·`PersonalityTagAttributionTest`에 옮긴이 제외 회귀 케이스 추가, 전체 테스트 그린(회귀 0). 적대적 검증(알라딘 포맷·화이트리스트 오탐·누락·회귀·요청충족 4축)으로 전각 괄호 방어(반각만 매칭 시 역할이 이름에 남는 사각)·공저 채택을 테스트로 명문화 보강. 백엔드만(프론트·번들·Flyway·DB 무변경 — topAuthors 라벨이 깔끔한 글쓴이명이 돼 프론트 `joinLabels`가 자연 정리). |
| 2026-06-28 | **feat: 책장↔책방 혼동 정리 — 책방 패널 라벨 '공개 책장'→'공개한 책' + 책장 하단 '내 책방' 네비 타일** — 책장(`/books`, 본인 전용 관리)과 책방(`/u/{id}`, 공개 프로필)은 청중(본인만 vs 누구나)이 정반대라 **통합 대신 분리 유지**하되, 혼동의 실제 원인인 용어 겹침·네비 비대칭만 정리(코드 전수 파악 + 멀티에이전트 분석으로 audience·thesis 근거 확인). **(A) 용어** 책방 내부 패널명 `공개 책장`→`공개한 책`(`ShelfPanel.vue` h2·aria-label, `ProfileApp.vue` 모바일 탭) — '책장' 단어가 `내 책장` 화면과 겹쳐 헷갈리던 걸 해소(빈상태 "아직 공개한 책이 없습니다"와 호응). 브랜드 어휘 `내 책장`/`내 책방`(QuickNav)은 보존. **(B) 네비** 책장 하단 `NavLinks`에 `내 책방`(`/u/{myLoginId}`, icon `user`) 타일 추가로 책장↔책방 양방향 대칭화(책방 하단엔 이미 `내 책장` 타일, 책장→책방은 본문 힌트뿐이던 비대칭 해소). **TDD RED→GREEN**: `books/pure.ts`에 `booksNavLinks(myLoginId)` 순수함수 신설(빈/공백/null `myLoginId`면 `/u/` 깨진 링크 방지 가드, N-055 정신) — `books-pure.test.ts`에 순서·icon/label 정확값·빈/공백/null 제외 RED→구현→GREEN(프론트 vitest 774+ 전부 통과, profile-app 회귀 0). CSS 일관성 리팩터는 클래스 이미 분리(`shop-*`/`shelf-*`)라 보류. **검증**: 3 lens 독립 adversarial 리뷰(누락·네비회귀·접근성) 전부 pass. 번들 재빌드(`books.js`·`profile.js`). |
| 2026-06-28 | **feat: 책장(/books) 데스크톱 와이드 내부 스크롤 복원 + 상단 텍스트 네비→하단 타일 단일화** — 데스크톱(≥880px)에서 책장이 책 수만큼 길어지고 이동 링크가 폭 따라 갈리던 걸, 와이드 `@media`의 스크롤 해제(`#books-app .shelf-list{max-height:none;overflow:visible}`, 2열 그리드 "시안")를 제거해 **grid 2열 + `.shelf-scroll` 60vh 내부 스크롤 공존**(책방 `.shop-shelf>.shop-books` 선례와 동일)으로 복원하고, 상단 `shelf-greeting-nav`(← 대시보드/📖 독서 기록 텍스트 링크) 마크업·CSS를 제거해 **하단 `.link-row` 타일(대시보드·독서기록·내 책방)을 전 폭 단일 이동 수단**으로 통일. 좁은 폭은 #190 이래 이미 60vh 스크롤+하단 타일이라 회귀 0. **검증**: 순수 CSS @media+마크업 제거(`.java` 0, `pure.ts` 불변, 단위테스트 무의미)라 프론트 검증 규칙대로 static-preview 하니스(실 `app.css`+production 클래스 체인+책 30권)로 와이드 1280/모바일 375 양폭 computed 실측 — 와이드 `.shelf-list` `display:grid`·`max-height 492px(60vh)`·`overflow-y:auto`·내부 스크롤(scrollH 2582>clientH 492)·하단 `.link-row` 노출·상단 nav 부재, 모바일 1열+60vh+하단 타일 무회귀 + 스크린샷 육안. 번들(`books.js`) 재빌드. |
| 2026-06-28 | **feat: 메인 페이지 명칭 '대시보드' → '홈' (사용자 대면 텍스트 전수)** — 이 웹의 메인(루트 `/`) 페이지 호칭을 '대시보드'에서 '홈'으로 통일. 화면에 보이는 '대시보드' 텍스트만 전수 변경: 전 페이지 하단 네비 홈 타일 라벨('대시보드'→'홈', Thymeleaf `navLink` 5곳 + Vue/ts `label` 7곳), 좌상단 로고 툴팁('메인 대시보드로'→'홈으로', 전 SSR ~13곳), `dashboard.html` `<title>`('홈 — BookTimer'), 마을 HUD '← 홈'·책방 not-found '홈'·admin 안내문('홈 인사말 자리'). **보존**: URL `/dashboard` 별칭(구 PWA 콜드런치 404 방지, `DashboardControllerTest`로 고정)·`/api/dashboard`·영문 식별자(`DashboardController`/`DashboardApp`/`dashboard` 디렉토리·파일명)·CSS(`.dashboard-page`/`#dashboard-app`/`.dash-*`)·코드 주석/문서(화면 비노출, 내부 dashboard 식별자 맥락) — 메인 URL은 이미 `/`라 사용자에게 영문 노출 0이므로 표시 텍스트만으로 충족. **TDD RED→GREEN**: `booksNavLinks` 홈 타일 `label='홈'` 단언 추가(`books-pure.test.ts`) → RED(코드가 '대시보드') → `pure.ts` label '홈' → GREEN(프론트 vitest 775 전부 통과, 회귀 0). **검증**: 순수 표시 텍스트 치환(로직 0, 로드순서·반응성 무관)이라 grep 전수로 표시 '대시보드' 잔존 0(주석만 보존)·번들 `label:"홈"` 반영·한글 무결성 + 단위테스트로 완결(실 브라우저 불요). 번들 8개(books·history·search·profile·personality·follow-list·block-list·garden) 재빌드. |
| 2026-06-29 | **docs: plan.md에 '독서 스토리'(인스타 스토리식 24h 문장 공유) 백로그 항목 추가** — 책을 읽다 인상 깊은 문장을 올리면 나를 팔로우한 사람에게 24시간만 보이는 인스타 "스토리" 차용 기능을 §SNS와 §책BTI 사이에 아이디어/백로그로 기록. 사용자 1차 방향 3종 확정(문장 출처=책 연결+자유 텍스트 둘 다·형식=텍스트 카드만 이미지 업로드 없음·착수=밀도 신호 뒤 백로그). 기존 토대 재사용 명시(follow 조인·`BaseTimeEntity` 24h 표시 필터·`Block`/`Report`/`RateLimitService` 게이트, N-037 정신) + 새 저장은 `story` 테이블(V53)·`Story` 엔티티/서비스/API·인스타식 Vue 섬뿐 + 열린 질문(공개범위·열람표시·다중카드·물리삭제). 구현 전 sns-design.md 설계 합의 필수 못 박음. plan.md 본문만(코드·DB 0). |
| 2026-06-29 | **feat: 책장·책방 제휴 수수료 안내문을 우상단 ⓘ 토글 팝오버로 전환** — 책장(`/books`)·책방(`/u/{id}`) 하단에 상시 노출되던 제휴(알라딘·쿠팡 파트너스) 수수료 고지문을, 책BTI(`/personality`)의 `?` 헬프 팝오버와 같은 **우상단 토글**로 옮겨 평소엔 접고 필요할 때만 펼치게 함(아이콘은 `?`와 구분되는 ⓘ 정보 — 사용자 선택). **공용 CSS 1세트**(`.affiliate-pop-*` — 앵커가 ⓘ 버튼 래퍼라 말풍선 꼬리가 버튼에 정확히 맞음, 책BTI `.pbti-help-*` 시각 복제이되 그쪽 무손상)로 두 페이지가 공유. 책장은 인사말 헤더 우상단, 책방은 '공개한 책' 패널 헤드 우측(필터칩과 `.shop-shelf-tools`로 묶음)에 배치. 노출 조건은 기존 고지문 그대로 — 책방은 `!self && books>0`(내 책방·빈 책방엔 ⓘ 없음), 쿠팡 줄은 `coupangEnabled`. 클릭 토글 / 투명 백드롭 밖 클릭·Esc 닫힘. 하단 `.affiliate-note`(BooksApp)·`.shop-affiliate`(ShelfPanel) 상시 문구 제거(`.affiliate-note` CSS 규칙은 book-detail이 써서 보존, `.shop-affiliate`는 유일 소비처가 사라져 정리). **검증**: 새 순수 로직 없음(불리언 토글+기존 조건)이라 vitest 회귀만 확인(775 통과)하고, 클라이언트 반응성이라 실 브라우저(로컬 bootRun+Chrome) 게이트 — `/books`(내 책장) 및 2번째 유저를 로컬 DB에 시드한 `/u/{id}`(`!self`)에서 ⓘ 우상단 배치·클릭 토글·Esc·밖 클릭 닫힘·콘솔 에러 0·하단 문구 제거 실측, 내 책방(`self`)에서 ⓘ 정확히 숨김 확인. 번들 `books.js`·`profile.js` 재빌드. |
| 2026-06-29 | **feat: 책장 제휴 고지 ⓘ를 '내 책장' 카드 안 우상단으로 이동 — 책방과 위치 통일** — 직전 #594에서 책장 ⓘ는 인사말 헤더(화면 우상단, 카드 **밖**)에, 책방 ⓘ는 '공개한 책' 카드 헤드 안에 있어 위치가 어긋났던 걸, **책방을 기준**으로 책장도 카드 안 우상단으로 맞춤(사용자 요청). 책장 ⓘ를 인사말 헤더에서 떼 **'내 책장' 카드**(책 목록·구매 버튼이 있는 카드 = 책방 '공개한 책'의 대응)의 헤드로 옮김 — `<h2>내 책장</h2>`를 신규 `.shelf-mine-head`(flex space-between) 행으로 감싸 제목(좌)+ⓘ(우) 배치(`.card h2`의 margin-bottom을 헤드 행으로 이관). 공용 `.affiliate-pop-*`는 그대로(앵커가 ⓘ 버튼 래퍼라 카드 안에서도 말풍선 꼬리 정확), `.card`는 `overflow:hidden`이 없어 팝오버 안 잘림(사전 확인). **책방(ShelfPanel)은 무변경**(기준이라 — `profile.js` 바이트 동일). **검증**: 새 로직 없어 vitest 회귀만(775 통과), 실 브라우저(로컬 bootRun+Chrome)로 `/books`에서 ⓘ가 '내 책장' 카드 헤드 우측 배치·클릭 토글·Esc·밖 클릭 닫힘·콘솔 0·인사말 헤더 ⓘ 제거 실측. 번들 `books.js` 재빌드(profile 무변경). |
| 2026-06-29 | **feat: 랜딩페이지 실사이트 정합성 + 누락 기능 + 디자인 통일** — #392 와이드 랜딩 이후 실사이트와 어긋난 부분 보정(멀티에이전트 감사로 불일치 도출). **(A) 정합성** 컨트롤러에 없어 404였던 `/social` 네비 + 비로그인 클릭 시 로그인으로 튕기던 `/village`를 **페이지 내 앵커**(`#village`·`#together`, `scroll-margin-top:80px`+`scroll-behavior:smooth`)로 전환 / 폐기된 '식물 심기' 마을 카피를 현행 컨셉(작가 모여듦·건물 들어섬, `[[garden-vision-coc-zoo]]`)으로 / '마을 둘러보기'→'무료로 시작하고 마을 만들기'(`/signup`). **(B) 누락 기능** 기능 그리드 4→6칸(`독서 기록`·`책BTI` 추가, CSS `repeat(4)`→`repeat(3)` 2행) + **'함께 읽기' 정식 섹션 신설**(검색→팔로우→공개 프로필 동선 정직 서술, 미확인 '응원' 기능 단정 회피, 우측은 placeholder 대신 **팔로잉 미리보기 CSS 목업**으로 placeholder 3개화 방지). **(C) 디자인 통일** 산재 CTA 라운드를 `--radius-sm`(8px)로(히어로 10→8)·카드(grass/feature/social) 라운드 `--radius`(14px)·패딩 24px로 수렴(feature 28·22→24, grass 16→14). placeholder 비주얼 실캡처 교체는 백로그 유지(사용자 선택). **검증**: 순수 Thymeleaf 마크업+CSS(Java·DB·번들 무변경 → 커밋 테스트 게이트 비대상)라 단위 TDD 무의미 → `.preview` static-preview(bootRun/Docker 없이 th 치환 렌더)로 정량 검증 — 콘솔 에러 0·네비 앵커 resolve·기능 6칸 3열·소셜 3행·미니잔디 색 재사용·버튼 8px·카드 14px/24px를 computed-style 실측(스크린샷은 환경상 타임아웃이라 eval/inspect 정량으로 갈음). **보정**: 순수 마크업이라도 기존 통합 `LandingPageTest`가 마크업 링크를 검증 중이라 `/village` 직접링크 기대가 CI에서 RED → 동선 검증(`#village`·`#together` 앵커+섹션 존재)으로 갱신하고 함께 읽기 섹션 회귀 가드 추가해 GREEN(T-114 옆 교훈: '단위 TDD 무의미' 판단 전 기존 테스트 그렙). |
| 2026-06-29 | **docs: troubleshooting T-115·T-116 추가 + 재발 트래커 'cwd-점유' 군 등록** — #596 랜딩 작업 sweep에서 나온 두 트랩 기록. T-115(워크트리에서 작업한 세션이 그 워크트리를 직접 정리하면 세션 cwd 점유로 최상위 빈 폴더가 안 지워짐 — 정션·메타·브랜치는 정리됨, 빈 폴더는 세션 종료 후 `rmdir`; T-105 재발 2회차라 재발 트래커에 'cwd-점유' 군 신설). T-116(순수 마크업/CSS 변경이라 '단위 TDD 무의미'라 보고 preview만 했다가 기존 통합 `LandingPageTest`가 `/village` 링크를 검증 중이라 CI RED — 변경 전 그 문자열·경로·뷰명 검증 테스트 grep). 목차 재생성. 문서 전용(코드·테스트 0). |
| 2026-06-29 | **feat: 랜딩 로고·아이콘 앱 통일(SVG) + '남의 잔디·마을 구경' 거짓 카피 정정** — 랜딩에만 남아 있던 옛 `📚` 이모지 브랜드 로고를 앱 전역 통일 SVG(`brand-ico` 책등)로 교체하고, 기능카드 6 이모지(⏱️🎯📖📊🧬👥)·잔디 footer `🌱`를 앱 라인아트 SVG 스킨(navIcons '이모지 금지' 원칙)으로 통일. **정합성**: 현재 남의 독서 잔디·마을은 열람 불가(마을·잔디 라우트 본인 전용, 남의 공개 잔디는 `ProfileResponse` 직렬화에서 빠진 dead-code — 멀티에이전트 감사 3/3 반증)인데 '함께 읽기' 섹션·기능카드가 "서로의 공개 프로필에서 독서 잔디와 마을을 구경"이라 광고하던 것을 실제 기능("팔로우 + 서로의 책방=공개한 책·책BTI 구경")으로 정정, '팔로잉 미리보기' 카드의 미니 잔디바를 실제 노출값('공개 책 N권')으로 리워크. placeholder(🌿🏡)·소셜 아바타(🦊🐢🦉)는 데모라 유지(후속). 순수 SSR 마크업+CSS(Java·번들 무변경), TDD 회귀 가드 2건(로고 통일·거짓광고 부재) RED→GREEN + static-preview 정량 검증(로고 22px·아이콘 32px sage accent·미니잔디 0). |
| 2026-06-29 | **chore: 남의 공개 잔디 dead-code 제거 (ProfileView.graph → publicContributionGraph → publicDailyHistory)** — 프로필 서비스가 남의 공개 독서 잔디를 계산해 `ProfileView.graph`에 담았으나 `ProfileResponse` 직렬화·프로필 UI 어디서도 읽지 않아(프로필 Vue 섬 전환 후 남은 SSR 잔재) **매 프로필 로드마다 결과가 버려지던 dead-code**였다. 남의 잔디 열람은 제품상 미지원이라 사슬 전체 제거: `ProfileView.graph` 필드(+import·javadoc) → ProfileService의 유일 사용처·미사용 `ReadingContributionService` 의존(필드·생성자 인자·import) → `ReadingContributionService.publicContributionGraph`(유일 호출자 소멸) → `ReadingHistoryService.publicDailyHistory`(유일 호출자 소멸)+전용 테스트 2건·헬퍼. 회귀 가드 추가: `GET /api/profile` 응답에 `graph` 키 부재 단언(남의 잔디 재유입 차단). 본인용 잔디(`contributionGraph`→dashboard/history)·`publicTotalSecondsByBook`(공개 책 시간, 직렬화됨)은 불변. 전체 테스트 그린, 잔여 참조 0(grep) 확인. 직전 랜딩 정정 작업서 발견된 dead-code의 후속 별도 PR. |
| 2026-06-29 | **feat: 친구 추천 하이브리드 계단식 1단계 — 랜덤 10명 → 신호 기반 + "추천 이유" 칩** — `/search`의 "친구 추천"이 `order by rand()` 순수 무작위였던 걸(주석에 "성향 기반으로 교체할 자리"라 못박혀 있던 후속 과제), 주요 SNS 조사(그래프 엔진 "아는 사람"=FoF·맞팔 + 관심사 엔진 "좋아할 사람"=같은 책, StoryGraph식 책 신호 재사용·콜드스타트 사다리·explainability)에 근거해 **우선순위 생성기 계단식**으로 교체. **사다리** = G1 맞팔 후보(나를 팔로우했는데 내가 안 함 → "나를 팔로우함") → G2 친구의 친구(공통 친구 수 desc → "공통 친구 N명") → C 같은 책(내 읽음/완독 isbn ∩ 후보 PUBLIC 읽음, 겹친 권수 desc → "같이 읽은 책 N권") → F 랜덤 폴백(콜드스타트 — 항상 채움). userId dedup·이유 합집합. **노출 불변식 4종**(ADMIN·본인·차단 양방향·login_id null 제외)을 새 쿼리 3개에 전부 복제 + **이미 팔로우한 사람 제외**(동작 변경: 기존 랜덤은 띄우고 "언팔로우" 표시) + 같은 책은 후보의 **PUBLIC만**(비공개 누수 가드). **스키마 변경 0** — `FollowRepository` self-join 2개(`findFriendsOfFriends`·`findFollowBackCandidateIds`)+`BookRepository` theta-join 1개(`findCoReadCandidates`, `followScopePopularity` 패턴 복제)+`findReadIsbnsByUser`. 추천 행만 `reasons` 칩을 더한 `RecommendedUser`/`RecommendedUserDto`로 감싸 **검색 행(`UserSearchResult`)·검색 응답은 무변경**(공유 타입 오염 방지). 프론트 `follow.ts`에 `RecommendedUser` 타입 + `UserSearchPanel.vue` 추천 섹션에 `.reason-chip`(전역 `app.css`, `.book-status-badge` pill 레시피 재사용). 성향 태그(종족) 유사도는 DB 미영속(매 요청 계산)이라 영속화 마이그레이션 필요 → **2단계로 보류**(활동량/인기 폴백·점수 블렌딩·캐시/레이트리밋도). **TDD RED→GREEN**: 서비스 스텁(`List.of()`)으로 RED 확인→사다리 구현→GREEN. 리포지토리 슬라이스(FoF·맞팔·같은책 + 누수/N-055 login_id null/차단 양방향/이미팔로우/PRIVATE·읽고싶음 제외 가드)·서비스(`@SpringBootTest` 사다리 순서·dedup 이유합집합·이미팔로우 제외·폴백·limit)·API(`MockMvc` reasons JSON 존재·검색 results 무변경)·프론트 vitest(`.reason-chip` DOM). 전체 Java 스위트+프론트 776 그린. 번들 `search.js` 재빌드. |
| 2026-06-29 | **docs: 친구 추천 작업 sweep — troubleshooting T-117 · learning-notes N-139 추가** — 직전 친구 추천 하이브리드(#600) 작업에서 나온 두 항목 기록. **T-117**(공유 Vue 컴포넌트에 `<style scoped>`를 넣으면 vite가 페이지가 `<link>` 안 하는 별도 `<bundle>.css`를 생성해 칩이 무스타일 — 헤드리스 vitest는 못 잡고 실 페이지에서만 드러남, 해결=공유 컴포넌트 스타일은 전역 `app.css` 클래스로). **N-139**(친구·사람 추천의 두 엔진 = 그래프 "아는 사람"(FoF·삼각닫힘) + 관심사 "좋아할 사람"(같은 항목·StoryGraph식), 콜드스타트 사다리 인기→속성→FoF→CF, explainability가 수락률↑ — 일반화·면접 설명 수준). 목차 갱신(troubleshooting은 훅 자동재생성). 문서 전용(코드·테스트 0). |
| 2026-06-29 | **fix: 친구 추천 재계산 비용 차단 — q 있는 검색 시 추천 미계산 + RECOMMEND 레이트리밋** — `/api/search`가 q·추천 무관함에도 매 검색 submit마다 무거운 `recommend()`(맞팔 self-join·FoF 집계·같은책 theta-join·배치조회)를 재계산하던 낭비를 제거. `SearchApiController.search()`를 `isSearch = q != null && !q.isBlank()` 분기로 교체: **q 있을 땐** 추천 재계산 없이 `List.of()` 반환, **q 없는 진입에서만** `RECOMMEND` 한도 내 계산. `RateLimitAction`에 `RECOMMEND(20/분)` 추가 → q 없는 연타도 차단. 프론트 `UserSearchPanel.vue`의 `fetchSearch`에 `if (!q.trim()) recommendations.value = data.recommendations` 조건 추가 — q 있는 검색 응답의 빈 추천이 진입 때 받은 추천 섹션을 덮어쓰지 않게. **TDD RED→GREEN**: 백엔드 3개(검색 시 추천 빈배열·진입 시 추천 채움 회귀가드·RECOMMEND 한도 초과 시 빈배열) + 프론트 vitest 2개(진입 추천 반영·검색 후 추천 유지) 전부 그린. 전체 Java 스위트 + vitest 778 통과. 번들 `search.js` 재빌드. `UserSearchService.recommend()` 무변경. |
| 2026-06-29 | **fix: /search 중복 "사용자 검색" greeting 제거 + 친구 추천 리스트 스크롤 박스** — /search가 다른 섬 페이지(profile·follow-list·book-readers·block-list)와 달리 SSR 템플릿(`search.html`)에도 greeting을 둬, Vue(`SearchApp.vue` page-stack)가 렌더하는 greeting과 합쳐 "사용자 검색"이 두 번(브랜드 아래 + "아이디로 찾기" 카드 위) 보이던 걸, **SSR 쪽을 제거**해 다른 섬과 동일하게 Vue 단일 출처로 통일(결과는 제목 1개, 통합 page-stack gap 안). 친구 추천 `<ul>`은 높이 제한이 없어 추천이 꽉 차면(최대 10명·`RECOMMEND_COUNT`) 페이지가 길어져 하단 네비가 밀리던 걸, **`.recommend-scroll`**(max-height 430px + overflow-y auto, `.search-scroll`과 동일 관성/거터/번짐방지 레시피)로 고정높이 스크롤 박스화(적으면 미도달=스크롤바 없음 자연 폴백). **검증**: 순수 시각/마크업 변경이라 새 단위테스트 무의미 → 기존 vitest 776 그린 + 실 브라우저(로컬 bootRun+Chrome)로 greeting 1개 실측·콘솔 0·`.recommend-scroll` computed(max-height 430·overflow auto)와 12행 합성 시 실제 스크롤 발동 확인. 번들 `search.js` 재빌드. |
| 2026-06-29 | **feat: 마을 모바일 portrait 강제 가로 뷰 — CSS rotate90 + Phaser 입력 좌표 역회전** — 모바일 세로 진입 시 `.village-shell` CW 90° CSS 회전·`InputManager.transformPointer` 몽키패치(`rotateTouchForPortrait` 순수함수)·matchMedia 리스너로 portrait↔landscape 전환 대응. vitest 784 그린. |
| 2026-06-29 | **feat: 친구 추천 사다리 활동량 폴백(E 생성기) 추가** — 콜드스타트 사다리의 F(랜덤) 앞에 E(활동량) 생성기를 끼워 넣음. 신호 = 최근 14일 distinct 활동일 수(`reading_session.started_at` DATE 기준), 이유 칩 "요즘 꾸준히 읽어요". 노출 불변식(ADMIN·본인·loginId null·차단 양방향·이미 팔로우 제외)은 기존과 동일. V53 인덱스(`reading_session(user_id, started_at)`), `ActiveDayCount` 프로젝션, `ReadingSessionRepository.findActiveReaderCandidates` JPQL, `UserSearchService`에 Clock·ReadingSessionRepository 주입 추가. TDD RED→GREEN: 6개 테스트(활동량 순서·14일 윈도우 경계·loginId null N-055·빈 추천 방지·E→F 우선순위·C+E dedup) 전부 그린. 컨트롤러·프론트·DTO 무변경. |
| 2026-06-30 | **fix: 마을 모바일 강제 가로 회전 제거 → 반응형** — #605 portrait CSS rotate90·`applyPortraitInputPatch`·`rotateTouchForPortrait`·matchMedia 리스너 일체 제거. portrait·landscape 모두 Phaser `Scale.RESIZE`+표준 `transformPointer`+`fitCamera` 반응형으로 동작. vitest 778 GREEN(삭제 6종 포함 전수 통과). 번들 재빌드. |
| 2026-06-30 | **feat: 마을 반응형 2모드 PoC — 세로 돌봄 뷰(PortraitVillage) + 가로 CoC 유지** — 6컨셉 심사 결론(모바일 세로는 CoC 부감 와우 구조 불가 → 세로 재미=애착/수집)에 따라 `VillageApp.vue`에 orientation 분기 추가: **세로(≤899px·portrait)=경량 돌봄 뷰 `PortraitVillage.vue` 신설**(슬림 HUD·"오늘의 작가" 히어로 카드〔SVG·정 Lv/칭호/미터〕·큰 밥 주기 버튼·식구 작가 카드 스택, 순수 Vue·Phaser 미사용), **가로·데스크탑·앱=기존 GardenGame(CoC) 그대로**. 같은 `/api/garden`·`/feed` 재사용(추가 API 0), 백엔드·DB·SVG·manifest 0변경. `pure.ts` `pickTodayAuthor`(보유 작가 중 정 레벨 최저 선정)·`affectionProgress`(AffectionLevel 임계 `[1,3,7,15,30]` 복제 → 다음 단계 진행도) 순수함수 **TDD RED→GREEN**(경계 18종: 빈/단일/다수/동률·레벨경계·만렙·음수). vitest 796 GREEN. 번들 `garden.js` 재빌드. **실 브라우저 모바일 세로 실측(bootRun, testid 시드)**: ①루프 완주(밥주기→박경리 Lv0→Lv1 레벨업·미터/food/식구 갱신) ②게임감=따뜻한 돌봄/수집 뷰(카드앱과 변별) ③가로 회전→GardenGame 정상 ④데스크탑 중앙 액자 회귀 0 ⑤#605 제거 후 세로 파란화면 없음·콘솔 에러 0. **PoC(전면화 아님)** — 전면화는 사용자 판단. |
| 2026-06-30 | **fix: 도감 정 배지 과개행 수정 + 넓은 화면 도감 확대** — 도감 셀의 정(affection) 배지(`Lv1 안면 텄어요 ❤️1`)가 좁은 셀에서 3~4줄로 깨지던 문제 수정. 근본 원인 = `.plant-affection`·`.affection-lv{N}` CSS가 **app.css에 아예 없어** 기본 1rem로 렌더되던 것. `.garden-grid .plant-affection`(font-size .6rem·line-height 1.25·word-break keep-all·중앙 정렬) 신설 → 배지 1줄 안착. `.affection-lv4/5`(단짝·베프)는 강조색(`--accent-hover`+볼드)으로 보상감. 사용자 제안대로 `.village-dex-panel` max-width 540→760px + `.garden-grid` 셀 최소폭 64→88px 상향 → 데스크탑 7열·모바일 3열로 시원하게. **CSS 전용**(garden.js 번들 무관, DexCell 마크업 무변경). 실 브라우저 검증: 모바일 세로(390)·데스크탑(1280) 도감 배지 1줄·회귀 0·콘솔 에러 0. |
| 2026-06-30 | **fix: 마을 모바일 HUD 홈·도감 버튼 다듬기 (PortraitVillage)** — 세로 돌봄 뷰 슬림 HUD에서 ① 홈 버튼이 테두리 없는 파란 텍스트 링크(`← 홈`)라 옆 제목과 붙어 버튼처럼 안 보이던 걸 **`🏠 홈` 테두리 칩**(border+card-bg+radius)으로 명시화, ② 도감 버튼(`📖`)이 전역 `button{width:100%}`(app.css:334)를 상속하고 상쇄가 없어 flex에서 가로로 늘어나던 걸 **`width:auto`로 콤팩트 pill화 + `📖 도감` 라벨 추가**(#286 가드 패턴, 트래커 width 4회차). `.pv-hud-home`/`.pv-hud-dex` 둘 다 `inline-flex`·`gap`·`nowrap`로 정돈. **CSS + PortraitVillage.vue 텍스트 2곳만**, 백엔드·DB·API 0변경, 데스크탑/가로는 GardenGame이라 이 클래스 자체가 없어 구조적 무영향. 순수 시각 → 단위테스트 N/A, 게이트=실 브라우저. 번들 `garden.js` 재빌드. **실 브라우저 모바일 세로(390) 실측**: 홈 칩 58px(테두리 1px·radius 16px)·도감 71px(CSS width 70.7px, 더 이상 100% 아님)·제목이 여백 채움·콘솔 에러 0. |
| 2026-06-30 | **feat: 프로필 사진 — 도감에서 모은 작가 얼굴을 아바타로** — 업로드 없이, 도감에서 보유(완독)한 작가의 SVG 스프라이트(`#sprite-{code}`)를 프로필 아바타로 재사용. `User.profile_character_code`(V54, nullable=미선택 시 로그인ID 이니셜 폴백, opt-in) 1칼럼만 추가 — 파일 저장소·이미지 검증 0. 선택 UI=설정 "프로필 사진" 카드(보유 작가 그리드 + "기본" 옵션), 노출=본인 헤더(`DashHeader`)·공개 프로필 헤더(`ShopHeader`) 2곳. **핵심 보안=보유 검증**: `ProfileCharacterService`가 먹이주기(`FeedingService`)와 동일하게 `GardenView.ownedCharacters()` 포함 여부로 **미보유 작가 위조(IDOR)를 차단**(빈 코드=선택 해제). 공용 `AuthorAvatar.vue`(code→`#sprite-{code}` / 없으면 이니셜), `profile.html`에 sprite fragment 추가(`dashboard.html`엔 기존). **TDD RED→GREEN**: ProfileCharacterService 5(보유 선택·미보유 거부·공백/null 해제·교체) + SettingsController 4(보유목록·현재선택 모델·POST 미보유 거부·해제) + Dashboard/Profile API 노출 2 — 전부 그린, 전체 Java 스위트 + `FlywayMigrationTest`(V54 H2 호환) GREEN. 번들 재빌드. **실 브라우저(bootRun+Chrome) 검증**: 설정 카드 보유작가(한강·김영하) sprite 그리드·보유검증(도감 20종 중 보유 2종만)·선택 저장 플래시·헤더 2곳 sprite 아바타(얼굴 크롭)·콘솔 에러 0. |
| 2026-06-30 | **chore: 건물(출판사)축 은퇴 PR-1 — 콘텐츠 제거 (작가 꾸미기 피벗)** — CoC 컨셉 이탈로 무용해진 출판사 건물축 은퇴의 1단계(콘텐츠). 건물의 유일 페이오프=CoC 맵 배치인데 케어 루프 없고 세로 돌봄 뷰엔 등장도 안 함 → 작가 꾸미기로 방향 전환(plan.md 「건물 은퇴 + 작가 꾸미기 피벗」). **삭제**: `Building`·`BuildingRepository`·`BuildingState`·`BuildingUnlockCalculator`(+Test)·`fragments/garden-building-sprites.html`. **수정**: `GardenService.view()` 건물 배선 제거, `GardenView` 건물 3필드 제거·`ownedPlants()`→빈목록(배치 엔진이 호출하므로 메서드 유지), `GardenApiResponse` `BuildingDto`·`CatalogDto` 건물 필드 제거, `GardenDex.vue`(건물 섹션·탭·DTO 제거, 단일 작가축이라 탭 폐지)·`GardenPanel.vue`/`dashboard/types.ts`(건물 카운트 제거)·`garden.html`(건물 스프라이트 include 제거)·`PlacementAxis.BUILDING` javadoc. **배치/편집 엔진은 inert化(코드 유지) — PR-2에서 제거 예정**(건물이 유일 배치 대상이라 함께 죽음). DB `publisher_building`·`garden_placement` 테이블·V46/V48/V49는 적용 이력으로 **보존**(소프트 제거, 새 마이그레이션 0). **TDD RED→GREEN**: `GardenApiControllerTest` catalog 건물 필드 부재 단언이 현 코드(건물 존재)에 RED→제거 후 GREEN. `GardenLayoutServiceTest`는 건물 은퇴 후 엔진 inert 안전 가드로 축소(고아 방어 회귀 핵심: 잔존 BUILDING/AUTHOR/TIME 배치행 → layoutOf 빈목록·크래시 0; 전 축 배치 거부). 전체 Java 테스트 + vitest 796 GREEN. 번들 `garden.js`·`dashboard.js` 재빌드. **실 브라우저 검증**: `/api/garden` catalog 작가 키만(건물 필드 부재)·owned/placed 빈배열, 도감 오버레이 작가 섹션만(건물 섹션·탭·텍스트 0·작가 20셀), 가로 GardenGame Phaser canvas 무크래시, 세로 PortraitVillage 정상, 콘솔 에러 0. (가로 뷰 "건물 놓아보세요" 빈상태 카피는 배치 엔진=PR-2 transient.) |
| 2026-06-30 | **fix: 프로필 아바타 — 작가 선택 시 초록 원 배경 제거** — 기본(미선택) 아바타는 초록 원+로그인ID 이니셜 그대로 두되, 도감 작가를 프로필로 고르면 그 초록 원·테두리 링을 지우고 캐릭터만 보이도록 수정. `app.css`에 `.dash-header-avatar:has(.dash-header-avatar-sprite){background:transparent;box-shadow:none}` 한 규칙만 추가 — 판별자(스프라이트 존재=작가 선택)가 정확해 `DashHeader`·`ShopHeader` 양쪽 헤더를 한 번에 덮고, 원형 크롭(border-radius+overflow:hidden)은 유지해 아바타 모양은 동일. 정적 CSS(프론트 번들·Java·DB 0변경). 순수 시각→단위테스트 N/A, 게이트=실 브라우저: 본인 헤더·공개 프로필 양쪽에서 작가 선택 시 초록 원 제거(computed `background:transparent`·`box-shadow:none`) + 미선택 시 초록 원·이니셜 유지 재확인, `:has()` 지원 확인, 콘솔 에러 0. |
| 2026-06-30 | **fix: 프로필 아바타 테두리 복원 — 배경에 묻힘 수정** — 직전 수정이 작가 선택 시 초록 원 배경과 함께 **테두리 링까지** 지워, 캐릭터가 헤더 배경(크림색)에 묻혀 경계가 안 보이던 문제 수정(사용자 피드백). 초록 원 배경만 지우고(`background:transparent`) `box-shadow`(외곽 1px border + 안쪽 3px card-bg 링)는 그대로 둬 **기본 이니셜 아바타와 동일한 테두리**로 배경과 구분되게 함. `app.css`의 `:has()` 규칙에서 `box-shadow:none` 한 줄만 제거 — 정적 CSS(프론트 번들·Java·DB 0변경). 순수 시각→단위테스트 N/A, 게이트=실 브라우저: 본인 헤더(`/`)·공개 프로필(`/u/…`, ShopHeader) 양쪽에서 작가 선택 시 `background` transparent 유지 + `box-shadow` 복원(외곽 border+card-bg 링) 확인, 콘솔 에러 0. |
| 2026-06-30 | **chore: 건물(출판사)축 은퇴 PR-2 — 배치/편집 엔진 제거 (작가 꾸미기 피벗)** — 건물이 *유일* 배치 대상이라 PR-1 후 콘텐츠 없는 좀비가 된 배치/편집 엔진을 통째 은퇴. **삭제**: `GardenLayoutService`·`GardenPlacement`·`GardenPlacementRepository`·`PlacedItem`·`PlacedPlant`·`OwnedPlant`·`PlacementRequest`·`LayoutSaveRequest`·`PlacementAxis`·`GardenIsoProjection`(`PlacedItem` 위임에만 쓰이던 고아 SSR 좌표 유틸 — S4 컷오버로 SSR 배치 렌더 사라짐)·테스트 `GardenLayoutServiceTest`·`GardenIsoProjectionTest`·무효화된 e2e `garden.spec.ts`(저장 흐름이 은퇴). **수정**: `GardenApiController`(`GardenLayoutService` 의존·`POST /layout` 제거, world 상수 출처 `GardenWorld`로), `GardenController`(`POST /village/layout`·의존 제거 → GET `/village` 셸·`/garden` 리다이렉트만), `GardenApiResponse`(`placed`·`owned`·`OwnedPlantItemDto` 제거 → world·nickname·catalog·characters·foodBalance만), `GardenView.ownedPlants()` 삭제, `scene.ts` 보기 전용화(드래그·선택·격자·식물 그림자·`spawnObject`/`addPlant`/`exportPlacements` 등 편집 경로 제거 → 배회·먹이주기·카메라만), `GardenGame.vue` 편집 모드 제거(VIEW+feeding만, 빈 마을 카피 "마을 식구가 없어요 — 작가의 책을 완독하면 찾아와요" + `chars.length===0` 기준), `VillageApp.vue` ✏️꾸미기 버튼·`gameRef` 제거. **신설**: `GardenWorld`(`WORLD_WIDTH`/`WORLD_HEIGHT` 상수 holder — 엔진과 분리, `/api/garden` 응답 `world` 출처 유지). DB `garden_placement`·`publisher_building` 테이블·기존 마이그레이션은 적용 이력으로 **보존**(소프트 제거, 새 마이그레이션 0). **TDD RED→GREEN**: `GardenApiControllerTest` `$.placed`/`$.owned` doesNotExist + POST `/api/garden/layout` 404, `GardenControllerTest` POST `/village/layout` 404가 현 코드(필드·엔드포인트 존재)에 RED → 제거 후 GREEN. `GardenServiceTest`·`GardenViewC2Test` `ownedPlants()` 참조 정리. 전체 Java 테스트 + vitest 796 GREEN. 번들 `garden.js` 재빌드. **실 브라우저 검증**(scene.ts 수술 = T-053/054 게이트): 로컬 로그인→`/village`에서 Phaser 캔버스 정상 마운트(아이소 다이아몬드+앰비언트 데코)·편집 격자 부재·HUD ✏️꾸미기 부재(도감·홈만)·빈 마을 새 카피·`/api/garden` placed/owned 없이 HUD 렌더·콘솔 에러 0. |
| 2026-06-30 | **docs: 트러블슈팅 재발·승격 트래커 1회 과거 백필 — 규칙 도입(6-25) 이전 누락 2군 소급 등재** — 트래커 표가 forward-only(새 T-### 쓸 때만 갱신)라 규칙 신설 전 2회+ 재발 후 멈춘 옛 트랩군이 구조적으로 누락됨을 전수 감사(T-001~T-117, 21-에이전트 워크플로)로 확인. 진짜 누락 2군을 승격: **csrf-buffer-commit(T-033·T-049, 4+회)** → CLAUDE.md 「🔒 CSRF 폼 세션 선확정」 절 신설, **fk-child-cleanup(T-023·T-029)** → CLAUDE.md TDD절 구체예 추가. 트래커 표에 2행 + 머리말 백필 마커 1줄. (감사가 부수 발견한 CSRF 선확정 코드 누락 6곳은 별도 작업으로 분리.) 문서 전용·회귀 0. |
| 2026-06-30 | **chore: 데스크톱 CoC 가로 무대 제거 — 돌봄 뷰(다마고치) 단일 모드화** — 반응형 2모드 PoC(세로=`PortraitVillage` 돌봄 뷰 / 가로·데스크탑=`GardenGame` CoC)를 **전면화** → 모든 화면에서 CoC 무대를 없애고 PortraitVillage 단독. **삭제**: `GardenGame.vue`·`scene.ts`(Phaser 무대)·`fragments/garden-ambient-sprites.html`(여백 섬·바위·나무, CoC 전용). **수정**: `VillageApp.vue` orientation 분기·미디어쿼리·가로 HUD 제거 → PortraitVillage 단독 + 도감 오버레이만, `pure.ts` CoC 순수함수 전부 삭제(좌표·줌·격자·아이소 `normToIso`·배회 `wanderStep`·걷기 `walkPose`/`idlePose`·여백 `AMBIENT_DECOR`) → 돌봄 공유분(`clampNorm`·`pickTodayAuthor`·`affectionProgress`)만 잔존, `app.css` dead CSS(`.village-game-root`·`.garden-phaser-fill`·`.village-hud*`) 제거 + `.portrait-village` 데스크톱 중앙 카드(액자, `min(460px)`·flex center) 레이아웃 추가, `garden.html` ambient include·낡은 layout CSRF 주석 정리. **TDD RED→GREEN**: 새 정적 가드 `village-care-view.test.ts`(garden 소스 Phaser import 0·`VillageApp`이 `GardenGame` 미마운트 — CoC 재유입 차단)가 현 코드(scene.ts·GardenGame 존재)에 RED → 제거·단일화 후 GREEN. `garden-pure.test.ts`는 삭제 순수함수 테스트 제거(돌봄 함수만 유지). vitest 323 GREEN. 번들 `garden.js` 재빌드(Phaser 제거로 6369줄↓·74.8kB). **실 브라우저 게이트**(전 사용자 마을 렌더 경로 전환): 데스크톱(1440) `/village`에서 PortraitVillage **단독 중앙 카드(액자)** 렌더·CoC Phaser canvas 미로드·도감 오버레이(작가 20종·건물 섹션 0) 정상·콘솔 에러 0(빈 계정이라 히어로·먹이주기 자체는 미실측 — PortraitVillage 무변경이라 2모드 PoC 모바일 검증분 계승). (정원/마을→서재 리네임은 후속 PR-B.) |
| 2026-06-30 | **fix: CSRF 폼 세션 선확정 누락 6곳 수정 + 공유 헬퍼 `CsrfTokenUtil`로 9곳 통일** — #615 백필 감사가 부수 발견한, `th:action` 폼을 가진 SSR GET 핸들러에 CSRF 토큰 선확정이 빠진 6곳(Settings·AdminFeedback·Feedback·EmailVerification·Unsubscribe·Onboarding) 수정. 큰 페이지·동적 콘텐츠에서 폼의 CSRF 숨김필드가 응답 버퍼(~8KB) 커밋 후 세션을 lazy 생성하려다 `IllegalStateException` 500 나는 함정(T-033·T-049, N-077) 방어. 인라인/private로 흩어져 있던 기존 3곳(Login·Signup·PasswordReset)까지 **`CsrfTokenUtil.precommit(request)` 공유 정적 헬퍼로 통일**(중복 제거), 6개 GET 핸들러 시그니처에 `HttpServletRequest` 주입. **TDD RED→GREEN**: 6곳 컨트롤러 유닛 회귀 테스트(`mock(HttpServletRequest)`+`verify(token).getToken()`)를 먼저 작성해 RED(precommit 없어 6 FAILED) 확인 → 헬퍼 적용 후 GREEN. 기존 3곳 선확정 테스트가 리팩터 회귀 가드(선확정 9개 전부 GREEN), 전체 Java 스위트 GREEN(회귀 0). 훅 미채택 사유=컨트롤러↔템플릿 역매핑·POST 재렌더로 FP 과다(CLAUDE.md 「🔒 CSRF」절). |
| 2026-06-30 | **docs: 멀티세션 워크트리 규칙에 「연속 구현 전용 세션」 절 추가** — 한 세션이 여러 작업을 순차 처리하는 "연속 구현 전용 세션"은 작업 사이에 **자기가 서 있는 워크트리(cwd)를 삭제하지 않고 재사용**하도록 못 박음. 배경: 연속 전용 세션이 PR 머지 후 정리 단계에서 자기 cwd 워크트리를 지워 이후 셸 명령이 전부 "no such directory"로 실패, 다음 지시에 무반응이 된 실제 사고(2026-06-30). 일회성 작업 세션용 「머지 후 정리 — remove-worktree」와 구분(연속 전용 세션은 그 정리를 자기 자신에게 적용하지 않음). 2회+ 재발 시 훅 승격 후보 명시. CLAUDE.md 「🪢 다중 세션」 절에 미니 섹션 신설. 문서 전용·회귀 0. (이전 세션이 메인 폴더에 미커밋·PR 미생성으로 남긴 변경을 정식 PR로 흡수.) |
| 2026-06-30 | **chore: 정원/마을 → "서재" 리네임 (앱 UI + 랜딩 마케팅)** — 사용자 대면 "정원/마을" 명칭을 전부 **서재**로 통일. 코드 식별자·라우트(`/village`·`/garden`)·DB·CSS 클래스(`village`·`garden`·`pv-`)는 내부값으로 **유지**(2026-06-18 리네임 원칙 — 거대 diff·리다이렉트 체인 회피). **앱 UI**: `VillageApp`(로드 에러), `PortraitVillage`(○○님의 서재·서재 식구·서재로 찾아와요), `GardenDex`("서재는 개발 중"), `GardenPanel`(서재·서재 가기 →), `garden.html` title("나의 서재"). **랜딩(`landing.html`)**: nav·섹션 라벨 "독서 서재", ④섹션 **다마고치 톤 재작성**("작가가 찾아오는 나의 서재" — 완독 작가가 서재로 찾아와 밥 주고 정 쌓으며 함께 지내는 재미), hero·meta·steps "작가가 찾아오는 서재가 됩니다", placeholder 라벨. `HistoryApp`의 "정원처럼" 비유는 기능명 혼동 방지로 중립화("차곡차곡"). **TDD RED→GREEN**: `LandingPageTest.landing_hasCoreKeywords`를 `containsString("독서 서재")`로 강화(닉네임 예시 "여우의서재" 우연 매칭 회피) → 현 랜딩(독서 마을)에 **RED** → 리네임 후 **GREEN**. vitest 326·LandingPageTest GREEN. 번들 `garden`·`dashboard`·`history.js` 재빌드. **실 브라우저**: 랜딩 "독서 서재" 다마고치 카피·`/village` 서재 HUD/빈상태·대시보드 서재 패널·콘솔 0. (잔여 "마을/정원"은 `garden` 내부 식별자 설명 주석뿐.) |
| 2026-07-01 | **fix: 커밋 테스트 게이트 무한 hang 방지 — 8분 타임아웃 + bootRun 데몬 반납 규칙 (T-078 하드픽스)** — 멀티세션 gradle 데몬·빌드 락 경합으로 커밋 훅의 `gradlew test`가 무한 freeze(2026-06-30 PR-B 커밋이 13:51→14:37 **45분 정지**, T-078 3회차)나는 사각을 하드픽스. **A1(하드)**: `require-tests-before-commit.ps1`의 gradle test를 `Process.Start`+`WaitForExit(타임아웃)`으로 감싸 **8분(`BOOKTIMER_TEST_GATE_TIMEOUT_MS`로 조정) 초과 시 프로세스 트리 `taskkill /T` + `gradlew --stop` 자가복구 후 `exit 2`(fail-closed)** 로 차단 → 무한 freeze가 8분짜리 자기보고 실패로 전환. 정상 완료 경로(통과=0·실패=2)·NativeCommandError 회피는 불변. **A2(소프트)**: CLAUDE.md 「🪢 8080」·「⚠️ 무한 hang」절에 *bootRun 정리 = 8080 Stop-Process **+ `./gradlew --stop`*** (포트만 죽이면 앱 JVM만 가고 데몬 잔존→다음 게이트 경합)·무거운 .java 커밋 전 데몬 선점검 명문화. troubleshooting T-078에 3회차 재발·하드픽스 승격 기록 + 재발·승격 트래커 갱신(2+→3+). **TDD RED→GREEN**: 신설 `test-require-tests-timeout.sh`(가짜 gradlew 6케이스 — pass→0·fail→2·**hang→타임아웃→2**·doc-only→0·SKIP_TESTS→0·broken JSON→0)에서 hang 케이스가 미구현 훅에 **RED(got 0 = 45분 freeze 후 커밋 허용)** → 타임아웃 구현 후 **GREEN(6/6)**. 훅·문서·테스트만 변경(.java 0)·회귀 0. (2026-06-30 45분 freeze 회고에서 사용자 승인한 예방책 A1+A2; B1 알림 훅은 후속 PR.) |
| 2026-07-01 | **feat: 확인 대기 시 Windows 토스트 알림 훅 (B1, probe 단계)** — 머지 승인 같은 확인을 기다리며 턴이 멈췄을 때, 다른 탭을 보던 사용자에게도 OS 토스트로 신호가 닿게 하는 `Notification` 훅. (2026-06-30 45분 freeze 회고의 B1 — "확인이 다른 탭의 사용자에게 안 닿음" 대응.) **신설** `.claude/hooks/notify-when-waiting.ps1`(UTF-8 BOM — 한글 토스트 본문): stdin JSON 파싱 → 디버그 로그 1줄 + WinRT 토스트 발사(등록된 시스템 AppID `Microsoft.Windows.Explorer` — 스파이크에서 육안 확인됨). **side-effect 전용 — 무슨 일이 있어도 `exit 0`(세션 비차단)**, 토스트 실패도 fail-open, `settings.json` `timeout: 5`로 이중 가드. `BOOKTIMER_NOTIFY_DRYRUN`(테스트용 토스트 억제)·`BOOKTIMER_NOTIFY_LOG`(로그 경로) 환경변수. **배선**: `settings.json`에 `Notification` 훅(probe라 matcher 생략 — 데스크톱 앱이 실제로 쏘는 알림 타입을 `notify-debug.log`로 1회 실측 후 `idle_prompt` 등으로 좁힐 예정). `.gitignore`에 런타임 로그 추가. **TDD RED→GREEN**: 신설 `test-notify-when-waiting.sh`(4케이스 — Notification→exit 0+WOULD_TOAST·permission_prompt→0·broken JSON→0·empty→0, dry-run으로 토스트 억제)가 스크립트 부재 시 **RED(exit 127)** → 작성 후 **GREEN(5/5)**. **스파이크 검증**(코드 전): WinRT 토스트가 이 PC에 실제로 뜸+소리 들림(육안 확인), 데스크톱 앱 훅 실행은 공식 문서 확인. **남은 실측**(머지 후 세션 재시작 필요): 데스크톱 앱이 `Notification`/idle 이벤트를 실제 발화하는지 → `notify-debug.log`로 확인 후 matcher 확정(후속). 훅·테스트·설정만(.java 0)·회귀 0. |
| 2026-07-01 | **feat: /settings(설정 페이지) Claude Design 재스킨** — 설정 페이지 5개 폼 카드(기본설정·프로필사진·비밀번호·알림·회원탈퇴)+헤더+미검증 배너+플래시를 종이톤+세이지 시안으로 재스킨. SSR Thymeleaf(Vue 섬 아님)·백엔드/폼 계약/JS 동작 로직 **무변경**. 격리: `body.settings-page` 스코프 + `.set-*` 전용 클래스 신설(전역 공유 `.card`·`.field`·`.btn-*`·`.alert`·`.link-row` 등 미접촉 — auth-page와 동일 규약, 타 폼 페이지 회귀 0). 이모지 라벨 제거 → 라인아이콘, **푸시 토글 2개를 스위치로 이관**: `notification-settings.js`의 `_update*Btn`을 `textContent`(이모지)→`classList.toggle('is-on')`+`aria-checked`로 바꾸고 knob 위치·트랙색·knob 아이콘(종/종끄기·확성기)은 `.btn-push-toggle`/`.is-on` CSS가 구동(클래스명·나머지 fetch 로직·iOS/VAPID 게이트 보존). 하단 네비 홈+내 책장 2타일(공유 nav-links fragment). **TDD RED→GREEN**: 신설 `SettingsControllerTest.getSettings_preservesNotificationJsContract`(JS 훅 ID·속성 + 접근성 `role="switch"`)가 현 템플릿(role 미부여)에 **RED** → 재스킨 후 **GREEN**(24/24). 재스킨이 유발한 회귀 1건(검증실패 재렌더 경로에서 `${!emailVerified}`가 null에 `!` → SpEL 500)을 게이트가 잡아 `${emailVerified == false}` null-safe로 수정. **실 브라우저 게이트**(bootRun+Chrome, SSR+`type=module` defer 바인딩 N-083/T-053): 5카드 렌더·콘솔 에러 0·JS 훅 계약 유지·VAPID 미설정 시 스위치 disabled+title·스위치 on-state CSS(트랙 세이지+knob translateX+아이콘 교체)·성공 플래시 PRG. **버그 발견·수정**: `.set-notif-row/.set-notif-subhint`의 `display:flex`가 UA `[hidden]{display:none}`을 특정성으로 덮어 JS로 숨긴 iOS/복귀 힌트가 새던 것 → `.settings-page [hidden]{display:none!important}` 리셋. 정적 서빙(번들 없음)·회귀 0. |
| 2026-07-01 | **docs: 45분 freeze·B1 작업의 학습 노트 sweep — N-143 + T-121·T-122** — 회고·구현에서 나온 일반화 가능한 통찰 3건 등재. **N-143**(learning-notes): "외부(harness) 타임아웃은 동기 native 자식 hang을 못 끊는다 — 무한 hang은 내부 타임아웃으로"(settings.json `timeout:600`이 45분 freeze를 못 막은 이유 = A1이 훅 *내부* 타임아웃인 근거, "방어는 위험을 직접 든 레이어에" 일반화). **T-121**(troubleshooting): WinRT 토스트가 미등록 AppUserModelID면 `.Show()` 성공해도 화면에 안 뜸(조용히 드랍) → 등록 시스템 AppID 사용. **T-122**(troubleshooting): 타임아웃/hang 수정의 RED 테스트는 하니스를 outer `timeout`으로 감싸야 테스트가 세션째 hang 안 함(가짜 느린 자식은 exit 0으로 RED/GREEN 가름). troubleshooting TOC는 `rebuild-troubleshooting-toc.ps1`로 재생성, 재발·승격 트래커는 미해당(전부 1회차 신규). 문서 전용·회귀 0. |
| 2026-07-01 | **fix: 전역 `[hidden]{display:none!important}` 리셋 — T-035/T-123 트랩군 하드픽스 승격** — author `display:flex/grid`가 CSS cascade origin 우선순위(author > UA)로 UA `[hidden]{display:none}`을 이겨, JS가 `el.hidden=true`로 감춘 요소가 화면에 남는 트랩(#189→T-035 닫힌 details→T-123 `/settings` iOS·복귀 힌트, **3회 재발**)을 앱 전역에서 제거. `app.css` 베이스(리셋 영역)에 전역 `[hidden]{display:none!important}` 추가 → `#623`이 넣었던 페이지 스코프 `.settings-page [hidden]` 리셋은 전역으로 승격돼 **삭제**(중복 제거). 전수 조사: 앱 전체 `[hidden]` 셀렉터는 `.month-section[hidden]{display:none}`(이미 명시 재숨김·중복이나 무해)뿐, `[hidden]` 요소를 보이게 하는 anti-pattern 없음 → 안전. ⚠️ 닫힌 `<details>` 자식(T-035)은 `[hidden]` 속성이 아니라 UA `details:not([open])` 메커니즘이라 이 리셋 밖 — 개별 `:not([open])` 재숨김 유지(문서 명시). **실 브라우저 게이트**(bootRun+Chrome): 스코프 리셋 제거 후에도 `/settings` iOS·복귀 힌트가 여전히 숨음(`getComputedStyle.display==='none'`)·설정/기록 등 회귀 0·콘솔 0. troubleshooting 재발·승격 트래커 해당 행 ✅ 승격 완료로 갱신 + T-123 본문 정정. 순수 CSS 리셋(단위테스트 N/A)·정적 서빙(번들 없음). |
| 2026-07-02 | **feat: Yes24 제휴 구매링크 병행 — 알라딘·쿠팡·Yes24 3제공자** — 쿠팡 `CoupangLinkBuilder` 패턴을 동형 미러링해 Yes24 "구매"를 추가. **신설** `Yes24LinkBuilder`(추적코드+템플릿 런타임 생성, `not-configured`면 `yes24Enabled=false`로 버튼·고지 숨김 — 가입 후 `BOOKTIMER_YES24_*` env 주입만으로 점등, 코드 변경 0), `Book.yes24ClickCount`(Flyway **V55**, 알라딘·쿠팡과 분리 집계), `BookService.recordYes24Click`/`recordPublicYes24Click`/`yes24Enabled`(IDOR·비활성·공개 게이트), `BookController` 라우트 `/buy/yes24`·`/u/{loginId}/books/{id}/buy/yes24`. `CoupangModelAdvice` → **`AffiliateModelAdvice`** 리네임(두 제휴 플래그 주입), `ShelfResponse`·`ProfileResponse`에 `yes24Enabled` 필드(두 `@RestController`에 빌더 직접 주입 — `@ModelAttribute` 미적용 회귀 방지). **프론트/SSR**: 3제공자 조합 분기(최대 7분기) 폭발을 없애려 뷰 3곳(`BooksApp`·`ShelfPanel`·`book-detail.html`)을 **`buyOptions` 리스트 렌더로 리팩터**(2개↑ 드롭다운/1개 제공자명 단일 버튼), SSR은 컨트롤러가 같은 리스트를 계산해 `th:each`, 제휴 고지 팝오버·note에 Yes24 줄 추가. `task-definition.json`에 SSM env 2개. **TDD RED→GREEN**: Yes24LinkBuilderTest·Yes24BookServiceTest·Yes24BuyControllerTest 신설(각 부재→RED→구현→GREEN) + BookApi/Profile/Dashboard/`profile-app.test.ts`에 `yes24ClickCount` 누수·`yes24Enabled` 노출 가드. 백엔드 전체 스위트 GREEN·vitest 326 GREEN·번들 `books`/`profile` 재빌드. **실 브라우저**(로컬 테스트 추적코드 주입): 내 책장 구매 드롭다운에 쿠팡+Yes24(href `/books/{id}/buy/yes24`)·book-detail SSR `th:each` 렌더·Yes24 제휴 고지문구 정상. **남은 점등**: Yes24 제휴(링크프라이스 등 CPS) 가입·약관상 필수 고지 문구 확정·SSM 주입은 사용자 외부 작업(없어도 비활성으로 안전 머지). |
| 2026-07-02 | **feat: /settings 헤더 — 상단 홈 브랜드 링크 추가 + 톱니 왼쪽 이동** — 재스킨된 설정 페이지 헤더 후속 다듬기(실사용 피드백). ① 최상단에 앱 전역 `.brand`/`.brand-home` 패턴(책등 SVG + `BookTimer`, `th:href="@{/}"`)을 **그대로 재사용**해 어디서든 홈으로 가는 링크 추가(dashboard·block-list 등 8개 페이지와 동일 — 전역 CSS 수정 아님, 사용만 §5 격리 준수). ② 톱니 아이콘을 제목 **오른쪽 끝 → 왼쪽**으로: `.set-header` DOM 순서를 [톱니 → 제목]으로 바꾸고 `justify-content:space-between` → `flex-start`+`gap:10px`. ③ 브랜드 `BookTimer`가 페이지 유일 `h1`이 되도록 `설정`을 `h1`→`h2`로 낮춤(앱 전역 brand-as-h1 관례·헤딩 계층 정상화; `.set-title` 클래스 기반이라 시각은 동일). 백엔드/폼 계약/JS(`notification-settings.js`) **무변경**(.java 0 → 커밋 테스트 게이트 자동 skip). **검증(경량 정적 프리뷰 + 실 app.css)**: DOM 지오메트리로 톱니가 제목 왼쪽(gap 10px)·`BookTimer` 홈 링크(`href="/"`) 최상단·480폭 가로 오버플로 0·콘솔 0 확인(순수 마크업+CSS라 로드순서·JS 무관 → §7 defer 사각 비해당). 정적 서빙(번들 없음)·회귀 0. |
| 2026-07-02 | **chore: Yes24 제휴 고지 문구를 집(house) 문체로 통일 — 점등 준비** — 임시로 쿠팡 문구를 복제해 둔 Yes24 고지("이 포스팅은 Yes24 제휴 활동의 일환으로…")를 알라딘 라인과 같은 집 문체(`Yes24 "구매" 링크는 제휴 링크로, 구매 시 일부 수수료를 받을 수 있습니다.`)로 **3곳 통일** — 책장 `BooksApp.vue` ⓘ 팝오버·책방 `ShelfPanel.vue` ⓘ 팝오버·`book-detail.html` note. BookTimer는 블로그가 아니라 "포스팅" 어색함 제거, 공정위 경제적 이해관계 공개(수수료 수취 명시)는 유지. 번들 `books`·`profile` 재빌드. 순수 텍스트(문구 문자열 검증 테스트 없음 → TDD 신규 N/A)·`.java` 0(커밋 게이트 자동 skip)·vitest 326 GREEN·회귀 0. **버튼 실제 점등은 코드가 아니라** SSM `YES24_TRACKING_CODE`를 실값(`A100705638`)으로 `--overwrite`+재배포(외부 작업). |
| 2026-07-02 | **feat: /settings 알림 카드 비활성화 오버레이 — 기능 개발 중 마스킹** — 알림 섹션(푸시 토글 2개 + 복귀 안내 메일 폼)을 제거하지 않고 반투명 회색 오버레이 + "알람 기능은 아직 개발 중 입니다" 문구로 덮음. `settings.html`: 섹션 자식 전체를 `<div class="set-card-disabled-body" inert>`로 감싸 클릭·Tab 포커스·스크린리더 접근을 전부 차단, 오버레이 `<div class="set-card-overlay" role="note">` 추가. `app.css`: `.set-card-disabled{position:relative}` + `.set-card-overlay`(중앙 정렬·`inset:0`·`rgba(128,128,128,.45)`·`border-radius:inherit`·`color:var(--text)`). `notification-settings.js`·id·th:* 속성은 무변경(점등 시 오버레이만 걷어내면 되도록). 순수 시각 변경(단위테스트 N/A) → **실 브라우저 게이트**(bootRun+testid 로그인): 오버레이 렌더·JS `.click()`/`.focus()` 무반응(inert 확인)·콘솔 에러 0·다른 카드(프로필·비밀번호·회원탈퇴) 무영향. |
| 2026-07-02 | **fix: /settings "밀린 독서 시간을 타이머에 합쳐 표시" 체크박스 시각 미갱신 — 인접 형제 선택자 사각** — 실사용 피드백("클릭해도 변화 없음"). 원인: `th:field="*{debtCarryover}"`가 체크박스 바로 뒤에 `<input type="hidden" name="_debtCarryover">`를 자동 삽입해, CSS `input:checked + .set-check-box`(인접 형제 `+`)의 사슬이 끊김 — `checked` 값 자체는 정상 바인딩·저장되지만 커스텀 체크박스(배경색·체크 아이콘)가 클릭해도 영원히 갱신 안 됨. `app.css` 3곳(`input:checked`·`input:checked svg`·`input:focus-visible`)을 `+` → `~`(일반 형제 선택자)로 교체 — 중간에 hidden input이 끼어도 매칭. 같은 `.set-check` 패턴을 쓰는 `marketingEmailConsent`(th:field 아님, hidden sibling 없음)는 원래도 정상 동작이었고 `~`로 바꿔도 회귀 없음(실 브라우저로 둘 다 재확인). 순수 CSS(단위테스트 N/A) → **실 브라우저 게이트**(bootRun+testid): 클릭 전/후 `getComputedStyle` 배경색·svg opacity가 이제 실제로 토글됨 확인, 콘솔 에러 0. |
| 2026-07-02 | **docs: 독서 스토리(인스타 스토리식 24h 문장 공유) 설계 확정 — sns-design §13 신설** — plan.md 열린 질문 4건 사용자 확정: 공개범위=**팔로워 한정 단일**(visibility 컬럼 없이 시작) · 열람 추적=**story_view v1 포함**(미열람 링이 어차피 요구, "누가 봤나" 열람자 목록은 그 위에 거의 공짜) · 장수=**여러 장**(활성 상한 20+레이트리밋) · 만료=**보존+표시 필터**(물리 삭제 배치 후속). 노출 경계 못 박음: 첨부는 **본인 소유+PUBLIC 책만**(§3.5 불변식 예외 없이 유지, 첨부 후 비공개 전환 시 표시 시점 재검사로 라벨만 숨김) · 차단/만료/ADMIN·login_id-null(N-055) 게이트 · id 직접 진입점은 전체 게이트 재검사(IDOR). 스키마 가안 `story`·`story_view`(번호 머지 직전 확정, 현재 최신 V55) · 신고=기존 Report 재사용(detail에 원문 발췌 자동 첨부) · `STORY_CREATE`(10/시간) · FK 자식 정리 3경로(탈퇴·책삭제·스토리삭제, T-023·T-029 계열 실 H2 필수) · 프론트=새 섬이 아니라 **shared 컴포넌트로 dashboard·profile 두 섬에 삽입**(plan.md 초안 `frontend/src/story/` 정정). plan.md §독서 스토리 상태 갱신(백로그→설계 확정·구현 대기, 착수는 2026-07-02 사용자 결정 — 밀도 원칙 근거 보존). 구현 핸드오프 계획 md는 `claude-docs/plans/`(gitignore). 문서 전용·코드 0. |
| 2026-07-02 | **feat: 독서 스토리 v1 백엔드 — story·story_view 스키마 + API 6종 + 노출 게이트 + FK 정리 (TDD 7단계)** — sns-design §13 구현 1/2(백엔드). 신규: `story`(V56)·`story_view`(V57, `uk_story_view` 멱등 유니크) + `com.booktimer.story` 패키지(엔티티 2·레포 2·서비스·응답 record 4) + `StoryApiController`(feed/of/작성/삭제/열람/열람자 6종 — 개별 GET 없음, 진입점 최소화). 노출 경계(§13.2): 피드=팔로우 theta 조인+만료 표시필터(24h — **정확히 24h부터 제외**, §13.8 정본에 맞춰 계획 스니펫 `>=`를 `>`로 보정)+ADMIN·login_id-null 작성자 제외(N-055), id 직접 진입점(열람 POST)은 canView 전체 게이트 재검사(차단·비팔로워·만료·미존재 → 404), `of/{loginId}`는 프로필과 동일 가드(`resolveVisibleTarget` public 승격 공유) + 비팔로워 200+빈 배열(존재 정보도 안 샘), 책 첨부=본인 소유+PUBLIC만+표시 시점 재검사(비공개 전환 시 라벨만 숨김). 게이트: `STORY_CREATE`(10/시간) 초과 429 안내(무음 드롭 금지 — 콘텐츠 소실)·활성 상한 20장 400. FK 자식 정리 3경로(T-023·T-029 재발 방지, 실 H2 통합): 탈퇴 `purge`(열람→내 스토리 열람→스토리를 책보다 앞에)·책 삭제 `unlinkBook`(문장 보존)·본인 삭제(view 먼저). 차단 불변식("차단→팔로우 해제→피드 소멸") 행동 회귀로 고정. TDD RED→GREEN 7단계(도메인·레포·서비스·행동회귀·FK통합·컨트롤러·Flyway) + 다중 에이전트 적대 리뷰(4관점×반박 3표) 반영: ① 열람 멱등의 DIV catch를 **트랜잭션 경계 밖(컨트롤러)**으로 이동(참여 TX rollback-only 마킹 → 삼켜도 커밋에서 UnexpectedRollbackException 500 나는 함정) ② 피드 `left join fetch s.book`+`@EntityGraph`(책 라벨 N+1 제거, RED→GREEN) ③ `AccountServiceTest` 목 배선 + 스토리→책 삭제 순서 inOrder 검증 ④ 활성 상한은 soft로 명시(경합 초과는 레이트리밋이 상한). 테스트 57건, 전 스위트 GREEN(exit code 검증). |
| 2026-07-02 | **feat: 독서 스토리 v1 프론트 — 홈 스트립·풀스크린 뷰어·작성 모달 + 책방 아바타 링 (구현 2/2)** — sns-design §13.7. `frontend/src/shared/story/` 신설: `storyFeed.ts`(순수 로직 — 스트립 링 구성·뷰어 그룹/시작 카드·진행바 세그먼트·신고 발췌 200/500 절삭·작성 검증 1~500자·시각 라벨, vitest 20건 RED→GREEN) · `storyApi.ts`(fetch 래퍼 — `getCsrfToken` 재사용, 비정형 응답 형태 가드) · `StoryStrip.vue`(가로 아바타 링 — 내 링 맨 앞·+배지 작성 진입·스토리 0이어도 표시, 미열람 강조는 서버 `allViewed` 기준) · `StoryViewer.vue`(풀스크린 z-150 — 장당 5초 진행바·자동 넘김·좌우 탭·Esc/배경 닫기, 카드 표시 시점 열람 POST(멱등·본인 생략), 본인=열람자 시트·삭제, 타인=신고) · `StoryComposer.vue`(500자 카운터·팔레트 6스와치·공개 책 드롭다운 — 기존 `/api/books` 재사용·429/400 상태코드 분기 안내). `ReportModal`을 profile→`shared/`로 이동 + `detailPrefix`(스토리 원문 발췌 자동 첨부 §13.5). 섬 삽입: dashboard(DashHeader↔QuoteCard 사이)·profile(ShopHeader 아바타에 활성 링 — 탭=뷰어, 비팔로워는 서버 빈 배열이라 링 자체 미표시). `app.css` `.story-*` 전용 네임스페이스(배경 팔레트 6색 = 백엔드 `Story.BG_CODES` 코드 일치). `vite.config.ts` 무변경(새 엔트리 없음), dashboard·profile 번들 재빌드. vitest 346 GREEN + 실 브라우저 게이트(bootRun+testid — 작성→링→뷰어 자동 넘김→열람 기록→열람자→삭제, 콘솔 0). |
| 2026-07-02 | **docs: 독서 스토리 구현 sweep — N-145·T-126·T-127 신설 + T-085 보강(3회차)** — ① N-145: 참여 @Transactional 안에서 DataIntegrityViolationException을 삼켜도 rollback-only 마킹 때문에 커밋에서 UnexpectedRollbackException — 멱등 catch는 트랜잭션 경계 밖(컨트롤러)에서(+mock 단위테스트 가짜 green 사각). ② T-126: 검증 명령을 `| tail`로 파이프하면 exit code가 마지막 명령 것으로 덮여 가짜 GREEN(실제 발생 — pipefail·PIPESTATUS 판정). ③ T-127: 크롬 확장 네트워크 로그의 간헐 503은 서비스워커 pass-through 합성 엔트리(페이지 실측·서버 로그·DB 3중 교차 검증). ④ T-085 보강: docker exec 인라인 한글 이중 인코딩 — UTF-8 파일 + `docker exec -i … < file.sql` + `--default-character-set=utf8mb4`(HEX 검증), 3회차(T-085→T-119→이번)라 재발·승격 트래커 등재. 문서 전용. |
| 2026-07-02 | **fix: Yes24 구매 링크 모바일 UA 분기 — 모바일은 제휴 래퍼 없이 m.yes24 검색 직행** — 실사용 보고(모바일 사파리에서 검색 결과 대신 Yes24 모바일 메인). 원인=Yes24 제휴 게이트(`lpfront.aspx`)가 모바일 UA면 목적지를 버리고 m.yes24 메인으로 치환(`tu`에 모바일 URL을 넣어도 동일 — 우회 불가, 운영 curl 실측, T-128). 수정=buy 2 엔드포인트에서 UA 판별(Yes24 자신의 기기 regex 미러링, `Yes24LinkBuilder.isMobileUserAgent`) 후 모바일이면 `mobile-search-url-template`(기본 `m.yes24.com/search?query={query}`) 직행 — 모바일 커미션 포기(사용자 승인)·데스크톱 래퍼 유지·집계 무변경. TDD RED→GREEN(빌더·서비스·컨트롤러 3계층). 프론트·SSM·번들 변경 0. |
| 2026-07-02 | **feat: 홈 명언 카드 → 브랜드 로고 아래 한 줄로 약화(스토리 도입 후속)** — QuoteCard(본문 카드형) 폐지, `BrandQuote` 신설: dashboard.html SSR 브랜드 로고 바로 아래 `#brand-quote-slot`(`header.brand` 밖 형제)으로 Vue Teleport 렌더(브랜드 SSR 즉시 렌더 유지, 백엔드 무변경). 6초 로테이션·hover 정지·reduced-motion 처리는 카드 시절 승계, 트랜지션은 겹침 롤(absolute)→`mode="out-in"`(사라진 뒤 나타남, 10px 소폭 슬라이드)으로 전환해 가변 줄 수에 대응. 폰트 1.05rem Gowun Batang italic·`--muted` 톤 다운, 긴 명언은 말줄임 대신 `overflow-wrap`으로 자연 개행, 모바일 포함 항상 노출. 순수 뷰/CSS(`nextQuoteIndex` 재사용 — 기존 vitest 커버, 새 로직 0), vitest 346 GREEN, `dashboard.js` 재빌드. **실 브라우저 게이트**(bootRun+testid, 조율 피드백 3회 반영 후 재검증): 슬롯 렌더·6초 롤·hover 정지·긴 명언 개행(4줄까지 확인)·모바일 노출·`/books` 헤더 회귀 0·콘솔 0. |
| 2026-07-02 | **feat: 홈 스토리 스트립 박스화 — 세이지 틴트 배경+카드 테두리** — 사용자 피드백(홈 최상단 빈 공간). app.css .story-strip에 배경 #EDF2E9·테두리 1px #D8CDB9·radius 18px·padding 12px 14px, .story-ring-plus 링 색을 새 배경에 일치. CSS 전용(번들·자바 변경 0), 실브라우저 게이트(bootRun+testid). |
| 2026-07-03 | **feat: 책 상세(/books/{id}) Claude Design 재스킨 — 메인보드 통일 디자인(종이톤+세이지)** — book-detail.html·app.css 순수 시각+마크업 변경(백엔드·라우트 무변경). 표지 무표지 플레이스홀더(고정 세이지 톤+제목 이니셜) 신설, 제목 Gowun Batang 격상(.bd-title, 전역 .card h2 무손상 스코프), 상태 배지 3색 분기 + 공개범위 칩(읽기전용) 신설, 누적시간 라벨/수치 분리 강조, 구매 버튼 세이지 pill+SVG 셰브론(전역 ▾▴ glyph는 body.bookdetail-page 스코프에서만 무력화), 월 브라우저 ◀▶ → SVG 원형 버튼, 빈 상태 모래시계 아이콘, 하단 네비 세로 스택. 공유 클래스(.record-date/.record-time/.month-nav-btn/.buy-menu/.book-status-badge 등, 책장·독서기록·history와 공유)는 전부 body.bookdetail-page 접두 스코프로 오버라이드해 타 화면 무손상. BookControllerTest 마크업 어서션 1건 갱신(누적 텍스트가 span 분리로 비연속 — 의미 보존, containsString 2개로 분리). 실브라우저 게이트(bootRun+testid): 표지 유/무·상태 3종·공개 2종·빈 기록·카드 radius 18px·하단 네비 column·콘솔 0·책장/독서기록 화면 회귀 0 확인. |
| 2026-07-03 | **feat: 책방 공개 책 정렬 — 기본 이름순 + 완독 한정 완독시각 정렬(최신순/오래된순)** — 완독 시각 데이터 소스 신설: `book.finished_at`(V58, 기존 완독 책은 updated_at 백필 근사) + `Book.changeStatus(status, now)`가 완독 진입 시 기록·이탈 시 클리어·완독→완독 멱등(시각 유지), 등록 경로(addManual/addFromSearch)도 완독이면 스탬프(BookService Clock 주입 — 엔티티는 now를 모름, 하우스 스타일). 책방 목록 기본 정렬을 등록 최신순→이름순으로 교체(`findByUserAndVisibilityOrderByTitleAscIdAsc`, 세 호출처 일괄 — 태그 드릴다운도 이름순·책BTI 집계는 순서 무관). `/api/profile/books`에 `sort=finished_desc/asc` 관대 파싱(그 외→이름순, null 완독시각은 뒤로+이름순 tie-break — N-055 경계). 프론트: ShelfPanel에 완독 필터 한정 정렬 칩(이름순/최신순/오래된순, .shop-sort), ProfileApp이 sort 상태·URL 딥링크(`?status=FINISHED&sort=…`) 왕복 소유, 완독 외 필터 전환 시 이름순 리셋 + **기존 딥링크 사각 수리**(?status= 진입 시 칩만 활성화되고 목록은 전체가 뜨던 것 — 초기 load 후 loadBooks 재조회). TDD RED→GREEN: BookTest 6·BookServiceTest 4·ProfileApiControllerTest 6 + vitest shelf-sort 8(354 전체 GREEN). profile.js 재빌드. |
| 2026-07-03 | **feat: 홈 명언 블록 높이 2줄 고정 — 긴 문장 폰트 축소로 아래 UI 밀림 제거** — 사용자 피드백(명언이 길어져 2줄이 되면 그 아래 `#dashboard-app`(타이머 히어로 등)이 6초 롤마다 상하로 흔들림). 원인=`.brand-quote` 높이가 콘텐츠 가변 + `.brand-quote-line`이 flex라 본문이 2줄이면 저자 span이 3번째 줄로 밀려 잘림. 해법 4겹: ① 본문+저자를 한 텍스트 흐름으로(flex→inline, 저자 `white-space:nowrap`) — 저자가 별도 줄을 안 차지해 좁은 폭에서도 대부분 2줄 이내 ② `.brand-quote`를 2줄 높이로 고정(`height: calc(1.05rem*1.5*2)` + flex 세로중앙 + `overflow:hidden`) → 명언 길이와 무관하게 아래 요소 불변 ③ `quoteFontScale(textLen, authorLen)` 순수 함수(`timerProgress.ts`)로 2줄에도 넘칠 최장급(eff>58)만 `--q-scale` 폰트 축소, `clamp(0.78rem, …, 1.05rem)`이 하한상한 하드가드 ④ 데스크톱(`@media min-width:640px`, 홈 컬럼 760px)은 축소 완화(원 크기). 임계값은 실측 기반(모바일 328~460px 전수) — 현 18개 명언 중 최장(eff 63)만 좁은 폭에서 0.84 축소, 나머지는 원크기. TDD RED→GREEN(`quoteFontScale` 경계·저자가중·불변식, vitest 88 GREEN). 실브라우저 게이트(실 `app.css` 링크 정적 하니스, 모바일 328·460 / 데스크톱 760 전수): 히어로 top·블록높이 불변, 최장 명언 저자까지 2줄 온전, 콘솔 0. `dashboard.js` 재빌드. |
| 2026-07-03 | **fix: 쿠팡 파트너스 딥링크 API 연동 — 자작 lptag 링크(추적 0)를 정식 추적링크(shortenUrl) 302로 교체** — 파트너스 리포트 클릭 0 조사(N-146/T-129): `CoupangLinkBuilder`가 `lptag`만 문자열로 붙인 검색 URL을 그대로 302 리다이렉트해, 파트너스가 "생성"하지 않은 링크라 구조적으로 미집계. 신규 `CoupangDeeplinkSigner`(HMAC-SHA256 순수 서명, openssl 독립 벡터로 검증)·`CoupangDeeplinkClient`(딥링크 API POST → `rCode`/`shortenUrl` 파싱, 429/5xx/인증실패는 `Optional.empty()`로 격리하되 예외 클래스명은 로그에 남겨 설정오류/일시장애 구분, 커넥트 3초·리드 5초 타임아웃, `computeIfAbsent` 기반 인메모리 캐시로 동일 URL 동시요청도 API 1회만 호출)·`CoupangDeeplinkProperties`(`booktimer.coupang.access-key`/`secret-key`/`sub-id`/`api-base-url`/`requested-by`, 셋 다(access-key·secret-key·sub-id) 실값이어야 활성 — sub-id 누락 시 클릭은 성공해도 정산 제외되는 사각을 게이트로 차단). **dark-launch + 버튼 노출 분리(코드리뷰로 발견한 회귀 수정)**: 딥링크 API 키는 파트너스 '최종 승인'(누적 판매 15만원) 게이트 뒤라 미발급 상태인데, 최초 구현은 `CoupangLinkBuilder.isEnabled()`를 이 키에 직접 묶어 "쿠팡 버튼 노출"과 "딥링크 추적 가능 여부"를 한 게이트로 합쳤다 — 그런데 쿠팡 파트너스 가입 자체는 2026-06-12(#319)에 이미 완료돼 운영에 버튼이 떠 있어, 그대로 배포하면 dark-launch(무영향)가 아니라 **운영에 떠 있던 버튼이 사라지는 회귀**가 됐다(다중 에이전트 코드리뷰로 8건 검증 후 발견, 사용자 확인 후 분리 결정). 해법: `CoupangLinkBuilder.isEnabled()`를 새 독립 플래그 `booktimer.coupang.partner-enabled`(기본 false, `task-definition.json`에서 운영만 명시적으로 true)로 되돌리고, 딥링크 API 키 게이트는 `CoupangDeeplinkClient`에서만 독립적으로 판단 — 버튼은 그대로 뜨고(raw 검색 링크는 키 없이도 유효), 키가 오면 추적만 조용히 업그레이드된다. `CoupangLinkBuilder.buildSearchLink`는 운영 SSM에 남아있을 옛 `{trackingCode}` 플레이스홀더도 방어적으로 제거. `BookService.recordCoupangClick`/`recordPublicCoupangClick`은 `deeplinkClient.toTrackingLink(raw).orElse(raw)`로 실패 시 raw 검색 URL 폴백(버튼 작동 유지, 집계는 그대로). `task-definition.json` SSM secrets 교체(`COUPANG_TRACKING_CODE` 제거 → `COUPANG_ACCESS_KEY`/`SECRET_KEY`/`SUB_ID` 추가) + `BOOKTIMER_COUPANG_PARTNER_ENABLED=true` 환경변수 추가. TDD RED→GREEN: CoupangDeeplinkSignerTest(고정 datetime+secret+path의 기대 hex를 openssl로 독립 산출해 하드코딩 — 자가채점 방지)·CoupangDeeplinkPropertiesTest(키+subId 게이트 7경계)·CoupangDeeplinkClientTest(MockRestServiceServer — 성공/rCode≠0/429/5xx/캐시/비활성 6케이스)·CoupangLinkBuilderTest(파트너 게이트로 재설계 + 옛 트래킹코드 플레이스홀더 방어 케이스)·CoupangBookServiceTest 확장(딥링크 성공 시 추적링크·실패 시 raw 폴백 2케이스 추가) + 기존 CoupangBuyControllerTest 4케이스 회귀 GREEN 유지(전체 1301 GREEN). 다중 에이전트 코드리뷰(파인더 8종 × 검증 7건, 전부 CONFIRMED) 반영 — 나머지 발견(cache race는 computeIfAbsent로 해소, 타임아웃 추가, 예외 로깅 개선, 정적 문서 정정)도 함께 커밋. 키 확보 전까지는 실 API 호출 검증 불가 — mock 서버 테스트로 대체, 실호출 1회 검증은 키 확보 후 후속 과제. |
| 2026-07-04 | **fix: 쿠팡 dark-launch 배포 언블록 — 미생성 SSM secret 참조 제거 + 승인 전 버튼 숨김** — #641 머지 후 ECS 배포가 서킷브레이커 롤백(운영은 구 리비전 유지, 무중단). 원인=`task-definition.json`이 #641로 새로 추가한 딥링크 secret 3개(`COUPANG_ACCESS_KEY`/`SECRET_KEY`/`SUB_ID`)를 `valueFrom`으로 참조하는데 해당 SSM 파라미터가 미생성이라 Fargate가 secret 주입 실패로 새 태스크 기동 불가. `application.properties`의 안전 기본값(`:not-configured`)이 있어도 task-def `valueFrom`이 "파라미터 존재 자체"를 강제해 dark-launch가 실제론 안 안전했음(T-130 후보). 해법=그 secret 3줄 제거(앱 기본값 적용 → `CoupangDeeplinkProperties.isEnabled()` false로 딥링크 호출 skip·raw 검색 URL 폴백 유지) — 키 확보 시 되돌림. 아울러 운영자 결정으로 `BOOKTIMER_COUPANG_PARTNER_ENABLED`를 true→false로 내려 승인·키 확보 전까지 추적 0인 쿠팡 "구매" 버튼·고지문구를 숨김(키 확보 시 true 복귀, 그때 raw 폴백이 아닌 실추적 점등). config 전용(.java·번들·프론트 변경 0), JSON 유효·CRLF 보존. |
| 2026-07-04 | **docs: troubleshooting T-130 신설 (#642 배포 언블록 sweep)** — dark-launch 기능의 secret을 `task-definition.json`의 `valueFrom`으로 배선하면, `application.properties`에 안전 기본값이 있어도 SSM 파라미터 미생성 시 ECS 배포가 서킷브레이커 롤백(태스크 기동 실패). 미점등 기능 secret은 task-def에서 빼거나 placeholder 파라미터를 먼저 생성하라. 문서 전용. |
| 2026-07-04 | **fix: 알라딘 제휴 추적 복구 — 응답 link에 TTBKey를 싣는 `includeKey=1` 누락 픽스 + 기존 저장분 백필** — 제휴 링크 감사(다중 에이전트 워크플로 7)로 발견, Chrome 운영 실측(`/books/{id}/buy` 302 목적지가 `…?ItemId=…&partner=openAPI&start=api`로 **ttbkey 부재**, 2건)으로 확정. 원인=`AladinBookSearchClient.buildSearchUrl`·`buildLookupUrl`이 `includeKey=1`을 안 보내(알라딘 기본 0) 응답 `link`에 TTBKey(제휴 식별자)가 빠져, 저장·302 재생되는 구매링크가 무추적 → 클릭이 귀속 안 돼 제휴 수익 0. 쿠팡 lptag(N-146/T-129)와 동일 계열이나, 알라딘은 '자작 합성'이 아니라 'API가 준 link를 그대로 쓰되 제휴키 옵션(`includeKey=1`)을 안 켬'이라는 한 겹 다른 함정(그래서 합성 함정은 회피했으나 결과는 동일). 테스트 사각도 확인: 기존 `parse_mapsItems` 픽스처가 link에 `ttbkey=x`를 손으로 박고 그 존재를 단언 → 파서 verbatim만 증명(가짜 GREEN), 실 API 계약 미검증. **핵심 픽스**=두 URL 빌더에 `includeKey=1` 추가(새 검색·백필부터 ttbkey 실림). **짝 픽스**(이미 저장된 무추적 링크는 픽스만으론 안 고쳐짐)=`BookCatalogBackfillService.backfillPurchaseLinks`(대상=purchaseLink에 `ttbkey=` 없고 isbn 있는 책, `lookupByIsbn` 재조회로 교체, 재조회에도 ttbkey 없으면 옛 링크 유지·notFound — 무추적 링크로 덮어쓰지 않음) + `Book.updatePurchaseLink` + Repository 파생쿼리 2개 + `POST /admin/books/backfill-purchase-links`(ADMIN·CSRF·limit 배치) + admin.html "제휴링크 백필" 버튼 신설. TDD RED→GREEN: AladinBookSearchClientTest `includeKey=1` 단언 + BookCatalogBackfillServiceTest 6케이스(교체·멱등·재조회미발견 유지·purchaseLink없음/isbn없음 제외·비활성) + AdminBackfillControllerTest 2케이스(ADMIN 실행/USER 403) 전부 GREEN. 배포 후 운영 ttbkey로 admin 백필 실행 → `/buy` 링크에 ttbkey 재확인은 후속(운영 검증). |
| 2026-07-04 | **fix: YES24 제휴 무성실패 가드 — 추적코드가 링크에 실릴 때만 버튼 노출(isEnabled에 템플릿 `{trackingCode}` 검증)** — 알라딘(#644)에 이은 제휴 감사 후속. Chrome 운영 실측으로 **YES24도 데스크톱에서 추적 0** 확정(`/books/{id}/buy/yes24` 302 목적지가 링크프라이스 래퍼 없이 순수 `www.yes24.com/product/search?query=<ISBN>`·추적코드 부재, 2건). 원인=운영 SSM `YES24_SEARCH_URL_TEMPLATE`가 링크프라이스 딥링크 래퍼가 아니라 순수 검색 URL이라 `{trackingCode}`가 실릴 자리가 없음(`YES24_TRACKING_CODE`는 실값으로 점등돼 버튼은 떠 있음 — plan.md의 "점등 스위치 하나" 서술과 달리 SEARCH_URL_TEMPLATE가 래퍼로 안 채워진 채 켜짐). 그런데 `Yes24LinkBuilder.isEnabled()`가 `trackingCode` 설정 여부만 봐서(래퍼 URL 여부 무검증) 버튼·고지문은 뜨는데 추적은 0인 무성실패(T-131 계열 — 알라딘 includeKey·쿠팡 lptag와 동일 클래스). 가드=`isEnabled()`에 `searchUrlTemplate.contains("{trackingCode}")` 조건 추가 → 추적코드가 실릴 자리가 없으면 비활성(버튼 숨김·`buildSearchLink` null). 모바일 템플릿은 T-128대로 추적을 의도적으로 포기하므로 게이트에서 제외(데스크톱 추적 가능 여부로 노출 결정). TDD RED→GREEN: Yes24LinkBuilderTest 신규 1케이스(순수 템플릿→비활성·링크 null) + 인코딩 테스트를 `{trackingCode}` 포함 템플릿으로 갱신(의도 보존), Yes24·Affiliate 관련 전부 GREEN. **근본 복구는 별개**(운영 작업): SSM `YES24_SEARCH_URL_TEMPLATE`를 링크프라이스 래퍼(`lpweb.kr/click.php?a_id={trackingCode}&…&tu=<Yes24 검색 URL>{query}`)로 `--overwrite` 교체해야 실제 추적 점등 — 이 가드는 "설정 틀리면 조용히 0"을 "버튼 숨김(티남)"으로 바꾸는 안전장치다. 모바일 전량 무추적은 Yes24 게이트 탓 구조적 한계로 별건. |
| 2026-07-04 | **chore: YES24(LinkPrice) 실적 조회 무인 스크립트 신설 — 제휴 커미션을 브라우저·세션 없이 API로 집계** — 실적 확인 자동화 A안(즉석 조회) 착수. LinkPrice 실적 조회 오픈 API(`api.linkprice.com/affiliate/translist.php`, `a_id=A100705638` 확보·`auth_key` 발급 대기)를 호출해 거래 배열(`order_list`)을 집계·요약하는 로컬 Node 스크립트 `.claude/scripts/affiliate-report.mjs` 신설(이번 달/특정일·`--test`·`--merchant`·`--json` 지원, `list_count>per_page`면 page 순회). 순수 집계함수 `summarize()`(전체합계 + 취소(300·310) 제외 `active` 합계 + 상태별·머천트별 분해, `sales`/`commission` 문자열 방어 캐스팅, 미지 status 라벨 폴백) TDD 7케이스 RED→GREEN(`node --test`, Java 게이트와 무관한 독립 스크립트). `auth_key`는 env `LINKPRICE_AUTH_KEY` 또는 gitignored `.claude/.secrets/linkprice-auth`에서만 읽어 커밋·로그 미노출(`.gitignore`에 `.claude/.secrets/` 추가), 부재 시 발급 안내+exit2. 라이브 스모크(`--test`)는 `auth_key` 발급 후 1회. 알라딘은 실적 조회 API 없음(TTB 수익프로그램 2022 종료·OpenAPI는 상품검색 전용)·쿠팡은 버튼 OFF라 대상 제외 — 스크립트는 확장 여지만 둠. |
| 2026-07-04 | **ops: 알라딘 제휴링크 백필 운영 실행 완료 — 무추적 45권 전량 ttbkey 복구(실측 확정)** — #644가 "배포 후 admin 백필 실행 → `/buy` ttbkey 재확인은 후속(운영 검증)"으로 남긴 운영 실행을 수행. 운영 admin `POST /admin/books/backfill-purchase-links` 1회 = **조회 45·갱신 45·미발견 0·남은 0**(등록 전량, limit 50 ≥ 45). Chrome 운영 실측 전후 대조: 책 60·59 `/buy` 302 목적지가 백필 전 `…&partner=openAPI&start=api`(**ttbkey 부재**) → 후 `…&copyPaper=1&ttbkey=ttbkimsadol…&partner=openAPI&start=api`(**ttbkey 실림**)로 전환 = 알라딘 제휴 추적 실제 복구 확정(kimsadol 공개 책방 경로로 검증 — admin 계정은 IDOR로 본인경로 막힘). 겸사겸사 Yes24 실측 재확인: `/buy/yes24`가 `www.yes24.com/Cooperate/Yes24Gateway.aspx?pid=…`(**링크프라이스 제휴 게이트** — 링크프라이스가 Yes24로 넘기며 `pid`를 실음) 경유 = 데스크톱 추적 정상, #646 실적 스크립트(링크프라이스 `a_id=A100705638`)로 잡힘. ⚠️ 다음 세션 주의: referrer의 `pid`만 보면 'Yes24 직접 제휴'로 오해하기 쉬우나 **앞단 `newtip.net`(링크프라이스) 경유가 정본**(plan.md 라인 492와 일치) — 채널 재조사 불필요. **운영 액션·문서만(코드·`.java` 0)** → 커밋 테스트 게이트 자동 스킵. plan.md 책 단위 기록 알라딘 라인에 '백필 실행 완료·실측'을 못박아 다음 세션 재조사 방지. |
| 2026-07-05 | **feat: 교보문고 제휴 서점 추가(dark-launch) — Yes24 링크프라이스 패턴 대칭 복제** — 링크프라이스에 교보문고 머천트가 있어 4번째 제휴 서점으로 추가. 서점은 enum이 아니라 제공자별 대칭 복제 구조라(주석이 '대칭 유지'를 의도적 설계로 명시) 참조 모델 `Yes24LinkBuilder`를 그대로 미러링. 신규 `KyoboLinkBuilder`(추적코드+URL 템플릿 런타임 치환, `isEnabled()` 이중 게이트 — 추적코드 설정 AND 템플릿에 `{trackingCode}` 자리 있음이라야 활성, "추적 0인데 버튼 뜨는" 무성실패 방어 T-131 대칭; 모바일 UA 분기 T-128 대칭; 목적지는 교보 통합검색 URL — 상품 상세는 내부ID(S000…) 기반이라 ISBN 조립 불가). 배선: `Book.kyoboClickCount` + `recordKyoboClick()` + `V59__book_kyobo_click_count.sql`(알라딘 V5·쿠팡 V34·Yes24 V55와 분리 집계), `BookService.recordKyoboClick`/`recordPublicKyoboClick`/`kyoboEnabled()`(Yes24 대칭 — IDOR/공개여부 게이트, 비활성이면 무집계), `BookController` `/books/{id}/buy/kyobo`·`/u/{loginId}/books/{bookId}/buy/kyobo` 2핸들러 + `ownedBuyOptions` 한 줄. 활성 플래그 3경로 전파(N-144 — @RestController는 ControllerAdvice 미적용이라 직접 주입): `AffiliateModelAdvice`(SSR), `BookApiController.ShelfResponse`(Vue 내책장), `ProfileApiController.ProfileResponse`(Vue 공개책방). 프론트 `BooksApp.vue`·`ShelfPanel.vue`·`ProfileApp.vue` buyOptions push + 고지문구 + `book-detail.html` + 번들 재빌드(books.js·profile.js). **dark-launch**: 앱 기본값 `not-configured`라 `isEnabled()` false로 버튼·고지문 숨김 — 교보 링크프라이스 실 URL(m/l/tu)은 대시보드 발급 후 운영 SSM `BOOKTIMER_KYOBO_*` 주입 시점에 점등(미점등 secret을 task-def valueFrom으로 걸지 않아 T-130 롤백 회피). `application.properties`는 Yes24 대칭으로 키 미기재(@Value 기본값에만 의존). 실적 조회는 `affiliate-report.mjs`가 이미 `m_id`로 머천트별 자동 집계라 코드 변경 0(a_id `A100705638` 계정 공용 가정). TDD RED→GREEN: `KyoboLinkBuilderTest`(무성실패 이중 게이트·추적코드 실림·한글/`&` 인코딩·queryFor ISBN13 우선) + `KyoboBuyControllerTest`(302 목적지·클릭집계·IDOR→/books·공개책 주인집계·비공개→프로필 폴백·모바일 UA) 전체 GREEN(회귀 포함). 점등 후 실 브라우저 302 실측(N-147)은 후속. |
| 2026-07-05 | **ops: 교보문고 제휴 점등 배선 — SSM 파라미터 생성 + task-definition.json valueFrom 2줄** — #653 dark-launch 코드를 운영 점등. 링크프라이스 대시보드에서 교보(kbbook) 딥링크 변환으로 실 URL 확정(m=kbbook·a=A100705638=Yes24와 계정 공용·l=9999·l_cd1=3·l_cd2=0, tu=교보 통합검색 URL 인코딩). SSM(ap-northeast-2) 파라미터 2개 생성 — `/booktimer/KYOBO_TRACKING_CODE`=A100705638, `/booktimer/KYOBO_SEARCH_URL_TEMPLATE`=`https://newtip.net/click.php?m=kbbook&a={trackingCode}&l=9999&l_cd1=3&l_cd2=0&tu=https%3A%2F%2Fsearch.kyobobook.co.kr%2Fsearch%3Fkeyword%3D{query}`(String 타입 — URL·제휴코드는 비밀 아님, ECS valueFrom은 String도 주입 지원). `task-definition.json` secrets에 `BOOKTIMER_KYOBO_TRACKING_CODE`·`BOOKTIMER_KYOBO_SEARCH_URL_TEMPLATE` 2줄 valueFrom 배선(SSM 파라미터를 **먼저** 생성해 T-130 롤백 회피). PC만 점등(모바일은 Yes24 T-128처럼 게이트가 딥링크를 버리는지 실측 전이라 순수 검색 기본값 유지 — PC 실측 후 게이트 살아있으면 모바일 template 추가). config 전용(.java·번들 0). 배포 후 `/buy/kyobo` 302 목적지 실측(N-147)은 후속. |
| 2026-07-05 | **ops: 교보문고 제휴 모바일까지 점등 — MOBILE_SEARCH_URL_TEMPLATE 배선(교보는 T-128 없음, 모바일도 `/search`)** — #654로 PC 점등 후 운영 실측. **PC 확정**: `/books/60/buy/kyobo` → 302 → `search.kyobobook.co.kr/search?keyword=9791158393083&LINK=PLP`(책 ISBN + 링크프라이스 추적 마커) 도착, 교보 버튼 노출. **모바일 실측**: 데스크톱 브라우저로 `/mobile/search` 딥링크를 열면 교보가 검색어를 잃고 기본검색으로 튕겼으나(데스크톱 UA+모바일 URL 불일치 조합), **실제 폰으로 `/search`(데스크톱 경로) 딥링크를 열자 검색결과 정상 노출** — 교보는 반응형이라 `/search`가 모바일 UA에서도 작동하고 링크프라이스 추적이 살아, Yes24 T-128(모바일 게이트가 딥링크 버림) 문제가 **없음** 확인. 그래서 모바일도 `/mobile/search`가 아니라 **데스크톱과 동일한 `/search` 추적 래퍼**를 쓴다. SSM `/booktimer/KYOBO_MOBILE_SEARCH_URL_TEMPLATE`=`KYOBO_SEARCH_URL_TEMPLATE`와 동일값(`…tu=…%2Fsearch%3Fkeyword%3D{query}`) 생성 → `task-definition.json`에 `BOOKTIMER_KYOBO_MOBILE_SEARCH_URL_TEMPLATE` valueFrom 1줄 배선(SSM 먼저 생성해 T-130 회피). 이로써 `KyoboLinkBuilder`의 mobile 기본값(순수 `/mobile/search`, 추적 0)이 추적 래퍼로 덮여 **모바일 클릭도 추적**. config 전용(.java·번들 0). 교보 제휴 PC+모바일 전량 점등 완료. |
| 2026-07-05 | **chore: phaser 죽은 의존성 제거 — 정원 Vue 이관 후 잔재 devDependency 삭제** — 정원(마을)이 Phaser CoC 무대에서 Vue 돌봄 뷰로 이관되며(scene.ts/GardenGame 은퇴) phaser는 `frontend/src` 어디에서도 import되지 않는 죽은 devDependency로 남았다(오히려 `test/village-care-view.test.ts`가 phaser import 재유입을 막는 회귀 가드). 런타임 번들에도 미포함(각 섬 번들 grep 0, CI 번들 stale 미발생으로 확증). dependabot이 매주 안 쓰는 라이브러리의 major 업 PR(#652 phaser 3→4)을 재생성하던 소음의 근본 제거 — `frontend/package.json` devDependencies에서 phaser 삭제 + lockfile 갱신(하위 eventemitter3 동반 제거). 회귀 가드 포함 vitest 전량 GREEN, `npm run build` 번들 바이트 불변(phaser 미포함 확증). 이후 phaser 계열 dependabot PR 미생성. |
| 2026-07-05 | **docs: T-132 — dependabot 프론트 dep bump 번들 stale 함정 기록** — 2026-07-05 dependabot 프론트 PR 정리 sweep 산출물. vue처럼 런타임 번들에 인라인되는 dep은 dependabot이 lockfile만 올려 CI `Verify bundle is not stale`가 실패(코드 회귀 아님) → PR 브랜치에서 재빌드 커밋을 얹어야 머지된다. vitest·plugin-vue처럼 번들 미포함 dep은 재빌드 없이 통과 — "그 dep이 런타임 번들에 들어가나"가 stale 실패 여부를 가른다. troubleshooting.md T-132 추가. |
| 2026-07-06 | **docs: 기능별 코드 지도(code-map.md) 신설 — 작업 전 진입점 직행용 린 참조** — 기능 작업 시작 전 진입점(URL→컨트롤러)·소속 패키지·패키지 넘는 배선을 먼저 잡고 해당 파일로 직행하는 참조 문서. 전 클래스 카탈로그(241개 나열)는 stale·이중기재 비용이 커 폐기하고, **트리가 못 보여주는 것만**(기능별로 안 나뉜 `web/` 컨트롤러 50개의 진입점 매핑 + 한 기능이 여러 패키지에 흩어지는 배선) 집는 린 버전으로 결정. 기능 15절(인증·온보딩·타이머부채·책제휴·잔디히스토리·마을·소셜·스토리·성향분석·운영자·피드백·이메일·푸시리텐션·격언·공통) 각각에 진입점·핵심클래스·⚠️배선주의·프론트·DB·설계문서, + 컨트롤러→기능 역인덱스. 진입점은 `@RequestMapping` 실측으로 검증(예: 부채로직은 `timer/`가 아닌 `session/`, 푸시 컨트롤러는 `web/`가 아닌 `push/PushApiController`). README §10 관련 문서에 링크 추가. 코드 무변경. |
| 2026-07-06 | **docs: CLAUDE.md CSRF 절 예시 stale 메서드명 정정** — CSRF 세션 선확정 예시가 리팩터로 사라진 `PasswordResetController.precommitCsrfToken(request)`을 헬퍼로 가리키던 것을, 실재하는 공용 정적 유틸 `web/CsrfTokenUtil.precommit(request)`(`web/PasswordResetController`가 렌더 전 호출) 기준으로 정정. code-map.md 신설(#658) 중 `@RequestMapping` 실측으로 발견한 문서 드리프트를 후속 처리. 문서 전용, 코드 무변경. |
| 2026-07-06 | **fix: LinkPrice 실적조회 auth_key 발급 완료 — 비밀키 파일 경로 버그(`.claude/scripts/.secrets/` → `.claude/.secrets/`) 수정 + 라이브 스모크 확인** — 링크프라이스 담당자가 실적조회 오픈 API 인증키(a_id=A100705638) 발급(2026-07-06). `affiliate-report.mjs`의 `readAuthKey()`가 비밀 파일을 `resolve(here, '.secrets', …)`로 **스크립트 폴더**(`.claude/scripts/.secrets/`) 기준으로 읽어, 주석·에러메시지·메모리·`.gitignore`가 모두 가리키는 `.claude/.secrets/`와 어긋난 무성 함정. 문서대로 저장하면 스크립트가 못 찾아 실패하고, 코드가 읽는 경로에 저장하면 `.gitignore`(`.claude/.secrets/`만 무시) 밖이라 키가 커밋되는 양방향 사각. 픽스=경로 해석을 순수 함수 `secretPathFor(scriptDir)`로 추출·export하고 `..`을 넣어 상위 `.claude/.secrets/`를 잡게 함. TDD RED→GREEN: `secretPathFor`가 `.claude/.secrets/linkprice-auth`로 끝나고 `scripts/` 밑이 아님을 단언(현 코드로 RED=`…/scripts/.secrets/…` 실측 → `..` 추가로 GREEN, `node --test` 10케이스 GREEN). 발급 실키를 gitignored `.claude/.secrets/linkprice-auth`에 저장 → `git check-ignore`로 무시 확인 + env var 없이 `--test` 라이브 스모크 정상(더미 10건 집계). |
| 2026-07-06 | **fix: affiliate-report.mjs — result=101(실적 0건)을 에러가 아닌 "실적 없음"으로 처리** — auth_key 점등 후 첫 실 조회에서 `result=101` "❌ API 오류"가 뜸. 가이드(`실적_조회_오픈_API_v1.6`) 확인 결과 101="정상 page 번호 아님"인데, **실적 0건이면 반환할 page가 없어** 이 코드가 온다(운영 실측: 202605·06·07 전부 `result=101`·`list_count=0`, `test=Y`는 `result=0`·10건 → auth·a_id·page 인덱싱 정상, 단지 데이터 0건). 기존 `fetchAllOrders`가 `result!='0'`을 전부 하드 에러로 처리해, 실적이 쌓이기 전까진 매번 오해 메시지 + exit 1. 순수 함수 `classifyResult(result)`(export) 신설로 3분류 — `'0'`=ok / `'101'`=no-data(빈 성공으로 종료) / 그 외(100 a_id·200/210 날짜·300 인증키·400 통화)=error 유지. `fetchAllOrders`가 no-data면 지금까지 모은 것(0건이면 빈 배열)으로 정상 종료 → `formatReport`의 "(해당 기간 실적 없음 …)" 경로. TDD RED→GREEN: `classifyResult` 9경계(0=ok·101/숫자101=no-data·100/200/210/300/400/undefined=error) — **101을 error로 두면 무데이터마다 가짜 오류, 실제 오류코드를 no-data로 삼키면 설정 버그가 침묵**하는 양방향 불변식. 통합 실측: 실 조회 `실적 없음`+exit 0 / 잘못된 키(300) `❌ API 오류`+exit 1 확인. 첫 실 조회 0건은 제휴 점등 직후라 정상(본인 클릭·구매 제외). |
| 2026-07-06 | **feat: 타이머 히어로를 오늘 읽은 시간 카운트업으로 (UX 개선 발견 3·4)** — 사용자 편의성 UX 리뷰(발견 12건) 중 핵심 프레이밍 전환 PR-A. 히어로 큰 숫자를 "남은 시간 카운트다운"(`remainingDisplay`)에서 **"오늘 읽은 시간 카운트업"**(`computeProgress().todayRead`)으로 전환 — 앱을 "갚을 빚(결손)"이 아니라 "오늘 쌓은 독서 시간(성취)" 중심으로. 측정 중엔 라이브 `remainingNow`가 줄며 `todayRead`가 매초 증가(별도 tick 없이 파생 반응), 이번 세션 경과는 "측정 중" 칩에 별도 표시(발견 4: 히어로와 세션이 딴 값을 세던 문제 해소). 목표 달성 시 히어로 숫자·진행바가 `--ok`(성취 초록 #2F8F6B)로 전환(`.dash-timer-num-ok`·`.dash-progress-fill-ok`, 색만 — 새 색 없음). 진행바 메타 우측을 "{pct}% 달성"에서 **"목표까지 M:SS"**(달성 시 "목표 달성 ✓")로 바꿔 남은 시간을 보조 정보로 강등. `carriedDebtSeconds>0`이면 점선 구분 아래 **7일 자동 용서 안내 줄**("밀린 N분은 최근 7일이 지나면 자동으로 사라져요 — 뒤처져도 괜찮아요")로 밀린 빚을 위협이 아닌 안심으로 프레이밍. **부채 계산·`@Scheduled`·IDOR·공개경계·`/api/dashboard` 응답 전부 무변경 — 표시(프레이밍)만 변경.** `computeProgress`에 `remainingToGoal = max(0, todayDebtLive)` 파생 필드 1개 추가(순수 표시용, 기존 반환 필드·계산식 불변). TDD RED→GREEN: `timerProgress.test.ts`에 카운트업(`todayRead` 라이브 증가·carryover 오늘분만·초과분)·`remainingToGoal` 7케이스 추가(RED=필드 미구현 → GREEN), 전체 vitest 370 GREEN. dashboard.js 재빌드. 정적 프리뷰 하니스(실제 번들+mock `/api/dashboard`)로 4시나리오(진행 중·달성 초록·측정 중 라이브 카운트업·밀린 빚 용서줄) 실 브라우저 확인. |
| 2026-07-06 | **feat: 대시보드 상단 정리·잔디 대비 강화·스토리 + 별도 타일 (UX 개선 발견 2·7·8)** — UX 리뷰 PR-B, 표시/레이아웃만 변경(부채·`@Scheduled`·IDOR 무변경). **발견 2(상단 4겹 정리)**: `DashboardApp.vue` 렌더 순서를 헤더 → 타이머 → 잔디 → 스토리 → 바로가기/서재 → 격언으로 재배치(스토리를 타이머 아래로 강등). 격언(`BrandQuote`)은 Teleport 대상 슬롯 `#brand-quote-slot`을 `dashboard.html`에서 `#dashboard-app` **뒤(발밑)**로 옮겨 상단에서 제거 — 상단이 브랜드→인사 2겹으로 축소. (브랜드+인사 완전 DOM 융합은 `DashHeader` Teleport화가 `dash-header.test`를 대폭 흔들어, 이번엔 순서 재배치로 상단 붐빔을 해소하고 완전 융합은 보류.) **발견 7(잔디 대비)**: 5단계 색을 `#EAE4D7·#C3D9B0·#94BE7F·#5E9250·#35662F`(대비↑)로. 히스토리 `.grass-cell.level-*`·대시보드 `.dash-grass-cell.s*`가 하드코딩 2벌로 drift 위험이던 것을 `:root` **CSS 변수 `--grass-0~4` 단일 출처**로 통합(값 바꾸면 둘 다 반영). manual 앰버 테두리·범례 유지. **발견 8(스토리 +)**: 아바타에 겹치던 `.story-ring-plus`(18px absolute) 제거 → 맨 앞 **독립 "+올리기" 타일**(`.story-add-tile`, 52px 점선 원)로 분리해 터치 타깃↑·겹침 제거. 내 스토리 없는 빈 내 링은 안 그리고(작성은 +타일로 일원화) 뷰잉 링만 표시. TDD RED→GREEN: `dashboard-app.test.ts`에 렌더 순서 단언(타이머<잔디<스토리<바로가기) + `story-strip.test.ts` 신규(+타일 존재·첫 자식·겹침 배지 부재·빈 내 링 미표시), 전체 vitest 374 GREEN. dashboard.js 재빌드. 프리뷰 하니스로 렌더 순서·잔디 5색(전 단계 목표 hex)·+타일 52px·격언 발밑 실 브라우저 확인. |
| 2026-07-06 | **feat: 서재 자동 스크롤 제거 + 푸시 명칭 "서재" 통일 (UX 개선 발견 5·6)** — UX 리뷰 PR-C. **발견 5(자동 스크롤 제거)**: 대시보드 서재 패널(`GardenPanel.vue`)이 2.5초마다 작가 갤러리를 자동 이동(`setInterval`)하고 `overflow-x:hidden`으로 사용자 스크롤을 막던 것을, **사용자가 직접 넘기는 가로 스크롤**로 전환. `setInterval`/`autoTick`/hover 정지/`onMounted`·`onUnmounted` 전량 제거, `.dash-garden-stage`를 `overflow-x:auto`(+ 스크롤바 숨김)로. 중앙 작가 이름은 자동이 아니라 **수동 스크롤에 맞춰 갱신**(`onScroll`·`centeredIndex` 유지 — 0-3 결정). 자동 스크롤 전용 순수 함수 `timerProgress.nextAutoScroll`은 유일 사용처가 사라져 삭제(테스트 9케이스 동반 제거), `centeredIndex`는 수동 갱신에 계속 사용해 보존. **발견 6(명칭 통일)**: 사용자 노출 라벨은 이미 대부분 "서재"였고, 남은 사용자 노출 2곳인 **푸시 알림 title**을 `PushReminderService`("독서 마을"→"독서 서재")·`RetentionPushService`("(광고) 독서 마을"→"(광고) 독서 서재")로 통일. 라우트(`/village`·`/garden`)·내부 javadoc·디렉터리명은 기술 식별자라 유지. TDD RED→GREEN: `garden-panel.test.ts` 신규(마운트 시 `setInterval` 미호출) + `PushReminderServiceTest`·`RetentionPushServiceTest`에 payload title "독서 서재" 포함·"독서 마을" 부재 단언(RED=현행 마을 → GREEN), 전체 vitest 367 GREEN·Java 게이트 통과. dashboard.js 재빌드. 프리뷰 하니스로 `overflow-x:auto`·수동 스크롤 시 중앙 작가명 갱신(끝→마지막 작가) 실 브라우저 확인. |
| 2026-07-06 | **feat: 온보딩 프레이밍 개선 — 부채 아코디언·목표 0방지·앰버 경고·실시간 미리보기 (UX 개선 발견 9·10·11·12)** — UX 리뷰 PR-D. 시작을 가로막지 않으면서 온보딩 표시만 손봄(부채·`@Scheduled`·IDOR 무변경). **발견 9(부채 문단 접기)**: 7일 윈도우·자동 용서 설명 문단을 `.entry-note` 상시 노출 → **기본 접힘 `<details>` 아코디언**("📖 목표를 못 채운 날은?")으로 숨겨 신규 사용자 위축 방지. **발견 10(목표 0방지)**: `OnboardingForm @Min(0)→@Min(1)`(0분은 목표의 의미가 사라짐) + 템플릿 `min="0"→min="1"` + **목표 프리셋 칩 [20][30][60][직접]**(`GoalPresets.vue`, 터치 타깃 ≥44px)로 작게 시작 유도. **발견 11(아이디 불변 경고)**: 흐린 `.field-hint muted` → **앰버 `.warn-box`**(#FFF6E5/#F1D48A/#8A6A1C, README 지정 앰버)로 "한번 정하면 못 바꿈"을 격상. **발견 12(실시간 미리보기)**: 신규 Vue 아일랜드 `frontend/src/onboarding/`(`OnboardingPreview.vue` 아이디↔닉네임 "이렇게 보여요" 카드 + `GoalPresets.vue` + `loginIdPreview.ts` + `main.ts` DOM 브리지) 신설 — 소셜 가입자가 불변 아이디를 확정 전 `@핸들`로 확인. **아이디 클라 정제(`sanitizeLoginId`)는 서버 `User.normalizeLoginId`의 거울**(소문자·`[a-z0-9_]`·20자)이고, 입력을 실시간 되써 미리보기=제출값 일치(간극 차단). `vite.config.ts`·`package.json`에 onboarding 아일랜드 배선. TDD RED→GREEN: `OnboardingControllerTest` 0분 제출 검증실패(RED=@Min(0) 통과 → GREEN=@Min(1)) + `onboarding-preview.test.ts`(sanitize 서버 동치성·미리보기 반응)·`goal-presets.test.ts`(프리셋 emit·활성) 신규, Java 게이트 통과·vitest 384 GREEN. onboarding.js 번들 신설·커밋. 프리뷰 하니스로 아코디언 접힘·앰버 배경·아이디 실시간 정제(@myname)·미리보기 갱신·프리셋 가로 세그먼트(#286 button width 상쇄 회귀 수정)·min=1 실 브라우저 확인. code-map §2·프론트 갱신. |
| 2026-07-07 | **feat: 책 없이 측정 시작 + 종료 후 책 태깅 (UX 개선 발견 1)** — UX 리뷰의 마지막·유일한 백엔드 계약 변경 PR-E(앞 A~D는 표시만, 이건 시작 흐름 자체를 연다). **책 없이 시작**: 측정 시작에서 책 선택을 강제하던 가드 3층(`ReadingSessionService.start`·`DashboardApiController.start`·폼 `ReadingSessionController.start`)을 완화 — bookId 없이도 책 미지정 세션 시작(발견 1: "시작을 절대 가로막지 않기"). 프론트 `BookPickForm`은 시작 버튼 항상 활성 + 드롭다운 맨 위 "책 없이 측정" 옵션 + 책 0권이어도 "책 없이 측정 시작" 버튼. **IDOR 경계 보존**: bookId를 '아예 안 주면' 허용하되 bookId가 '있는데' 남의 것/미존재면 여전히 404(API)·에러(폼) — `startSession_otherUserBook_404`·`start_withOtherUsersBookId` 회귀 가드 유지. **종료 후 태깅**: `ReadingSession.tagBook` + `ReadingSessionService.tagBook`(세션 IDOR=`findByIdAndUser`) + `POST /api/sessions/{id}/tag-book`(책·세션 이중 IDOR, 재태깅 409). `StopResponse`에 `sessionId`·`untagged`(=책 없이 시작한 세션이면 true) 추가 → 프론트가 종료 시 신규 `TagBookSheet`(읽는중+완독+읽고싶음 셋 중 선택, 읽고싶음 태깅 시 READING 자동전환, 건너뛰기 가능) 표시. `DashboardResponse.wantToReadBooks` 추가(태깅 시트용, 초기 로드만). **집계·데이터모델·`@Scheduled`·부채 무변경**: `ReadingSession.book`은 이미 nullable(Flyway 마이그레이션 0), 잔디·연속일·부채는 시간 기반이라 null-book 세션을 이미 포함하고 책별 통계는 이미 `where s.book is not null`로 제외 — 원하는 동작이 이미 구현돼 있어 쿼리 한 줄도 안 고침(`ReadingSessionRepositoryTest`가 불변식 잠금). `recordManual`은 스코프 밖(책 명시가 자연스러움)이라 책 필수 유지. TDD RED→GREEN(3커밋): 서비스·엔티티·repository·API(stop sessionId/untagged·tag-book IDOR 404·재태깅 409·wantToReadBooks) 백엔드 + 프론트(BookPickForm null emit·TagBookSheet 3목록·dashboard-tag-flow untagged→시트→tag-book) — 새 API 부재로 RED 확인 후 구현해 GREEN(Java 게이트 통과·vitest 393). dashboard.js 재빌드. 프리뷰 하니스(실번들+mock)로 측정 종료→태깅 시트 등장→책 선택→`POST /api/sessions/99/tag-book`→시트 닫힘, 44px 터치타깃·토큰색 실 브라우저 확인, 콘솔 에러 0. code-map §3 갱신. |
| 2026-07-07 | **feat: 책 고르기 하단 시트 — 드롭다운→표지 칩+바텀시트 (UX 설계 잔여분 §6.5, 발견 1 확장)** — 설계 파일(§6.5 "책 선택·측정 시작 상세")이 의도한 리치 바텀시트가 발견 1(PR-E)에선 드롭다운+옵션으로 축소돼 있던 것을 설계대로 승격(설계 잔여분 3종 중 1번). **백엔드 0**(기존 4계약 재사용: `/api/books/search`·`POST /api/books`·`/api/sessions/start`·`tag-book`), 부채·`@Scheduled`·IDOR 무변경. **통합 시트**: 종료 후 태깅 시트(`TagBookSheet`)를 신규 `BookPickSheet.vue`로 흡수해 `mode`로 겸용 — `start`("측정할 책을 고르세요", 고르면 그 책으로 시작·하단 "책 없이 측정하기") / `tag`("무슨 책을 읽으셨나요?", 고르면 방금 세션 태깅·하단 "건너뛰기"). 시트 = 핸들바 + 제목·✕ + **검색(돋보기)해서 담기**(카탈로그 검색→`POST /api/books`, 담은 직후 로컬 `owned` 전환+`added` emit으로 대시보드 목록 낙관적 갱신) + **상태 필터칩**(전체·읽는중·완독·읽고싶음) + **표지 목록**(34×48 표지/이니셜 + 제목·저자 + 상태배지). 시트가 열릴 때 `/api/books`로 표지·검색가능 여부 로드(실패 시 props 제목-only 폴백). **`BookPickForm`은 드롭다운→표지 칩**(기본=최근 읽은 책=이어 읽기) + "바꾸기"(→시트) + "측정 시작"(1탭) + "책 없이 시작"; 0권이면 "책 없이 측정 시작"+"책 고르기". 설계 결정(AskUserQuestion): 시트 하나로 통합 · 시작 전 목록에 읽고싶음 포함(시작 시 READING 자동전환). 새 색은 설계 지정 시트 배경 `#F6F2E9`·백드롭 `rgba(34,32,26,.34)`(§6.5/§8)만, 나머지 토큰 재사용. `prefers-reduced-motion` 시 슬라이드업 애니 끔. TDD RED→GREEN: `book-pick-sheet`(start/tag pick·bookless/skip·필터·검색담기 owned전환·overlay close·로드실패 폴백)·`book-pick-form`(칩·바꾸기 openSheet·책없이·0권)·`dashboard-start-flow`(바꾸기→시트→pick→start→**측정중 전환**·책없이 null)·`dashboard-tag-flow`(종료→tag 시트→tag-book)·`timer-card`(select→칩) — 신규/개편으로 RED 확인 후 GREEN, 전체 vitest 401 GREEN. dashboard.js 재빌드(121 kB). 프리뷰 하니스로 시트 열림·필터(완독→싯다르타)·검색담기(→"📚 있음")·태깅(→`tag-book` 200)·CSS 지정값(#F6F2E9·백드롭·표지 34×48·터치타깃 44px) 실 브라우저 확인, 콘솔 에러 0(시작→측정중 전환은 하니스 응답 body 이중소비 아티팩트라 jsdom 단위테스트로 별도 단언). code-map §3 갱신. |
| 2026-07-07 | **feat: 서재 도감 풀 리디자인 — 필터칩·진행바·상세시트 (UX 설계 잔여분 §6.6)** — 설계 파일(§6.6 "서재 도감")이 의도한 도감 시각 개편이 발견 5·6(#665)에선 자동 스크롤 제거·푸시 명칭만 손대고 도감 본체는 "🚧 베타 · 개발 중" 배너를 단 채 남아 있던 것을 완성(설계 잔여분 3종 중 2번). **백엔드 0** — `/api/garden`의 `AuthorCharacterDto`(emoji·name·spriteId·owned·matchName·affection·level·title)를 그대로 재사용, 부채·`@Scheduled`·IDOR 무변경. **①상태 필터칩**: `GardenDex.vue`의 단일 체크박스("보유한 것만") → 필터칩 3상태(전체/보유/미보유, 기존 `.garden-tab` pill 재사용, 부모가 `visibleCharacters`로 필터). **②시각 진행바**: 텍스트 "owned/total" → 진행바(신규 `.garden-meter`/`.garden-meter-fill`, `.pv-meter` 비주얼 복제, 폭=owned/total%). **③캐릭터 상세시트**: `DexCell`을 `<div>`→클릭 가능한 `<button>`(키보드·포커스 a11y, `ownedOnly`/`v-show` 제거)으로 승격 + `select` emit → 신규 `DexDetailSheet.vue`(바텀시트, z 60) — 보유는 큰 스프라이트·Lv·칭호·정 진행바(`affectionProgress` 재사용), 미보유는 "『작가』 작가의 책을 완독하면 서재로 찾아와요" 해금 힌트. 상세시트 ESC는 `@keydown.esc.stop`으로 상위 도감 오버레이(VillageApp document ESC)까지 함께 닫히지 않게 전파 차단(중첩 모달 한 레벨 닫기). **④"🚧 베타 개발 중" 배너 제거**(도감 완성). 새 색 0 — 기존 토큰(`--accent`·`--sage-soft`·`--paper-2`·`--sage-6`·`--ok`·`--border`)·프리픽스 재사용, `prefers-reduced-motion` 시 진행바 트랜지션 끔. TDD RED→GREEN: `garden-dex`(필터칩 3상태·진행바 폭 50%·베타배너 부재·셀 클릭→상세시트·닫기)·`dex-cell`(button 시맨틱·select emit)·`dex-detail-sheet`(보유/미보유 변형·정 진행바·닫기·ESC 전파차단) 신규 — RED(상세시트 부재·체크박스·배너·비버튼) 확인 후 GREEN, 전체 vitest 414. garden.js 재빌드(75 kB). 프리뷰 하니스(실번들+mock `/api/garden`)로 필터 전환(보유3/미보유5/전체8)·진행바 37.5%·상세시트(보유 Lv3·단짝·미터 12.5% / 미보유 해금힌트)·ESC 한 레벨 닫힘(상세시트→도감) 실 브라우저 확인, 콘솔 에러 0. 설계 잔여분 3번(가입완료 흐름 §6.4)만 남음. code-map 갱신. |
| 2026-07-07 | **chore: 서재 개명 잔여 — 옛 명칭(정원·마을) 주석·javadoc 정리 (명칭 통일 A안)** — 사용자 노출 라벨은 #619·발견 6(#665)에서 이미 100% "서재"로 통일됐고, 남은 건 **사용자에게 안 보이는 내부 주석·javadoc**이 아직 옛 명칭(정원/마을)으로 현재 살아있는 기능을 부르는 드리프트뿐이었다. 전면 식별자 개명(B안: 라우트·패키지·CSS 클래스·DB)은 사용자 무변화인데도 code↔DB 불일치를 새로 만들고 거대·고위험이라 **의도적으로 보류**하고, **주석/javadoc만 '서재'로 정리(A안)**. 대상: `garden/` 패키지 javadoc(`GardenController`·`GardenService`·`GardenView`·`GardenWorld`·`GardenApiController`·`GardenApiResponse`·`AuthorCharacter`)·`WriterName`·`DashboardApiController`의 "독서 마을/정원" 설명, 템플릿 주석(`dashboard.html`·`garden-character-sprites.html`), `app.css`·`landing.css`의 현재 기능 설명 주석. **역사적으로 정확한 은퇴 CoC 무대·자유배치 CSS 주석(`app.css` --garden-water·내 정원 꾸미기·무대 stage·정원 페이지 wide)과 식별자(라우트 `/village`·`/garden`, 패키지 `com.booktimer.garden`, CSS 클래스, DB 테이블)·docs·Flyway 마이그레이션 주석은 그대로 보존**(개명하면 code↔DB 불일치 발생). 순수 주석 스왑 29↔29(로직·시그니처·동작 0), 사용자 노출 텍스트 무변화. 커밋 테스트 게이트(`.java` 스테이징) 통과. |
| 2026-07-07 | **feat: 가입완료 환영 배너 — 온보딩 직후 1회 (UX 설계 잔여분 §6.4)** — 설계 파일 §6.4가 의도한 가입완료 흐름 완성(설계 잔여분 3종 중 마지막 3번). 온보딩 완료(`POST /onboarding`)가 `redirect:/`만 하고 첫 대시보드에 "환영" 순간이 없던 것을, **1회성 환영 배너**로 채움. **신호 방식(계획 A: 서버 신호→셸 dataset→Vue prop)** — 온보딩 성공 시 `OnboardingController`가 세션에 `justOnboarded`를 심고, `DashboardController` GET /가 **읽은 즉시 삭제(show-once)**해 모델→셸 `data-just-onboarded="true"`로 1회만 넘긴다. `dashboard/main.ts`가 dataset을 읽어 `DashboardApp`에 prop 주입 → 신규 `WelcomeBanner.vue`(닉네임 환영 + 첫 측정 유도 + ✕ 닫기)를 `DashHeader` 아래 렌더. **부채·`@Scheduled`·IDOR·`/api/dashboard` 무변경**(신호는 세션 1개, DTO 무변경). **신호 채널을 flash→session으로 정한 이유**: MockMvc 보안 필터체인이 요청 세션을 session-fixation으로 갈아끼워 flash·session 모두 `.flashAttr`/`.sessionAttr`로는 컨트롤러가 읽는 세션에 결정적 주입이 불가 → **컨트롤러 직접 호출 단위테스트**(mock 세션)로 신호 set/read·show-once 삭제를 결정적으로 검증하고, 템플릿 렌더(data 속성)·배너는 프리뷰 실 브라우저가 커버. 새 색 0 — `WelcomeBanner`는 `.alert` 확장 + `--sage-soft`/`--accent`. TDD RED→GREEN: `OnboardingControllerTest`(성공→세션 신호 set·검증실패→미set)·`DashboardControllerTest`(세션 신호→모델+즉시 삭제·무신호→모델 없음) 직접호출 + `welcome-banner.test.ts`(배너 닉네임·✕ close·prop 유무) 신규 — RED(세션 미set·모델 없음·배너 부재) 확인 후 GREEN, Java 게이트 통과·전체 vitest 419. dashboard.js 재빌드(122 kB). 프리뷰 하니스(`/d/welcome` = data 속성 주입)로 배너 렌더(환영 문구·닉네임)·✕ 닫힘·일반 로드 배너 부재·bg #E7EEE2·border #6E8A6A 실 브라우저 확인, 콘솔 에러 0. **설계 잔여분 3종(§6.5·§6.6·§6.4) 전부 완료.** (code-map은 대시보드/온보딩 배선을 별도 추적하지 않아 무변경.) |
| 2026-07-08 | **docs: SES 프로덕션 액세스 승인 반영 — 샌드박스 해제(50,000통/일·14통/초)** — 4차 재요청(case 178123901400162, 6/29 제출)이 2026-07-06 승인. plan.md retention 레버 ① 보류 박스·2단계 점등 runbook 전제 ①을 "2차 거부/재요청 중"→"✅ 승인·샌드박스 해제"로, N-091(2차 거부→최종 4차 승인으로 결론 갱신: 상세·정직·끈기가 실적 0에서도 통함)·T-058(reopen 재요청 경로로 실제 승인 확인)을 최신화. DMARC `p=none`은 #317에서 이미 게시됨을 명확화(남은 건 안정화 후 quarantine 상향). 문서 전용. |
